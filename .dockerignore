# Keep the repo-root Docker build context lean. The Dockerfile builds the SPA
# from frontend/ and the Go binary from cmd/ + internal/.
.git
.github
.claude
**/node_modules
.cache
bin
dist
frontend/dist
**/*.log
docs
.ci
docker/.env
