# Copy to docker/.env.dev before running the local Compose examples.
# Keep this value pinned after the broker volume exists. Changing it requires the
# staged RabbitMQ procedure in docs/runbook.md; never downgrade a retained volume.
CERBIX_RABBITMQ_IMAGE=rabbitmq:4.3-management
