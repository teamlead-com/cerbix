# Copy to docker/.env.geo only for a fresh geo-demo broker volume.
# Keep this value pinned after the volume exists. Changing it requires the
# staged RabbitMQ procedure in docs/runbook.md; never downgrade a retained volume.
CERBIX_RABBITMQ_IMAGE=rabbitmq:4.3-management
