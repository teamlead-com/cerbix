# iter-0001 — Scaffold + docs foundation

## 1. Summary

Bootstrapped the cerbix monorepo: a building, tested Go backend skeleton (config,
logging, metrics, HTTP operational endpoints, CLI with `--role` dispatch and graceful
shutdown), the docs-driven documentation foundation (AGENTS.md adapted to cerbix, PRD,
spec skeletons, status/decisions/traceability/runbook), the local dev stack
(`docker-compose`), and path-scoped GitLab CI. This establishes the conventions and the
iteration workflow all later phases build on.

## 2. Input artifacts

- Approved plan: `~/.claude/plans/cerbix-hazy-kazoo.md`.
- Reference service conventions: `example-svc/example-isp-cert-rotator` (AGENTS.md,
  `internal/{buildinfo,logging,httpsrv}`, Makefile, `.golangci.yml`, `.ci/*`).
- Company infra facts (Ansible): Postgres role, RabbitMQ 3.12 (user `golang`, `amqproxy`),
  Keycloak 18 at `keycloak.example.com`.

## 3. What changed (by role)

### Agent A — Implementation
- `backend/go.mod` (module `git.example.com/monitoring/cerbix`, go 1.24, dep `yaml.v3`).
- `backend/cmd/cerbix/main.go`; `internal/buildinfo`, `internal/config` (strict YAML,
  `KnownFields`, `Validate`), `internal/logging` (slog + CRITICAL), `internal/metrics`
  (`cerbix_` text exposition + readiness), `internal/httpsrv` (`/healthz` `/readyz`
  `/metrics`), `internal/cli` (`serve --role`, `version`, signal-based graceful shutdown).
- `backend/packaging/config.example.yaml`, `backend/scripts/build-native-binary.sh`,
  `backend/Makefile`, `backend/.golangci.yml`, `backend/Dockerfile`.
- `deploy/docker-compose.yml` + `deploy/config.dev.yaml` (Postgres+Timescale, RabbitMQ,
  Keycloak, cerbix); `.gitlab-ci.yml` + `.ci/*`; `.gitignore`; root `README.md`;
  `frontend/README.md` (placeholder).

### Agent B — Tests
- Unit tests for buildinfo, config (defaults, unknown-key rejection, validation errors,
  OIDC partial/complete), logging (levels, CRITICAL, text), metrics (series + readiness),
  httpsrv (health/ready/metrics), cli (usage, version, serve arg validation, fail-fast).
- Smoke test of the built binary (see §5).

### Agent C — Docs + Traceability
- `AGENTS.md` retargeted from the reference to cerbix (methodology, roles, invariants,
  tenant-isolation invariant, security notes).
- `docs/project-description.md` (PRD); `docs/specs/README.md` + 9 spec skeletons
  (`func-*`, `ops-*`, `sec-*`); `docs/status.md` (FR/NFR/AC/DoD); `docs/decisions.md`
  (D-0001…D-0007); `docs/traceability.md`; `docs/runbook.md`; `docs/checks/README.md`.

### Agent D — Observability + Ops
- Metrics `cerbix_build_info`, `cerbix_up`, `cerbix_ready`, `cerbix_uptime_seconds`.
- Health/readiness endpoints; readiness gates on process state (later: DB/broker).
- CI coverage gate (`COVERAGE_GATE=70`), `-race` in `backend-test`.
- Runbook operational sections; `docs/specs/ops-monitoring.md` for future alerts.

## 4. Requirements closed

FR-001, FR-002, FR-003; NFR-001, NFR-002, NFR-003, NFR-004, NFR-005, NFR-008;
AC-0001-1…6; DoD-1…6. See `docs/status.md`.

## 5. Tests run + results

From `backend/` (GOCACHE/GOMODCACHE in-repo):

- `go build ./...` — OK.
- `go vet ./...` — clean.
- `gofmt -l cmd internal` — clean.
- `go test ./...` — all packages `ok` (buildinfo, cli, config, httpsrv, logging, metrics).
- `go test -race ./...` — all packages `ok`.
- Binary smoke (`serve --role all`): `/healthz` → `{"status":"ok"}`, `/readyz` →
  `{"status":"ready"}`, `/metrics` emits the four `cerbix_*` series; `version` prints
  injected version/commit; SIGTERM → graceful `stopped`.

## 6. Decisions

Recorded D-0001…D-0007 in `docs/decisions.md` (monorepo, Keycloak=authN, RabbitMQ +
Dispatcher, advisory-lock leader, strict config, Go module at `backend/`, status-page
default `internal`).

## 7. Open issues / Next iteration plan

- **iter-0002 (P0):** flesh out `sec-authn-authz.md` + `func-tenancy-rbac.md`; add
  `internal/store` (pgx + sqlc + goose), `organizations`/`projects`/`users`/`memberships`
  migrations; DB-gated readiness.
- **iter-0003 (P0):** OIDC login + sessions + JIT provisioning; `authz.Can` + isolation
  middleware; org/project CRUD + membership management; Global Admin bootstrap.
- Open questions carried forward: Keycloak realm/client naming; session store (PG vs Redis);
  TimescaleDB availability on target Postgres.

## 8. Analysis artifacts removal confirmation

No temporary root analysis artifacts were produced; nothing to remove. All conclusions
live in canonical `docs/`.

## Role-rule / self-healing / dedup checklist

- Changed-rule residence: config validation → `infra`; role/permission rules → not yet
  implemented (planned `domain`/`application`).
- Runtime self-healing for config: **no** (fail-fast, exits non-zero).
- Duplicate checks: none introduced.
- Regression coverage: config negative cases and fail-fast covered by tests.
- Conclusions transferred to canonical docs before any artifact removal: yes.
