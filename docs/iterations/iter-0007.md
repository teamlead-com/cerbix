# iter-0007 — SPA serving skeleton + OpenAPI contract

## 1. Summary

Prepared the backend for the frontend: an `embed.FS`-backed SPA handler that serves the
(future) Vue build from inside the binary with client-side-routing fallback, wired as the
catch-all behind `/auth` and `/api`; and `openapi.yaml`, the OpenAPI 3.0.3 contract for the
current API used to generate the TypeScript client in iter-0008. Advances FR-011.

## 2. Input artifacts

- Canon: `project-description.md`, `status.md`, `decisions.md`, `iterations/iter-0006.md`.
- Existing API surface (iters 0002–0006) documented into the contract.
- End-of-day resume note (frontend is next; design track + openapi + Vue SPA).

## 3. What changed (by role)

### Agent A — Implementation
- `internal/web/web.go`: `embed.FS` over `dist/`, SPA handler (serve real assets, fall back
  to `index.html`). Committed placeholder `dist/index.html` + `dist/assets/placeholder.txt`.
- `internal/cli/cli.go`: mount `web.New()` as the `/` catch-all inside the app mux (after
  `/auth` and `/api`), when auth is enabled.
- `openapi.yaml` (repo root): OpenAPI 3.0.3 — 19 paths, 23 schemas (auth, me, orgs, projects,
  members, monitors, heartbeats, SLA, targets, maintenance) with `cookieAuth` scheme.

### Agent B — Tests
- `web/web_test.go`: serves the shell at `/`, SPA fallback for a client route, serves a real
  asset (distinct from the shell). Validated `openapi.yaml` parses (yaml.v3): 19 paths / 23
  schemas.

### Agent C — Docs + Traceability
- `status.md` (FR-011 IN_PROGRESS, AC-0007), `traceability.md`, `decisions.md` D-0018
  (embed.FS SPA + OpenAPI contract), `specs/ops-cicd.md` (frontend build/copy step).

### Agent D — Observability + Ops
- Documented route precedence (operational + `/api` + `/auth` over the SPA catch-all) and the
  build step (copy `frontend/dist` → `backend/internal/web/dist` before `go build`).

## 4. Requirements advanced

FR-011 IN_PROGRESS (SPA serving + contract ready; Vue build is iter-0008); AC-0007-1…5; DoD.

## 5. Tests run + results

- `gofmt -l`, `go vet ./...` — clean. `go test ./...` — all `ok`; `web` package covered.
- `openapi.yaml` parses (yaml.v3): `openapi 3.0.3 — paths:19 schemas:23 OK`.
- **Real-binary smoke** (local auth + DB): `GET /` → 200 `text/html` with `id="app"`;
  `GET /orgs/x` → 200 (SPA fallback); `GET /api/v1/me` → 401 (API precedence + auth enforced);
  `GET /healthz` → `{"status":"ok"}` (operational precedence).

## 6. Decisions

D-0018 (SPA embedded via `embed.FS` with index fallback, mounted behind `/auth` and `/api`;
`openapi.yaml` as the API contract).

## 7. Open issues / Next iteration plan

- **iter-0008:** the Vue 3 + Vite SPA itself — driven by the **design track (claude.ai/design)**
  and the generated TS client from `openapi.yaml`. Build output copies into
  `backend/internal/web/dist`. Screens: login (OIDC + local), org/project switcher, project
  dashboard (monitor grid + status + 90-day/uptime), monitor detail (heartbeats + SLA + error
  budget), monitor form, members & roles, password change.
- Backlog unchanged: notifications (FR-010), status pages/incidents (FR-012), RabbitMQ
  dispatcher + ICMP/push, TimescaleDB rollups, NFR-010 rate-limit, live-Keycloak realm naming.

## 8. Analysis artifacts removal confirmation

No temporary root analysis artifacts committed (the yaml validator ran in the scratchpad).
Throwaway Postgres container removed after testing.

## Role-rule / self-healing / dedup checklist

- Rule residence: static serving → `transport` (`web`); API contract is documentation
  (`openapi.yaml`), not a second source of rules — handlers remain authoritative.
- Runtime self-healing: **no** — missing SPA files fall back to the shell by design (client
  routing), not to mask errors; API/operational routes are unaffected.
- Duplicate checks: none; OpenAPI describes existing behavior, does not re-implement it.
- Conclusions transferred to canonical docs before artifact removal: yes.
