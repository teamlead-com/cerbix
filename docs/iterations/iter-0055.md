# iter-0055 — Docs & dead-scan cleanup (package 2)

## 1. Summary

First slice of `func-audit-gaps-2` (the re-audit's second layer): documentation drift
and cosmetic dead code.

## 2. What was done

- `docs/overview.md`: the `agent` role no longer claims to need RabbitMQ (table cell and
  the prose below it — the code gates `rabbitmq.url` on api/scheduler/worker only); the
  inline agent command gains `serve` and `--config` (as written it exited 2); the serve
  flags cell documents `--region`; `reencrypt` documents its `database.dsn` requirement;
  the mail row spells the real keys (`smtp_port`, not `port`).
- `internal/config/config.go` + `deploy/config.example.yaml`: the management-URL comment
  now says derivation is scheme-based (amqp→:15672, amqps→:15671) — a non-standard AMQP
  port is not carried over (matches `mqadmin.FromAMQP`).
- `domain.OIDCSettings.UpdatedAt` removed — scanned from the DB and used by nothing
  (absent from the API view and the effective config); the column stays.
- Test-support store functions (`SetMonitorStatus`, `LatestHeartbeat`,
  `GetUserByOIDCSub`, `ListMembershipsByOrg`) carry explicit doc comments so future
  audits don't re-flag them as dead.

## 3. Verification

`go build ./...` green; OIDC store tests green; overview claims cross-checked against
`internal/cli/cli.go` role gating and `mqadmin.FromAMQP`.
