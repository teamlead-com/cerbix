# iter-0094 — Monitoring as Code: UI, OpenAPI & acceptance close-out (FR-017/NFR-014 → DONE)

## 1. Summary

Final phase-4 slice: the read-only UI surface (badges, disabled declarative controls, source
filter), the OpenAPI `management`/diagnostics schema with a regenerated typed client, and the
watcher change-detection test. With this, every spec §19 acceptance criterion is met and
FR-017/NFR-014 move to **DONE**.

## 2. Mandatory role outputs

### Agent A — Implementation
- `openapi.yaml`: `MonitorManagement` block on the `Monitor` schema; `FileProviderDiagnostic`
  schema; `GET /api/v1/admin/file-providers` and `GET /api/v1/organizations/{orgID}/file-providers`
  paths. `frontend/src/api/schema.d.ts` regenerated (openapi-typescript, docker).
- `frontend/src/views/MonitorsView.vue`: a source filter (All / UI / File, shown only when a
  file-managed monitor exists) and a `file` badge with a provider/path tooltip.
- `frontend/src/views/MonitorDetailView.vue`: a "Managed by file" panel (provider + relative
  path + explanation) that replaces the edit/delete/pause controls for a file-managed monitor;
  ordinary monitors keep the RBAC-gated controls.

### Agent B — Tests
- `runtime.TestFingerprintDetectsChanges`: the directory fingerprint changes on create /
  content write / rename and is stable otherwise; a missing directory yields the sentinel
  (observed as a change → reconcile keeps LKG). Closes the §18.3 watcher-detection coverage.
- Frontend `vue-tsc` type-check + `vite build` — green (the `management` field is typed from
  the regenerated schema). `go test -race ./...` — 32 packages green; `go vet`; `git diff --check`.
  `golangci-lint` unavailable (`Error 127`).

### Agent C — Docs + Traceability
- FR-017/NFR-014 flipped to DONE in `docs/status.md` with the evidence chain; `docs/traceability.md`
  iter-0094 row; this report with the acceptance table below.

### Agent D — Observability + Ops
- No new signal; the UI consumes the already-shipped `management` provenance and diagnostics.

## 3. Acceptance criteria (spec §19) — evidence

| AC | Criterion | Evidence |
| --- | --- | --- |
| 1 | format-1 bundle creates/updates without restart; scheduler sees the change | `store.ApplyFileManagedBundle` + `monitor_config_changed` NOTIFY; `e2e/mac-smoke.sh` (live, same PID) |
| 2 | invalid input changes no state, retains LKG | `TestApplyFileManagedBundle`; `GroupBundles`; live smoke (broken YAML) |
| 3 | one invalid bundle doesn't block a valid one; ambiguous can't orphan | `GroupBundles`/`SuspendOrphan` + `runtime.TestReconcileSuspendsOrphanOnUnbound` |
| 4 | UI/API monitors coexist, never mutated by provider diff/absence | ownership model (apply only touches owned set); `TestFileManagedOwnershipGuard` |
| 5 | file-managed CRUD rejected atomically; source metadata tenant-safe + visible | `409 managed_by_file` (`TestFileManagedMonitorReadOnly`), `management` block (`TestMonitorManagementProvenance`), diagnostics (`TestFileProviderDiagnostics`), UI badges/read-only |
| 6 | no-op/touch/rename → no semantic write or revision bump | `TestApplyFileManagedBundle` (no-op path); D-0146; live smoke |
| 7 | removal disables after valid absence + grace; restore reuses id/history/token | `TestApplyFileManagedBundle` (orphan/restore); live smoke (same id + token) |
| 8 | multi-replica leadership/failover + watcher resync race-tested + bounded | `runtime.TestProviderLeadershipFailover` (real PG), `TestFingerprintDetectsChanges`, dirty-bit loop |
| 9 | metrics/logs/status + degraded/LKG alerts documented + tested | `cerbix_file_provider_*` (`TestFileProviderMetrics`), runbook HA-invariant + stale-success alert |
| 10 | `go test -race ./...` + real-binary no-restart/LKG E2E green | 32 packages `-race` green; `e2e/mac-smoke.sh` PASS |

## 4. Verification
- `go vet ./...`, `git diff --check`, `go test ./...` (32 pkgs), `go test -race ./...` — PASS.
- Frontend `npm run build` (vue-tsc + vite) — PASS.
- `e2e/mac-smoke.sh` — PASS (real binary, no restart).
- `golangci-lint run ./...` — NOT RUN (unavailable, `Error 127`) — the only tooling gap;
  everything else green.

## 5. Status

FR-017 and NFR-014 are **DONE** — the full delivery order (spec §20) is complete across
iter-0087…0094: contract foundation → ownership/persistence → atomic apply → scan/group →
reconcile loop + HA → observability/provenance → diagnostics/failover → UI/OpenAPI. No
controller role, no inline secrets, no auto-adoption, no hard delete, no silent fallback.
Recorded deviations: D-0145 addendum (v1 supported types, spec §3.1) and D-0146 (canonical
hash excludes dependency edges); the `orphan_grace_period: 0` scalar→pointer change is a §4.1
conformance fix. No undocumented deviation from the specification.
