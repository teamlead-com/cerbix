# iter-0110 — Monitoring as Code: acceptance close-out, FR-017/NFR-014 → DONE

## 1. Summary

Every defect from both external reviews is fixed and tested (iter-0104…0109), and the last
outstanding *executed*-verification gap — the browser E2E UI assertions — has now been run green
against a current-build stack. FR-017/NFR-014 flip to **DONE**.

## 2. Browser E2E executed (the previously-skipped gap)

A throwaway stack was built from the current source (Dockerfile multi-stage: SPA build → embed →
Go), run isolated (separate DB `cerbix_be2e`, port 8090, own image), and driven by Playwright.
The user's live dev stack was not touched; the throwaway env was torn down afterward.

```
✓ setup — authenticate as bootstrap admin
✓ global-admin diagnostics endpoint returns the {bundles,providers} contract
✓ file-managed monitor shows badge + read-only controls + named-provider filter
3 passed
```

End-to-end observed live: the `platform` provider acquired leadership, reconciled a real
`acme/payments` bundle into a file-managed monitor (`managed_monitors=1`, bundle `applied@gen1`)
with no restart; the diagnostics API returned the `{bundles, providers}` contract to a global
admin; and the monitor-list named-provider filter chip, the "Managed by file" badge, and the
hidden edit/delete/pause controls on the detail page all asserted true.

## 3. §19 acceptance evidence (final)

| # | Criterion | Evidence |
|---|-----------|----------|
| 1 | Create/update without restart; scheduler observes promptly | `store.TestApplyFileManagedBundle`; `e2e/mac-smoke.sh` (create → scheduler-exec heartbeat → in-place update → **config-change propagation**: target change bumps `execution_revision` + fresh heartbeat via the `ConfigNotifier`→scheduler reload) |
| 2 | Invalid input changes no owned state; retains LKG | `runtime.TestReconcile{ScanErrorSuspendsOrphan,DegradedOnUnreadableDir}`, `store.TestApplyBundleAbsenceGuard`; smoke LKG step |
| 3 | One invalid project can't block a valid one; ambiguous can't orphan | `fileprovider.TestGroupBundlesDuplicateFreezesNotSuspend`, `runtime.TestReconcile{DuplicateDoesNotOrphan,ScanErrorSuspendsOrphan,AbsenceGuardThreadedToApply}` (§9.1) |
| 4 | UI/API monitors coexist; never mutated by provider absence/diff | `store.TestFileManagedOwnershipGuard`, `runtime.TestReconcileOrphansDisappearedProject` |
| 5 | File-managed CRUD rejected atomically; source metadata tenant-safe & visible | `api.TestFileManagedMonitorReadOnly` (409), `api.TestFileProviderDiagnostics` (`?provider=` filter, foreign-org denied); **browser**: badge/read-only/filter |
| 6 | No-op/touch/rename → no semantic write or revision bump | `store.TestApplyFileManagedBundle` (no-op `NoChange`, no generation bump), `TestApplyBundleDiagnosticsLifecycle`, `TestApplyRestore{NoConfigChangeNoRevisionBump,EnabledSemantics}` (D-0146/D-0148) |
| 7 | Removal disables after valid absence + grace; restore reuses ID/history/token | `store.TestApplyFileManagedBundle`; smoke restore (same DB ids + push token) |
| 8 | Multi-replica leadership/failover + watcher resync race-tested and bounded | `store.TestLeaderSessionFencing`, `runtime.TestProviderLeadershipFailover`, `TestTwoLiveReplicasSingleLeaderAndFailover`, `TestLeaderLoopEventStormBounded` |
| 9 | Metrics/logs/status + degraded/LKG alerts documented and tested | `metrics.TestFileProvider{Metrics,LeaderZeroRegisteredBeforeElection}`, `store.TestRecordBundleAttempt{,ByPath}`, `runtime.TestReconcileCountsErrorNoFalseSuccess`; `deploy/alerts/monitoring-as-code.rules.yml` (promtool SUCCESS) + `metrics.TestMonitoringAsCodeAlertRules`; runbook |
| 10 | `go test -race ./...` + real-binary no-restart/LKG E2E green | 32 packages `-race` green; `e2e/mac-smoke.sh` PASS; `e2e/tests/file-providers.spec.ts` 3/3 PASS |

## 4. Review disposition
Both external reviews' P0 + all P1 items: FIXED, tested, and (for the UI surface) executed.
golangci-lint over MaC packages: no findings in MaC-changed source. FR-017/NFR-014 = **DONE**.
