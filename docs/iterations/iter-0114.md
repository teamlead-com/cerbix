# iter-0114 — Governance/DoD correction for the MaC hardening pass (corrects iter-0113)

## 1. Why this report exists

[iter-0113](iter-0113.md) recorded the seven-finding Monitoring-as-Code hardening pass and its
commit `35373b1` announced the re-close of FR-017. That close was premature against
`AGENTS.md`: the report lacked the mandatory P0→P1→P2 task plan and Agent A/B/C/D role outputs,
and the Definition-of-Done evidence (the full-repo lint gate) was not recorded verbatim. A
later commit then edited the already-closed iter-0113, which violates iteration-report
immutability. Per prior project practice (iter-0095 corrected iter-0094), the correction lives
here in a new sequential report; **iter-0113 is restored to its `35373b1` snapshot and left
immutable** as the historical record (including the premature-close evidence). The technical
code approval from the `monitoring-review` agent is unchanged — this report changes no code.

## 2. Iteration plan (7 tasks, ordered P0 → P1 → P2)

| Task | P | Finding | Commit(s) |
|------|---|---------|-----------|
| T1 | P0 | Pool starvation: file-provider leaders pin pool connections for life; past the pool floor providers block forever and the winners' reconcile queries deadlock. | `f1ed288`, `efc0f1a`, `956e7d4` |
| T2 | P0 | Orphan-safety: a tenant-bound invalid bundle over-suspended provider-wide; the naive per-project fix keyed on the file header, letting a header typo / duplicate / over-limit orphan the real prior tenant. | `7568572`, `4ccf765`, `e19aa47` |
| T3 | P1 | Target inline secrets: URL userinfo and secret query keys reached the DB/diagnostics in cleartext. | `61b1396`, `22aacb0`, `bf88cb4` |
| T4 | P1 | Mid-reconcile absorb: a write during a reconcile was folded into `lastFP`, not re-reconciled until resync. | `54e6b29` |
| T5 | P1 | Scope-change over-orphaning: a same-name provider with a changed scope value orphaned its prior-scope monitors. | `3786539`, `42a7b92` |
| T6 | P2 | Leader-key collision: FNV-1a-32 advisory key let two names collide so one provider never led. | `e8078dc`, `ea0cb85` |
| T7 | P2 | Unbounded warn-throttle map; and, once bounded, a wide working set re-logged every scan. | `f809524`, `ae85bf2`, `63b6195` |

Governance/lint close-out: `45ba456` (introduced-lint fixes), `11c27ed` (superseded — edited the
closed iter-0113; corrected by this report).

## 3. Agent role outputs

Roles simulated sequentially; outputs recorded.

- **Agent A — Implementation.** `internal/store/store.go` (pool sizing, `pgx.ParseConfig` cap
  detection, `PoolMaxConns`), `internal/cli/cli.go` (provider pool wiring + startup
  `AssertDistinctLeaderKeys`), `internal/fileprovider/parse.go`+`bundle.go` (target
  userinfo/secret-query-key/malformed-URL rejection; bindable-tenant with bind-before-policy,
  authoritative rebind, `tenantHeaderPoisoned`/string-scalar guard), `internal/fileprovider/scan.go`
  (`Frozen`→`map[string]Reason`; duplicate-by-claim; per-path duplicate errors),
  `internal/fileprovider/runtime/provider.go` (fingerprint-before-reconcile; `presentPaths` vs
  `cleanlyApplied` path freeze; scope `Includes`; FNV-1a-64 key; bounded warn-throttle LRU +
  window budget), `internal/store/fileapply.go` (`TenantRef.SourcePath`),
  `internal/config/providers.go` (`Includes`).
- **Agent B — Tests.** Per-finding regressions across `internal/{store,config,fileprovider,
  fileprovider/runtime}`, including negative/adversarial cases; every destructive-case
  regression verified to FAIL when its fix is reverted; DB-gated suites `-race` in both storage
  modes.
- **Agent C — Docs + Traceability.** D-0152, D-0153; spec `func-monitoring-as-code.md` §10/§14;
  `status.md` FR-017/NFR-014 reopen→reclose; `traceability.md` hardening row + relabel of the
  historical FR-017 phase rows (that file is live edited-in-place, not an immutable report);
  iter-0113 and this iter-0114.
- **Agent D — Observability + Ops.** New bounded log signals
  `file_provider_warnings_suppressed`, `file_provider_owned_out_of_scope`,
  `file_provider_owned_path_rejected`; `db_pool_sized` logs actual + required_min; startup
  fail-fast on a leader-key collision. No new metrics/alerts required; health/readiness contract
  unchanged.

## 4. Verification — recorded gate evidence

**Tests.** `go build ./...` clean; `gofmt -l` on all changed `.go` empty.
`go test -race ./...` → **32 packages OK, zero failures**, run twice with
`CERBIX_TEST_DATABASE_DSN` at a plain `postgres:16` (declarative daily partitions) and a
`timescaledb-pg16` (hypertable). Every destructive-case regression verified to FAIL when its fix
is reverted.

**Lint (verbatim, full repo per Makefile:24-25).**

```
$ golangci-lint run ./...      # golangci-lint v2.12.2, config .golangci.yml
42 issues:
* errcheck: 32
* forbidigo: 2
* staticcheck: 8
exit status 1
```

- Scope of this pass is **clean**: `golangci-lint run ./internal/fileprovider/... ./internal/config/...`
  = **0 issues**; no finding falls on a line this pass changed (the three it briefly introduced —
  2 gofmt + 1 QF1008 — are fixed in `45ba456`).
- The 42 full-repo findings are **pre-existing debt in files this pass did not modify**
  (errcheck e.g. `internal/cli/cli.go:91-93,612` help-text `Fprintln`/`disp.Close`,
  `internal/store/projects.go`, test-file `conn.Close`/`tx.Rollback`; forbidigo ×2 in
  `internal/store/{oidc.go,reencrypt.go}` where the `client_secret` column is legitimately
  queried; staticcheck ×8 across unrelated packages).
- **DoD honesty:** the repo-wide lint gate is therefore **RED (exit 1) on pre-existing debt**.
  Per `AGENTS.md`, this pass does not fix unrelated files without scope authority and does **not**
  declare the repo-wide gate green. The FR-017 hardening scope meets DoD; clearing the repo-wide
  lint gate is an **owner decision** — either an explicit waiver of the pre-existing debt for this
  branch, or a separate scoped lint-cleanup MR (the `.golangci.yml` header already notes
  style-heavy linters "deferred to a dedicated cleanup MR").

**Smoke/e2e (changed scope).** `e2e/mac-smoke.sh` → **PASS**: real `cerbix serve --role all`,
no restart, over create → scheduler-exec → in-place update (generation bump) → config-change
propagation (revision bump + fresh heartbeat) → last-known-good on invalid input →
orphan-disable → restore (same DB id + push token).

**Review.** Independent per-commit review by the `monitoring-review` agent; all seven findings
signed off.

## 5. Status of DONE

- FR-017 / NFR-014 hardening scope: **DONE** for this pass's changed scope (code approved, tests
  green in both storage modes, changed-scope lint 0 issues, smoke green).
- Repo-wide `golangci-lint run ./...`: **RED on pre-existing debt** — carried as an open owner
  decision (see §4), not silently marked green.

## 6. Follow-ups

- Repo-wide lint debt (errcheck/forbidigo/staticcheck outside MaC source): owner waiver or a
  dedicated cleanup MR.
- Keep `secretSettingKeys` the single source for target + settings secret classification.
- The scope-change quarantine (D-0153) is deliberate; an explicit provider release/migration
  operation remains future work.
- Rejected-never-applied diagnostics-row retention/lifecycle should be made explicit (non-blocking;
  candidate: prune bundle rows that never reached an applied generation once their source path is
  gone).
