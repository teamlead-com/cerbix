# iter-0115 — Secret inventory foundation (FR-020/NFR-015, iteration 1 of 3)

## 1. Summary

First delivery slice of the secret inventory (spec
[`func-secret-inventory.md`](../specs/func-secret-inventory.md), design-approved r6 after six
independent passes; UI mock owner-approved; decision **D-0155**): the AAD crypto layer, the
fail-closed feature/keyring configuration, the inventory storage with its integrity/fencing
contract, the Secrets API, the domain-owned typed settings validator (unwired), and the
Settings → Secrets panel. Monitors cannot reference secrets yet — ref acceptance switches on
with the dispatch envelope (iteration 2), so no accepted-but-undispatchable window exists.
Every commit was independently reviewed (commit-by-commit) by the `cerbix-reviewer` agent;
review rounds and their fixes are part of this iteration's history.

## 2. Iteration plan (7 tasks, ordered P0 → P1 → P2)

| Task | P | Deliverable | Commits |
|------|---|-------------|---------|
| T1 | P0 | AAD-bound byte crypto (`enc:v2a`, `CanonicalAAD`), fail-closed both directions | `5d11042`, `60e9759` |
| T2 | P0 | `secrets.enabled` + per-region dispatch keyrings; role-aware presence rules and runtime ownership (worker never owns generic outbox/API/materializer) | `d8f69ea`, `de69e6e`, `3083f02` |
| T3 | P0 | Migration 00061 + `store/secrets.go`: canonical-UUID txs, quota lock, AAD at rest, §4.5 rotation fence (D-0142 + in-tx audit + NOTIFY), deferred-FK delete guard, invariant-checked UI re-point | `6a94a89`, `1204232`, `162b231`, `38a2a98` |
| T4 | P1 | Secrets API §5 + OpenAPI/TS: no-store everywhere, honest 32KiB body bound, typed 409s, in-tx-audited mutations + common audit-identity contract | `2c6d727`, `066955f`, `3b33f93`, `3f19eed` |
| T5 | P1 | Settings → Secrets panel per approved mock; project-bound request generation/abort guard; viewer read-only | `71e2744`, `16c891a` |
| T6 | P2 | Single exported `PrepareCredentialSettings` (normalize+validate, fail-closed; UNWIRED until iteration 2) | `a724df8`, `1b9324b`, `1a18667` |
| T7 | P2 | Docs: D-0155, status rows, traceability, resolved role-ownership gate, this report | close-out commit |

## 3. Agent role outputs (simulated sequentially)

- **Agent A — Implementation.** `internal/secret/aad.go` (+ legacy `Decrypt` reserving
  `enc:v2a`); `internal/config/secrets.go` + `ValidateSecretsForRole` + cli fail-fast wiring;
  `internal/store/migrations/00061_project_secrets.sql` + `internal/store/secrets.go`;
  `internal/api/handlers_secrets.go` + routes/interface + `WithSecretsEnabled`;
  `internal/domain/credsettings.go` (`PrepareCredentialSettings` + `ValidSecretName` single
  owners); `frontend/src/components/settings/SecretsPanel.vue` + SettingsView tab; strict
  role ownership in `internal/cli/cli.go`; openapi.yaml (strict schemas) + regenerated TS.
- **Agent B — Tests.** Secret AAD matrix (round-trip, cross-context transplant, no-legacy-
  fallback both directions, malformed/truncated, keyring rotation, canonical-encoding
  unambiguity); config 13-case role matrix + keyring validation + trust rules; store DB suite
  (CRUD/quota + **deterministic canonical-lock blocking proof with UUID-spelling variants**, canonical-spelling
  round-trips, at-rest AAD transplant both ways, delete/rename guards, **rotation fence with a
  real LISTEN/NOTIFY proof**, repoint broken-invariant, **audit atomicity in both directions**
  incl. forced-audit-failure rollback, tenant isolation, project-delete cascade); api matrices
  (authz incl. foreign PATCH/DELETE, feature-off, never-echo with raw-body asserts, no-store on
  every response class, 4096-byte control-char body-bound contract, no-op PATCH audit pin);
  domain settings matrix through the single prepare gate + normalization/leak tests;
  component tests for deferred GET/POST across a project switch and viewer read-only.
- **Agent C — Docs + Traceability.** D-0155; FR-020/NFR-015 `IN_PROGRESS` rows in status.md;
  traceability row; spec role-ownership gate (§4.1) recording and closing the DB-worker/outbox
  master-key conflict found in review; this report.
- **Agent D — Observability + Ops.** Fail-fast startup errors (`secrets_role_config_invalid`
  and config-load messages name the exact key/rule); audit events `secret.create/update/delete`
  (names + renamed/rotated/repointed flags, never values); `monitor_config_changed` NOTIFY on
  rotation feeds the existing scheduler ConfigNotifier; `Cache-Control: no-store` across the
  secret surface; runtime metrics (`cerbix_secret_*`) intentionally land with the materializer
  in iteration 2.

## 4. Verification (recorded evidence)

- `go build ./...`, `go vet ./...`, and repo-wide `gofmt -l` — clean at close-out.
- Store `go test -race ./internal/store/ -count=1` — green against both the declarative
  Postgres (`:55442`) and hypertable (`:55443`) test databases after the review fixes.
- `go test -race` on `internal/{secret,config,domain,authz,api,cli}` — green.
- Frontend: deterministic Vitest component suite 3/3 green; the mandated
  `node:22-alpine` `npm run build` (`vue-tsc` + Vite) green.
- Repo-wide `golangci-lint run ./...` remains RED on the same 42 pre-existing findings
  recorded by iter-0114/D-0154; `--new-from-rev=main` reports **0 issues** for this branch.
- Independent read-only review confirmed the role-ownership, tenant-context, normalization,
  quota-lock, audit-identity and fake rollback-equivalence fixes on the close-out HEAD.

## 5. Follow-ups (iteration 2 inputs)

- Ref-table wiring in every monitor write + MaC apply; ciphertext-only snapshot; authoritative
  batch materializer; envelope/AAD dispatch + v2 wire barrier (AMQP jobs/tests queues, pull
  protocol_version claims, agent capabilities); typed `probe_error`; reencrypt hardening;
  prober TLS options; monitor-form ref UI.
- Rename/delete-vs-apply interleaving tests activate when ref writes land (the FOR KEY SHARE
  side exists only from iteration 2).
