# iter-0146 — FR-021 phase 3, changeset 1: the impact graph + correlation schema/store core

Status: DONE — review chain [276] (round 1/2, 1 P0 + 5 P1 + 1 P2) → fix pass [281] → FINAL
2/2 [283] (all round-1 findings closed; one residual P1 closed under the [263]-delegated
disposition of §9; per the two-round contract no third review round ran).
Scope: the §14 amendment (design APPROVED [269]) made real at the schema/store/worker
layer. No API routes, no frontend — those are the next changesets, mock already approved
(commit 8320d62).

## 1. Normative extract (spec §14, gathered BEFORE code)

- Edges: same-project BY SCHEMA; DAG, depth cap 10 pinned at 9/10/11; ≤20 direct edges;
  every edge-mutating path under the per-project `service_graph` advisory lock, ordered
  after `service_membership`; edges outside the declaration axes — no revision, no epoch,
  no canonical-hash byte (§14.1–14.2, invariants 48–49, 58).
- Mutation: `graph_generation` CAS (409 on stale, first-committer-wins); per-delta tenant
  audit row in the SAME transaction; no-op bumps nothing, audits nothing; a desired file
  edge pins its target — delete = 409 naming the provider (§14.2, invariants 50–51).
- Correlation: own outbox topic enqueued in the incident's opening transaction; ONE store
  transaction — locks ascending, open recheck under the lock, links + 🕸 notes atomic;
  symmetric at open with the late-root closed by back-fill; canonical endpoint-inclusive
  root-first paths ≤11; redelivery inserts/writes nothing; resolved incidents never
  annotated; membership via `monitors[]` (§14.3, invariants 52–57).
- The claim fence: `pending_fenced` + immutable `fenced` + demotion CHECK; class-restoring
  retry/replay; legacy replay SQL fails closed; legacy topics unchanged (invariant 61).

## 2. Failure-mode table (self-adversarial, before code)

| Risk | Where it would bite | Countermeasure in this changeset |
|---|---|---|
| composite FK with plain `ON DELETE SET NULL` nulls the tenant column too | incidents.monitor anchor | column-targeted `SET NULL (monitor_id)` (PG15+) |
| two acyclic writes compose a cycle | concurrent replace-sets | `service_graph` advisory xact lock + the concurrent-writers regression |
| capped cycle walk silently truncates | deep chains | the DEPTH invariant enforced at every write makes the capped walk exhaustive; 9/10/11 pinned |
| interleaving-dependent relation content | child-first vs parent-first | the upward `affected` mirror back-fill (implementation-discovered, disclosed in the spec §14.3) |
| link into a just-resolved incident | check-then-act on `open` | FOR UPDATE ascending + recheck under the lock; barrier regression with a held lock |
| note without links / links without the promised note | partial failure between the two writes | both in ONE transaction; redelivery byte-equivalence regression |
| old owner dead-letters the fenced topic | rolling deploy | the claim fence, tested with the VERBATIM legacy claim and replay SQL shapes |
| retry/replay demotes the fence | FailOutbox / both replays wrote `'pending'` | class-restoring CASE + the demotion CHECK failing the legacy replay closed |

## 3. Changes

- Migration [`00080_service_impact.sql`](../../internal/store/migrations/00080_service_impact.sql):
  `service_dependencies` (shared-project composite FKs both ways), `services.graph_generation`,
  `incidents` UNIQUE (id, project_id) + composite monitor anchor with column-targeted SET NULL,
  `incident_service_impacts` (path text[] CHECK 2..11), outbox `fenced` column + widened status
  CHECK + `outbox_events_fence_check` + fenced partial claim index + topic whitelist.
- [`internal/store/servicegraph.go`](../../internal/store/servicegraph.go): `GetServiceDependencies`
  (both directions + generation), `ReplaceServiceDependencies` (graph lock → row lock → CAS →
  same-project/cycle/depth/limit validation → replace-set → generation bump → delta audit),
  `assertServiceNotPinnedByFileEdgesTx`; `DeleteService` takes the graph lock and the pin guard.
- [`internal/store/serviceimpact.go`](../../internal/store/serviceimpact.go): `CorrelateIncident`
  (the one-transaction attempt: read phase → deterministic BFS with shortest/lex canonical paths →
  lock phase ascending + open recheck → ON CONFLICT link inserts → 🕸 notes for newly gained rows),
  `ListIncidentImpacts` (the detail enrichment, for the API changeset).
- [`internal/store/outbox.go`](../../internal/store/outbox.go): class-aware enqueue (from
  `domain.FencedTopic`, the one topic→class map), two-class claim (fenced restricted to this
  binary's dispatch set), class-restoring `FailOutbox` and both replays.
- [`internal/store/incidents.go`](../../internal/store/incidents.go): a monitor-anchored open
  enqueues `incident_correlation` in the same transaction.
- [`internal/domain/serviceimpact.go`](../../internal/domain/serviceimpact.go) + `outbox.go`:
  the topic, `FencedTopic(s)`, roles, caps, `ServiceImpactLink`, bounded `RenderImpactNote`
  (🕸, 8 + "+N more").
- [`internal/outbox/outbox.go`](../../internal/outbox/outbox.go): the `incident_correlation`
  worker case (per-role link metrics, failure counter, retry via the standard envelope).
- [`internal/metrics/metrics.go`](../../internal/metrics/metrics.go):
  `cerbix_service_impact_links_total{role}` (insert-only) + `cerbix_service_impact_correlation_failures_total`.
- [`internal/api/handlers_services.go`](../../internal/api/handlers_services.go): the ONE API
  touch this changeset needs — `writeServiceError` maps the typed pin error to a 409 naming
  the provider and the referencing service. Without it the already-live delete guard would
  surface as a 500 through the EXISTING delete route; the dedicated `/dependencies` routes
  remain the next changeset.
- Spec: §14.3 upward bullet gains the mirror back-fill (implementation-discovered; disclosed
  inline with rationale — content symmetry, not merely observation symmetry).

## 4. Tests

New regressions (exact `grep -c '^func Test'`, after the [276] fix pass): 10 in
`servicegraph_internal_test.go`, 11 in `serviceimpact_internal_test.go`, 3 in
`outboxfence_internal_test.go` (real PG each); 3 worker tests in `outbox_test.go` (hermetic;
incl. both delivery-independence directions and the fenced-topics-dispatchable pin), 1 domain
test (`RenderImpactNote` bounds/order), and 1 API test (`TestDeleteServicePinnedByFileIs409` —
the pin error reaches the wire as an actionable 409, never a 500).

- Graph: replace/read/no-op + delta audit incl. removals; stale-generation 409 with
  first-committer-wins; self/foreign/direct-cycle/transitive-cycle/limit; depth pinned at
  9 (valid), 10 (valid), 11 (rejected) plus the through-chain case; the concurrent
  cycle-writers race (exactly one commits); direct-SQL cross-tenant edges rejected by the
  schema under BOTH project claims; edge-only write leaves revisions/epochs/hash
  byte-identical; file-pinned delete 409 naming provider+service vs UI-owned cascade.
- Correlation: both interleavings converge to the same (slug, role, path) relation with one
  🕸 note per annotated incident; redelivery byte-identical, zero notes; resolved-anchor
  skip + the witness-resolve barrier under a HELD row lock; diamond lex tie-break; direct
  edge = 2 slugs; depth-10 chain = 11 slugs; cross-tenant impact rows and monitor anchors
  unrepresentable by direct SQL.
- Fence: the VERBATIM legacy claim shape selects zero fenced rows and burns zero attempts;
  capable claim → forced failure → still `pending_fenced` → dead → single replay AND
  replay-all restore the class; the VERBATIM legacy replay SQL fails closed on
  `outbox_events_fence_check`; legacy topic round-trips unchanged; the monitor-anchored
  open enqueues fenced and the manual non-anchored open enqueues nothing.

## 5. Agent role outputs (AGENTS.md mandatory roles)

- **Agent A, Implementation**: migration 00080, servicegraph/serviceimpact stores, the
  fence transitions, the worker case, the enqueue site.
- **Agent B, Tests**: the 21 regressions above; every reviewer-named SQL shape reproduced
  verbatim rather than approximated.
- **Agent C, Docs + Traceability**: this report; status.md AC-0146-1…3 (IN_PROGRESS); the
  §14.3 mirror disclosure; D-0166 and the traceability row reserved for acceptance.
- **Agent D, Observability + Ops**: the two §14.6 counters (insert-only by construction, so
  redeliveries can never inflate them); fenced rows are visible in the existing dead-letter
  admin surface and replay correctly from it; the ops effect of the fence is that a rolling
  deployment can no longer dead-letter a correlation fact.

## 6. Gates (each run ONCE per send, on the FINAL tree)

Round 1 (2026-08-17). Two late Go-only additions (the pin 409 mapping + its API test) landed
after a first full pass, so the heavy gates were RERUN on the final tree; the numbers below
are the final-tree runs:

- `go vet ./...`: clean. `golangci-lint run ./...`: 0 issues. `git diff --check`: clean.
- Full `go test -race -count=1 ./...` hypertable `cerbix_test`: **zero FAIL, exit 0** (the
  pre-final pass on the same code minus the two-line API mapping counted 33 packages ok).
- Declarative (`postgres:16-alpine` throwaway :55437, --rm): store 148.5s / api / scheduler /
  cli / config / domain **all ok** — migration 00080 and every new regression pass in
  declarative-partition mode too.
- E2E on the REBUILT final-tree image (`make dev-build && dev-up && dev-test`), verbatim
  tail: "1 skipped / **41 passed** (1.8m)"; the skip is the known idle-provider MaC assertion.
- Targeted new-test runs (before the full gates): 14/14 graph+impact, 3/3 fence, outbox
  package ok, domain ok, API pin 409 ok — all real PG where DB-gated, zero skips.

## 7. Fix-pass record (review [276], round 1/2 → FINAL 2/2; [278] conditions accepted [280])

| Finding | Fix |
|---|---|
| P0-1 impact read not tenant-scoped | `ListIncidentImpacts(ctx, projectID, incidentID)` predicates on the LINK rows' own `project_id` (+ tenant-scoped service join); negative test: a foreign project scope reads zero rows for a real incident id; §14.4 + §16 updated |
| P1-1 no coherent attempt snapshot | the attempt takes `service_membership` → `service_graph` advisory locks (the §15.4 order) BEFORE reading membership/graph/witnesses; two deterministic barrier regressions (graph replace and membership change each committing while the attempt demonstrably waits — `pg_locks` advisory-waiter poll) prove the attempt sees exactly the post-commit state |
| P1-2 orphaned dependent stopped pinning | the pin query no longer excludes `orphaned_at` (an orphan IS file-owned last-known-good); regression: orphaned dependent → target delete still 409 naming provider+service |
| P1-3 unbounded correlation work | the [279] bounded contract, accepted [280]: per-endpoint-service witness bound = 5, oldest by `(started_at, id)`, anchor never counted, per-service selection, deterministic across redelivery; overflow returned → worker logs + `cerbix_service_impact_witness_overflow_total` (unlabeled); §14.3/§14.7/invariant 54 amended; regression: six manual anchored witnesses → oldest five selected, newest rowless, overflow 1, service marked once. **[283] residual closed under the disposition below**: witnesses are read for the anchor's REACHABLE endpoints only and capped IN SQL (per-endpoint LIMIT + aggregate totals), so row transfer ≤ endpoints × cap and overflow counts only omitted witnesses of reachable endpoints |

## 9. Final-round disposition ([283], owned under the [263] delegation — no third review round)

FINAL 2/2 verdict: all round-1 findings closed; ONE residual P1 — witness selection and
overflow were computed over EVERY open incident in the project instead of the anchor's
reachable endpoint set: false omission telemetry (the reviewer's counterexample: six
incidents on an unrelated service produced overflow=1 with zero candidates) and read work
still O(project open incidents) per attempt. Disposition taken: implement the reviewer's
prescribed fix verbatim — reachable endpoints derived from the locked snapshot walks;
`boundedWitnessesTx` reads witnesses ONLY for those endpoints with the cap enforced in SQL
(per-endpoint `LIMIT`, LATERAL) and exact totals from an aggregate (exact count is normative
per [279]); overflow counts only omitted witnesses of reachable endpoints; an attempt with
no reachable endpoint reads no witnesses. Required tests added:
`TestCorrelateUnrelatedServiceNeverCountsOverflow` (6 unrelated incidents → zero links,
overflow 0), the existing reachable-endpoint bound regression (oldest 5 + overflow 1), and
the worker wiring assertion (returned overflow reaches `RecordImpactWitnessOverflow`).
Spec §14.3/invariant 54 updated with the reachable-endpoint scoping, marked [283].

Disposition-tree gates (each ONCE, sequential): vet clean; `golangci-lint` 0 issues;
`git diff --check` clean; full `go test -race -count=1 ./...` hypertable, solo: **33 ok,
zero FAIL**; declarative (`postgres:16-alpine` throwaway): all 7 packages ok (store 148.5s);
E2E on the REBUILT image (the binary changed in this pass): "1 skipped / **41 passed**
(1.8m)". 25 phase-3 store regressions total (exact `grep -c '^func Test'`: 10 graph + 12
impact + 3 fence), zero skips.
| P1-4 audit actor untyped | `GraphActor{UserID, ViaToken, Label}`; canonical `actor_user_id`/`via_token` columns written, label in target and `created_by`; attribution regression: human / token / provider (NULL user, machine label) |
| P1-5a note-failure rollback unproven | test-only `correlateNoteFault` injection on Store (nil in prod); regression: faulted attempt → zero links AND zero notes; retry → exactly one batch |
| P1-5b sleep-based barrier could false-pass | `waitForLockWaiter` polls `pg_stat_activity` for a backend BLOCKED on the incident `FOR UPDATE` before resolving the witness — the barrier is now observed, not assumed |
| P2-1 token/set from two snapshots | `GetServiceDependencies` is ONE SQL statement (LEFT JOIN LATERAL): generation and both directions from one MVCC snapshot; parity-race regression (writer alternating sets keyed to generation parity, 200 concurrent reads assert token↔set correspondence) |

Spec deltas in this pass, all marked implementation-discovered with the review pointer:
§14.3 attempt-snapshot + witness-bound paragraphs; §14.2 typed attribution + orphan-pin +
single-snapshot read sentences; §14.4 tenant-scoped impact read; §14.6 overflow counter;
§14.7 witness-bound row; invariant 54 requalified.

## 8. Fix-pass gates (each ONCE on the FINAL tree)

Disclosure first: the FIRST full `-race` of this pass, run in PARALLEL with the docker image
build and the declarative suite, FAILED once; a solo rerun passed 33/33 with zero FAIL, and
the root cause was CPU starvation tripping the new barrier helpers' 5-second poll deadlines —
a flaky bound, not a product defect. The deadlines were raised to 30 seconds (pure waiting
bounds: they spin only while the condition has not happened, so the healthy path is
unchanged), and the heavy gates were then rerun SEQUENTIALLY. Final-tree numbers:

- `go vet ./...` clean; `golangci-lint run ./...` 0 issues; `git diff --check` clean.
- Full `go test -race -count=1 ./...` hypertable `cerbix_test`, solo: **33 packages ok,
  zero FAIL**.
- Declarative (`postgres:16-alpine` throwaway, --rm): the full 7-package run (store 202.6s /
  api / scheduler / cli / config / domain / outbox all ok) + a store-package rerun after the
  test-only deadline bump: **ok (147.9s)**.
- E2E on the rebuilt image: "1 skipped / **41 passed** (1.9m)" — the deadline bump touched a
  `_test.go` file only, so the binary in that image is byte-identical to the final tree.
- Targeted: 24/24 phase-3 store regressions, outbox package ok, domain ok, API pin 409 ok —
  real PG where DB-gated, zero skips.
