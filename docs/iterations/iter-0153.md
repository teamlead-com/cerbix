# iter-0153 — FR-021 phase 5: reconciling the living documents with the frozen ones

Spec: `specs/func-service-reliability.md` §16. Decision record: **D-0168**, unchanged.
Predecessors: **iter-0151**, closed at `475a0d3`; **iter-0152**, closed at `cc247ce`. Both are
immutable from those commits and NOTHING in this arc edits either of them — that constraint is the
subject of §2.1.

> **Lifecycle: OPEN.** It closes when the reconciliation below has been reviewed and nothing further
> is found in it, and the closing commit adds the CLOSED line and the final §2 count. Until then this
> is the writable current iteration and `docs/status.md` names it as such — which is precisely what
> was wrong before (§2.2).

> **Why this report exists.** Round [409] reviewed the closure of iter-0152 and found no product
> defect, no P0 and no P1 — and two P2s that are both the same mistake in two places: I deleted a
> thing and left the sentences that describe it. The scoreboard is gone; three documents still carried
> five statements saying where it lives. iter-0152 was frozen with that contradiction inside it, so part of this correction
> cannot be a fix at all — only a record.

## 0. The plan, and who did what (AGENTS §Iteration Workflow)

No P0 in this arc. Two P1s — one from round [411], one from the owner's instruction to reconcile the
documents with the code 1:1 — then the P2s, in the order the reviews raised them ([409] P2-1 before
[409] P2-2, then [411]).

| # | Priority | Task | Outcome |
|---|---|---|---|
| 1 | P1 | Owner instruction: reconcile the LIVING documents with the code citation by citation, both directions, and leave the agreement enforced rather than reviewed | §2.5 |
| 2 | P1 | [411] P1: the report was opened without opening its live status items — AGENTS step 2 asks for atomic `AC`/`DoD` rows, not a header | §2.3 |
| 3 | P2 | [409] P2-1: the deleted scoreboard contract was still asserted in the LIVING documents — `docs/status.md`, `docs/traceability.md` | §2.1 |
| 4 | P2 | [409] P2-1, second half: the FROZEN iter-0152 contradicts itself and may not be edited — correct it by record, and stop the round enumeration that keeps going stale | §2.1, §3 |
| 5 | P2 | [409] P2-2: a CLOSED snapshot was still the "current iteration" — hand the writable role to this report | §2.2 |
| 6 | P2 | [411] P2-1 and P2-2: this report miscounted its own source documents and misdescribed its own task ordering | §2.4 |

Role outputs, per AGENTS' mandatory roles:

- **Agent A (implementation).** No product code, no schema, no API, no SPA — but not nothing:
  `scripts/check-docs-references.py` (the reference gate of §2.5), `Makefile` (`docs-check`, `.PHONY`)
  and the command recorded in `CLAUDE.md` beside `go vet`. `go build ./...` stays a guard that no
  product file moved, not evidence of a change (§4).
- **Agent B (tests).** No Go or TS test changes — a documentation claim is not falsified by a Go test.
  What stands in for the mutation discipline is (a) a set of COMMANDS whose output is the claim (§5):
  each assertion here is a `diff` or a `grep` a reader can run, two of them the checkers whose
  disagreement with prose produced findings [399], [403] and [405]; and (b) three MUTATION PROBES
  against the new gate, run and reverted — an invented path and an invented `Test*` name are both
  named by it; the same invented path with a removal marker 300 characters away still fails; with the
  marker adjacent it is excused, which is the one behaviour the gate is allowed to have.
- **Agent C (docs + traceability).** This report; `docs/status.md` (current-iteration header, the
  counter's scope, the stale DoD-0151 sentence, and — after [411] — this iteration's own
  `AC-0153-1..3`/`DoD-0153` rows with the previous arc's rows moved under their own heading);
  `docs/traceability.md` (the FR-021 phase 5 row).
- **Agent D (observability + ops).** Nothing. No metric, no readiness predicate, no operational
  surface is touched by this arc, and saying so explicitly is cheaper than leaving the reader to
  infer it.

## 1. What this arc makes true

**A frozen document is corrected in the next document, no living document quotes a contract that no
longer exists, and no living document cites code that is not there.**

The first half is the contract's own rule and the reason it exists: an immutable report is evidence
of what was believed at a point in time, and evidence that gets rewritten is not evidence. The second
half is the part I keep failing at — three rounds in a row ([403], [405], [407]) found a sentence that
described a mechanism I had already replaced. Removing a mechanism is two acts, and I keep performing
one.

## 2. What round [409] found

### 2.1 The deleted scoreboard was still being quoted — five statements in three documents, one frozen

`cc247ce` deleted the cumulative "reviewer findings" scoreboard from `docs/status.md` and recorded
the reason in iter-0152 §2.10: a live self-referential total cannot converge, because every review of
the total is itself a finding that changes it. What `cc247ce` did NOT do is propagate that deletion
through the prose describing it. At the moment of freezing:

- `iter-0152.md:26` — "the CUMULATIVE counters live in `docs/status.md`";
- `iter-0152.md:54` — Agent C's output listed as "`docs/status.md` (the cumulative counters …)";
- `iter-0152.md:235-241` — "The cumulative counters now have exactly one owner, `docs/status.md`",
  followed by a paragraph analysing "the two totals" and how their SETS differ;
- `docs/status.md:28` — DoD-0151: "the cumulative totals are stated once, at the top of this
  document";
- `docs/traceability.md:203` — "the totals stated once at the top of `status.md`".

There is now exactly one counter — §2 finding SECTIONS in the frozen reports — and explicitly no
lifetime total. So all five sentences describe a document layout that no longer exists, and the first
three of them sit inside the very snapshot whose §2.10 announces the deletion.

The two living documents are corrected in this arc. The three frozen lines are NOT: see §3.

Why it happened, without dressing it up: I treated "delete the scoreboard" as a diff to one table
instead of a change to a contract. The contract was stated in five places because I had earlier been
told — correctly — to state where cumulative numbers live. Retiring that rule required retiring every
statement of it, and I checked the number instead of the rule.

### 2.2 A CLOSED snapshot was still the current, writable iteration

`docs/status.md:5` still read "Current iteration (iter-0152 …)" after `cc247ce` closed iter-0152 and
declared it immutable. Three consequences, all visible in the file:

- the living index pointed the next writer at a document that refuses writes;
- its round enumeration stopped at [403], while the frozen §2.10 of the report it was describing
  records [405] and [407] — the index was stale about its own subject;
- `423b2a6` is a commit made AFTER the closure, and the frozen report says anything later belongs in
  `iter-0153` — which did not exist, so the only writable home for it was the closed report's own
  header.

This is the same category as finding [401]: not an arithmetic error but an invalid place to put a
changing fact. Closing a report without opening its successor leaves the changing facts nowhere legal
to go, and they land back in the frozen one.

Two things change:

1. **This report is the current iteration.** `status.md` names iter-0152 as CLOSED at `cc247ce` and
   points here for the arc in progress.
2. **The living documents stop ENUMERATING review rounds.** `status.md:5` and
   `traceability.md:203` each carried a list of round IDs, and each had gone stale at a different
   round ([403] and [399] respectively) because a list in a living document grows with every round
   while nothing forces it to. Round IDs are per-arc evidence and they live in the report for that
   arc, which freezes. The living documents point at the reports instead. This is the same fix as
   deleting the scoreboard, applied to the other growing thing I had left in place — and it is why
   this arc's own round, [409], appears in THIS report and in no living index.

### 2.3 The report was opened; its live status items were not

Opening iter-0153 fixed where the writable iteration IS and left out what AGENTS' workflow step 2
actually asks for: `docs/status.md` updated with atomic `AC`/`DoD` items in `TODO`/`IN_PROGRESS`/`DONE`.
The file named iter-0153 as current and then went straight into the `AC-0151-*`/`DoD-0151` rows, all
still `DONE` — so the live checklist had nothing that was live. `rg 'AC-0153|DoD-0153'` returned
nothing.

That is the same failure as [409] P2-2 one level down, and worth naming as such rather than as a
missing table: I restored the POINTER to the writable iteration without restoring the thing the
pointer is for. A current-iteration header with no in-progress items is a heading, not a status.

Fixed by `AC-0153-1..3` and `DoD-0153`, all `IN_PROGRESS`, each linked to the section here that
defines it and to the §5 command that checks it; they flip to `DONE` only in the commit that closes
this report. The previous arc's rows keep their `DONE` state and move under their own
`## Previous iteration` heading, because they document iter-0151/iter-0152 and not this one.

### 2.4 This report miscounted its own sources, and misstated its own ordering

Two self-description defects in the document you are reading — [411] P2-1 and P2-2, one section
because they are the same class:

- it said the deleted contract survived in **four documents** — in the note above and in the §2.1
  heading — while §2.1's own list enumerates **five statements in three documents**: iter-0152, `docs/status.md`,
  `docs/traceability.md`. I counted the sentences in one place and the files in another, then wrote a
  number that matched neither. Now stated as three documents and five statements, which is what the
  list holds;
- §0 claimed "the tasks are in the reviewer's order" while the table reversed [409]'s two P2s. The
  claim was gratuitous — every row was P2, so the ordering rule was already satisfied and nothing
  needed asserting. It is now true instead of deleted: the rows follow the reviews' order, and §2's
  numbering already did.

The pattern this arc keeps producing is not carelessness about facts but carelessness about the
SENTENCES ABOUT facts — a count restated beside its source, a claim added for reassurance. Both were
caught by a reader comparing two lines of mine, which is the cheapest possible check and one I can run
myself.

### 2.5 The living documents and the code had drifted apart, citation by citation

Not a review finding — the owner asked for the documents and the code to be reconciled 1:1. Both
directions were swept: FORWARD, every backticked repo path and every `Test*` name cited by a document
that is edited in place; REVERSE, every migration, non-test Go file, e2e spec and Vue component,
against the documents that should name it. Twenty-two forward citations did not resolve, and they fall
into five classes that are worth separating because they fail differently.

**1. The canonical spec asserted what the code was deliberately changed to stop doing.** Invariant 47
of `docs/specs/func-service-reliability.md` read "service SLOs do not alert, and a service-scoped
`burn_alert_enabled` is rejected" — while §16.4 of the SAME file says the phase-2 rejection is
"lifted", migration 00082 drops `sla_targets_service_no_burn_chk`, and a service SLO now pages per
burn rule. This is the most serious kind of drift in this list: §16.8's invariant list is what reviews
measure an implementation against, so an invariant that contradicts its own document can either fail a
correct implementation or bless a wrong one. Invariant 47 is now marked SUPERSEDED at its number
(other documents cite it by number), states what replaced it, and points at what survives — invariant
46, monitor-level burn behaviour unchanged.

**2. A live requirement row carried the same reversed claim.** `docs/status.md` AC-0138-2 still said
"service burn alerting rejected at the schema AND absent from the app layer (invariant 47)". Marked as
delivered-in-phase-2 and superseded by §16.4/D-0168, with the phase-5 test named.

**3. Three cited test names have never existed in any commit** (`git log -S` over `*.go` finds
nothing): `TestPartitionAdoptionFencesConcurrentInserts` (`status.md` AC-0134-2 and the immutable
iter-0134), `TestComponentMutationsTakeThePageFirst` (AC-0150-5) and `TestSendBuildsMessage`
(`traceability.md`, the subscriber-mail row). In all three cases the PROPERTY is proven and the NAME
was invented while writing the row: page-first lock ordering is asserted by
`TestComponentMutationsCannotDeadlock` (its T1 comment is literally "the confirm-shaped path — page,
then the component"), adoption fencing by `TestAdoptionReconcilesConcurrentUpdatesAtTheFence` plus
`TestAdoptionFenceIsBoundedAndAbortsSafely`, and the mailer by `TestLiveMailerResolvesPerSend`. Real
evidence cited under a name that cannot be run is still a broken claim: a reader checking the row
concludes the proof is missing, which is the opposite of what the row is for.

**4. Five tests were renamed with their behaviour intact and the citing rows stopped resolving.**
`TestServiceLimitsClampToTheHardMaxima` → `config.TestServiceCapsAreRejectedNotReinterpreted`;
`TestMaintenanceWindowCRUD` → `TestMaintenanceWindowLifecycleIsArchiveNotDelete`;
`TestPullJobsClaimOnceAndTTL` → `TestPullJobsClaimLeaseAckReclaimAndTTL`;
`TestRecordResultDedupAndFreshness` → `TestRecordScheduledResultPipeline`; and
`TestServiceScopedBurnAlertIsRejectedEverywhere` → `TestServiceScopedBurnTargetIsSupported`, where the
rename records an INVERSION of behaviour and is therefore class 1 as well.

**5. Renamed symbols, wrong packages and malformed paths in the requirement→code map.** `IncidentSink`
→ the outbox's `WebhookDeliverer` (D-0036); `StartOIDC` → `RunOIDC`; `ProbeRunner`/`WithProber` →
`RegionTester`/`WithTester` (D-0087, which the very next row already documented); the agent backfill's
bulk path → `RecordHistoricalResults`; openapi `RecentIncident` → `IncidentDetail`;
`views/MembersView.vue` → `components/settings/MembersPanel.vue`, cited as live by four rows and as
deleted by a fifth; `users_reset.go` attributed to `internal/auth` when it lives in `internal/store`;
`frontend/.../DashboardView.vue`, `frontend/NewMonitorView.vue`, `frontend/MonitorsView.vue` missing
their `src/views/` segment; `e2e/tests/topology-geo.ts` missing `.spec`; and two spec placeholders
never given their numbers (`0003X_escalation.sql` → `00035_escalation.sql`, `0003X_pull_jobs.sql` →
`00036_pull_agent.sql`) plus a spec pointing at a `handlers_projects.go` that has never existed
(project deletion is `internal/api/handlers.go`, `deleteProject`).

**The reverse direction is clean but for one file.** Every migration, every non-test Go file and every
e2e spec is named by some document; `frontend/src/components/BrandMark.vue` was named by none, and now
belongs to the branding row that describes the glyph fallback it draws. Worth recording how nearly I
got this wrong: my first reverse pass reported seventeen "undocumented" migrations, and every one was
a false positive from my own regex — `\b00050\b` cannot match inside `00050_outbox_claim_token.sql`,
because `_` is a word character. A gate that under-reports is more dangerous than no gate, so the
count was re-derived with the boundary fixed before anything was written down.

**The agreement is now enforced instead of reviewed.** `scripts/check-docs-references.py`, wired as
`make docs-check` and recorded in `CLAUDE.md` beside `go vet`, fails when a document that is edited in
place cites a path or a `Test*` name the tree does not have. It expands brace shorthand
(`store/{users,sessions}.go`), and it excuses a citation when the sentence around it says the artifact
was deleted or renamed — because "`MembersView.vue` deleted" is a correct sentence that must not
resolve. What it deliberately does NOT check: `docs/iterations/*.md`, `docs/checks/*` and
`docs/decisions.md`. Those are immutable or historical by contract; they record what was true when
they were written, and a later rename does not make them wrong. Excluding them is the same rule as §3
— you do not repair history, you correct it forward.

> **Correction of record for iter-0134.** Its §2 and its evidence list cite
> `TestPartitionAdoptionFencesConcurrentInserts`, a test that has never existed. The property it
> claims — adoption fenced against concurrent writes — is proven by
> `TestAdoptionReconcilesConcurrentUpdatesAtTheFence` (a holder parks the fence at the parent lock
> while a recompute upserts the same key and commits, so only the fence's delta sweep can reconcile)
> and by `TestAdoptionFenceIsBoundedAndAbortsSafely`. iter-0134 is immutable and stays as written;
> `docs/status.md` now cites the two real tests and points here.


## 3. What cannot be fixed, and why that is the correct outcome

Three sentences inside iter-0152 are wrong and will stay wrong. Correcting them would mean editing a
report that its own text declares immutable from `cc247ce` — the exact defect [401] named, committed a
second time and with the freeze as the excuse. So the disposition is a record, not a repair:

> **Correction of record for iter-0152.** Where iter-0152 says cumulative counters live in
> `docs/status.md` (lines 26, 54 and 235–241), read its §2.10 instead, which is authoritative: the
> cumulative scoreboard was DELETED by `cc247ce`, not relocated. The only surviving counter is the
> count of §2 finding sections in the frozen reports, and `docs/status.md` states no lifetime total.

That is the price of immutability, and it is worth paying: a reader who runs
`git show cc247ce:docs/iterations/iter-0152.md` and a reader who opens the file today see the same
bytes, and both find the correction here, in a document dated after it. The alternative — a clean
snapshot with an untraceable edit history — is worse in exactly the way the rule was written to
prevent.

## 4. Gates

Documentation, plus one new build-time gate and its wiring. No product code, no schema, no API, no
SPA, no generated file, and no Go or TS test changed in this arc: the non-documentation diff is
`scripts/check-docs-references.py` (new), `Makefile` (`docs-check` + `.PHONY`) and one command line in
`CLAUDE.md`.

- `go build -buildvcs=false ./...` — clean. Run as a guard that no product file moved, and it is not
  evidence about the change itself.
- `make docs-check` — passes, and is proven to FAIL by the three mutation probes recorded in §5. It
  ran on every commit of this arc, which is what AC-0153-4 and DoD-0153 require.
- The full Go suite, the frontend suite, the SPA build and `make dev-test` are NOT re-run for this
  arc and no result from iter-0151/iter-0152 is restated here as if it were. The last product commit
  is `6613e64`; everything after it is documentation and one spec test.
- The claims in §2 and §5 are checked by the commands in §5, run on the tree this report is committed
  with.

## 5. The checkers

Each command below IS the claim it is listed under. Together they are what Agent B contributes to a
documentation arc.

| Claim | Command | Expected |
|---|---|---|
| Both predecessors are byte-identical to their closure commits | `diff <(git show 475a0d3:docs/iterations/iter-0151.md) docs/iterations/iter-0151.md` and the same for `cc247ce`/iter-0152 | empty, both |
| No living document asserts the deleted contract | `grep -rn 'cumulative totals are stated once\|totals stated once' docs/status.md docs/traceability.md` | no match |
| No living document enumerates review rounds for this phase | `sed -n '5,20p' docs/status.md \| grep -o '\[[0-9]\{3\}\]'` and `grep '^| FR-021 phase 5 — alerting ownership' docs/traceability.md \| grep -o '\[[0-9]\{3\}\]'` | empty, both — while all three reports stay linked from each |
| The living index names the OPEN iteration | `sed -n '5p' docs/status.md` | names iter-0153, and names iter-0152 as CLOSED at `cc247ce` |
| The live checklist carries THIS iteration's own items | `grep -c '^| AC-0153\|^| DoD-0153' docs/status.md` (row starts only — the same string also appears in the previous-arc heading) | 5, every one `IN_PROGRESS` until the closing commit |
| Every path and `Test*` name in the LIVING documents resolves in the tree | `make docs-check` | `OK — every path and Test* name in the living documents resolves` |
| That gate can actually fail — probe 1 | append `` `internal/store/no_such_file.go` `` and `` `TestThisNeverExisted` `` to `docs/status.md`, run `make docs-check`, revert | both named, exit 1 |
| …and its one excuse is narrow — probes 2 and 3 | the same invented path with `deleted` 300 characters away, then adjacent | fails (exit 1), then excused (exit 0) |
| The spec no longer contradicts the code it specifies | `grep -n 'SUPERSEDED by phase 5' docs/specs/func-service-reliability.md` and `grep -c 'sla_targets_service_no_burn_chk' internal/store/migrations/00082_alerting_ownership.sql` | invariant 47 marked superseded; the CHECK is dropped by 00082 |
| The reverse direction holds: no code artifact is nameless | every migration by filename-or-number, every non-test Go file, every e2e spec and every Vue component appears in some document (§2.5) | `BrandMark.vue` was the only exception and is now in the branding row |
| The surviving counter is per frozen report and stops moving | `grep -c '^### 2\.' docs/iterations/iter-0151.md docs/iterations/iter-0152.md` | 15 and 10; this report's own count is stated here at closure, not before |

## 6. Disposition

Both [409] findings and all three [411] findings ACCEPTED, none contested. The 1:1 reconciliation in
§2.5 is the owner's instruction rather than a review finding, and it is the largest item in this arc.

iter-0152's closure and the product approval from round [399] are not reopened, and no product file is
touched. The forward-only instruction in [409] is followed literally: the frozen reports are read-only
from here, and every correction lands either in this report or in a document that is edited in place —
`docs/status.md`, `docs/traceability.md`, `docs/specs/func-service-reliability.md`,
`func-oncall-synthetic-pull.md`, `func-project-deletion.md`, `func-settings-members.md`, plus the new
gate and its wiring. The next finding — if there is one — belongs in this report while it is OPEN,
which is the whole point of having opened it.
