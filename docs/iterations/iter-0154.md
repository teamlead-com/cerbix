# iter-0154 — three sentences whose condition had already passed

Spec: `specs/func-service-reliability.md`. Decision records: none new — nothing here changes a
decision, and D-0163/D-0167/D-0168/D-0169 all stand as written.
Predecessor: **iter-0153**, closed at `d390e90` and immutable from that commit. This report exists
because three edits arrived AFTER that closure, and the contract sends them here rather than into the
frozen report — the rule iter-0152 §2.8 was written about.

> **Lifecycle: CLOSED by the commit that added this line, and immutable from it.** Its final `§2` count
> is **1** finding section. Anything found later belongs in `iter-0155`.
>
> It opens and closes in one arc because the whole arc is three sentences and one comment, there is no
> reviewer round to wait for (the reviewing agent ended during iter-0153 — see that report's lifecycle
> note), and a report kept open for a one-paragraph change is the surface iter-0152 was frozen to
> remove. `AC-0154-1` and `DoD-0154` therefore land as `DONE` in the same commit: they are true at
> every committed point, which is what AGENTS step 2 asks for — the finding it exists to prevent
> ([411] P1) was a CURRENT iteration with no live rows under it, not an arc that never had a
> half-finished state.

## 0. The plan, and who did what (AGENTS §Iteration Workflow)

No P0 and no P1: nothing here changes behaviour, and the discharge map of iter-0153 §2.7 still holds
every invariant it held before. Three P2 documentation defects of one class, found by the owner's
question "is service-reliability closed COMPLETELY?" and by taking that question to the files.

| # | Priority | Task | Outcome |
|---|---|---|---|
| 1 | P2 | The §12.2 burn card said "until phase 5 owns alerting" — phase 5 came and left the card alone | §2.1 |
| 2 | P2 | Two places said status-page impacts are absent "until phase 4 opts in" — phase 4 came and DECLINED | §2.1 |
| 3 | P2 | `store/servicereport.go` told a reader service burn is "REPORTING ONLY until phase 5" | §2.1 |

Role outputs:

- **Agent A (implementation).** One comment in `internal/store/servicereport.go`. The production diff
  is comment-only and §4 shows it in full, because "I only changed a comment" is a claim a reader
  should not have to take on trust.
- **Agent B (tests).** Nothing, and deliberately: a comment cannot be falsified by a test, and no
  behaviour moved. What stands in for a run is the diff itself plus `go build`/`go vet`/`gofmt`.
- **Agent C (docs + traceability).** `docs/specs/func-service-reliability.md` (three sentences,
  including invariant 59's text), this report, `docs/status.md`, `docs/traceability.md`.
- **Agent D (observability + ops).** Nothing. No metric, no readiness predicate, no alert.

## 1. What this arc makes true

**A document may not keep a condition that has already been decided.** "Until phase 5" and "until
phase 4 opts in" were honest when written and became misleading the moment those phases landed — one
because the phase arrived and changed nothing, the other because the phase arrived and said no.

## 2. What the closure question found

### 2.1 Three conditions that had already resolved, still written as pending

| Where | What it said | What is true |
|---|---|---|
| `func-service-reliability.md:1399` (§12.2 amendment) | "until phase 5 owns alerting, the displayed burn pair is fixed at 1h/6h" | Phase 5 owns alerting and did NOT touch this card. Service paging rides the operator's declared `burn_rules` on `sla_targets` (§16.4) — a different object, with its own windows, latches and generations. The reporting pair is still fixed at 1h/6h and still never alerts. |
| `func-service-reliability.md:1799` and invariant **59** (§19) | "absent from every public and internal status-page projection **until phase 4 opts in**" | Phase 4 arrived and DECLINED the opt-in (§15.0; the §14 non-goal list records it). The rule is unconditional today, and a later opt-in needs its own decision. As written, a reader would think phase 4 might still turn it on. |
| `internal/store/servicereport.go` | "Service burn is REPORTING ONLY until phase 5; these windows never alert" | Service burn DOES alert since phase 5 — through `burn_rules`, never through this pair. The comment sent a reader looking for the alerting path to the wrong object. |

None of the three changes behaviour, and none weakens the discharge: invariant 59 is still held by
`TestIncidentImpactsDetailOnlyAndNeverPublic`, and the reporting pair is still held by
`TestReportBurnIsBoundedBySealedCoverage` — only the prose around them moved. The reason to fix them
anyway is that all three are load-bearing for a reader: two tell you a rule might change, and the third
points at the wrong file when a burn alert fires in production.

This is the same class as invariant 47 (iter-0153 §2.5) and as the 28 stale `IN_PROGRESS` rows
(iter-0153 §2.6): **a statement conditioned on a future event, left standing after the event.** Three
arcs in a row have now found one, which says the class is worth a habit rather than another fix: when a
phase lands, grep the spec for the phase number before closing it. Recorded here rather than as a
process rule nobody reads.

## 3. Gates

- The production diff, in full — comment lines only, no code:
  `git diff -U0 internal/store/servicereport.go` shows five added `//` lines and two removed ones.
- `go build -buildvcs=false ./...` clean; `go vet ./...` clean; `gofmt -l` empty.
- `make docs-check` green: every path and `Test*` name in the living documents resolves, and all 91
  invariants + 24 §16.10 scenarios remain discharged (editing invariant 59's text does not change its
  number or its discharge row).
- The Go suite and the browser suite are NOT re-run and no earlier result is restated as this arc's:
  nothing executable changed. iter-0153's runs — 33 packages `-race` in hypertable mode, `store`/`api`/
  `scheduler` in declarative mode — belong to that report.

## 4. Disposition

Three P2 documentation defects, accepted and fixed; no product behaviour, no schema, no API, no SPA,
no test. FR-021 and NFR-016 remain `DONE` (D-0169) and §16.9 remains an explicit non-goal. There is
**no active iteration** after this commit; the next commissioned work opens `iter-0155`.
