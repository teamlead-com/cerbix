# iter-0161 — a production upgrade failed on PostgreSQL 14, and two claims were corrected

> **Lifecycle: OPEN.** No requirement. It closes when the corrections below are discharged.

`iter-0160` is closed and immutable, so findings that arrived after its closing commit go here.

## 1. The failure, and what my testing never covered

An upgrade to `v0.1.5-beta.1` on a production host running **PostgreSQL 14** applied migrations
`00061`…`00069` and died on `00070`:

```
ERROR 00070_owner_fk_and_maintenance_provenance.sql: failed to execute SQL query
  "... ON DELETE SET NULL (escalation_policy_id) ..."
  ERROR: syntax error at or near "(" (SQLSTATE 42601)
```

Five migrations (`00070`, `00080`, `00081`, `00082`, `00084`) use the **column-list**
`ON DELETE SET NULL (col)` form, which arrived in PostgreSQL 15. The plain form cannot substitute: on a
composite FK it nulls EVERY referencing column including the NOT NULL `project_id`, which is the exact bug
`00070` exists to fix.

**Reproduced rather than reasoned about.** A throwaway `postgres:14-alpine` (14.24) with the guard removed
fails exactly as production did, on the same file, with the same SQLSTATE. That is what turned a hypothesis
into a diagnosis, and it also proved the failure is SYNTAX rather than data — no cleanup of the operator's
rows was needed.

**Why nothing caught it.** Every test and both CI matrix legs run PostgreSQL 16, every image and document
says 16, and the test helpers always migrate **from zero on an empty database**. The requirement was real
from the day `00070` landed and was never stated where an operator would see it, never checked by the code,
and never exercised against an older server.

## 2. The guard, and a bug in its own first version

`Migrate` now reads `server_version_num` and refuses a server older than 15, naming the version, the
requirement, the five migrations and the fact that nothing was applied. Verified on 14.24: the refusal
arrives with **zero** tables created, `goose_db_version` included.

The first version of that check sat after `pg_advisory_lock` but before the unlock `defer`, so its early
returns left the session lock held and relied on `db.Close()` tearing the backend down. It now runs BEFORE
the lock is taken: reading a GUC needs no lock, so the question disappears instead of being handled.

`TestMigrateRefusesAServerTooOldForItsOwnSchema` pins it against whatever database it is given — a refusal
naming version, requirement and "Nothing has been applied" on an older server, and no refusal at all on
15+, because a guard that blocks a supported server is worse than none.

## 3. A partial upgrade has one sharp edge, and it is documented now

`00070` is transactional and rolled back, so the database sits consistently at `00069`. But `00065` did
apply, and it sets `monitors.slug` NOT NULL with **no default** — while a pre-`00065` binary does not write
that column. A system left in this state on the OLD binary keeps serving, alerting and recording, and
**cannot create a monitor**. The runbook now carries that fact and the one-line relaxation for operators
who must keep creating monitors before the upgrade completes.

## 4. A stale UI claim, found by an operator's screenshot

The service page footer said availability, the error budget and the burn rate "arrive with the next
iteration" (written 2026-08-16, before phase 2). They arrived in iter-0144 and the page already renders
them from the report. What is actually missing on a fresh service is sealed facts and a declared objective —
which the page says elsewhere, correctly, and now says here too.

This is the third stale-claim defect in three iterations (`openapi.yaml`'s `reliability` field, the README's
positioning, this footer). The pattern is the same every time: a sentence written truthfully at one moment
and never revisited when the moment passed. `docs-check` cannot catch these — they are not broken
citations, they are true-then statements.

## 5. A blank dropdown an operator photographed, and the cast that hid it

Creating a status-page component from a Service showed an EMPTY option in the picker. The invisible half
was worse than the visible one: the option carried **no value** either, so the component could not be
created at all.

One cause for both. `GET …/projects/{id}/services` answers `ServiceSummary` — the service WRAPPED with its
rollup counts — and the view read `sv.name` / `sv.id` directly. What kept the compiler quiet was an
`as Service[]` cast on the load, which is a promise to TypeScript that the shape is something it is not. I
hit the same endpoint in `IncidentsView` two iterations ago and got a clean type error there, precisely
because I wrote no cast.

**The test fixture had the same wrong assumption**, answering the flat shape, which is why six passing
tests never saw it. Fixing the fixture to the real shape and asserting the option's value AND label pins
both halves; reverting the read makes it fail with "the dropdown cannot bind a service".

Three of my own fixture mistakes on the way to that test, all mine and all in the test: the picker only
exists once the ADD form's source is Service, the form itself is gated on `canManage`, and I deleted the
`mockLoads()` call while restructuring so nothing rendered at all. Worth recording because the pattern
repeats: when a UI test cannot find an element, the first suspect is the state the test failed to set up.

## 6. Correction of record — a scope error in my own release note

The `v0.1.5-beta.2` tag message ends with an "STILL OPEN" block naming *"the production decision between
upgrading PostgreSQL and a compatibility branch of those five migrations"*. The owner rejected that, and
correctly: whether one operator's database host gets upgraded is **their infrastructure decision**, not an
open item of the software. A release tag describes the software.

Two things went wrong from one confusion:

1. **The tag** carried one deployment's operational state into an artefact meant to describe a version. The
   product-side fact is complete and needs no caveat: the schema requires PostgreSQL 15+, the requirement is
   enforced before the first migration and documented in three places.
2. **`DoD-0161`** made this iteration's Definition of Done depend on an action nobody here controls. The
   iteration's work — the guard, the documentation, the two UI fixes — was finished. The row is now DONE.

What survives as a legitimate PRODUCT question, recorded as a question rather than as a blocker: should
cerbix support PostgreSQL 14? It would mean emulating the column-list `ON DELETE SET NULL` semantics with
triggers across five migrations and carrying that forever, so it needs its own requirement and a decision,
not a footnote in a DoD row.

The tag itself is left as pushed. Rewriting a published tag to improve its prose would be worse than the
prose: anyone who already fetched it would hold a different `v0.1.5-beta.2` than the remote. The record is
corrected here instead, which is what this file is for.

## 7. Documentation described a race the lock had already removed (issue #40)

`teamlead-com/cerbix#40` asked for migrations to stop running automatically for every role, on the
grounds that unexpected migration execution is undesirable. The capability the author wanted already
exists: `cerbix migrate` is a standalone command, and running it before the roles leaves nothing
pending for their startup call to apply.

The premise came from our own text. `docs/overview.md` §2.2 still said:

> Roles auto-apply migrations at startup, and on the first deploy of a new migration, simultaneously
> starting roles race for it (goose does not take a cross-process lock) — one applies it, the others
> fail with `relation already exists`.

`store.Migrate` has held a session `pg_advisory_lock` for the whole goose run since `ed652d0`
(2026-08-08). The paragraph dates from the initial commit three days earlier and was never revisited,
so for seventeen days the documentation warned operators about a failure mode the code had already
closed. Corrected in `75123c2`: the one-off `migrate` stays recommended, now for the reason that
actually applies (keeping a schema change a deliberate act), and `D-0088` carries a superseding note
rather than being rewritten.

What remains open, and was put back to the reporter rather than assumed: whether the requirement is a
runtime database user with **no DDL rights at all**. That is the one thing the recommended workflow
cannot give, since nothing enforces the operator's discipline. Also worth fixing regardless of the
answer: the migrate call keys off `database.dsn` rather than the role, so a `worker` sharing one config
file runs migrations even though `servicesForRole` gives it no database work at all.

## 8. A partly unmeasured hour rendered green

An operator's screenshot of the Reliability card, 24h window: availability `99.967%`, coverage `100%`,
storage contiguous, and one hourly tick painted solid red. `99.967%` of 86400 seconds is about **28
seconds** of bad time, so the tick claims 3600 seconds where 28 exist.

**That part is not a defect, and my first reading of it was wrong.** §9.1 fixes the display rule and
states its rationale in the spec itself: the rule is *deliberately pessimistic — a one-second outage
colours the whole tick — so the picture never hides an outage, while the number underneath stays
exact.* The screenshot is the design working. Recorded here because I called it a bug before reading
the paragraph that governs it.

Reading that paragraph found the real fault. §9.1 orders the tick
`bad > 0 → BAD; else unknown > 0 → UNKNOWN; else good > 0 → GOOD; else EXCLUDED`, and
`ServiceReliability.vue` evaluated `bad → good → excluded → unknown`. Unknown outranking good is the
half that matters: a step holding both measured-good and unmeasured time rendered **green**, which is
the timeline claiming to have watched an hour it only partly watched. The pessimism the spec spends a
paragraph defending was inverted in exactly the case where it earns its keep.

Why the suite missed it: the one fixture exercising an unknown tick carries unknown time **alone**, so
both orders satisfy it. A rule about precedence cannot be pinned by an input with nothing to precede.
The new regression uses mixed durations per step and asserts the sequence `['unknown', 'bad',
'excluded']` — mostly-good-with-a-sliver-unmeasured, bad-plus-unknown, and excluded-with-no-good.

**Mutation evidence:** restoring the previous order fails the regression with
`expected [ 'good', 'bad', 'excluded' ] to deeply equal [ 'unknown', 'bad', 'excluded' ]` — the green
tick, reproduced on demand.

Left alone deliberately: the whole-tick colouring itself. It is spec text with a written rationale, so
replacing it is an amendment and a product decision, not a fix. Recorded as a question for whoever takes
it up: at hourly granularity the rule overstates a 28-second outage by roughly 126×, and a proportional
fill with a minimum visible sliver would keep "never hide an outage" while dropping the overstatement.
The durations needed for it (`GoodUs`, `BadUs`, `UnknownUs`) already reach the component.

## 9. Resolved was terminal only until two writers overlapped

An external review of `27c63f7` found no P0 and six P1s. This is the first of them discharged, and it
is the one the other two service-incident findings rest on.

`addIncidentUpdate` read the incident, refused if `Status.Terminal()`, and then called
`AddIncidentUpdate`, which wrote `UPDATE incidents SET status = $2 ... WHERE id = $1` with no guard
and no row lock. The read and the write are in different transactions, so the interleaving is
ordinary rather than exotic:

1. request B reads `investigating` and is admitted by the pre-check;
2. request A resolves the incident and commits;
3. B's unguarded write lands, and the incident is `investigating` again.

`resolved_at` stays stamped, because the CASE only fills it on the way IN to resolved. The result is a
row no legal sequence produces: an open incident that claims a resolution time. For a service
auto-incident it is worse than untidy — the row re-enters `incidents_service_open_auto_idx`, whose
whole job is to admit one open auto-incident per service, so the NEXT outage's `ON CONFLICT DO
NOTHING` finds a squatter and opens nothing.

The guard now lives in the write itself: `WHERE id = $1 AND status <> 'resolved' RETURNING`, and it
runs BEFORE the timeline insert so a refusal leaves no trace at all — no update row, no lifecycle
event for a change that did not happen. Zero rows is classified rather than collapsed: the incident is
missing (`ErrNotFound`) or it is terminal (`ErrIncidentTerminal`, new), and the handler answers 404 or
400 instead of 500. The pre-check stays, demoted to what it always was — a fast, friendly answer that
does not hold under concurrency.

**Mutation evidence:** removing `AND status <> 'resolved'` fails the regression with
`write against a resolved incident: <nil>, want ErrIncidentTerminal`.

The regression asserts the whole refusal, not just the error: status still resolved, `resolved_at`
unmoved, timeline count unchanged, `incident_event` count unchanged, and a missing incident answered
`ErrNotFound` rather than the same error as a terminal one.

This is step one of the fix order agreed with the reviewer. The primitive is now the guarded write;
what follows is moving the service auto-open and auto-resolve THROUGH it (which also discharges the
missing `incident_event` on the service path), then reason-aware resolution on the lifecycle closes.
The class those three share was already seen once in this phase, on the outbox topic whitelist: a new
path built BESIDE an existing guarantee instead of THROUGH it.

## 10. The service incident announced itself to nobody

Second of the review's P1s, and the one the first step made cheap.

`incident_event` is the topic that reaches incident webhooks and the confirmed subscribers of every
status page surfacing the project (D-0041, `decisions.md:986`). `OpenServiceIncidentTx` enqueued its
correlation attempt and nothing else; `ResolveServiceIncidentTx` enqueued nothing at all. So a service
outage was visible in the database and in the UI while the people who explicitly asked to be told
heard nothing, in either direction.

The framing that makes it a defect rather than a design choice: it is not manual-versus-automatic.
Monitor auto-incidents are opened by the reconciler through `CreateIncident`
(`ingest/reconciler.go:102` → `incidents.go:117`), which has always enqueued the event. Two AUTOMATIC
paths therefore disagreed about whether an outage is announceable, and which one you got depended on
whether the outage was expressed as a monitor incident or a service incident. The service's own
`service_alert` does not cover the gap: it pages the service's RECIPIENTS, a different audience from a
status page's subscribers.

Both halves now enqueue in the transaction that carries the fact. Two details are load-bearing:

- the opening enqueue sits AFTER the `ON CONFLICT DO NOTHING` branch, so an evaluator that loses the
  open race returns `created == false` and announces nothing. One opening, one announcement;
- the resolve's `UPDATE ... RETURNING` now hands back the whole row and the timeline insert returns
  its own, so the closing payload describes the incident as it IS and carries the update it wrote,
  matching the shape `AddIncidentUpdate` publishes. A second read would have been a second opinion.

**Mutation evidence**, one per half: dropping the open enqueue fails with `after the open, events = [];
want exactly one incident.opened for <id>`; dropping the resolve enqueue fails with `after the resolve,
1 events, want 2`. The regression also pins the two silences that must stay silent — a losing racer and
a no-op second resolve each announce nothing.

## 11. The live occurrence ended in halves, and the incident was the half left behind

Third of the review's P1s, and the largest: it is the one that leaves an operator somewhere they
cannot leave without SQL.

Every lifecycle path that ends a live announcement — ownership switched off, a `page_on` edit that no
longer covers the announced state, the same two through a MaC apply, and deleting the service — closed
the health EPISODE and stopped there. The auto-incident stayed open, and the only thing that resolves
one is `ResolveServiceIncidentTx`, whose single caller was the evaluator's recovery path
(`servicealerteval.go:234`). The evaluator's slice is `WHERE s.owns_paging`, so after ownership is
switched off the service is never looked at again: the incident could not be resolved by ANY code
path, not merely "not until it recovers".

Two consequences, both verified rather than reasoned about:

- delete the service mid-outage and the incident survives with `service_id` nulled by 00084's
  `ON DELETE SET NULL (service_id)`, an open project incident about a service that no longer exists;
- disown a service mid-outage and re-own it later, and the survivor still occupies
  `incidents_service_open_auto_idx` — so the NEXT outage's `ON CONFLICT DO NOTHING` opens nothing and
  the second failure has no incident at all.

The resolution now lives inside `closeServiceEpisodesTx`, the one function all five call sites already
go through, and fires when the close actually ended a HEALTH episode. Putting it there rather than at
the call sites is the whole point: a lifecycle path added later inherits the ending instead of
re-earning it, which is the property this phase has now failed to have three times. BURN closes never
touch the incident — `burn_disabled` and `rule_removed` say nothing about whether the service is down.

The timeline entry names the reason and denies the recovery in words, because `resolved` on a status
page is otherwise read as "it is fine now": *paging ownership was turned off for this service, so
nothing evaluates it any more. This is not a statement that the service recovered.* The ending is
announced through §10's event, so subscribers learn it ended.

**Mutation evidence, both directions of the rule.** Removing the resolution fails with `incident is
"investigating"/resolved_at=<nil> after disowning`. Making EVERY close resolve fails with `incident is
"resolved" after a BURN close; a budget alert is not the outage record`.

**A test defect found by that second mutation, and worth recording as itself.** The burn regression
passed the mutation on its first writing — because the fixture had no open burn episode, so the close
loop never ran and the assertion held for the wrong reason. It is the same vacuity §8 found in the
unknown-tick fixture: a rule about *which* case is excluded cannot be pinned by an input that never
reaches the case. The fixture now opens a real burn episode with its latch and asserts one was closed
before checking what survived.

## 12. The postmortem could name a declaration that had not started governing

Fourth of the review's P1s. `snapshotServiceMembersTx` resolved the declaration itself, with
`state = 'effective' ORDER BY effective_at DESC LIMIT 1` — and no time bound, while the nine other
sites that ask this question all carry `effective_at <= now()`.

The reason those nine carry it: `service_definition_revisions.state` defaults to `'effective'` at
INSERT (00064), so a revision is 'effective' from the moment it is authored and its `effective_at`
sits on the coming bucket boundary. The reviewer framed this as scheduled future revisions, which
understates it — every ORDINARY edit opens the window. An edit at 12:00:30 leaves until 12:01 in which
"the latest effective revision" and "the revision governing now" are different rows, and an incident
opened in that gap took its members from a declaration that was governing nothing, while the outage
that opened it had been computed from the previous one.

The evaluator already resolves the governing revision for the verdict itself (`servicealerteval.go:99`,
`effective_at <= $2` against the snapshot instant). That exact id is now passed down through
`OpenServiceIncidentTx`, and the snapshot resolves nothing on its own. A NULL revision snapshots
NOTHING rather than reaching for the newest one: no declaration governs, so there is no membership to
name, and the fallback would be the same defect one level down.

**Mutation evidence:** restoring the local unbounded resolution fails with `the snapshot named
[checkout-dns] — it took a declaration that governs only from the next boundary`. The fixture asserts
the future revision really is `state = 'effective'` before relying on it, so the test cannot quietly
stop exercising the window it was written for.

## 13. The ladder an incident climbs is now the one it started with

Fifth of the review's P1s, and the only one that needed a schema change.

`AdvanceEscalations` joined `services.escalation_policy_id` LIVE and timed every step from
`incidents.started_at`. Attach a policy to a service whose incident has been open for two hours and
the next pass finds every delay already elapsed, so the whole ladder pages at once — for an outage
nobody had asked it to page for. `func-service-escalation.md` §8 lists exactly that as a NON-GOAL, so
the code was contradicting the spec rather than filling a gap in it.

**Pinning the policy id would not have been enough**, and this is the part the review's plan did not
cover: `escalation_policies` keeps its ladder in a `steps` jsonb column and carries no version. An
in-place edit therefore moves the ladder under every open incident that names it — the same defect
reached by a third route, and the one an operator is least likely to expect. So 00085 snapshots the
STEPS: `incident_escalation_snapshots`, one row per incident, with the policy id kept for the record
and deliberately not a foreign key, exactly as the member snapshot outlives its declaration.

Three behaviours result, and only the middle one is new in kind:

- **attach after the fact** — no snapshot exists, the incident is not selected, nothing pages. The
  non-goal is now enforced by a join rather than by prose;
- **edit or replace the policy mid-incident** — the incident climbs the ladder it froze;
- **detach the policy entirely** — escalation STOPS. The snapshot freezes what the steps ARE; it does
  not make a ladder unstoppable, and detaching is an operator saying "stop paging through this".

That third rule is not a decision I made: an existing regression
(`TestAServiceLadderIgnoresAHumansIncidentAndAPolicylessService`) failed when my first version dropped
the live attachment check, which is the suite doing its job. The live `escalation_policy_id IS NOT
NULL` is back alongside the snapshot join.

`CreateIncident` takes the same snapshot when an incident arrives with a service anchor. No production
caller does that today — the evaluator uses `OpenServiceIncidentTx` — but an incident that reached the
table through the other door would silently never escalate, and "the other door forgot" is the shape
of three defects already fixed in this iteration.

**Mutation evidence, one per half.** Reading the live policy instead of the snapshot fails with `the
open incident paged the REWRITTEN ladder's target; it must climb the one it froze at open`. Making the
snapshot join a LEFT JOIN fails the retroactive-attach regression.

## 14. The successor picker read the wrong object, and a cast is why

Last of the review's P1s, and the cheapest — but the interesting part is how it survived.

`GET /projects/{projectID}/services` answers with SUMMARIES: the service nested under `service`,
beside the revision and coverage fields a picker has no use for. `MonitorDetailView` asserted the
response was `Service[]` and read `sv.id` / `sv.name` off the row, where both are undefined. The
dropdown rendered a list of blank options and would have posted `undefined` as the successor id, and
the "current successor" label beside it was empty for the same reason.

The frontend uses `openapi-fetch` with the generated `paths`, so `res.data` was ALREADY typed
correctly. The only thing standing between this defect and a build failure was the cast:

```ts
services.value = (res.data as components["schemas"]["Service"][] | undefined) ?? [];
```

That makes the fix a class fix rather than a line fix, which matters because this same defect was
repaired once already in this iteration's §5 for the status-page picker. Mapping instead of asserting
puts `vue-tsc` back in charge of a shape the SPA does not own. A sweep found exactly one other cast of
this family on the same screen — `(res.data as { monitor: Monitor }).monitor` — and removing it
immediately produced a real type error: `monitor` is OPTIONAL in `CompositeConversion`, so the cast had
been asserting a guarantee the API does not give. It is a checked access now.

The regression pins the API's real shape in the fixture, not a convenient one: a `ServiceSummary` row
with `service`, `revision`, `context_members`, `sli_members`, `epoch_seq` and `repairing_count`. A
fixture shaped like a bare `Service` would let the defect return invisibly.

**Mutation evidence:** restoring the cast fails with `expected undefined to be 'svc1'` — the blank
option, reproduced.

## 15. Documents that described a product from before it shipped

The review's P2 list, discharged, plus the gate that makes the class fail loudly next time.

Four documents were telling a reader something that stopped being true:

- `func-service-incidents.md` and `func-service-escalation.md` opened with **"Nothing is implementable
  until this file has been reviewed"** while FR-022, FR-023 and both their NFRs are DONE and shipped.
  Somebody reading the spec to learn what cerbix does was told the feature did not exist. Both banners
  now declare the delivery, name the iteration and the acceptance rows, and keep the old gate visible
  as history rather than deleting it;
- `func-sla-sli.md` still listed "project-level SLO targets" under *Open questions / next* — delivered
  as AC-0155-3 with migration 00083;
- `func-audit-gaps-2.md` still recorded `sla_targets.project_id` as dormant schema and a NON-GOAL,
  reserved for "a real feature, not a gap fix". It became exactly that feature. The line carries a
  superseding note rather than being rewritten, the same convention D-0088 got in §7.

`DoD-0155` also stayed IN_PROGRESS with all four of its items discharged. Its evidence pointed at
iter-0155 §0, whose table reads "UI awaits mock approval" for the instance audit — true when written,
and the mock was approved and the panel built shortly after (AC-0155-1 carries the UI evidence). The
row is DONE now and cites the acceptance rows rather than the frozen table.

**The gate.** `make docs-check` verified citations and table shape and could not see any of this, so it
grew a fifth check: a spec whose OWN requirement row is DONE in `status.md` may not carry an
unbuilt claim ("nothing is implementable", "awaiting review", "awaiting a UI mock") in its first thirty
lines. Two details keep it honest rather than annoying — it reads the REQUIREMENT's row and not the
acceptance rows, because an AC row names its requirement only when its prose happens to; and a banner
that declares `STATUS: DELIVERED` may quote the claim it replaced, since the correction reads better
with the old words in it.

**Mutation evidence:** restoring both banners fails with `docs/specs/func-service-escalation.md:3
[stale-status] says unbuilt while FR-023, NFR-018 is DONE in status.md` and the matching line for
FR-022.

What is NOT fixed here, and is left as the reviewer's open question rather than a defect: the migrate
call keys off `database.dsn` rather than the role, so a `worker` handed a shared config file runs
migrations although `servicesForRole` gives it no database work. It waits on the same answer as issue
#40 — whether anyone needs a runtime database user with no DDL rights.

## 16. The snapshot table shipped without an upgrade path, and without a tenant

An adversarial review of §9–§15 found six P1s. These three are mine, in code committed hours earlier,
and all three were confirmed independently before being fixed.

**The migration had no backfill.** 00085 created `incident_escalation_snapshots` empty while the
selection that reads it INNER JOINs it, so every service auto-incident already open at upgrade time
loses its ladder — permanently, silently, with the service still owning paging and its policy still
attached. An upgrade performed DURING an outage is exactly when that lands. The backfill now runs in
the migration itself, so the table is never observable in the empty-but-required state: after that
transaction, "every open service incident that can escalate has a snapshot" holds for old rows and new
ones alike.

**The table had two independent foreign keys where it needed one composite.** `incident_id →
incidents(id)` and `project_id → projects(id)` written separately make each column individually valid
while the PAIR is not, so direct SQL attaches project B's ladder to project A's incident. The row that
decides who gets paged is the wrong one to leave un-tenanted, and the `(id, project_id)` key this now
points at has existed since 00080 for exactly this purpose.

**§12's fix weakened the member snapshot.** Passing the revision id down (right) also made the query's
only predicate a value from outside (wrong): it filtered on `revision_id` alone and no longer checked
the service and project it was handed. Today's single caller passes correct values, which is an
argument for checking them rather than against. The revision is now joined and scoped.

**A test defect of my own, again caught by a mutation.** The first upgrade regression re-typed 00085's
backfill statement and ran that — so it would have passed unchanged after somebody deleted the
original. It now migrates a throwaway database to 84, opens a service incident there the way a running
installation would have one, migrates to head and requires the snapshot. That is the third time in
this iteration a mutation has exposed a test that passed for the wrong reason (§8's unknown tick,
§11's burn fixture, this one), and the common shape is a fixture that never reaches the case it
claims to cover.

**Mutation evidence.** Deleting the backfill from the migration fails with `the incident carried
across the upgrade has no ladder snapshot, so it will never escalate again: sql: no rows in result
set`. Splitting the composite key back into two fails with `an incident of one project accepted
another project's escalation snapshot`. The second mutation had to be redone: the first attempt left a
dangling comment and died on a SQL syntax error, which is not evidence about behaviour.

## 17. The backfill's mirror image, caught before it shipped

The review's first response to §16 was that a naive backfill trades one upgrade defect for its
opposite, and it was right. Recorded here because the fix arrived within the hour and the reasoning is
the interesting part.

Steps are fired for every offset that has ALREADY elapsed, all of them in one pass
(`escalation.go`'s `for step < len(p.Steps)`), and the offsets were measured from `incidents.started_at`.
So a snapshot backfilled onto a two-hour-old incident would empty its whole ladder into the on-call
rotation on the first pass after the upgrade — the same retroactive paging §13 had just made
impossible for new incidents, reintroduced through the migration.

It cannot be narrowed by looking at the data. WHEN a policy was attached to a service is recorded
nowhere, so an incident that opened with no policy at all is indistinguishable from one that had this
policy from the first second. Any rule that tries to page for the elapsed time is guessing.

The snapshot therefore carries `due_base`, the instant its offsets are measured from: the incident's
own `started_at` for a row written at open time, which is the behaviour the ladder has always had, and
the MIGRATION's instant for a backfilled row. A carried-over ladder starts at the upgrade — its first
step fires on the next pass, its later steps wait their real delays. Monitor incidents set `due_base`
to their start explicitly, so the arm FR-023 promised not to touch keeps measuring exactly where it
did.

**That is an EXCEPTION, and the first draft of this section wrongly called it "the same direction §13
chose".** It is not the same rule: a native attach-after-open never escalates that incident at all,
while a carried-over one does, prospectively. The two are justified differently — §13 refuses because
the operator's intent is knowable and was "not for this incident"; this refuses only for the ELAPSED
time, because intent is unknowable, and prefers continuity to silence for the time that follows. It
applies to exactly one population, the rows the backfill wrote, and cannot recur. Recorded as
**D-0175** rather than left as a sentence in a report.

**Mutation evidence, both statements of the rule.** Making the backfill copy `i.started_at` fails the
column assertion; making the ladder measure from `startedAt` again fails the behavioural one with `the
first pass after the upgrade fired 3 steps, want exactly its first` — the burst itself, reproduced.

## 18. Three follow-ups the review found in the follow-ups

**The upgrade test migrated to HEAD while claiming to be about 00085.** `goose.UpContext` applies
everything, so a future migration touching these rows would have been exercised here under a name that
says otherwise, and the failure would read as this test's subject. It stops at 85 now.

**The scoped member read answered "nothing" when it meant "not yours".** §16 fixed the missing tenant
binding by joining the revision inside the members query — which makes a foreign revision produce zero
rows, and zero rows are then stored as `members: []`. That is a false statement in the incident record,
not a refusal: a postmortem reads it as "the declaration named nobody". Ownership is now established in
its own statement first, and its failure is an error. The regression asserts the refusal names the
ownership failure rather than merely erroring.

**The `due_base` rule is an EXCEPTION and is now recorded as one (D-0175).** The review accepted the
mechanism and rejected the framing, correctly: §17's first draft called it "the same direction §13
chose", and it is not the same rule. A native attach-after-open never escalates that incident; a
carried-over one escalates prospectively. Both refuse to page for elapsed time, but §13 refuses because
intent is knowable and was "not this incident", while D-0175 refuses because intent is UNKNOWABLE and
prefers continuity to silence for the time that follows. Every place that promised "from the incident's
start" now names the exception: `domain.EscalationStep`, the two comments in `escalation.go`, and
AC-0157-1's row. iter-0157 is closed and immutable, so that correction is forward-only.

**Mutation evidence:** returning the ownership check to the members join fails the refusal regression.

## 19. Forward-flowing was written down and enforced at one step only

`docs/decisions.md`'s incident section says the lifecycle is forward-flowing toward Resolved. The
store enforced the LAST step of that — §9 made `resolved` terminal against concurrent writers — and
nothing anywhere enforced the rest. `monitoring → identified` and `identified → investigating` were
accepted through the ordinary API, sequentially, no race required. The public timeline would then read
as though the operators had un-diagnosed the outage.

The race made it worse rather than causing it, and it had its own mechanism. `addIncidentUpdate`
filled an omitted status from the incident it had read a moment earlier — `body.Status =
string(inc.Status)`, in a different transaction — so a plain comment published an opinion about the
lifecycle that was true when the screen loaded. Landing after somebody else's transition, it reverted
it silently. The SPA did the same thing one layer up (`composer.status || incident.value?.status`),
and `openapi.yaml` has said `Omit to keep the current status (a plain comment)` the whole time: the
contract was right and both implementations were materializing around it.

Three rules now, and their ORDER is the invariant:

1. **Lock first.** The incident row is taken `FOR UPDATE` before anything is decided. Every rule below
   is about the row's current status, and a rule evaluated against a value read outside the lock is a
   rule about the past.
2. **Then read the clock.** `now()` is the transaction's START, so a writer that waited on the lock
   stamps its timeline entry with a moment before the update it is following, and
   `ListIncidentUpdates` orders by `created_at` — the record would claim a sequence the database never
   had. One `statement_timestamp()`, in its own statement after the lock, is threaded through the
   incident, the timeline row and the outbox event.
3. **Then write**, with the monotonicity rule applied to the locked row: forward or staying put is
   legal, backward is `ErrStatusRegression` (400). An empty status means "whatever this lands on" and
   is resolved HERE, never by the caller.

`IncidentStatus.CanFollow` owns the ordering in the domain, where the lifecycle is described. The SPA
sends no status for a plain comment.

**Mutation evidence, one per rule.** Allowing backward ranks fails with `monitoring → identified:
<nil>, want ErrStatusRegression`. Resolving an empty status to a constant instead of the locked row
fails the comment regression. Swapping `statement_timestamp()` for `now()` fails the waiter regression
with `the timeline entry is stamped …, before the lock was released at …`. The SPA echoing its loaded
status again fails with `expected { status: 'identified', … } to deeply equal { body: 'adding a
note' }`.

The waiter regression holds the INCIDENT row, not the service row — a distinction the review insisted
on, and correctly: `statement_timestamp()` is the start of the current statement, not a clock that
knows about waiting, so the instant is only post-linearization if every participating lock is already
held when it is read.

**Not fixed here, and named rather than left implied:** delivery ORDER. One instant fixes what the
records say; it does not make the outbox hand `incident.opened` to a worker before `incident.resolved`.
`ClaimDueOutbox` returns `UPDATE … RETURNING` with no `ORDER BY`, and `incident_event` carries no
per-incident sequence the way `service_alert` does. That is a real gap, it predates this arc, and §10's
claim about a paired lifecycle should be read as "both events are enqueued in the transaction that
causes them", never as "they are delivered in that order".

## 20. Finishing §19, including the parts its own tests could not see

The review read §19 while it was being written and named five things it did not cover. All five were
right, and two of them are about the tests rather than the code — which is the more useful half.

**The waiting-writer regression slept instead of waiting.** `time.Sleep(600ms)` proves nothing about
whether the goroutine has begun its transaction: under load it can reach the lock after the release,
and then a `now()` mutant passes because there was no wait to be early about. The package already had
`waitForLockWait`, a `pg_stat_activity` barrier, and the test uses it now — the release instant is read
only once a backend is genuinely blocked.

**The plain-comment regression could not kill the handler's materialization**, because it called the
store directly with an empty status. The defect lived in the handler, so the regression has to go
through it: `internal/api` now posts a body with no status and asserts the handler passed the intent
down untouched. Its mutation — restoring `body.Status = string(inc.Status)` — fails with `the handler
passed status "identified" for a comment that carried none`.

**The refusal was asserted by its outcome only.** A backward transition now also has to leave no trace:
the timeline count and the `incident_event` count are checked after each refused regression, not just
the final status.

**The event was still scheduled on the transaction's clock.** `outbox_events` defaults
`created_at`/`next_attempt_at` to `now()`, and `next_attempt_at` is what the claim orders by — so an
event caused by a later action could come due before the one it followed. `enqueueOutboxAtTx` states
the instant instead, and only the callers that own a post-lock one use it: changing the default would
re-time paths that never take a row lock. This does NOT fix delivery order (§19), it removes a
knowingly inverted schedule.

**`AcknowledgeIncident` had the identical defect one function over.** A single UPDATE looks atomic and
is, but its `now()` is fixed when its transaction began, so an acknowledgement queued behind a timeline
update stamped the incident's `updated_at` before the update it waited for — walking the row's own
modification time backwards. Same contract now: lock, post-lock clock, guarded write, with its own
waiter regression.

**And one new defect, found by the review in the same area.** `CreateIncident` accepts an initial
status of `resolved` — the API and the new-incident form both offer the whole list, so recording a past
outage is a supported act — and did not stamp `resolved_at`. Such an incident falls out of BOTH
status-page lists at once: the active one filters `status <> 'resolved'`, the recent one `resolved_at IS
NOT NULL`. The operator writes it down and it appears nowhere. It is stamped at creation now, and an
open incident still carries no stamp, because the stamp means "reached Resolved" rather than "was
written".

**The UI stopped offering steps the server refuses.** Both screens listed the whole ladder, so from
`monitoring` an operator could press `investigating` and get a 400 — or, in the list view, silence,
since `setStatus` swallowed the error. One shared `forwardStatuses` helper now returns the current
status and the ones after it; resolving stays its own action with its own confirmation.

**Mutations:** the handler materialization (above); `now()` for the acknowledgement's clock; offering
the whole ladder again fails the picker regression.

## 21. A service opened incidents nobody could be told about

Found by the review, outside the commits it was reviewing and inside the FR-022 behaviour they rest
on. FR-022 invariant 5 reads "opens an incident ONLY on a live onset, only while that signal's
coverage is ARMED", and the evaluator checked exactly one of arming's five conditions —
`owns_paging`, which the slice already filters on. Two of the others can be false at the moment a
service decides to speak:

- **no route.** Ownership on, a pageable DOWN, no enabled channel and no populated schedule: the
  announcement reaches nobody, `activeLiveDelegationSQL` says DIS-ARMED through its routable clause,
  and the members are correctly paging for themselves. On top of that the service opened an incident
  — the thing FR-022's own test comment calls worse than opening none;
- **no governing declaration.** Before the first revision takes effect, `revision_id` is NULL, the
  verdict was computed from nothing, and delegation is dis-armed for the same reason.

**The latch made it permanent, and that is the worse half.** Emitting nothing while advancing
`live_firing` and `emitted_state` leaves the service looking announced, so restoring the channel
produces no edge and the outage is never paged at all. The silence outlives its cause.

Onsets are now withheld unless a route resolves and a declaration governs, and a withheld onset does
NOT latch: the next pass sees the same candidate, the same streak, and announces the moment it may.
CLOSES are never withheld — §16's polarity rule — because an announcement already made must be able
to end whatever the route looks like now. Withheld onsets are counted separately as `Unroutable`,
since silence that means "nobody could be told" must not read as "nothing was wrong".

The existing invariant-5 regression only toggled `owns_paging`, which is why four conditions went
unexercised. Two behavioural tests now cover the route and the declaration, and the route one carries
the recovery: take the channel away, watch nothing be announced, give it back, and the SAME outage
opens its incident.

**Mutation evidence.** Dropping the routability gate fails with `an UNROUTABLE service announced
{… Onsets:1 … IncidentsOpened:1 …}`. Withholding the announcement while still latching fails with
`the service latched (firing=true emitted=<nil>) while announcing nothing`.

## 22. Routability was decided by counting array entries

A P0, found by the review while checking §21's route gate, and the sharpest defect in the arc: both
paths could go silent at once.

`oncall_schedules.participants` is a JSON array of CHANNEL IDS
(`api/tenant_scope.go`), and nothing prunes it. `DeleteNotificationChannel` removes the channel row
and leaves its id in every schedule that named it; disabling a channel changes no JSON at all. Two
places then read that array without looking inside it:

- `routablePredicate` armed a service when `jsonb_array_length(participants) > 0`, so the member's own
  alert was SUPPRESSED on the strength of a rotation naming a channel that no longer exists;
- `resolveServiceRecipientsTx` returned `sc.OnCall(at)` unchecked, so the service's own page went to
  that same dead id.

The member is silenced because the service covers it; the service's page reaches nobody. §16.1 exists
to prevent exactly this, and §16.6 already says what should happen instead — "a deleted or empty
target falls back to the project's channels" — a disabled one being the same fact wearing a different
hat.

Both now require the channel to EXIST, be ENABLED and belong to the same project, and the predicate is
deliberately no more permissive than the resolver: it asks for a live participant, the resolver falls
back to project channels, so anything armed has somewhere to land. The channel id is compared as TEXT
rather than cast to `uuid`, because a participant that is not a uuid must resolve to nothing rather
than error the slice — one malformed entry would otherwise stop evaluation for every service in it.

**Four fixtures across three files were inventing participants** (`"ch-first"`, `"ch-live"`,
`{"kind":"email",...}`), which the old predicate accepted because it never looked. One of them,
`TestUnroutableServiceDisarmsBothSignals`, "restored the route" with a participant that resolves to
nobody — it passed only because arming counted entries. They now create real channels. This is the
fourth fixture-shaped finding in this arc, and the pattern is identical each time: a test that never
reaches the case it claims to cover.

**Mutation evidence.** Arming on `jsonb_array_length > 0` again fails with `a schedule whose only
target is DISABLED still armed live coverage`. Returning the rotation's channel unchecked fails with
`the resolver returned [<id>] for a schedule pointing at a disabled channel`.

## 23. The withheld onset, decided rather than assumed

§21 changed a documented behaviour without saying so, and the review caught it. §16.6 said an alert
with no resolvable recipient "is recorded and counted with `cerbix_service_alert_undeliverable_total`
rather than silently dropped"; §21 withholds it entirely. Both cannot be the contract.

The owner chose WITHHOLD, and **D-0176** records why rather than leaving it as a diff. The short
version: recording is not free, because it has to LATCH. A latched announcement nobody received is an
announcement that can never happen — restoring the route produces no edge, while delegation arms and
silences the member who had been paging correctly all along. The rejected alternative (record without
latching or opening an incident) satisfies both documents and writes an undeliverable episode plus an
outbox row on every pass, a few seconds apart, for as long as the route stays broken.

Three things followed from the decision, and the first two were the review's:

- **the counter is real now.** `ServiceAlertEvaluation.Unroutable` was returned and read by nobody, so
  §21's line about keeping silences distinguishable was a claim about observability that did not
  exist. It is wired through the scheduler pass as
  `cerbix_service_alert_evaluations_total{outcome="withheld"}` — a new VALUE on an existing bounded
  label, beside `ok`/`error`/`skipped`, so no new series family appears. `skipped` keeps its meaning:
  the burn arm's HOLDs are verdicts that cannot be QUOTED, which is a different fact from cannot be
  DELIVERED;
- **the burn arm withholds too.** §21 fixed the live signal only, and leaving the sealed one alone
  would have kept exactly the same swallow in the second signal, with a sharper edge: after the route
  returns, `activeBurnDelegationSQL` arms and suppresses the member's own burn alert while the
  service's onset was already spent on an empty route;
- **§16.6 carries a superseding note** naming D-0176, and the runbook documents what a non-zero
  `withheld` rate means — somebody's paging configuration is broken, not that the service is fine.

**Mutation evidence:** removing the burn gate fails with `an unroutable burn FIRE announced
{… Onsets:1 … Withheld:0}`.

Also approved by the owner in the same round: **D-0175**'s legacy escalation semantics, and building
the outbox ordering fence now rather than deferring it (§24).

## 24. The ordering fence, built rather than deferred

The review would not accept narrowing §19's claim about the paired lifecycle, and was right not to:
a subscriber really can be told an incident resolved and then, from a retried older row, that it
opened. The owner chose to build the fence now. **D-0177** records it.

Two independent causes. Within one claim, `UPDATE … RETURNING` has no defined row order — the
`ORDER BY` in its sub-select picks which rows are claimed, not the order they come back — so a batch
holding an opening and its resolution could hand them over either way round. Across claims and
workers there is no order at all: a retry lands minutes later, beside another worker mid-delivery.

`incidents.event_seq` (00086) is advanced by every path that enqueues a lifecycle event, in the
transaction that writes the fact, and stamped into the payload; delivery drops a superseded ONSET and
never a RESOLUTION, exactly as §16.5's gate never drops a close. A payload without a sequence predates
the fence and is delivered rather than guessed about. `ClaimDueOutbox` sorts its batch by
(`next_attempt_at`, `created_at`, `id`), which handles the in-batch half.

What it does not claim is stated in the decision rather than left to a reader: the gate makes a wrong
order unobservable by dropping the stale half of the pair; it does not serialize two workers. That is
the same guarantee `service_alert` has carried since §16.5, and it is enough for the failure that
motivated it.

**Mutation evidence:** removing the gate delivers the superseded opening; letting it apply to
resolutions drops an ending.

## 25. Coverage follows the state, the onset, and a route that is really a route

Three findings closed together, because they are one question asked at three depths: what does it
take before a service may silence its members?

**The policy must cover the state the service is IN.** `activeLiveDelegationSQL` asked whether the
policy pages SOMETHING — `cardinality(page_on) > 0 OR page_on_unknown` — while §16.1 asks whether it
can page the CURRENT state. A service at DEGRADED with `page_on = {down}` announces nothing and
suppressed its member's DOWN anyway. Both clauses are now separate and both are required: "pages
nothing" and "does not page THIS" have different fixes and now have different reasons.

**The announcement must be COMMITTED, not merely possible.** This one my own §21 created. A withheld
onset leaves a service pageable-in-principle and silent in fact, so restoring the route armed coverage
in the instant before the next evaluation announced anything: the member fell silent first and the
service spoke second. Coverage for a pageable state now requires `live_firing` with `emitted_state`
equal to what is observed; `healthy`/`excluded` need neither, since there is nothing to replace.

**A participant must be a channel.** §22 fixed the read side; the write side still accepted anything.
The check that matters is not the SHAPE but the TENANCY, and only the write path can make it: a
participant must be a notification channel OF THIS PROJECT. At read time the two failures are
indistinguishable — a value naming nothing looks exactly like a channel deleted since — and they have
opposite correct answers, because §16.6 falls back for a deletion while a typo must be fixed by a
person rather than papered over forever. So the evaluator keeps the crude half (`domain.IsChannelID`)
and treats corruption as an evaluation error, which collapses the lease, dis-arms, and puts the reason
where an operator reads it.

**A design correction along the way.** The first version of the write-path rule lived in
`domain.OnCallSchedule.Validate` as a uuid-shape check, and it failed the API tests immediately —
their fake channels are named `nc1`. That was the rule telling me it was in the wrong place: the
domain cannot know what a channel id looks like in a store it has never seen, while the store can
simply ask whether the channel exists. The shape helper survives only where the cheap answer is the
only one available.

**Mutations, one per rule.** Reverting to "pages anything" fails with `a DEGRADED service with
page_on={down} silenced its member`; arming without a committed onset fails with `coverage armed for a
DOWN service that has announced nothing`; dropping the write-path check accepts `ops-team`; letting
the resolver fall back for a corrupt participant fails with `a corrupt participant resolved silently`.

**And a fifth fixture-shaped finding.** The state test first passed its own mutation, because it
cleared the latch along with the state and disarmed through the OTHER clause. It now leaves the latch
committed and matching, so it exercises the clause it names. Five times in one arc, always the same
shape: a fixture that never reaches the case the test claims to cover.

## 26. The same window in the second signal, and two claims that outran the code

**The burn arm armed on a verdict rather than on an announcement.** §25 closed this for the live
signal and I reported it as closed; `activeBurnDelegationSQL` still read `last_verdict IN ('fire',
'clear')`. A withheld FIRE leaves the verdict at `fire` with `firing` false, so restoring the route
armed burn coverage in the instant before the next evaluation — the member's own burn alert falls
silent for an onset that was never sent. A CLEAR covers by itself (the rule looked and had nothing to
say); a FIRE covers only with `firing` set and a sequence issued. The regression now asserts the
dis-armed state BETWEEN re-enabling the route and the next evaluation, which is the half that was
missing, and that nothing was WRITTEN on the withheld pass — zero episodes, zero outbox rows — rather
than only that counters were zero.

**Two claims in §23 were wider than the code.** Both were caught by the review reading the diff
rather than the prose:

- `withheld` was added as a fourth value on the evaluations `outcome` label, but `ok` still counted
  every service evaluated, so the two OVERLAPPED and the label stopped partitioning anything. Worse,
  the burn pass never forwarded its count at all, so half the telemetry the section claimed was
  structurally zero. It is its own bounded family now, `cerbix_service_alert_withheld_total{signal}`,
  fed by both arms, and a pass that withholds is no longer silent in the structured log;
- D-0176 stated that `cerbix_service_alert_undeliverable_total` "is not implemented". It is, in the
  outbox, and it keeps a job this change does not touch: an announcement that WAS made to a
  resolvable route whose channels have gone since. What §16.6 supersedes is the ENQUEUE-time half of
  its sentence, not the metric. The decision and the spec now say which of the two is which.

**Mutation evidence:** arming burn on the verdict alone fails with `burn coverage armed on a restored
route before the withheld onset was announced`.

## 27. A counter that named the wrong cause, and a contract that was true only of its comment

**The withheld counter reported a missing declaration as a broken route.** §23 introduced it and §26
gave it its own family; both described it as "nothing could receive the onset", while the evaluator
also incremented it when no declaration governed the service yet. Those are different problems with
different owners — one says somebody's paging configuration is broken NOW, the other says a
declaration has not taken effect — so the number sent the wrong person to look. The counter carries a
bounded `reason` (`unroutable`, `no_governing_revision`), both arms report through the same shape, and
the two cases have their own assertions.

**`outbox.New` documented metrics as optional and the code had stopped honouring it.** The delegation
path grew several unconditional `w.metrics.Record*` calls, so a worker built without a registry
panicked — not at construction, where a test would have caught it, but on the first SUPPRESSED event.
That is to say, in production, on the path this whole arc exists to protect. A nil registry becomes a
no-op at construction now, which makes the contract true at every call site including ones nobody has
written yet.

**Mutation evidence:** labelling the missing-declaration case `unroutable` fails with `withheld =
map[unroutable:1], want one under "no_governing_revision"`; removing the no-op substitution panics the
nil-metrics regression with a nil pointer dereference.

## 28. Ordering that survives a mixed fleet

§24 built the fence and the review found two ways it was still only half a guarantee. Both were right.

**The claim released both ends of a lifecycle at once.** Dropping a superseded onset at DELIVERY keeps
a subscriber from being told an outage began after it ended; it does not keep the two events in order,
and one batch holding an opening and its resolution could hand them over either way round — or two
workers could take one each. The claim now refuses an event while an earlier event of the same
incident is undelivered. A DEAD predecessor blocks as well, deliberately: releasing past it delivers a
resolution whose opening was parked for an operator. Migration 00087 indexes the question so the poll
does not scan the outbox to ask it.

**A pre-fence worker bypassed the whole thing.** The ordering lives in the claim, and an old binary
runs the old claim — so in a mixed fleet the new rows were still delivered out of order, and the fix
was correct only after full convergence. `incident_event` is a FENCED topic now: an old worker does
not list it, so its capable claim cannot match, and the rows wait for a worker that can order them.
That is the fenced class's standing trade — a delay rather than a wrong order — and it is why the
class exists. Rows enqueued before the upgrade keep the legacy class and the old behaviour, since they
carry no sequence and the gate never turns a missing number into a missing page.

The topic-parity gate earned its keep here: adding the topic to the fenced set failed
`TestFencedTopicsAreDispatchable` immediately, which is the check that a fenced topic no worker
dispatches would wait forever.

**Mutation evidence:** dropping the causal predicate fails with `2 lifecycle events claimed at once`;
letting a dead predecessor release its successor fails with `the resolution was released past a DEAD
opening`; unfencing the topic fails with `fenced incident_event rows = 0, want 1`.

## 29. The fence was neither causal nor a fence, and the claim sorted the wrong clock

Round two of the review, against the code as committed. Four findings, and the first two are about the
mechanism I had just described as done.

**The delivery gate dropped openings nobody had received.** It compared the event's sequence with the
incident's CURRENT one — a fact about the row, not about what had been delivered — so an opening was
dropped whenever any later event existed. The subscriber got the update, or the resolution, for an
outage they had never been told began: the exact silence the fence was built against, reached by
another road. It was also read BEFORE the deliverer was called, so two workers could interleave around
it. A check whose answer can change while the thing it authorises is in flight is not a fence.

It is gone. §28's claim already refuses to release an event while an earlier one of the same incident
is undelivered, and a CLAIMED row is not a delivered one, so the ordering is durable without anything
guessing at delivery time. The sequence stays in the payload for consumers and for diagnosis.

**The claim sorted by the lease it had just written.** `UPDATE … RETURNING next_attempt_at` returns
the NEW value, and the new value is a backoff — a row on its second attempt earns a longer one than a
first-attempt row, so the older retry came back after the newer event and the batch was delivered in
the reverse of the order it was owed in. A CTE captures the original due time before the update
rewrites it.

**Burn coverage accepted a contradiction.** `clear` while still firing is a state no ordinary
evaluation writes, and reading it as coverage silences a member on the strength of an inconsistency.
The verdict and the latch must agree now: `(clear AND NOT firing) OR (fire AND firing AND seq > 0)`.
The regression walks all five shapes — three incoherent, two coherent.

**And a correction to §25 from the reviewer, adopted.** Requiring `emitted_state = observed_state` is
stricter than the invariant needs: during a confirmed pageable→pageable transition the service is
already firing and already announced, the observation has moved on, and equality dis-arms — which
pages the members a second time for an outage somebody is already handling. Both directions fail OPEN,
so neither loses a page; the looser one (`live_firing` with an `emitted_state` that exists) avoids the
duplicate.

**Mutation evidence:** sorting on the rewritten lease fails with `claim order = [newer older]`; letting
a `clear` verdict cover while firing fails the ambiguous-row regression; dropping the causal predicate
and unfencing the topic remain covered by §28's mutations. The gate's own test was rewritten into its
opposite: an undelivered opening must never be dropped for being old.

**A sixth invalid mutation, recorded like the others.** The first attempt at the burn-consistency
mutation cut a closing parenthesis with the clause and produced a SQL syntax error. Two tests failed,
neither for the reason under test.

## 30. The barrier fenced the consumers and left the producers alone

The review's next round found the hole in §28's barrier, and it is the kind that only shows up if you
ask WHO decides.

`enqueueOutboxTx` chooses the class from `domain.FencedTopics()` — of the process doing the insert. So
fencing the topic protected against old CONSUMERS while an old PRODUCER, still running through the
same rolling upgrade, went on writing legacy rows. For exactly the window the barrier exists to cover,
new sequenced events were being created in a class any old worker could claim. My own decision text
said "rows enqueued BEFORE the upgrade", which is false while a separate old api or scheduler is still
alive DURING it.

Migration 00088 moves the rule into the database: a BEFORE INSERT trigger forces the class for fenced
topics whoever writes them, and promotes the rows an old producer already wrote. The topic list is
duplicated into SQL deliberately — two copies with a parity gate beat one copy only one side can
enforce — and `TestOutboxFencedClassMatchesTheBinary` checks both directions, including that the
trigger does not fence something the binary dispatches as legacy, which would strand it for everyone.

**And legacy rows tied at zero.** A row from a pre-sequence producer carries none, so every such row of
an incident compared equal — and a tie is not a predecessor, so the whole legacy backlog of one
incident went out in a single batch, in whatever order it came back. The claim orders on (sequence,
`created_at`, id) now.

**A mutation found a missing test rather than a defect, which is the good outcome.** Removing the
tie-break changed nothing in the suite, because every existing test used sequenced rows: the legacy
case had no coverage at all. The test that reaches it came first, and then the mutation failed
properly with `2 unsequenced lifecycle events claimed at once`.

**A test that quietly stopped testing what it named.** `TestDeadOutboxListAndReplay` seeded
`incident_event`, which became fenced in §28 — so its "replays as pending" assertion was about a topic
that now replays as `pending_fenced`, and the two branches of the replay had collapsed into one. It
seeds a legacy topic again, and the fenced branch has its own test.

**Mutation evidence:** dropping `incident_event` from the trigger fails the parity gate and the
old-producer regression; removing the tie-break fails the legacy-ordering regression.

## 31. The guarantee narrowed to what a lease can actually buy

The review's design pass on the remaining hazard — a worker whose lease expires mid-call, whose stale
HTTP request lands after a later event — made a point that no amount of implementation answers.
Cancelling a call does not un-send bytes a receiver has already accepted, and no CAS on this side
reaches into a remote queue. "No subscriber ever sees `opened` after `resolved`" is not provable for
an arbitrary webhook by any lease, renewal or ownership fence.

**Owner decision: D-0177 promises DISPATCH order.** cerbix orders what it controls — which row the
claim releases, and the order the worker calls the deliverer in — and says so instead of implying
more. What a receiver gets is the means to finish the job itself: every payload carries the incident's
id and this event's `seq`, unique per event, monotonic per incident, and stable across the retries
at-least-once delivery guarantees will happen. A receiver that keeps the highest applied `seq` has an
exact answer; one that ignores it has accepted its transport's order, which was always the real
situation.

The runbook now separates the two explicitly.

> **Corrected in §35.** Two sentences in this section were wrong and are superseded there: "a receiver
> that keeps the highest applied `seq` has an exact answer" (it has an exact CURRENT STATE, and loses
> the events it discards), and the diagnostic that an update for an unannounced outage *is* a receiver
> ignoring `seq` (it can equally be an opening that never arrived).

The bounded delivery budget the review sketched — one whole-call deadline from the DB's `lease_until`,
every branch context-aware including SMTP, bounded fanout — was not taken at the time and is recorded
here as available rather than done. **It was built on 2026-08-27 (D-0186, §50):** the owner asked for
the remaining items, and closing the window inside cerbix while leaving the unprovable part exactly
where it is turned out to be the right shape after all.

**Mutation evidence:** a path that stops advancing the sequence fails with `sequences = [1 1 1 1]`.

## 32. What the barrier reaches, and what it admits it does not

The review's round on the barrier found two rows it did not cover and three places where the prose
had drifted behind the code.

**Dead rows were not promoted.** 00088 only touched `status = 'pending'`, and `ReplayDeadOutbox`
restores a row's claimable class from the PERSISTED `fenced` column — so a dead pre-barrier event
replayed months later would come back as legacy `pending`, into the hands of the worker the barrier
exists to keep away from it. Dead rows get the flag now, keeping their status: promoting a class is
not resurrecting a row.

**A pre-barrier CLAIM stayed valid.** An old worker that took a legacy row before the migration ran
still held its token, so its `MarkOutboxDelivered` would win the CAS, settle the row and release the
successor behind it. The promotion replaces the token. It cannot recall an external call that worker
may already have made — nothing on this side can, which is exactly what §31 stopped pretending —
so the honest statement is now written where it belongs: the barrier is about ROWS, and draining old
workers before migrating is the operational answer for calls in flight.

**Three comments described a system that no longer exists**: D-0177's normative paragraph still
specified the delivery gate deleted in §29, `FencedTopics` still said pre-upgrade rows keep the legacy
class, and 00088 called its own rows "unclaimed by anyone capable", which is the very thing the review
disproved. All three now say what the code does.

**Mutation evidence, against the MIGRATION rather than a copy of its SQL:** skipping dead rows fails
with `the barrier left a DEAD pre-barrier row unfenced`; keeping the old token fails with `the
promotion left the pre-barrier claim token valid`.

**And a self-inflicted lesson worth the line.** Undoing a mutation with `git checkout` on a file that
also held new uncommitted tests deleted both of them, silently — the suite then passed because the
tests were gone. Mutations get a file copy, never a checkout, when the file has unstaged work in it.

## 33. The code required a stop the operator was never told to make

The barrier's P0s were confirmed independently on a real 87→88 migration, and the review then named
what was left: the CODE now requires draining the outbox owners before migrating, and the runbook
still promised a rolling upgrade with no such step. That gap is mine — I wrote the constraint into a
code comment and a decision record, neither of which an operator reads while performing an upgrade.

The runbook carries the procedure now: stop `all`/`api`/`scheduler` (the roles that run the outbox
worker), let them exit, migrate with the new binary, start them again. Probers and pull agents keep
running — they deliver no outbox events, so probing continues and results queue.

It also says what skipping the stop actually costs, because "required" without a consequence invites
somebody to weigh it wrongly: nothing is corrupted and no event is lost, and the risk is exactly one
out-of-order delivery from the worker that was mid-call — the defect 00088 exists to end. A deployment
that cannot take the pause should expect the ordering guarantee to begin when the last old worker
exits, not when the migration commits.

**And a contradiction the rewrite exposed.** `overview.md` explained that roles auto-apply migrations
at startup and that concurrent starts are safe, which is true and, for this upgrade, beside the point:
letting a role auto-apply 00088 while other old roles are still running is exactly the window the
migration exists to close. The one-off `cerbix migrate` is now stated as REQUIRED for upgrades crossing
it, rather than recommended for tidiness.

## 34. Two names for one failure, and only one of them was true

The screen and the counter disagreed by construction. `ServiceAlertingState`, the badge read, worked
out WHICH clause of §16.1 was unsatisfied and said so: `unroutable`, `stale_lease`, `revision_changed`,
eleven values in all, each a different thing for an operator to go and fix. `ActiveDelegation`, the
lookup that runs when a member's alert is about to be delivered, computed the same conjunction and then
threw the answer away: whatever had failed, the fail-open metric got `no_active_owner`.

So the two surfaces an operator actually uses could not corroborate each other. The service page says a
channel is dead; `cerbix_alert_delegation_fail_open_total` says only that coverage was absent, which is
also what it says for the overwhelming majority of monitors that no service claims at all. The signal
worth having — a replacement EXISTS and went quiet — was mixed into the one value guaranteed to be
noise.

The fix is not to teach the delivery path the badge's vocabulary; two implementations of one
classification agree until they don't. The clause SQL and the verdict are now ONE thing,
`serviceCoverageClausesSQL` + `serviceCoverageClauses.liveVerdict()/burnVerdict()` in
`internal/store/servicealertstate.go`, and both callers evaluate it. `ActiveDelegation` selects the same
columns for every candidate owner, ranks the reasons when several services could have covered the
monitor and none did (`coverageReasonRank`, so the most actionable cause wins over the most common one),
and reports that. A candidate's badge and the reason delivery computes for that candidate are now the
same string by construction.

> **Narrowed and extended in §36.** "A disagreement between the screen and the counter is not
> expressible" was too strong for several candidates, the parity test's evidence claim named a case it
> does not cover, and three operational values the delivery path emits were missing from the
> normative table. All three corrected there.

One value is new because delivery can reach a case the badge cannot: `no_owning_service`, meaning
nothing in the project names this monitor as an SLI. The badge is always asked about a service that
exists, so it never has to say this; the metric almost always does, and separating it from the eleven
real causes is what makes the rest legible.

`TestDeliveryReportsTheSameReasonTheBadgeShows` asserts the parity directly: for ownership withdrawn,
a never-evaluated service and an unroutable route it reads the badge and the delivery verdict at the
same instant and requires the same string, plus the no-owning-service case. The MUTATION collapses the
rank back to a constant `no_active_owner`; it fails with `delivery reported "no_active_owner", want
"unroutable"`. §16.6b now carries the vocabulary as a NORMATIVE table with the fix for each value, and
says a new value needs a decision record, because the UI renders these as translated text and the
metric's label set is part of the contract. Invariant 92.

## 35. Two sentences that promised more than the mechanism

The review read the committed D-0177 against itself and found the decision text still carrying the
claim the owner had just rejected. After saying plainly that remote arrival order is unconstrained, it
closed with "it is enough for the failure that motivated it — nobody is told an outage began after it
ended", and cited as verification the mutations of the delivery gate that this same decision DELETES.
So the strongest sentence in the document was false under its own narrowing, and its evidence
described a mechanism no longer in the tree. That is the worst combination available: the part an
operator quotes is the part that is wrong, and the citation makes it look checked.

Replaced, not amended. What is true is two statements with a gap between them that belongs to nobody
here: cerbix will not CALL a deliverer with an incident's events out of order, and a receiver that
uses `seq` will not APPLY them out of order. The network is in between. The verification now names the
committed sequence regression and the claim-ordering tests.

**And "keep the highest `seq`" is not "dedupe and order exactly".** I had written that a receiver
doing it "has an exact answer". It does not. Discarding a lower sequence prevents state REGRESSION and
loses the event: the receiver ends up right about the present and blind to a step it skipped. Exact
history needs the other strategy — track the next expected sequence, buffer what runs ahead of it —
and that one needs something the first does not: a bounded wait, because an event can be dead-lettered
on our side and then never arrives, and a receiver waiting for it stalls that incident forever. Both
are legitimate; they answer different questions, and the decision, the runbook and the payload's own
comment now state them as two rather than conflating them into one flattering sentence.

The runbook's diagnostic was wrong in the same direction. "An update for an outage they were never
told began means a receiver ignoring `seq`" excludes the case where the opening genuinely never
arrived — dead-lettered, or in flight from a worker that lost its lease, both of which this arc spent
its time proving are possible. It now names both candidates and says how `seq` tells them apart. The
ordering prose also sat INSIDE the metric table, splitting it after the first row so the remaining
rows rendered as loose text; it is a section of its own now.

**Half the writers were carrying the whole claim.** `TestEveryLifecycleEventCarriesAUniqueMonotonicSequence`
says "every path that enqueues one" and exercises `CreateIncident` and `AddIncidentUpdate` — the human
doors. A service outage opens and resolves through `serviceincidents.go`, different code with its own
INSERTs, and nothing asserted the sequence there. The product code was right; the evidence was not,
and evidence that stops at half the writers is how the other half ships without a sequence one day.

`TestServiceAutoIncidentPathsCarryTheSameSequence` covers both machine doors, and writing it turned up
something worth keeping: the open and the resolve commit in ONE transaction, so their `created_at` is
the same instant and their ids are random uuids. There is no way to read them back in insert order
from the row itself. That is exactly why the claim tie-breaks on the sequence FIRST, and it makes the
sharper assertion available — order by the sequence and require the TYPES to come back opened-then-
resolved, which asks whether the number agrees with what actually happened rather than restating
itself.

**Mutation evidence, both paths:** dropping the sequence in the resolve path fails with
`sequences = [1 0] on the SERVICE paths`; dropping it in the open path fails on the causal assertion,
`events in SEQUENCE order = [incident.resolved incident.opened]` — the outage replayed backwards.

## 36. Parity has a selection step, and it needed its own contract and its own evidence

Three findings against §34, all of them the same failure mode in different places: a claim stated
more broadly than the mechanism supports.

**"Cannot disagree" is false with more than one candidate.** A monitor can be named as an SLI by
several services. Each is classified by the shared evaluator — that part is real, and it is what §34
fixed. But the counter has no service label and cannot have one, so with two candidates missing at
different depths, `ActiveDelegation` reports the FURTHEST one and each service's badge reports its
own. The review proved it on real Postgres: badges `not_owned` and `stale_lease`, counter
`stale_lease`. The contract is now written in two parts. Classification: a candidate's badge and the
reason computed for that candidate are the same string by construction. Selection: the reported value
is ALWAYS one of the candidates' badge reasons, chosen by the table's own order, and it is NOT
promised to equal the badge of whichever service somebody is looking at.

**Three values the product emits were outside the "normative" table.** `error` when the lookup itself
fails, `record_failed` when coverage was confirmed and the suppression record could not be written,
and `unspecified` as a defensive default in the registry. A dashboard or a translation built from a
table calling itself complete would meet undocumented series, and the sentence "every value is a badge
clause" was simply false. They are now their own table, marked as having no badge counterpart, with
`error` explicitly distinguished from `evaluation_error` — a successful lookup that FOUND a recorded
failure. Those two names sitting next to each other is a trap I built and the spec now says so.

**And the burn arm's reason could be named by a target that is not part of the answer.** The
conjunction looks only at ENABLED targets; the diagnostic aggregates that turn "blind" into a WORD
(`anyHold`, `anyGenMismatch`, `burnErr`, and the two `min()` timestamps) scanned every latch row of
the service. So a HOLD left on a DISABLED target answered for an enabled target that was merely stale:
`held` instead of `stale_lease`, sending an operator to look at a window that cannot fire. The
supported write path prunes those rows, which is why no existing test saw it — and legacy state, a
repair, or a half-finished migration is exactly the situation where the reason has to still be true.
All five are scoped through `burnEligibleTargetSQL`, the conjunction's own eligibility, named once.

**The mutation I claimed was not covered.** Replacing `coverageReasonRank`'s body with `return 0`
left the committed parity test green: it has ONE candidate and tests LIVE only, so the ranking never
ran. That is the second time in this arc that evidence for a mechanism did not reach the mechanism.
`TestSeveralCandidatesReportTheFurthestMiss` and its burn twin fix it, with the alphabetically FIRST
row deliberately carrying the SHALLOWER miss so that a lookup returning whatever came back first
fails. And §34's own text named `evaluation_error` among the parity cases; the table covers
`not_owned`, `never_evaluated`, `unroutable` and the no-owner case. Corrected in place.

**Mutation evidence:** `coverageReasonRank => 0` fails with `monitor reason = "not_owned", want
"unroutable"` and `monitor burn reason = "no_enabled_target", want "unroutable"`; widening
`burnEligibleTargetSQL` back to service-scope fails with `delivery reported "held", want
"stale_lease"`. Invariants 92 (rewritten) and 94.

> **Two of this section's own artefacts were wrong, and §37 fixes them.** The published selection
> order did not match `coverageReasonRank`, and the eligibility regression's fixture wrote an
> unannounced FIRE while its prose said "stale" — codifying a defect instead of catching one.

**One stale comment, same class.** `ClaimDueOutbox`'s sort still described a batch holding both ends
of one incident and pointed at "the per-incident sequence gate in the outbox worker" — a batch the
causal NOT EXISTS now prevents, and a gate this arc deleted. The sort is still there and still
earns its place (fairness and determinism by due time); the comment says what it actually does.

## 37. `stale_lease` was standing in for everything the burn classifier could not name

`burnRuleCoversSQL` folds five facts into one boolean — the verdict/firing/sequence shape, both
generations, a recorded error, and the lease. The reason was then re-derived from three aggregates
that did not map onto those five, and whatever was left over came out as `stale_lease`.

So the ordinary D-0176 shape was reported as a stalled evaluator. A burn FIRE withheld for want of a
route persists as `fire`, not firing, `emitted_seq = 0`, with a FRESH lease: that is deliberate, so the
next pass finds the same unannounced onset and announces it the moment somebody can be told. The
classifier had no fact for it and said `stale_lease` — the one answer that is certainly wrong, because
it sends an operator to a scheduler that is working while the thing to fix is a notification channel.
The review proved it twice on real Postgres against the committed tree, before and after restoring the
route.

The fix is not another aggregate. `burnVerdict` is now `burnRuleCoversSQL` taken apart in the
conjunction's own order, so every fact the predicate uses has a clause and a name. `stale_lease` means
an eligible latch whose lease has expired, and nothing else. The withheld onset is diagnosed the way
LIVE diagnoses its own: `unroutable` while the route is gone (the actionable truth), `onset_pending`
once it is back and the announcement is still owed.

One value is added, with a decision record because §16.6b makes the vocabulary normative: after every
legitimate cause is named, a latch that still fails the predicate is in a shape the evaluator does not
write — `clear` while still marked firing. `latch_inconsistent` names it, and is also the classifier's
LAST case on purpose: an unexplained refusal is an uninterpretable row, and saying that beats picking a
cause that is not the cause. D-0178.

**The published order was not the implemented order.** §16.6b said the selection followed "the table's
own order from top to bottom", and the table was grouped for readability, so it disagreed with
`coverageReasonRank` on two pairs. Every selection test I had written happened to pick pairs the two
spellings agreed on — the same failure as §36's uncovered mutation, one section later. The rank is now
an explicit COLUMN, the rows are written in it, and `TestTheRankOrderIsTheOneTheSpecPublishes` pins
`never_evaluated` against `policy_pages_nothing`, which used to be inverted between the two.

**And my own eligibility regression had the defect baked into its fixture.** Its prose said the enabled
target was stale; the code wrote an unannounced FIRE and asserted `stale_lease`, which passed only
because that was the catch-all. It now expires a lease for real, and the withheld-onset case is its own
test — which is what it should have been from the start, since the two prove different things.

Three smaller corrections in the same pass: the multi-candidate test's preamble claimed the
alphabetically first service was the further-along one when the fixture deliberately makes it the
shallower one; `status.md` AC-0161-10 still opened on "the same failing clause" before narrowing to the
two-part contract two sentences later; and the expanded `RecordDelegationFailOpen` documentation was
sitting above `RecordServiceDelegation`, so Go attributed the whole comment group to the wrong method.

**Mutation evidence:** removing the two new burn cases fails with `delivery reported
"latch_inconsistent" once the route was back, want "onset_pending"`; swapping the rank pair fails with
`monitor reason = "never_evaluated", want "policy_pages_nothing"`. Invariants 95 and 96.

## 38. The review's disposition, recorded as the reviewer left it

The independent review ran under a two-round contract and finished at `4c4bae3` without approving it.
The exact record, which the reviewer asked for by name rather than a summary that rounds it up:

- `6e0b6a4` was **finally REJECTED** — no P0, but one product P1 (the withheld burn onset diagnosed as
  `stale_lease`), one normative-contract P1 (the published rank not matching the implementation) and
  three P2s, of which one was a regression whose fixture codified the defect it claimed to catch.
- `4c4bae3` is **closed under implementer authority**, not reviewer-approved. The contract gives no
  third implementation round, so nobody independent has read the fix for the finding that closed the
  second one. What stands behind it is the evidence in §37: two mutations that fail with their own
  safety sentences, `-race` over all 30 packages, and the frontend gates the enum change required.

That distinction is worth keeping precisely because it is unflattering. The last round found five
things, and four of them were the same failure: evidence that does not reach the mechanism it names.
A mutation left green by a single-candidate fixture (§36), a rank order pinned only on pairs both
spellings agreed about (§37), a "stale lease" test that wrote a fresh unannounced FIRE (§37), and a
completeness claim resting on half the writers (§35). Each individual product fix was small; the
pattern is not, and it is the thing to carry forward rather than the count.

## 39. Correction of record — a process claim of mine that was simply untrue

In three separate reports to the review I wrote that the test container was removed as soon as each
run finished. It was not. `cerbix-ts` (`timescale/timescaledb:2.17.2-pg16`, port 55492, `cerbix_test`)
had been up for two hours and survived every run of the session. It is gone now, and no cerbix or
Postgres container is running.

Nothing in the results moves: the DSN is the one the reports name, the `-race` runs went against that
database, and the migrations applied from this tree. What moves is how much the "gates" line is worth,
because I asserted a discipline I was not keeping — in the same messages where I was reporting
findings about claims that outran their mechanism. That is the arc's own defect class, this time in my
account of my own process rather than in the code, and it is the easiest kind to miss because nobody
independent checks the housekeeping.

The standing rule it violated is not a nicety: leftover containers have exhausted this machine's
memory before. The check is one command and belongs at the end of a run, not in the sentence
describing it.

## 40. Coverage meant "we sent it", and the difference is a silent member

> The review closed at §38. What follows is the backlog it left open, worked under implementer
> authority with the same discipline: a mutation per fix, both storage modes where the schema moves.

D-0176 closed the case where the evaluator finds no route AT EVALUATION. The review kept saying that
is not the whole class, and it was right: the window between the ENQUEUE and the DELIVERY produces the
same swallow, one step later and without any withholding.

The evaluator resolves a route, enqueues the onset, latches firing. The channel is deleted before the
worker gets there. The worker resolves zero recipients, counts it and terminally SUCCEEDS — correctly,
because no retry reaches a channel that is gone. The latch still says firing, so the evaluator has no
edge and announces nothing further. The route comes back, and the conjunction — which asked only
whether the onset was COMMITTED — arms coverage and silences every member monitor for a page nobody
received. The same chain runs with no race at all whenever the on-call participant changes between the
onset and the restoration, because §16.4a deliberately keeps the ORIGINAL recipients for the close.

`delivered_seq` (migration 00089) is advanced by the worker only when a delivery resolved at least one
recipient, and arming requires `delivered_seq >= emitted_seq`. Until an announcement is RECEIVED,
members page for themselves. The credit is monotonic and guarded by `emitted_seq = $seq`, so a retry of
a superseded onset cannot credit the current one, and a CLOSE is refused by the worker AND by the
store — a safety property with one guard is one refactor from having none.

The review's other option, re-edging on zero resolved so a restored route gets a fresh onset, was not
taken at the time: it needs an episode-identity decision, because the current episode is open and its
index says one per service. Not arming is the safe half and complete on its own — nobody is silenced.
**It was built on 2026-08-27 (D-0187, §51),** and the identity question answered itself once the
re-announcement went through the ORDINARY onset path, which already supersedes an open episode. D-0179.

**The upgrade goes the other way on purpose.** Existing rows have no delivery evidence, so a truthful
zero would declare every armed service undelivered at once and page an entire installation in one
minute. 00089 seeds `delivered_seq = emitted_seq`: existing latches keep the coverage they had, and the
rule governs from the first announcement after the upgrade. One already-emitted onset per latch, closed
at that latch's next edge. Same class of legacy exception as D-0175, and named as one.

**The first version of this had the defect it was fixing.** My burn mutation — removing the delivered
term from `burnRuleCoversSQL` — left the tests GREEN. The reason was that I had given the classifier
its own `burnOnsetUndelivered` case sitting BESIDE the gate instead of inside it, so the fact was
decided twice and weakening the gate changed nothing. That is two implementations of one rule: exactly
what §34 removed from between the badge and delivery, reintroduced by me six sections later. Every
blindness diagnostic now lives inside the gate's branch, so each is reachable only when the gate has
refused, and each is mutation-testable through it.

**And two queries had been dead since §34.** `activeLiveDelegationSQL` and `activeBurnDelegationSQL`
stopped running the moment `ActiveDelegation` moved to `candidateCoverageSQL`. Nothing said so — Go
does not complain about an unused package-level var — and three comments in two other files still sent
maintainers to them as the place where coverage is decided. Deleted with their orphaned clauses
(`routableClause`, `evaluatedCurrentRevisionClause`), and the comments now name the conjunction.

**One older assertion is inverted rather than deleted**, because the reason is the point.
`TestBurnWithholdsAnOnsetNobodyCanReceiveAndDoesNotLatch` ended on "only NOW may it suppress the
member's own burn alert" after the evaluator announced. Under D-0179 an enqueue is not an
announcement; the test now requires the delivery credit before coverage returns, and says why it
changed. Three other fixtures gained `delivered_seq` for the same reason.

**Mutation evidence:** removing the delivered term from the LIVE gate fails with `coverage armed for
an onset NOBODY received`; from the BURN gate, with `burn coverage armed for an onset nobody
received`. Invariants 97 and 98.

## 41. The page showed the outage and the mail went to nobody

Three surfaces answered "which projects does this page report" and all three answered differently.
The render seeded an empty set and added each component's resolved project, so a project-scoped page
whose components are all MANUAL reported none of its own project's incidents. The feed seeded the own
project and then walked `components → monitors`, one lookup per component, so a service-backed
component contributed nothing. The subscriber fan-out was that same monitor JOIN with neither half of
the axis.

The third is the one that matters to a person. A page made entirely of Service components renders the
incident, the webhooks fire, and the readers who explicitly asked to be told get no mail. They are
looking at the outage on the page they subscribed to.

One owner: `page.project_id` when the page is project-scoped, UNION every non-NULL
`components.source_project`. `store.StatusPageProjectIDs` is the forward direction and
`ConfirmedSubscriberEmailsForProject` is now its exact inverse, because a subscriber is entitled to
mail about precisely the incidents their page shows them, and two hand-maintained queries agree only
until somebody edits one.

Deliberately NOT filtered by `source`. A conversion keeps a dormant binding's `source_project` and the
renderer still resolves that component to its project, so filtering would narrow the mail below the
page — the same disagreement, mirrored. Whether such a component *should* still report that project is
a real question and it belongs to its own owner, not to a bug fix. D-0180.

The regressions use the two shapes each old spelling lost, and neither is masked by a monitor
component of the same project — that masking is why three surfaces could disagree for this long
without a test noticing.

**Mutation evidence:** dropping the own-project arm fails with `a project-scoped page with only manual
components reports [], want its OWN project`; restoring the monitor JOIN in the inverse fails with
`fan-out for a Service-only page = [], want the page's subscriber`. Invariant 99.

## 42. A non-goal that was true, defended by a sentence that was not

FR-023's D8 says services get no renotify knob. That decision is right and it stands. Its
justification was "the policy's own steps (including its repeat) are the repeat mechanism", and that
is false: the repeat branch is `RepeatLast && renotifySeconds > 0`, and the service arm passes zero,
so `repeat_last` on a policy attached to a service does nothing at all.

Nothing was broken — a service ladder ending at its last step is what should happen. What was wrong
sat around it. The spec told a reader that a mechanism existed which does not, so anyone reasoning
about escalation coverage from the document reached the wrong conclusion. And the form offers
`repeat last step (renotify)` on a policy that may be attached to a service, with nothing saying the
toggle is inert there; one policy shared by monitors and services behaves differently on each, which
is a thing to state, not to hide.

All three places say it now: D8 carries a forward-only correction naming the false sentence rather
than deleting it, the form says repeat uses each monitor's renotify interval and service incidents
have none, and the behaviour is a TEST instead of a zero that happens to disable a branch. Giving
services a cadence stays a separate requirement — inventing a default here is exactly the "random
cadence" the review warned against. D-0181.

**And the test's first version proved nothing.** It set `repeat_last` on the LIVE policy, while an
open incident climbs the ladder FROZEN when it opened (D-0175), so the incident's own `repeat_last`
stayed false and the branch was never reached. The mutation — giving the service arm a 300-second
renotify — left it green, which is the only reason I found out. It now writes the snapshot and
ASSERTS the frozen value before proceeding.

That is the fourth time in this arc that a test named a mechanism it did not reach, and the second
time the mutation was the only thing that noticed. The pattern is not incidental to these fixes; it
is the thing I am worst at.

**Mutation evidence:** `o.renotifySeconds = 300` in the service arm fails with `a finished service
ladder fired 1 more step(s) with repeat_last on`. Invariant 100.

## 43. The URL said one thing and the page did another

Two defects that compound into one bad minute for an operator.

`SearchBox` switched the workspace for PROJECT hits only. A monitor or incident hit from another
workspace navigated fine and left the tenant behind, so the detail page's reads and permission checks
ran against the previous one: the successor and convert pickers listed a stranger's services, and
`canWrite` compares the workspace's org against the LOADED subject's project — so a legitimate editor
of the incident on screen watched acknowledge, resolve and postmortem vanish. Every hit already
carries `org_id` and `project_id`; every hit type now uses them, which is one fix instead of one per
detail view.

Underneath it, both detail views read `route.params.id` once and loaded on mount, and Vue Router
reuses a component when only the params change. So an A→B navigation between two monitors — or two
incidents — changed the address bar and nothing else, with pause, retire, delete, acknowledge and
resolve all still aimed at A. `ServiceDetail` had the right shape already:
`watch(() => [route.params.id, ws.projectId], load)`.

Three layers, because each closes a different entrance. The id is reactive and the views watch it AND
the workspace — the route covers a search hit, the workspace covers a switcher change with no
navigation. A load ticket stops a slower response for the previous subject from overwriting the
current one, which a remount alone does not. And `App.vue` keys the `RouterView` by PATH, the layer
that does not depend on a view remembering any of this; by path and not `fullPath`, because several
views keep state in the query string and remounting on `?tab=` would throw it away.

**Mutation evidence:** removing the tenant switch fails with `expected "spy" to be called with
arguments: [ 'org-b' ]` on both hit types; removing the identity watch fails with `expected [ 'mon-a' ]
to include 'mon-b'`; removing the load ticket fails with `expected 'monitor mon-a…' to contain
'monitor mon-b'`. Invariant 101.

## 44. The fixes stopped new damage and left the old damage where it was

Every lifecycle fix in this arc proves a FUTURE write is correct. The review kept saying that is not
the same as discharging the defect, and for anything that has already written customer history it is
right.

Four classes, and the interesting part is the line between them.

**Repairable, because the row itself says what the answer is.** A RESURRECTED incident carries a
`resolved_at` with a status that walked back off `resolved` — only a resolve stamps that column and
nothing clears it, so the status is the field that lied. These are not cosmetic: the row occupies the
partial unique index that permits one open incident per subject, so the next outage cannot open one
and the second failure never appears. A STRANDED service incident is an open auto-incident whose
health episode is closed and whose service is not firing; disowning used to end the alert and leave
the incident open forever, with the operator reading "investigating" on a service that recovered
hours ago.

**Not repairable, and the second reason is the decisive one.** `incident_member_snapshots` stores the
member list and NO revision id, so a snapshot taken from a not-yet-governing revision cannot be told
from a correct one by looking at it. And the obvious substitute — rebuild from the revision effective
at `started_at` — is precisely the guess the review named: `started_at` is the transaction clock while
the evaluator selected at `statement_timestamp()`, so a transaction crossing a boundary carries a
`started_at` earlier than the revision that actually governed. The migration emits a BOUND instead,
labelled as an upper bound rather than a defect count, because most of that set is fine. Anchorless
auto-incidents are the same shape of problem with no data at all behind them.

Reporting is not a lesser outcome here. An operator who knows the row exists can decide; a migration
that guessed cannot be un-guessed, and this is immutable customer history.

The repair predicate is deliberately narrow and the test says why: two negative controls, a service
that is STILL FIRING and an incident a PERSON opened, both of which must come through untouched. A
repair that closes a live outage is worse than the defect it repairs.

`CHECK ((status = 'resolved') = (resolved_at IS NOT NULL))` goes on after the repair, both directions.
"Resolved is terminal" was already once true in a read and false in the write; that is what a
constraint is for.

**And one live instance of a class I had already declared fixed.** D-0177's clock work moved the
manual incident paths onto a post-lock `statement_timestamp()` — `AddIncidentUpdate`,
`AcknowledgeIncident`, `CreateIncident`. `resolveServiceIncidentTx` was still reading `now()`, the
TRANSACTION's start, while both of its callers already held the instant they took after their locks.
A lifecycle close that waited on a row lock therefore dated the resolution before the action that
caused it, and `ListIncidentUpdates` orders by `created_at`, so the timeline rendered it that way.
Threading `asOf` through is three lines; noticing that the class was not actually discharged is the
part worth recording, since I had said it was. Its mutation fails with `want the caller's post-lock
instant`. Invariant 103.

**Mutation evidence:** dropping the status correction fails the migration outright — the new CHECK
refuses to install over a row it just declined to fix, which is a stronger kill than the assertion I
wrote. Dropping the `live_firing = false` guard fails with `the repair closed an incident for a
service that is STILL FIRING`. Invariant 102.

## 45. Three of my own fixes were weaker than the documents that described them

The review of `7a9e87d^..af7a7a2` rejected it on three P1s. All three are the same failure in
different clothes: something claimed at a level the code did not reach.

**LIVE coverage armed on an announcement about a DIFFERENT state.** `liveOnsetCommittedSQL` asked only
`emitted_state IS NOT NULL`. So a delivered DEGRADED announcement covered a service that had since
observed DOWN and was still collecting its confirm streak — the streak exists so the service does not
announce too early, and treating the previous announcement as cover for the new state handed that
caution to the members as silence. The canonical §16.1 in the spec ALREADY said
`emitted_state = observed_state`; the code was behind its own requirement, which is worse than a
missing rule, because everybody reading the spec would have believed it. And the delivered clause
waved `emitted_seq = 0` through, so a state that was never announced counted as delivered. Both are
one predicate now: pageable-or-healthy, firing, the emitted state IS the observed one, a real
sequence, and that sequence delivered.

**D-0180 did not have the single owner it claimed.** The feed called the store; the render rebuilt the
set inline and the subscriber inverse re-typed the rule. The results agreed, which is exactly the
condition under which nobody notices — and the review proved it by deleting the own-project arm from
the render's own loop and watching every API test stay green. "Rebuilt the same way" is not one owner.
The render now asks `StatusPageProjectIDs`, the inverse shares the predicate TEXT with the forward
query, and `TestPublicRenderReportsThePagesOwnProject` fails when the owner loses that arm.

**And the 00090 test proved half its contract.** It never created a suspect snapshot or an anchorless
incident, never read the warnings, and its comment claimed a rerun check it did not perform.
Replacing both reporting queries with `SELECT 0` left it green. It now seeds both reported classes,
captures the migration's notices through `pgx.OnNotice` — `sql.Open("pgx", …)` discards them, which is
why the first version could not have checked this — asserts all four report lines, and runs 90 → 89 →
90 so the `🔧 Repaired:` marker is tested rather than asserted in a comment.

Two smaller things in the same set. D-0182 counted four classes while the migration touches five; the
fifth (`resolved` with no resolution time) has always been repaired, because the CHECK is a
biconditional and would refuse to install otherwise. And twelve invariants existed only in
`docs/traceability.md`: the gate hard-coded 1–91 and ignored everything above it, so a discharge row
above that number was neither required nor checked. They are in §16.8 now, at the numbers the map
already used, and `check_docs_references` DERIVES the count from the spec — which immediately caught
one of my own rows (98) naming no test at all.

**One contract loosened rather than tightened.** The delivery credit was gated on `err == nil` as well
as `Resolved > 0`, so a partial delivery — three channels reached, a fourth timing out — left the
service covering nobody until a retry came back clean. D-0179 says at least one recipient, not a clean
call. The event still fails and still retries; the credit is monotonic and sequence-guarded, so the
retry changes nothing.

**Mutation evidence:** restoring `emitted_state IS NOT NULL` fails with `the previous DEGRADED
announcement covered a service that is now DOWN` and `a latch carrying an emitted STATE with no
emitted SEQUENCE armed coverage`; dropping the own-project arm from the store owner fails
`TestPublicRenderReportsThePagesOwnProject`; the review's `SELECT 0` on both reporting queries now
fails with `the migration did not report`; re-gating the credit on `err == nil` fails with `0 delivery
credits for an announcement ONE recipient received`.

**On the frontend type-check.** The declared gate is `npm run build`, which runs `type-check` and
`build-only` together; both are green here, with and without `env.d.ts` in `tsconfig.include`. I could
not reproduce the red. `env.d.ts` is added to `include` anyway — it exists and was not being checked,
which is worth fixing whatever produced the red elsewhere.

## 46. The fix for the swallow contained the swallow

`ChannelDelivery.Resolved` is set immediately after `EnabledChannelsByIDs` — it counts channel ROWS
that exist and are enabled, before any SMTP session or HTTP request has happened. D-0179's credit read
it as if it meant "somebody was told".

So a service whose only channel returns 500 resolves ONE, delivers NONE, and was credited. Its
members' own alerts went silent for an announcement nobody received, and stayed silent: once the
outbox dead-letters the retry the latch is still firing and no further edge is coming. That is
precisely the failure D-0179 exists to close, rebuilt inside its own fix — and my
`TestAPartialDeliveryStillCountsAsAnAnnouncement` could not see it, because the fake had ONE field for
both numbers. A test asserting "somebody was told" passed on a delivery nobody got.

`ChannelDelivery` carries three numbers now — `Requested`, `Resolved`, `Delivered` — because "asked",
"existed" and "succeeded" are three facts and collapsing any two loses the one that matters. The
credit reads `Delivered`, still independently of `err`, so a genuine partial (two resolved, one
delivered) credits and retries.

**The clock fix had the same shape.** §44 threaded the caller's `asOf` into the service resolve and
called the class discharged. A caller's instant is post-ITS-locks, not post-THIS-one: the evaluator
takes `asOf` when it opens its snapshot, before the config-row locks and long before the incident
UPDATE can queue behind a manual writer. So the resolution was stamped from before the wait that
preceded it — the same defect wearing a parameter as a disguise. And `enqueueOutboxTx` was still there,
so the outbox row's three timestamps stayed at transaction start, while invariant 103 said "its outbox
row". The resolver takes the incident `FOR UPDATE` itself, reads `statement_timestamp()` after that
wait, and uses it for all six columns. The `asOf` parameter is REMOVED rather than ignored: an
argument the function overrides invites the belief that it is honoured.

The test is a real lock wait — a second connection holds the row, `pg_locks` is polled until the
resolver is genuinely blocked, a floor is read on a third connection, and every timestamp must be at
or after it. A timer would have passed whether or not anything ever queued.

**And the docs gate still accepted both of the review's mutations.** `max()` checked 1..max: an extra
discharge row above it was invisible, a hole below it was invisible, and a renamed heading would have
produced zero and accepted everything. It compares SETS now, in both directions, refuses a hole in the
spec's numbering, and dies loudly if either invariant section goes missing. Both mutations are red.

Two smaller ones. The design paragraph above the LIVE clause still argued for the predicate §45
replaced, explaining why equality had been rejected directly above the code requiring it — kept as a
record of the trade, marked as decided the other way, because the duplicate-page cost it names is real
and somebody will propose the swap again. And D-0182's inventory disagreed with itself across four
documents after "four" became five; it is now stated once as 3 repaired / 2 report-only, with the
third repaired class explicitly noteless — nothing about that row's history is wrong, so a repair note
would tell a reader something untrue.

**Mutation evidence:** crediting on `Resolved` fails with `coverage received 1 delivery credit(s), but
the only resolved channel failed`; reading the clock before the lock, and swapping `enqueueOutboxAtTx`
back, each fail with `is BEFORE the lock was released`; the extra discharge row and the spec hole are
both caught by name.

## 47. The P0 was fixed at the fake and never at the notifier

Four findings, and the first is the one that matters: nothing in `internal/notify` had ever called
`DeliverChannelsReporting`. The whole D-0179 credit rests on `ChannelDelivery.Delivered`, that method
is the only place it is computed, and the tests proving the fix drove a FAKE whose field a test set by
hand. Delete the production `out.Delivered++` and the entire suite stays green while the real notifier
reports zero for every successful send — so the fix I had just shipped for a P0 was pinned at the
boundary and absent at the mechanism.

That is the same defect class this arc keeps producing, now in its sharpest form: the evidence stopped
one layer above the code. `TestChannelDeliveryReportsAskedExistedAndSucceeded` drives the real
Dispatcher over `httptest` — all four shapes, including the P0's own (a channel that exists and
returns 500) and §16.4a's (a snapshot recipient deleted since the announcement) — and
`TestChannelDeliveryCountsEmailSendsThatSucceeded` does the SMTP branch, which has its own increment on
its own path. Removing either production increment now fails.

**The spec still described the clock the code had stopped using.** Invariant 103 said "the CALLER's
post-lock instant" while §46 had removed the caller's instant entirely in favour of the resolver's own
lock and its own `statement_timestamp()`. Under AGENTS the spec wins, so for that range the canonical
contract was literally unimplemented — and the discharge map and status.md were both describing the
CODE. Corrected in the spec, which is where the requirement lives.

**The SET gate folded duplicates away.** Two `103.` entries with different text passed, because
`fr021_invariant_numbers()` built a set before anything could notice. The discharge map can only match
one of them, so the other is a requirement nothing checks. It collects a list now and refuses
duplicates by name before comparing.

**And the lock-wait proof was not scoped to the lock it claimed.** `waitUntilBlocked` took an
`incidentID` it never used and accepted ANY ungranted tuple lock in the cluster, or any active query
matching `FROM incidents … FOR UPDATE`. On a shared or concurrent database a stranger's waiter
satisfies that, and the floor read afterwards is one the test never earned — a determinism argument
that was itself indeterminate. It captures both backends' pids and polls `pg_blocking_pids` for
exactly that pair.

**Mutation evidence:** deleting the HTTP increment fails with `requested/resolved/delivered = 2/2/0,
want 2/2/2`; deleting the email increment fails with `a successful mail reported delivered=0`; the
duplicate `103.` is refused by name; and the clock mutation still fails through the newly scoped wait
with `is BEFORE the lock was released`.

## 48. Three descriptions of a test that no longer exists, and the edit that caused one

The review approved the product implementation and closed on one P2: the living evidence still
described the superseded test and the superseded clock in three places. The test's own doc comment
opened on "the caller's post-lock instant" when there is no caller instant any more, and both
`docs/traceability.md` and `docs/status.md` said the proof "polls `pg_locks`" when it polls
`pg_blocking_pids` for one specific waiter/holder pair.

All three are corrected in the living documents. The chronological §46 → §47 narrative keeps its
history, because a report that rewrites what it thought at the time is not a report.

One of the three is worth naming as a mistake in HOW I edit, not in what I wrote. The traceability
row had been fixed — and then unfixed in the same script, because a later `write_text` wrote from a
variable that predated the earlier replacement. Two independent edits to one file, the second losing
the first, and nothing complained: the file was written, the gate passed, and the sentence quietly
reverted. Sequential `p.write_text(s.replace(...))` calls that each start from the ORIGINAL `s` are
the shape to avoid; chain the replacements into one value and write once.

The test comment now also records why the caller's `asOf` was not the same thing, since the parameter
is gone and the reasoning would otherwise be findable only in a decision record.

## 49. The gate the whole arc never ran

Every finding in this report was discharged with `-race`, mutations and, where the schema moved, both
storage modes. None of it had been near a browser. The process in `CLAUDE.md` asks for E2E on a live
stack, and forty-nine sections in, that step had simply never happened — the omission was invisible
because each individual slice was gated so thoroughly.

It matters more than usual here. This arc changed the incident detail view, the monitor detail view,
the search box, the escalation form, the service alerting badge's vocabulary, the public status-page
render and the feed — surfaces whose defects are exactly the kind unit tests do not see, and two of
them (the tenant context and the A→B navigation) were found by an operator's screenshot in the first
place.

`make dev-up` → `make dev-test`: **48 passed, 1 skipped** against a stack built from this branch. The
skip is `file-providers.spec.ts:23`, the documented idle-provider case — no file-managed monitor
exists in a stack with no file provider configured — and it is the known skip `CLAUDE.md` names.

The stack's own migration run is worth recording as evidence in its own right: `dev-up` migrates
before starting, so 00089 and 00090 applied to the DEV database, which holds months of accumulated
rows rather than a fresh fixture. `goose: successfully migrated database to version: 90`, and the
repair was a no-op there — zero resurrected rows, zero repair notes — which is the correct outcome
for data that never hit the race, and the only way to see that a repair migration leaves healthy
data alone is to run it over some.

## 50. Four items the owner asked for, and the two fakes that could not have caught the fifth

The owner took the remaining backlog: PostgreSQL 14 declined (D-0183), the dormant-binding axis kept
as it stands (D-0184), the service renotify cadence built (D-0185), the bounded delivery budget built
(D-0186). One item was left at the time of writing — the re-edge on a zero-resolved delivery, held by
an episode-identity decision rather than by nobody getting to it — and it was built in the same
sitting: §51, D-0187.

**The cadence.** D8 declined the knob and justified it with a false claim; D-0181 corrected the claim
and left the knob to its own decision, because inventing a cadence inside a bug fix is the thing the
review warned about. Built now with three properties that make it an added control rather than an
imposed one: zero by default so nothing starts repeating because a column appeared, a 60-second floor
because a shorter cadence is a way to page somebody every few seconds by typing one number, and the
value read LIVE rather than frozen into the ladder — the snapshot exists so an incident climbs the
steps it started with, while turning a repeat down mid-incident is an instruction about the present.

**The budget.** The claim token fences the SETTLE and cannot fence the SEND, because the fence lives
in the database and the send does not. The delivery context's deadline now comes from the claim's
lease minus headroom; a spent lease sends nothing at all; SMTP, which dialled on
`context.Background()` and ignored every budget, takes the caller's context and sets its connection
deadline to the EARLIER of the session span and the deadline.

The load-bearing detail is what is NOT bounded. The settling writes run on the caller's context,
because a delivery that used its whole budget would otherwise reach `MarkOutboxDelivered` with a dead
context — and a successful send recorded as a failure goes back to the queue and pages everyone
twice. The fix would have introduced the duplicate it exists to reduce. The headroom is the same
concern from the other end.

**And two fakes had to be corrected before the mutations meant anything.** The notifier fake blocked
on `ctx.Done()` with no fallback, so removing the bound HUNG the suite rather than failing it — I
found that by running the mutation, not by reading the code, and a mutation that hangs teaches
nothing. The store fake ignored its context entirely, so the mutation moving the settle onto the
bounded context passed cleanly. Both are the same lesson this arc has been repeating: a double that
does not model the property being asserted cannot witness its absence. It is the fiftieth section and
it caught me twice more.

**Mutation evidence:** removing the bound fails with `the deliverer was called 1 time(s) for a row
this worker no longer owns`; removing the headroom, with `the delivery budget is 29.99s of a 30s
lease`; moving the settle onto the bounded context, with `a delivery that used its whole budget was
not settled`. For the cadence: a constant zero fails with `the repeat fired 0 step(s) after six
minutes of a five-minute cadence`, and dropping the column from the generation trigger with `changing
the repeat cadence left coverage armed`. Invariants 100 (rewritten) and 104.

## 51. The outage nobody heard about, finally announced

The last item, and the one that had a real reason to wait. D-0179 stopped the swallow by refusing to
arm coverage for an announcement that reached nobody, so the members keep paging — safe, and silent
about the service's own alert forever. The latch says firing, the evaluator sees no edge, and fixing
the channel changes nothing. The incident stays open with nobody ever told.

What blocked it was episode identity: one open episode per (service, signal, target, rule), so a
second onset cannot be opened beside the first. The answer turned out to be that the ordinary onset
path already closes an open episode before opening the next one — so a re-announcement needs no
special case at all, and gets a new sequence, a fresh recipient snapshot and its own episode by
construction. Inheriting the old snapshot would have been the wrong half: it names people who could
not be reached.

**The trigger needed a second column, and that is the part worth the words.** `delivered_seq <
emitted_seq` is also true of an event still sitting in the outbox, so re-announcing on it would send
a duplicate of something merely slow. `undelivered_seq` says CONDEMNED — attempted, reached nobody, no
retry owed — and the worker sets it only on the two terminal paths. A 500 is retried and does not
condemn, which is its own test case because getting that boundary wrong is a pager duplicate in one
direction and permanent silence in the other.

Two smaller decisions inside it. The superseded episode is closed as `undelivered`, a new reason,
because the onset path's `policy_changed` would be a false cause in a record an operator reads during
an incident. And D-0176 still applies: a re-announcement with nobody to tell is WITHHELD and counted,
because announcing into the same emptiness on every cadence is a loop rather than a fix — the test
asserts the third pass is silent for the same reason.

What is NOT covered is named rather than implied: an event that dies by exhausting its retries and
dead-letters. That path is generic to the outbox and does not know it is a service alert.

**Mutation evidence:** disabling the re-announcement fails with `the outage was not re-announced after
the route came back`; triggering on "not delivered yet" instead of "condemned" fails with `a service
that stayed down announced again`. Invariant 105.

## 52. Six findings, and the one that says the most about how I test

The review returned D-0185 through D-0187. No P0, six P1s, and the first is the one worth leading
with.

**`renotify_seconds` could not be saved.** `alertPolicyDiff` is the audit line AND the no-op gate, and
the new field was missing from it — so a renotify-only PATCH returned 200, echoed the new value back,
and committed nothing. The feature I had just built, tested and documented did not work through the
only door an operator uses. Every test of it wrote the column with `UPDATE services`, which is why
the suite was green: the cadence was pinned everywhere except the write path. `TestEveryPolicyFieldSurvivesTheWritePath`
walks each field through `UpdateServiceAlertPolicy` and reads the row back, which is the shape that
would have caught it on the day.

**And a file-managed service could not set it at all.** Those services refuse UI paging edits with
409 by design, so a field the API accepts and the bundle does not is a field those operators simply
cannot have. `rawServiceAlerting` carries it now, with a pointer for the same reason as
`confirm_evaluations`: `renotify_seconds: 0` means "never repeat" and must stay distinguishable from
an omitted key.

**Three gaps in the delivery budget, all of them its own shape.** The RECORDING writes — the coverage
credit and the condemnation — were still on the bounded context one layer below the settle I had
carefully unbounded, so a send that used its whole budget lost both. The subscriber confirmation still
dialled on `context.Background()`. And the budget was `time.Until(leaseUntil)`: a database timestamp
minus a worker timestamp, which under skew either delivers past the lease or skips a valid claim. The
claim now returns the database's clock alongside the lease and the budget is their difference.

**A claim taken and never attempted was burning its retry.** A batch takes up to fifty rows and
increments every attempt; the worker delivers one at a time; a slow event at the front leaves the ones
behind it past their lease. Returning without delivering is right, spending their attempt is not —
enough turns and an event dead-letters having never been sent once.

**The burn arm had none of D-0187.** The migration touched both latch tables and the worker could
condemn either signal, and I gave only the live evaluator a re-announce branch. An undelivered burn
FIRE stayed firing forever, on the signal nobody watches.

**And "not covered: an event that exhausts its retries" did not survive its own invariant.** 105 says
the trigger is "no retry owed"; an announcement that ran out of attempts is as permanently unheard as
one whose channels were deleted. I had written the exclusion as though naming it made it acceptable —
it is a real exclusion only if the words above it exclude it too, and they did not.

Two fakes needed correcting again before the mutations meant anything: `MarkServiceAlertDelivered` and
`MarkServiceAlertUndeliverable` ignored their context, so moving the credit onto the bounded context
passed cleanly. That is the third time in two sections and the fifth in the arc.

**Mutation evidence:** removing the field from the diff fails with `renotify_seconds reads back as 0
after a write that returned success`; a 30-second cadence from a bundle is rejected by name; moving
the credit onto the bounded context fails; an unbounded confirmation fails; removing the refund fails
with `released = []`; the cross-clock comparison fails with `a valid 30-second lease was skipped`;
disabling the burn re-announce fails; removing the dead-letter condemnation fails with `0
condemnations for an announcement that exhausted its retries`.

**Also corrected:** the canonical spec still said a service has no cadence (invariant 100), §16.6a
listed four alerting fields, and this report still said D-0187 was open. `make docs-check` cannot see
any of that — it checks numbers and links, not whether a sentence is still true.

## 53. Gates

`go build`, `go vet`, `gofmt`, `make docs-check` clean; `internal/store` `-race` green; the guard verified
on a real PostgreSQL 14 container (both directions); frontend `vue-tsc` and unit tests green; SPA snapshot
refreshed with the footer change.

For §7 and §8: `make docs-check` clean; frontend unit suite `138 passed (17 files)` including the new
display-order regression, with its mutation verified to fail; `npm run build` (`vue-tsc` + vite) clean;
`make spa-snapshot` refreshed, because `go install` embeds what is committed under `internal/web/dist`
and nothing else; `go test ./internal/web/` green on the placeholder fixture the snapshot restores.

For §9 through §13: `go test -race -count=1 ./...` green against a fresh Timescale container
(`internal/store` 338s, 324s, 328s, 329s, 332s), every new regression's mutations verified to fail —
including the one that exposed a vacuous fixture, `go build`, `go vet`, `gofmt` and `make docs-check`
clean. §13 adds migration 00085, so it also ran in DECLARATIVE-PARTITION mode on a throwaway
`postgres:16-alpine` (`internal/store` 312s, green). Containers were removed as soon as each run
finished.

For §37: `go test -race -count=1 ./...` green, both mutations verified to fail with their own safety
sentences, `go build`, `go vet`, `gofmt` and `make docs-check` clean. The reason enum grew, so also
`npm run gen:api`, `vue-tsc` + `npm run build` green and `make spa-snapshot` refreshed and committed —
`go install` embeds what is under `internal/web/dist` and nothing else. No migration.

For §52: `go test -race -count=1 ./...` green in BOTH storage modes (Timescale 367s, throwaway
`postgres:16-alpine` 340s), eight mutations verified to fail, frontend build green, `go vet`,
`gofmt`, `make docs-check` and `git diff --check` clean. Containers removed and `docker ps` checked.

For §51: `go test -race -count=1 ./...` green in BOTH storage modes (Timescale 368s, throwaway
`postgres:16-alpine` 345s — 00092 changes the schema), two mutations verified to fail, `go build`,
`go vet`, `gofmt`, `make docs-check` and `git diff --check` clean. Containers removed and `docker ps`
checked.

For §50: `go test -race -count=1 ./...` green in BOTH storage modes (Timescale 370s, throwaway
`postgres:16-alpine` 342s — 00091 changes the schema), five mutations verified to fail, frontend
build and 148 tests green, `make spa-snapshot` refreshed, `go vet`, `gofmt`, `make docs-check` and
`git diff --check` clean. Containers removed and `docker ps` checked.

For §49: `make dev-up` on a stack built from this branch, then `make dev-test` — 48 passed, 1 skipped
(`file-providers.spec.ts`, the documented idle-provider case). Migrations 00089 and 00090 applied to
the dev database on the way up. Stack torn down with `make dev-down` afterwards.

For §48: documents and one test comment; no product change. `go test -race -count=1 ./...` green,
`go vet`, `gofmt`, `make docs-check` and `git diff --check` clean.

For §47: `go test -race -count=1 ./...` green in BOTH storage modes (Timescale 361s, throwaway
`postgres:16-alpine` 337s), four mutations verified to fail — both production `Delivered++`
increments, the duplicate invariant number, and the clock through the re-scoped wait — `go build`,
`go vet`, `gofmt`, `make docs-check` and `git diff --check` clean. Containers removed and `docker ps`
checked.

For §46: `go test -race -count=1 ./...` green in BOTH storage modes (Timescale 360s, throwaway
`postgres:16-alpine` 339s), five mutations verified to fail — the credit source, the pre-lock clock,
the outbox enqueue, and both of the review's docs mutations — `go build`, `go vet`, `gofmt` clean,
`make docs-check` clean under the SET-comparing gate, and `git diff --check` clean. Containers removed
and `docker ps` checked.

For §45: `go test -race -count=1 ./...` green in BOTH storage modes (Timescale 371s, throwaway
`postgres:16-alpine` 338s), four mutations verified to fail including the reviewer's own `SELECT 0`,
`go build`, `go vet`, `gofmt`, `make docs-check` clean under the STRENGTHENED gate, frontend build and
148 tests green, `make spa-snapshot` refreshed. Containers removed and `docker ps` checked.

For §44: `go test -race -count=1 ./...` green in BOTH storage modes — Timescale (`internal/store`
359s) and a throwaway `postgres:16-alpine` (336s), because 00090 adds a constraint — both mutations
verified to fail, `go build`, `go vet`, `gofmt` and `make docs-check` clean. `cerbix_test` was dropped
and recreated first, since the migration is new. Containers removed and `docker ps` checked.

For §43: frontend only — `vue-tsc` clean, 148 unit tests in 19 files, `npm run build` green, three
mutations verified to fail, `make spa-snapshot` refreshed and committed.

For §42: `go test -race -count=1 ./...` green, the mutation verified to fail AFTER the test was fixed
to reach its branch, `go build`, `go vet`, `gofmt` and `make docs-check` clean. Frontend `vue-tsc` +
build green and `make spa-snapshot` refreshed. No behaviour change in Go, no migration.

For §41: `go test -race -count=1 ./...` green, both mutations verified to fail, `go build`, `go vet`,
`gofmt` and `make docs-check` clean. No migration and no schema change — one new query, one rewritten
one, and two call sites.

For §40: `go test -race -count=1 ./...` green in BOTH storage modes — Timescale (`internal/store` 371s)
and a throwaway `postgres:16-alpine` (333s), because 00089 changes the schema — both mutations verified
to fail, `go build`, `go vet`, `gofmt` and `make docs-check` clean. The reason enum grew again, so
`npm run gen:api`, `vue-tsc` + build, and `make spa-snapshot` refreshed and committed. Containers
removed as each run finished, and `docker ps` checked rather than asserted.

For §36: `go test -race -count=1 ./...` green, three mutations verified to fail with their own
safety sentences, `go build`, `go vet`, `gofmt` and `make docs-check` clean. No migration.

For §35: `go test -race -count=1 ./...` green, both service-path mutations verified to fail with
their own safety sentences, `go build`, `go vet`, `gofmt` and `make docs-check` clean. Docs and one
test comment only besides the new regression; no migration.

For §34: `go test -race -count=1 ./...` green (`internal/store` 363s) against the Timescale container,
the parity mutation verified to fail with the safety message, `go build`, `go vet`, `gofmt` and
`make docs-check` clean. No migration, so one storage mode is the whole gate. Container removed
immediately after the run.

For §14: `vue-tsc` clean (it caught a second latent defect the moment the cast went), frontend unit
suite `139 passed (17 files)` with the new regression's mutation verified to fail, `npm run build`
green, `make spa-snapshot` refreshed and committed with the change, `go test ./internal/web/` green.

For §15: `make docs-check` clean with the new check in place, and failing on both restored banners
when mutated.

For §16: `go test -race -count=1 ./...` green on a fresh Timescale container (`internal/store` 334s)
and the store suite green again in DECLARATIVE-PARTITION mode on `postgres:16-alpine` (314s), since
00085 changed; both migration mutations verified to fail, the invalid one redone; `go vet`, `gofmt`,
`make docs-check` clean; containers removed.

For §17: the same pair again because 00085 changed again — full `-race` suite on Timescale
(`internal/store` 335s) and the store suite in declarative-partition mode (317s); both mutations
verified to fail; static gates clean; containers removed.

For §18: full `-race` suite on a fresh Timescale container (`internal/store` 337s), the new refusal
regression's mutation verified to fail, `go vet`, `gofmt` and `make docs-check` clean. No migration
changed, so the declarative run from §17 stands.

For §19: full `-race` suite on a fresh Timescale container (`internal/store` 337s); four mutations
verified to fail, one per rule plus the SPA's; frontend `141 passed (17 files)`, `vue-tsc` and build
green, `make spa-snapshot` refreshed and committed with the change, `go test ./internal/web/` green;
static gates clean. No migration changed.

For §20: full `-race` suite (`internal/store` 339s), frontend `142 passed (17 files)`, `vue-tsc` and
build green, snapshot refreshed, `go test ./internal/web/` green, static gates clean. Three mutations
verified to fail; one of them had to be redone after the first attempt broke the build instead of the
behaviour — a syntax error is not evidence, and this is the third time in this arc that trap has been
worth naming.

For §21: full `-race` suite (`internal/store` 346s), both mutations verified to fail, static gates
clean, no migration changed.

For §22: full `-race` suite (`internal/store` 354s) after repairing four fixtures, both mutations
verified to fail, static gates clean, no migration changed.

For §23: full `-race` suite (`internal/store` 341s, `metrics` and `scheduler` green), the burn
mutation verified to fail, static gates and `make docs-check` clean, no migration changed.

For §24: full `-race` suite on Timescale (`internal/store` 336s) and the store suite in
declarative-partition mode (317s), since 00086 is a new migration; both gate mutations verified to
fail; static gates and `make docs-check` clean.

For §25: full `-race` suite (`internal/store` 350s), four mutations verified to fail, frontend
`142 passed (17 files)` with `vue-tsc`, build and a regenerated `schema.d.ts` after the OpenAPI enum
grew two reasons, `make spa-snapshot` refreshed, static gates clean. No migration changed.

For §26: full `-race` suite (`internal/store` 338s, `metrics` and `scheduler` green), the burn
mutation verified to fail, static gates and `make docs-check` clean. No migration changed.

For §27: full `-race` suite (`internal/store` 341s, `metrics`, `outbox` and `scheduler` green), both
mutations verified to fail, static gates and `make docs-check` clean. No migration changed.

For §28: full `-race` suite on Timescale (`internal/store` 356s) and the store suite in
declarative-partition mode (325s), since 00087 is a new migration; three mutations verified to fail;
static gates and `make docs-check` clean.

For §29: full `-race` suite (`internal/store` 350s, `outbox`, `api` and `scheduler` green), two
mutations verified to fail plus one redone after an invalid attempt, static gates and
`make docs-check` clean. No migration changed.

For §30: full `-race` suite on Timescale (`internal/store` 359s) and the store suite in
declarative-partition mode (327s), since 00088 is a new migration; three mutations verified to fail;
static gates and `make docs-check` clean.

For §31: full `-race` suite (`internal/store` 351s, `domain` and `outbox` green), the sequence
mutation verified to fail, static gates and `make docs-check` clean. No migration changed.

For §32: full `-race` suite on Timescale (`internal/store` 353s) and the store suite in
declarative-partition mode (334s), since 00088 changed; both mutations verified to fail against the
migration itself; static gates and `make docs-check` clean.

For §33: documentation only — `make docs-check` clean. No code changed, so the §32 suite runs stand.
