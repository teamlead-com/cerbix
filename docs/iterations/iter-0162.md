# iter-0162 — the FR-024 design gate, its UI mock, and the owner's decision to defer

> **Lifecycle: CLOSED 2026-08-28 (D-0202).** Docs only: no product code, no migration, no Vue file. This
> iteration records work that happened outside the iteration workflow — the reviewer's [63] named the
> omission — so that the design gate, the mock and the deferral exist as an iteration with atomic
> acceptance criteria and role outputs, not only as commits. The closing commit is the one that adds
> this file; the report is immutable from it.

`iter-0161` is closed and immutable, so this arc — which ran in parallel with its close-out and after it —
gets its own report rather than a §55.

## 1. What happened, in order

1. In the party opened after `v0.1.5`, the reviewer proposed "Reliability Change Control" — a deploy gate a
   CI pipeline asks before releasing a service, decided from the service's error budget and live alerts.
   The owner answered the five design questions D9–D13 (D-0188) and said to write it as a **design gate**:
   a specification reviewed to approval before any code.
2. `docs/specs/func-reliability-gate.md` (FR-024) with NFR-019 went through **thirteen revisions and twelve
   focused review rounds** (party [15] → [47]). Owner decisions: D-0188 (D9–D13), D-0189 (round-1
   questions), D-0190 (the seal-lag bound), D-0193 (capacity and partition-drop retention), D-0194 (daily
   partitions). Implementer-authority revisions: D-0191, D-0192, D-0195 … D-0200. Design **APPROVED at
   revision 13** (party [47], D-0201); the close-out's own stale wording was corrected on [49] and the
   approval commit amended in place (`07a1e9b`).
3. The **UI mock** `docs/design/mock-reliability-gate.html` — six screens, tokens verbatim from
   `frontend/src/style.css`, no new colour — went through three revisions against the reviewer's static
   contract pass ([55], [57]) and was **static-contract APPROVED at revision 3** ([59]). It was never
   visually approved: the reviewer has no browser, and the owner deferred before looking.
4. The owner asked what the specification is for. The answer — and its origin in the reviewer's proposal
   rather than a user ask — led to **option 2 of three: defer** (D-0202). FR-024/NFR-019 stay `TODO`; the
   design and the mock are kept; nothing is built.
5. Two close-out corrections on the owner's word and the reviewer's [63]: revision commits are not made
   one per review round (the standing commit policy; revisions 10–13 were kept in the working tree and
   committed once), and the deferral is stated in row text rather than as a fourth status value — the
   `docs-check` vocabulary stays at AGENTS' three states.

## 2. Plan, as executed

| Priority | Task | Outcome |
| --- | --- | --- |
| P0 | Close the design gate: banner, D-0201, status rows, the guard's post-approval retirements | done at `07a1e9b` |
| P0 | Produce the UI mock before any frontend code, per the standing gate | `docs/design/mock-reliability-gate.html`, three revisions, static contract approved [59] |
| P1 | Record the owner's deferral so the design cannot rot silently and the resume path is written | D-0202, spec banner, status rows, traceability row, notes.md |
| P1 | Keep `make docs-check` authoritative over the arc: retired spellings of every superseded revision, fixture tests | `scripts/check-docs-references.py`, `scripts/check_docs_references_test.py` (18 fixtures) |
| P2 | Close the arc as an iteration with AC/DoD and role outputs | this report |

## 3. Role outputs

- **Agent A — Implementation:** none, by the owner's decision (D-0202). No product code, no migration, no
  configuration, no Vue file was touched in this arc. The specification cites code that exists
  (`LeaderSession`, `serviceFactMaintenanceLoop`, `subCadenceTimeout`, the storage-mode branches) as the
  shapes an implementation would use; none of it was changed.
- **Agent B — Tests:** the documentation gate's own tests. `scripts/check_docs_references_test.py` grew from
  0 to 18 fixture cases across the arc (stale-spelling scope, fenced-block handling, decision sections by
  heading, status rows, the retired vocabulary of revisions 6–12, post-approval wording); the mock's script
  was syntax-checked with `node --check` and both raw-record JSON examples machine-validated after HTML
  stripping. `make docs-check` ran green on make's exit code after every revision. No Go or E2E tests were
  added or run: there is no code under test.
- **Agent C — Docs + Traceability:** `docs/specs/func-reliability-gate.md` (revision 13, banner DEFERRED);
  `docs/status.md` (FR-024/NFR-019 rows `TODO` with the deferral and the resume path; this iteration's
  block); `docs/decisions.md` D-0188 … D-0202; `docs/traceability.md` (FR-024 row, deferred, no evidence);
  `docs/design/notes.md` (the mock's section, marked deferred — neither approved nor rejected visually);
  `docs/specs/README.md` (the spec's row).
- **Agent D — Observability + Ops:** no runtime delta. The metric and alert names in the specification
  (`cerbix_gate_*`, the runbook thresholds of §4) are design; nothing is emitted, nothing is alerted on,
  no runbook section was added because there is nothing to operate.

## 4. Checks run

`make docs-check` (18 fixture tests + the full references/acceptance-map pass) — PASS on every revision
and on the close-out; `git diff --check` — clean; `node --check` on the mock's script — OK; both mock JSON
examples valid after span stripping. No `-race`, no E2E: no code changed.

## 5. Corrections of record made in this arc

- **Per-review-round commits, contrary to the standing policy.** Revisions 6–9 were committed one per
  reviewer round (`4e5bd15`, `476643a`, `63c1d57`, `80a89ff`) although the commit policy of 2026-08-16
  says otherwise. The owner repeated the instruction; revisions 10–13 stayed in the working tree and were
  committed once on approval. The four earlier commits remain in local history; squashing them is the
  owner's call.
- **A fourth status value, reverted.** The first deferral commit set FR-024/NFR-019 to `DEFERRED` and
  widened the `docs-check` vocabulary to admit it. AGENTS.md and `docs/status.md` allow three states; the
  owner asked why `TODO` would not do, and the reviewer's [63] named the same defect. Reverted in place;
  the deferral lives in the row text, the banner and D-0202.
- **The "why" arrived twelve rounds late.** A design gate opened on a reviewer's proposal needs the
  owner's reason in the specification's first paragraph before round 1. It was inferred from a "давайте"
  instead, and the question "what is this for?" came after approval. Recorded in D-0202 so the next gate
  starts with it.
- **The arc ran outside the iteration workflow.** `docs/status.md` named `iter-0161` CLOSED as current
  while two commits added and closed a design phase with no iteration, no AC/DoD and no role outputs.
  This report is the correction; it records what happened, not a feature shipped.

## 6. Resume path (verbatim from the spec banner and D-0202)

A fresh focused review of the specification against the then-current code, then the owner's visual pass
on the mock in both themes, then implementation discharging all 21 invariants of §6 and the full §7 matrix
with named tests and mutations, including the PostgreSQL 15.8 prepared-statement case run on a throwaway
container. Until someone asks for the gate from CI, none of that is scheduled.
