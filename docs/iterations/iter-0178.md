# iter-0178 — the release-readiness sweep: a topology that disagreed with itself, a carrier that admitted nothing, and a guard with a hole

> **Lifecycle: OPENED 2026-09-06.** Requirements: FR-020 (the wiring defect and the carrier that
> admitted nothing), NFR-025c (the remaining half of the zone contract), and the release paperwork.
> Decision: `D-0246`. Scope for the two FR-020 fixes was granted by the independent reviewer at
> party [111] and extended at [115]; **his results are audit conclusions and are not closure,
> commit, push, tag or release authority**, and he is mid-audit of this slice at [117].
> **Nothing here is a release declaration.** The owner asked whether cerbix is ready to
> release; this iteration exists because the honest answer was "the gates that would tell you
> have not all been run", and running them found two defects, one behind the other — and closing the
> last half of NFR-025 found three more claims that were not true.
>
> `iter-0177` is CLOSED and closed reports are never edited, so this work goes forward into its
> own iteration rather than into a report that already has a conclusion.

## 1. The defect: two halves of one topology disagreeing about credential envelopes

`make dev-test-distributed` failed on the tree that had just passed every other gate:

```
  1 failed
    [chromium] › probers.spec.ts:49:7 › probers against the stack itself ›
      test-connection probes postgres/tcp instantly; unknown region → 502
  1 skipped
  10 passed (1.3m)
```

The API answered `no worker queue for region "core" (AMQP 312 NO_ROUTE)` for a test-connection
against a region whose worker was up, healthy and consuming.

**It had nothing to do with the code being tested.** The distributed topology mounts
`config.dev.yaml` in `api` and `scheduler`, and `config.worker-core.yaml` in `worker`. The first
carries `secrets.dispatch_envelope: "enforced"`; the second carried no `secrets` block at all. So
the API published a credentialed test onto the v2/v3 carrier — `EnvelopeEnforced()` decides which
prefix a publish takes — while the worker, with the same predicate reading false, had started only
the v1 consumers. Nothing was bound to the queue the publish named, and RabbitMQ said so.

**The generalisable part, and the reason this is written down rather than just fixed:** a
capability whose two halves live in two configuration files is not covered by any test that reads
one file. Both sides had tests. The *agreement between them* had none — the same wiring-boundary
shape D-0239 recorded earlier in this arc — a builder defined, documented, and never called.
Both ends proven; the wire proven by nothing.

The second-order fact is worse than the failure: **FR-020's credentialed dispatch was exercised
only by the single-process stack.** In the topology that most resembles production it had been
broken for as long as `config.dev.yaml` has enforced envelopes, and nothing said so, because the
one gate that would have noticed is not part of `make dev-test`.

## 2. The fix, and the boundary it does not cross

`docker/config.worker-core.yaml` gains two blocks:

- `secrets: {enabled: true, dispatch_envelope: "enforced"}` — the executor now starts the v2/v3
  consumers, so it serves the carrier the API actually publishes on.
- `security.dispatch.regions.core.primary` — the same fixed, public, DEVELOPMENT-ONLY key id
  `dev-2026a` that `config.dev.yaml` seals with. A keyring that does not match is worse than none:
  it accepts the delivery and fails to open it, later and less legibly.

It does **not** gain `security.encryption_key`. The at-rest master never leaves core; an executor
holds only its own region's dispatch keys, so a leaked executor exposes at most that region's
payloads. `ValidateSecretsForRole` already refuses a worker config carrying the master or
`previous_keys` — the regression below pins that it stays refused, because the easy wrong fix for
this defect is to copy the whole `security:` block across.

**Geo is deliberately unchanged.** The reviewer's scope grant named the geo worker too, on the
assumption that it had the same split. It does not: `config.geo.yaml` (the geo central role),
`config.worker.yaml` and `config.agent.yaml` carry no `secrets` block at all, so that topology
publishes generation-1 carriers and its executors have nothing to open. "Matching the API" there
means changing nothing. Handing the geo worker a keyring would start consumers nobody publishes to
— configuration that looks configured and does nothing.

**Recorded as an open coverage gap rather than a passed gate** (reviewer, party [113]): FR-020's
credentialed dispatch is not exercised by the geo topology at all. That is pre-existing, it is not
a regression, and it is not closed here.

## 3. The regression, and the four mutations that prove it reaches its mechanism

`TestDevRoleConfigsKeepAtRestMasterOutOfWorker` in `internal/cli/compose_wiring_test.go` loads both
real files through the real `config.Load` and asserts in both directions: the two sides agree on
whether envelopes are enforced, and when they do enforce, the executor holds the API's core key and
does not hold the master.

Each mutation reverts exactly one property of the fix. All four were planted and killed:

| # | Mutation | The assertion that failed |
| --- | --- | --- |
| M1 | remove the executor's keyring (the shipped state) | `secrets.dispatch_envelope: "enforced" requires at least one security.dispatch keyring` |
| M2 | executor stops enforcing, so the two sides disagree | `secrets.dispatch_envelope must be empty or "enforced": "off"` |
| M3 | keyring present, but not the id the API publishes with | `the executor's core key "dev-other" is not the one the API publishes with ("dev-2026a")` |
| M4 | executor handed the at-rest master | `worker config must not carry the at-rest master keyring` |

M3 is why the assertion is not "a keyring is present". M1 and M2 fail inside `config.Load` rather
than in the test body, which is worth naming honestly: for those two the *validator* is the guard
and the test's contribution is that it loads the file at all. M3 and M4 are the test's own.

## 4. What the fix uncovered: the generation-3 test carrier accepted nothing

The rerun was red again, and differently. The routing was repaired — the queue existed and had a
consumer — but nothing answered:

```
POST /api/v1/projects/…/monitors/test  →  502  {"error":"no worker responded in region \"core\""}
dur_ms = 10008
```

`serveEnvelopeTestsOnce` in `internal/dispatch/amqp.go` serves BOTH envelope test carriers —
`serveTestsGeneration(&d.testsV2Once, …, ProtocolV2, …)` and `(&d.testsV3Once, …, ProtocolV3, …)` —
and its admission check compared the delivery's own field against a hardcoded constant:

```go
if err := json.Unmarshal(msg.Body, &job); err != nil || job.ProtocolVersion != ProtocolV2 || job.CredentialEnvelope == nil {
    d.deadLetter("tests.v2", msg.Body)
    _ = msg.Nack(false, false)
```

So **every generation-3 Test Connection was dead-lettered by the consumer bound to its own queue.**
The caller is answered only on success, so it waited out its timeout and reported the region as
having no worker — the same sentence an operator gets when the region really is empty.

Not inferred. `checks.dead` held exactly one message, read through the management API:

```
header x-cerbix-source: "tests.v2"
"protocol_version": 3,
"credential_envelope": {"v":2,"region":"core","key_id":"dev-2026a", …}
```

The dead-letter label was hardcoded too, so the header named a carrier the message had not come
from.

**Why it survived, which is the same geometry as §1.** The v2 carrier has a test
(`amqp_test.go:135`) and it passes, because the hardcoded constant happens to be its generation.
The v3 carrier had none. The inproc dev stack never takes this path. The one gate that does is
`make dev-test-distributed`, and it was red for §1's reason, which masked this one. The comment the
wiring's own author left at `cli.go:1211` reads: *"A published generation with no consumer is a
queue that fills until TTL, which is how 'generation 3 for jobs only' would have silently broken
Test Connection."* The consumer was wired. It admitted nothing.

## 5. The dispatch fix and its regression

The body is no longer asked about its generation at all — **the queue is the generation**, exactly
as the jobs path already states in its own comment (§4.7, D-0160: *"the body's own ProtocolVersion
is attacker-editable … the payload's claim is never consulted here"*). The test path was the one
place that contradicted it, and it was wrong in both ways at once: it trusted a field it should not
have, and it compared that field to the wrong value. What remains is the single property a
generation ≥ 2 test carrier is DEFINED by — it carries a credential envelope. The dead-letter label
is derived from the generation by `deadLetterSourceForTests`.

`internal/dispatch/amqptestcarrier_test.go`:

- `TestEveryTestCarrierAnswersOnItsOwnQueue` — a table over EVERY test carrier (v1 plain, v2 with
  envelope v1, v3 with envelope v2), one runner behind all three exactly as `cli.go` wires them.
  A reply proves the delivery was admitted; the `CarrierGeneration` stamp proves the right consumer
  admitted it. Live broker, opt-in on `CERBIX_TEST_RABBITMQ_URL` like its neighbour.
- `TestTheTestCarrierTableCoversEveryServeEntryPoint` — a `go/ast` scan of `amqp.go` for every
  exported `ServeTests…` method, requiring a row in the table. **The set, not the count**: a fourth
  test carrier cannot be added and left unreachable the way the third was. Runs with no broker.

| # | Mutation | The assertion that failed |
| --- | --- | --- |
| M5 | the hardcoded `ProtocolV2` admission check restored | `v3_envelope_v2: test RPC on carrier 3: no worker responded in region "testcarrier"` — the production symptom, word for word |
| M6 | the v3 row dropped from the table | `test carriers with no row in testCarrierTable: [ServeTestsV3]` |

**Stated rather than left unsaid**, two things this does not do:

- The dead-letter LABEL is diagnostic text and no test asserts it. It is correct now; it is not
  guarded.
- **A refusal is still silence.** The consumer answers only on success, so any future
  dead-lettered test still reaches the operator as `no worker responded in region …`, which is
  indistinguishable from an empty region — and that indistinguishability is what cost this arc a
  debugging cycle rather than a glance. Changing it means replying with a `ProbeError` heartbeat,
  which is a change in wire behaviour, outside the scope granted for this iteration, and is
  recorded here as an open item rather than done quietly.

## 5a. A P0 the fix in §5 made reachable: an envelope older than its carrier defines

Found by the reviewer auditing **`0a1557c..d0a8fed`** — the four commits the owner authorized —
and it is the sharper half of §4. The commits that answer it, `4b5939d` onwards, are this session's
RESPONSE to the audit and are not part of the slice that was read; naming them as the audit's
boundary conflated the two, which is a reviewer P1 in its own right.

**What the code did.** Three places agreed that an envelope must not ride a generation-1 carrier,
and none of them checked WHICH envelope the carrier actually defines:

| Where | What it checked | What it did not |
| --- | --- | --- |
| `Jobs()` | the capability CEILING, `capability < envelope.V` | an envelope OLDER than the carrier |
| `serveEnvelopeTestsOnce` (after §5) | that an envelope is PRESENT | which generation it is |
| `ValidateAndMaterialize` | `CarrierGeneration < ProtocolV2` | generations 2, 3 and 4 |
| `store.envelopeForCarrier` | **exact, since generation 3 shipped** | — |

So the producer had been exact all along and every consumer enforced a floor. A generation-1
envelope published onto the generation-3 carrier opened normally and its credential reached the
prober.

**Why that matters, and it is not symmetry for its own sake.** Generation 3 exists to add the
EXECUTION BODY BINDING: from envelope v2 a digest of the body is mixed into every field's AAD, so
a credential cannot be replayed against a different target, type or transport. `bindingFor`
computes no digest below v2. Accepting v1 on a v3 carrier therefore reinstates exactly the
property the generation was introduced to remove — and under the specification's OWN threat model,
where the body is attacker-editable and the carrier is the trusted out-of-band signal, that is a
downgrade an attacker selects rather than an accident.

**Reproduced before it was fixed**, at the gate, with the credential materialized:
`ADMITTED: a v1 envelope rode the generation-3 carrier to the runner, used_credential=true`.

**The repair is ONE owner, not four matching `if`s.** `dispatch.EnvelopeForCarrier` holds the
mapping and `dispatch.CarrierEnvelopeAdmissible` the rule; `store.envelopeForCarrier` DELEGATES to
it, so producer and consumer evaluate one expression rather than two that agree today. The gate
enforces it for every executor path, and the two AMQP consumers additionally dead-letter — one
predicate, two dispositions, the same split `RequireLedgerFields` already uses.

| # | Mutation | The assertion that failed |
| --- | --- | --- |
| P1 | the mapping stops being exact (the shipped state) | the admissibility table AND the live v3 test carrier answering a v1 envelope |
| P2 | the TEST consumer stops checking, gate still does | the live tests path — the runner was invoked |
| P3 | the JOBS consumer stops checking, gate still does | the live jobs path — the downgraded job was delivered first |
| P4 | the producer reinstates a local switch that drifts | both producer tests, one of them by naming TWO OWNERS |
| P5 | a pull path builds a delivery and skips the gate | the `go/ast` scan, by function name |
| P6 | the TEST consumer drops the poison body instead of dead-lettering it | `no dead letter carrying "job-mismatch-carrierenvelope" arrived — the poison body did not survive` |
| P7 | the JOBS consumer drops instead of dead-lettering | the same, on the jobs path |
| P8 | the carrier question is asked before the unknown-version one | `carrier 3, envelope v3 reported "decrypt_auth_failed", want "unsupported_version"` |
| P9 | the legacy-carrier refusal goes back above the version question | `carrier 1, envelope v3 reported "decrypt_auth_failed", want "unsupported_version"` |

P5 is the one that keeps the design honest: enforcing at a single gate is only sound while the
gate is unavoidable, so a scan requires `ValidateAndMaterialize` in the same function as every
`DeliveredJob` construction outside the transport package.

**P6 and P7 exist because of a reviewer P1 on this patch, and the objection was right.** The
runner-not-called assertion proves the consumer REFUSED, and says nothing about how it disposed of
the message: with the consumer's check removed, the materializer gate would still stop the probe,
so "one predicate, two dispositions" was a claim the evidence did not reach. The live test now also
requires the poison body to arrive on `checks.dead` tagged with the carrier that dropped it —
`tests.v3` and `jobs.v3` — which is exactly what an operator reads when a Test Connection times
out (`docs/runbook.md`). Both halves are mutation-verified separately.

### 5b. The rule was right and four existing tests were not, which the reviewer found and I had not

**A gap in my own verification, and it is worth naming before the findings.** After the P0 fix I
ran `-run` filters over the tests I had just written and never the WHOLE package. The reviewer ran
`go test ./internal/dispatch` and it was RED. Verifying the new evidence and not the existing suite
is the same defect as a test that never reaches its mechanism, pointed the other way.

`TestScenarioBindingIsSubstitutedIntoTheScenario` failed with
`envelope generation 2 on carrier generation 2, which carries envelope 1` — a CORRECT firing of the
new rule against a fixture that paired envelope v2 with carrier generation 2, a combination the
producer never emits. And the fixture's four negative sub-tests, which all passed, were passing for
a reason none of them names: the carrier rule refused the delivery before the property under test
was reached. Repaired to generation 3, **except** "envelope that does not bind the body", which
KEEPS generation 2 deliberately — envelope v1 on generation 2 is a legitimate pair, so the carrier
rule admits it and the BINDING rule is what refuses, which is the property that case exists for.

**Verified rather than assumed:** with `CarrierEnvelopeAdmissible` neutered, every scenario-binding
case still passes. Each fails for its own reason.

**The sweep the reviewer asked for found four more, all in `internal/agent`**, all the same shape:
a fixture sealing envelope v1 and serving it from `/api/v1/agent/v3/...`, where an absent
`protocol_versions` legitimately falls back to the ENDPOINT's generation — 3, which carries
envelope v2. No core would put a v1 envelope there. Fixtures corrected.

**One of those four was not a fixture problem, and it is a wire-visible property.**
`TestFutureCredentialEnvelopeIsProbeErrorWithoutReadinessDowngrade` reported
`decrypt_auth_failed` where it required `unsupported_version`: my check answered a FUTURE envelope
version as a carrier mismatch, and the bounded probe-error taxonomy sends every structural
rejection to `decrypt_auth_failed` on purpose — the executor never tells a prober which way its
forgery was wrong. But an envelope version nobody implements already had an honest member of that
vocabulary, and reporting it as a decryption failure sends an operator hunting a key problem in the
middle of a rolling upgrade. **The taxonomy is a wire contract and is NOT widened:** the
unknown-version question is simply asked FIRST, so it keeps the sentence `Open` uses and the
existing mapping. `TestAnUnknownEnvelopeVersionKeepsItsOwnProbeErrorReason` pins both directions —
a future version reports `unsupported_version`, a KNOWN envelope on the wrong carrier stays
non-oracular — and P8 (asking the carrier question first) kills it.

**And the first version of that fix did not do what its own comment said** — reviewer P1, and the
defect class this arc is named for. The legacy-carrier refusal still ran ABOVE the version check,
so a future envelope on a generation-1 carrier reported `decrypt_auth_failed` while the comment
claimed "asked first" without exception; the test enumerated carriers 2 to 4 and did not look where
they differed. The rule is now one sentence and the code obeys it: **the question "is this a
version this executor implements at all?" is about the ENVELOPE alone, does not depend on the
carrier, and is asked first for every carrier, generation 1 included.** The test covers all four,
and the known-envelope case covers BOTH shapes — the wrong generation, and the carrier that takes
none. P9 (moving the legacy refusal back on top) fails it by naming carrier 1.

**A second reviewer P1 on this patch, and it was a real contradiction in a public function.**
`EnvelopeForCarrier(ProtocolV1)` answered `EnvelopeV1` — inherited from the switch it was lifted
from — while its own documentation and `CarrierEnvelopeAdmissible` both say generation 1 carries no
envelope and refuse every envelope there. A caller would have been handed a version the very next
check rejects. It is an ERROR now. Unreachable from `CarrierFor` today, which returns generation 1
only when there is nothing to seal — and a public function that is wrong in a way nobody currently
exercises is a trap laid for the next caller, which is the whole reason the reviewer raised it
rather than waiting for it to matter.

**What this says about §4.** The generation-3 test carrier had been unreachable since it was
wired, so nothing had ever ridden it. Repairing that did not create the hole — the jobs path
carried it too, and `Jobs()` was never broken — but it did make the test path reach it. A defect
hidden behind another defect is the ordinary shape of a second finding, and it is the argument for
auditing a repair rather than only its symptom.

### 5c. Two more P1s on the response itself, and both are about evidence rather than code

**Provenance.** This report and `D-0246` named the P0 as "reviewer P0 on `0a1557c..4b5939d`". The
audit slice is `0a1557c..d0a8fed` — the four commits the owner authorized — and `4b5939d` is one of
this session's own answers to the audit. Naming a response commit as the audit's boundary quietly
credits the reviewer with reading work he had not seen, and it contradicted `status.md`, which said
"mid-audit" correctly. Corrected in seven places, including the two Go source comments that carried
it.

**A live test that ate what it was reading.** `TestAMismatchedEnvelopeIsDeadLetteredOnBothAMQPPaths`
claimed a unique job id and used a CONSTANT, and it consumed the shared `checks.dead` queue with
`autoAck`. Two consequences, both fair: a concurrent run could take another run's dead letter as
its own proof, and — worse — the test permanently destroyed every unrelated operational dead letter
on that queue. The runbook tells an operator to read exactly those.

Both repaired: a per-run token (pid plus a nanosecond clock) now names the region, the queues and
the job id, so two runs cannot collide; and acknowledgement is manual — this run's own poison bodies
are acked, everything else is held UNACKED and requeued by the broker when the channel closes.
**Verified rather than asserted:** a foreign message was published to `checks.dead`, the test was
run, and the queue still held it afterwards.

## 6. NFR-025c: the hand-rolled date surface, and three claims about it that were false

The requirement's last open half. `func-truthful-rendering.md` §9 had it enumerated — 46 matches
on 26 lines across 13 product files, bounded by a per-file ratchet with a reason each — and the
enumeration is where the interesting part turned out to be. **The decisions are in §9's table;
what belongs here is what the enumeration got wrong.**

1. **"day-grid MAP KEYS … never rendered" was false for all three strips.** Each 90-day strip
   wrote `label: key`, and that label is a tooltip: `2026-06-08 · no data`. The key and the
   rendering were the same string, so exempting the key exempted a rendering. Split now —
   `utcDayKey` for the lookup, `utcDayLabel` for what a reader sees — and pinned by a surface
   assertion in `MonitorDetailView.spec.ts`, because no source guard can catch this one:
   `utcDayKey` is a legitimate call.

2. **"already honest in its own words (` Z`)" pointed at dead code.** `clockSecondsLabel` was
   exported from `lib/changes.ts`, documented as the honest one, and called by nothing in the
   repository. Deleted rather than moved.

3. **A site was missing from the enumeration entirely, and the ratchet is the reason.** Its idiom
   required the slice to be CHAINED onto the call, so this, in `lib/changesTimeline.ts`:

   ```ts
   const s = d.toISOString();
   const clock = s.slice(11, 16);          // "16:40" — rendered to an operator, no zone at all
   ```

   was never counted. **It was found by reading, not by the guard whose purpose is to make reading
   unnecessary.** That is the finding worth keeping out of this whole half: a bound expressed as a
   count over an idiom is only as good as the idiom, and an allow-list of thirteen entries reads
   as thoroughness while the hole sits outside it.

So the ratchet is **replaced by an absolute guard**: outside `lib/wallclock.ts` (renderings) and
the new `lib/datekeys.ts` (keys, wire values, control values), no product file calls `toISOString`
or pulls a field off a `Date`. Nothing to enumerate, no list to rot, and an indirection cannot
walk past it because the CALL is banned rather than the shape. Closing it cost ~20 mechanical
substitutions to `isoInstant` at request-body sites, which is the price of the guard being
absolute.

Four guards sit beside it, each answering a way the first one could be green while wrong: every
export has a real caller; every zone-less renderer pins the literal `"UTC"`, selected by
SIGNATURE and not by name (`utcCellExtentLabel` takes a `zone` and is local by contract — a
name-based scan flagged it, which is how that selector was found to be wrong); both owner modules
are asserted to exist AND to contain the banned idiom, so the exemption is load-bearing rather
than a typo; and the three strips are held to the key/label split.

| # | Mutation | The assertion that failed |
| --- | --- | --- |
| N1 | `phaseInstantLabel` back to slicing a `toISOString` held in a variable — **the case the old ratchet missed** | the absolute guard, naming the file and the call |
| N2 | one panel back to a bare UTC date | the absolute guard |
| N3 | a zone-less renderer drops its pinned `"UTC"` | `a zone-less renderer that does not pin UTC renders in whatever zone the viewer has` |
| N4 | an export loses its only CALL, keeping its import | `utcMillisLabel is exported and nothing calls it` |
| N5 | a strip tooltip shows its lookup key again | the key/label split guard AND the surface assertion |

**N4 survived its first version and that is recorded rather than tidied:** the caller check matched
the bare name, and an unused `import` line satisfied it — which is precisely the state a removed
call site leaves behind. Import statements are stripped before the check now.

**Zone-independence is evidence, not a claim:** the whole frontend suite runs green at `TZ=UTC`
and again at `TZ=Asia/Yekaterinburg` — 653 tests over 52 files, both times.

### 6a. A fourth false claim, in this iteration's own prose — reviewer finding on `0a1557c..d0a8fed`

`wallclock.ts`, §9 of the specification, `status.md` and `traceability.md` all said **"ONE SUFFIX,
decided once: every human-facing UTC rendering ends in ` UTC`"**, carving out `utcInstantLabel`
alone as the canonical `Z` form. `utcSecondsLabel` and `utcMillisLabel` were added beside it in
this same iteration and return `2026-08-28 14:03:02Z` and `2026-08-28 14:03:02.417Z` — the carve-out
did not follow them, and the tests expected exactly what the sentence denied.

**Not a zone leak:** `Z` designates UTC unambiguously and those four exist so an engineer can paste
the value into a log query. The mechanism is right; the sentence about it was false. That is the
defect class this arc keeps producing — prose promising what the code beside it does not do — and
it survived a mutation-verified guard suite because no guard was pointed at the claim.

The rule is a TEST now: `wallclock.spec.ts` CALLS every zone-less renderer and requires each to end
in ` UTC` (a phrase) or in `Z` (a canonical instant), with the two buckets checked against the
module's own exports in **both** directions — so a new renderer cannot be added until somebody
decides which form it is in, and a rename cannot leave a stale entry behind. Three further
mutations, all killed:

| # | Mutation | The assertion that failed |
| --- | --- | --- |
| N6 | `utcSecondsLabel` moved into the phrase bucket — **the claim exactly as it was written** | `utcSecondsLabel -> 2026-08-28 14:03:02Z: expected … to match / UTC$/` |
| N7 | a renderer added to the module and to neither bucket | the export list AND the bucket set, both by name |
| N8 | a phrase renderer drops its suffix | `utcClockLabel -> 14:03: expected '14:03' to match / UTC$/` |

**The generalisable part:** this is the same shape as the zone-argument guard at party [199], whose
hand-written list stopped covering two exports added later. A claim about a SET, written as prose,
outlives the set. Every one of those should be a derivation.

## 7. Gates

**Both failed distributed runs are kept as recorded, not rewritten** — run #1 in §1
(`no worker queue … AMQP 312 NO_ROUTE`, 10 passed / 1 failed / 1 skipped) and run #2 in §4
(`no worker responded`, same counts). The second is the more valuable of the two: it is what a
config repair looks like when it works and something else is behind it.

**`make dev-test-distributed`, run #3, on the rebuilt image — GREEN: 11 passed / 1 skipped /
1.3 min.** The condition matters as much as the count: this is the first run of the three in which
`probers.spec.ts:49` — the case that failed in both — passes, and it now takes **115 ms instead of
10.1 s**. The ten-second figure was the RPC timeout being waited out; its disappearance is what a
delivery being admitted rather than dead-lettered looks like from the outside.

| Gate | Result |
| --- | --- |
| `go build -buildvcs=false ./...` | clean |
| `go vet ./...` | clean |
| `gofmt -l` / `git diff --check` | clean |
| `make docs-check` | OK — 139 checker tests plus reference and acceptance validation |
| full `-race`, `-timeout 40m`, isolated database, `CERBIX_TEST_RABBITMQ_URL` SET | **33 packages, exit 0** — `internal/store` 731.6 s, `internal/scheduler` 69.1 s, `internal/dispatch` 21.5 s (the live-broker carrier and dead-letter cases really ran rather than skipping), `internal/agent` 1.1 s |
| `make dev-test-distributed` | **11 passed / 1 skipped / 1.3 min / exit 0** — `probers.spec.ts:49` at **113 ms** |
| `make dev-test` | **68 passed / 1 skipped / 7.3 min / exit 0** |
| `make geo-test` | **13 passed / 2.3 min / exit 0** |
| `make secret-smoke` | **PASS / exit 0** — including live generation-3 emission and the mixed-capability barrier |
| `make mac-smoke` | **PASS / exit 0** |
| `npm test` at `TZ=UTC` and at `TZ=Asia/Yekaterinburg` | **653 passed / 52 files**, both |
| `npm run type-check` + `npm run build` | clean |
| `make spa-snapshot` | refreshed, 44 assets; unchanged by the P0 work, which touches no frontend file |

**Every row above ran on the FINAL tree**, with the P0 fix and the three responses to the reviewer's
P1s in it. The earlier runs of the topology gates and the two smokes — taken before the P0 was
found — are superseded rather than cited: the P0 changed `internal/dispatch`, and a gate result from
before a change to what it exercises is a stale number, not a cheap one. The two red distributed
runs in §1 and §4 stay, because they are the evidence for what was wrong.

**Two procedural notes, kept because each was one step from becoming a false number.** The full
`-race` was started three times and the first two were STOPPED, not reported: the tree changed
underneath them and a mixed-tree result is worse than an absent one. And the first combined
topology run was piped through `tail -45`, which pushed the distributed suite's own counts out of
the log — the distributed gate was re-run alone with a full log rather than having its result
inferred from the chain's exit code.

**Not claimed, and worth naming:** the reviewer is mid-audit of this slice at party [117]. Nothing
here is an approval, and no tag, push or release is authorized by any of it.

## 8. The independent audit's result, and what it is not

**Result, in the reviewer's own boundary:** the owner-authorized slice `0a1557c..d0a8fed` was read;
the P0 it contained and the P1 beside it were resolved in a response tree examined separately,
through `8175cd0`; every P1 raised on that response is closed verifiably.
**No blocking findings remain in the verified final tree.**

### 8.1 The findings, enumerated — and the count is DERIVED from these rows

| # | Severity | Found in | The finding | Recorded in |
| --- | --- | --- | --- | --- |
| 1 | P0 | the slice `0a1557c..d0a8fed` | an envelope OLDER than its carrier defines was admitted on every path, losing the execution-body binding | §5a, `D-0246` addendum 2 |
| 2 | P1 | the slice | "ONE SUFFIX, decided once" was false the moment two renderers joined it | §6a, `D-0246` addendum |
| 3 | P1 | the response | `EnvelopeForCarrier(ProtocolV1)` answered a version its own rule refuses | §5a |
| 4 | P1 | the response | the runner-not-called assertion proved the REFUSAL and not the DISPOSAL | §5a |
| 5 | P1 | the response | the taxonomy-ordering fix did not do what its own comment said | §5b |
| 6 | P1 | the response | the records named a RESPONSE commit as the audit's boundary | §5c |
| 7 | P1 | the response | the dead-letter tap was destructive, and its correlation was not unique | §5c |

**Tally: 7 findings — 1 P0 and 6 P1s; 2 in the slice and 5 on the response.**

**This table exists because the number drifted.** §8 and `D-0246` first said "three P1s on the
response" while this report already recorded five, and `status.md` — written from the table's
contents rather than from memory — said six P1s in total and was right. A hand-written count beside
its own rows is the one number nobody re-derives, which is the sentence this arc has now written
three times: `check_iteration_finding_counts` exists because `iter-0177`'s count drifted, and it did
not fire here because the claim was phrased "P1s" rather than "findings". `check_iter0178_findings`
derives every number in the tally span above from these rows, and the reader documents are held to
it.

### 8.1a The post-result ledger — R1 and R2, deliberately OUTSIDE §8.1's tally

Two things happened AFTER the audit conclusion above, while the owner's record of it was being
checked. Neither is a finding of the technical audit, and neither is added to §8.1 retroactively —
that table is the audit of the original slice and of the response tree examined through `8175cd0`,
and rewriting it afterwards would be the same backdating the provenance P1 (row 6) was about.

They are identified `R1`/`R2` rather than numbered, which is deliberate twice over: it keeps them
visibly a different class, and it keeps the derived parser in `check_iter0178_findings` from
reading a second table as more audit rows.

| Id | Raised by | What it was | Where it ended |
| --- | --- | --- | --- |
| R1 | the reviewer, as a P1 | the summary sentence said "three P1s on the response" by hand while this report already recorded five | found on `ef0479f`, fixed in `2449f52` — §8.1's table, its **Tally:** span, and `check_iter0178_findings` |
| R2 | this session | three FR-032 guards were defined, advertised by name in the success line, and never reached — found while implementing R1; NOT raised by the reviewer and not severity-rated by him | §8.2, fixed in `2449f52` with a reachability test over `main()` |

**Read the absence correctly:** R1 and R2 post-date the audit's conclusion, so they are not part of
`1 P0 + 6 P1 / 2 in the slice + 5 on the response`. They are recorded here, in full, so that
completeness and an honest provenance boundary do not have to be traded against each other.
Classification and boundary per the reviewer's ruling at party [161].

### 8.2 Three guards this repository advertises had never run

Found while fixing the count above, and it is the same class one level up.
`check_fr032_drain_surfaces`, `check_fr032_transport_matrix` and
`check_fr032_carrier_gate_contract` were indented INTO the body of

```python
for msg in check_iteration_finding_counts():
```

so they executed only when that guard FAILED — that is, never, because it was green. Their own
fixture tests passed throughout, because a fixture test calls the function directly. And the
success line `make docs-check` prints named all three by name, on every run, for as long as they
have existed.

**Both ends were tested and the wire between them by nothing** — the shape D-0239 recorded, in the
tooling this time rather than in the product. The three now run, and the tree passes all three.

The structural answer is a test of EXECUTION, not of definition:
`EveryGuardIsActuallyReached.test_the_entry_point_reaches_every_guard` wraps every `check_*` in the
module in a proxy that records it and calls through, runs `main()` — the entry point `make
docs-check` actually invokes, not one of its halves — and names any guard the run never reached.
Restoring the dead branch fails it by naming all three.

**The count guard itself gained two holes on the way, both mutation-found:** a reader scan that
attributed `iter-0177`'s own rows about a drift to `iter-0178` (a markdown table has no blank
lines, so a paragraph-sized unit merges rows that are separate claims — each `|` line is its own
unit now), and one that read a QUOTED number as a fresh claim, which would have punished the very
sentences that keep a correction visible. Four mutations killed: the dead branch restored; the
tally's origin split changed; a findings row losing its origin cell; and a reader document stating
a P1 count that is not the table's.

**One report from the reviewer is deliberately NOT a numbered row.** After the P0 fix he ran
`go test ./internal/dispatch` and it was RED — five existing tests, one failing and four passing
for a reason none of them names (§5b). He raised it as a regression report on the work in progress
rather than as a severity-rated finding, and it is counted the way he raised it. It cost more to
fix than several of the rows above; the classification is his, and inventing an eighth row to make
the work look larger would be the same dishonesty as the drift this table replaces.

**What he verified himself:** scoped Go tests, `git diff --check`, `make docs-check`, the final
response diff, and the provenance. **What stays attributed to this session:** the stable `-race`
run and every live gate — distributed, single, geo, and both smokes — which he records as
consistent rather than as his own runs.

**What this is NOT, stated because the distinction has cost this arc twice.** It is an audit
conclusion. It does not close `iter-0178`, does not change `status.md`, and authorizes no commit,
push, tag or release. The commits in this iteration were made on the OWNER's instruction, before
this result existed and independently of it. **The lifecycle and the VCS are the owner's**, and
`iter-0177` was once marked closed on an audit result exactly like this one — with three more
findings arriving afterwards.

**Still OPEN, and none of them closed by this result:**

1. **A dispatch refusal is answered with silence**, so it reaches an operator as `no worker
   responded in region …` — the sentence an empty region gives. Fixing it means replying with a
   `ProbeError` heartbeat, a change in wire behaviour the reviewer held out of scope.
2. **FR-020's credentialed dispatch is exercised by NO geo run.** Pre-existing, not a regression,
   recorded as a coverage gap rather than a passed gate.
3. **The dead-letter source label is correct and untested.** It is diagnostic text; it is guarded
   by nothing.

## 9. NFR-025 sent for review as a whole, and the exemption it documented was false

The owner asked for a dedicated review of **NFR-025 as a requirement** rather than as part of the
release sweep. That is a different question from the audit in §8: (a) and (b) closed at iter-0174
and on 2026-09-04 and were in no audit at all, and (c) had been read only as a diff. The claim put
up for checking was the one that has already been false once: *no timestamp in the SPA is rendered
without naming its zone.*

**It was false again, in the one place the specification had exempted.** §9 recorded the zone-free
HTML controls as legitimate — an `<input type="datetime-local">` value cannot carry an offset —
with the justification that "the surface that owns the input says which zone it is typing in". No
surface did:

| Control | Surface | What it said |
| --- | --- | --- |
| `datetime-local` ×2 | `SlaView.vue` — maintenance window | "Starts", "Ends" |
| `datetime-local` | `ServiceGate.vue` — override | "Until" |
| `datetime-local` | `ServiceDeclarationView.vue` — backfill | "Backfill from" |
| `datetime-local` ×3 | `EscalationView.vue` — vacation cover, schedule anchor | nothing, nothing, "from" |
| `datetime-local` | `SettingsView.vue` — instance silence | "Until — optional" |
| `date` ×2 | `ServiceChangesView.vue` — range | "From", "To" |
| `date` ×2 | `GateDecisionsView.vue` — range | "From", "To" |

The reviewer found all but one of the `datetime-local` sites and raised it as a P1; checking it made
it worse in two directions. **`SettingsView.vue` — the site the specification NAMED as the
documented case — does not name the zone either**, and the only mention of one anywhere in those
files is a source comment at `SettingsView.vue:212` that an operator never sees. And he then added
the `date` controls, which are worse than unlabelled: `lib/gateLedger.ts` reads their values
as UTC CALENDAR DAYS, the ledger's partition unit, so an operator at UTC+05 picking a day is not
asking for their own.

**P1 rather than cosmetics, because of what these controls choose:** a maintenance window suppresses
alerting, a gate override lifts a block for a bounded time, a backfill adopts history. Each entered
against the wrong clock is wrong by hours.

**The prose that carried the false justification is mine**, in `lib/datekeys.ts`, written in the
fix for exactly this defect class. That is the third time in this iteration.

**The repair:** `localInputZoneHint` renders the offset AT the typed instant — a window entered
across a DST change is genuinely in the other offset, and a hint taken from `now` would mislabel
the case an operator is most likely to get wrong — and `utcDayInputHint` says "UTC days" for the
controls whose value is one, and `localInputRangeZoneHint` where two controls are one subject.
Every control in the table above carries one. The exemption is a CHECK: a `.vue` file
rendering such a control must render the matching hint, counted; a hint that honestly covers a
RANGE declares `data-covers="N"`, so a seventh input still needs its own or a visible decision.
Surface assertions in `SlaView.spec.ts` and `GateDecisionsView.spec.ts` read what an operator sees,
because the source guard can only see that a hint exists somewhere in the file.

| # | Mutation | The assertion that failed |
| --- | --- | --- |
| S1 | a `datetime-local` surface loses its hint — the shipped state | the guard AND the surface test, independently |
| S2 | a `date` surface loses its UTC label | the guard AND the surface test |
| S3 | a seventh `datetime-local` control appears with no hint | the guard, by file name and count |
| S4 | the offset is taken from `now` instead of the typed instant | `expected 'local time (UTC+02:00)' to be 'local time (UTC+01:00)'` — the DST case |
| S5 | the range hint reads only the START — **the state the reviewer refused** | the source scan, in every zone: `both ends are ovDraft(…).starts_at — the second control is labelled with the first one's instant` |
| S6 | the range hint names one offset even when the two differ | `expected 'local time (UTC+01:00)' to be 'local time (UTC+01:00 → UTC+02:00)'` |
| S7 | a CORRECTLY hinted control is added | the per-file inventory, by naming the file whose count changed |
| S8 | the inventory is right and the total disagrees with it | `the totals disagree with the inventory above: expected 12 to be 10` |
| S9 | a hint is wrapped in an HTML comment — **the reviewer's own mutation**, on `ServiceDeclarationView`, the one surface no component test covers | the guard, after comments are stripped; and the range form of it fails the distinct-ends scan too |
| S10 | the comment filter is made stricter and eats a `https://` URL | `expected 'const u = "https: ' to contain 'https://example.test/x'` |
| S11 | an export's only CALL is commented out | `utcMillisLabel is exported and nothing calls it` |
| S12 | an owner module's only banned idiom is left inside a comment | `lib/datekeys.ts is not where dates are built` — the exemption stops being load-bearing |
| S13 | **the reviewer's own N5 mutation**: the hint leaves the DOM and its name survives in a TRAILING comment | the inventory, and separately the real-tree invariant that stripping may change no count |
| S14 | the same evasion with a mid-line BLOCK comment | the same two |
| S15 | **the reviewer's N6 mutation**: the hint leaves the DOM and its name survives in a LIVE string literal | the narrowed guard AND the new `ServiceDeclarationView` surface test, independently |
| S16 | a control file has no named surface spec | the map, by naming the file missing from it |

**N6 is the finding that changed where the guarantee rests.** A source scan can say a hint is
WRITTEN; only a mounted view can say it is RENDERED. He reached it with a live string literal, and
he chose `ServiceDeclarationView` for every one of his mutations precisely because nothing reached
that view. Two answers, both taken: the guard now counts hints only inside `{{ … }}` in the
TEMPLATE section — so a name in `<script>` proves nothing — and **every one of the seven control
files now has a surface assertion**, named in a map that is checked against the inventory, so a new
control file cannot ship without one.

The source guard's job is smaller and honestly stated now: catch a NEW file or control that nobody
hinted. What an operator sees is settled by the specs.

**N5 is why a regex was never going to be enough.** Protecting `https://` by stripping only
line-start comments left every TRAILING comment standing, and a trailing comment is a comment. The
filter is a small string-aware lexer now — it tracks quote context, so a `//` inside a literal
survives and one after code does not — and JS comments are stripped ONLY inside `<script>`, because
in a Vue template `//` is not a comment marker at all and a bare URL in visible text would otherwise
take the rest of its line.

**A fixture written for that split immediately found a bug in it:** keying the SFC path on
`<script>` alone sent a template-only input down the code path and ate the URL. It keys on either
block now. The boundary that remains is stated rather than claimed away — a backtick opens a
template literal until the next backtick, so nested `${...}` and regex literals are read
conservatively, which keeps MORE text and can therefore only cost a false positive in a guard,
never a hidden missing hint.

**S9 is the sharpest of the nine**, because the guard was green for it and the surface tests do
not reach that view: a hint inside `<!-- … -->` leaves the DOM and stays in the file, so an
operator sees no zone while the count is satisfied. Comments are stripped before every counting
scan now — in BOTH directions, which is verified separately: a commented-out `toISOString` must
not be reported either, since a guard that cries wolf teaches its reader to ignore it. That fix
also let `changesTimeline.ts` say `toISOString()` in its own comment again; the wording had been
bent to dodge the guard, which is the wrong way round.

**"Every counting scan" was a claim, and it was false when I made it.** The commit that introduced
the filter applied it to FOUR scans and said "every". Eight more read raw source: the export list,
the has-a-caller check, the zone-argument map and its call-site guard, the UTC-pinning scan, the
two-suffix derivation, and the owner-module check. Each was wrong in a way that mattered — a
commented-out `export function` counted as an export, a commented-out call counted as a caller, and
an owner module whose only banned idiom sat in a comment satisfied the check that its exemption is
LOAD-BEARING. The reviewer said he would verify how the scope was worded rather than take the
sentence; the sentence was the defect. Every source read strips now — twelve of them — except the
filter's own fixture test, which must read raw by definition. S11 and S12 are the two that prove it
where it changes an answer.

**The filter is a mechanism, so it is pinned rather than trusted.** It must remove what an operator
cannot see AND leave code that only LOOKS like a comment — a `https://` in a template, a glob in a
doc comment — because a filter that eats real source makes every scan above quietly weaker. Both
directions are asserted, and one of them over the REAL TREE: stripping may change no count that any
scan depends on. S10 makes the line-comment rule greedy and is killed by the URL case.

**The count drifted a second time, in the paragraph describing the first drift.** The prose said
"six `datetime-local` controls across five views" and there are eight; the guard stayed green
because its self-check was `toBeGreaterThan(8)` — a FLOOR, which is the same evasion as a
hand-written total: it cannot contradict a number nobody derived. Reviewer P1, twice in two commits
on the same class.

The guard asserts the exact inventory now — **per file, not one number**, because one number cannot
say where it changed — and a separate assertion ties the total to it. Every count is out of the
prose; §9's table enumerates the controls and the guard holds the set.

**S5 SURVIVED its first version, and that is the whole reason the source scan exists.** The repair
put ONE `localInputZoneHint(starts_at)` beside the vacation range's two inputs and declared
`data-covers="2"` — and the reviewer refused it as a declaration rather than evidence about the
second control: a range crossing a DST change genuinely has two offsets, so the End was being
labelled with the Start's. `localInputRangeZoneHint(start, end)` reads both and names both when they
differ, which is the rule `instantRangeLabel` in the same module already followed; a range hint
counts for two controls because of what it READS, not because of an attribute beside it.

The surface test alone could not have caught it. In a zone with no DST in the window under test —
most zones, most of the year — a hint computed from the start twice renders exactly what the correct
one renders. So the binding is held by a source scan that requires two DISTINCT arguments at every
call site, in every zone, and the surface test asserts the reaction only when the runner's zone can
actually distinguish the two ends. Stating that boundary is the point: a test that cannot see a
defect must say so rather than be counted as covering it.

### 9.1 The NFR-025 review's own ledger, separate from §8.1 and §8.1a

This review is a THIRD find-set: not the technical audit of the release slice (§8.1), and not the
post-result audit of the record (§8.1a). Letter identifiers again, for the reason the reviewer gave
at party [161] — they keep the classes visibly apart and keep the derived parser reading one
findings table.

| Id | What it was | Where it ended |
| --- | --- | --- |
| N1 | the zone-free-control exemption was documented with a justification no surface implemented — every `datetime-local` and `date` control named no zone, including the one §9 cited | `localInputZoneHint`, `utcDayInputHint`, the guard, surface tests; S1–S4 |
| N2 | the first repair let ONE single-instant hint cover two controls by declaring `data-covers="2"` — a declaration, not evidence about the second control, so a DST-crossing range labelled its End with the Start's offset | `localInputRangeZoneHint` reading both ends; a source scan requiring two DISTINCT arguments; S5–S6 |
| N3 | the count drifted twice — "six controls across five views" against eight, and the guard's own self-check was a FLOOR that could not contradict it | the exact per-file inventory with its total; every count out of the prose; S7–S8 |
| N3a | `docs/status.md` — the CANONICAL statement of the requirement's state — still carried "six controls", "all ten" and `data-covers="N"` after every other document had been corrected | the row states no count and names the guard instead; this ledger exists because that cell also had to stop counting P1s |
| N4 | the guard read RAW source, so a hint wrapped in an HTML comment still counted: the element leaves the DOM, the helper call stays in the file, and the operator sees no zone | comments stripped before every counting scan; S9 |
| N4a | the fix said "every counting scan" and reached FOUR of twelve — a commented-out export counted as an export, a commented-out call as a caller, and an owner module's exemption was satisfied by its own documentation | all twelve strip; S11–S12 |
| N5 | the filter kept TRAILING line comments, on purpose, to protect `https://` — so `const noop = 0; // localInputZoneHint(x)` satisfied the inventory with nothing in the DOM | a string-aware lexer, and JS comments stripped only inside `<script>`; S13–S14 |
| N6 | a SOURCE guard cannot prove a RENDER: the helper's name in a LIVE string literal satisfied the file-wide count with no hint in the DOM | hints counted only inside `{{ … }}` in the TEMPLATE, and a surface assertion for EVERY control file, named in a map checked against the inventory; S15–S16 |

**No total and no range of identifiers is written here.** The heading said `N1…N3` and the table
grew to five rows the same afternoon — a label is a count wearing different clothes, and this is the
third shape the same drift has taken today: a total beside its own rows, a floor that could not
contradict prose, and now a range in a heading. The rows are the enumeration; the identifiers are
labels for reference, not a sequence anyone should read a bound from. This one the reviewer refused
to assume was fixed and said he would check on the final tree, which is the right instinct — the
previous two were reported as fixed while a third document still carried them.

**Status:** `DONE` was premature and the reviewer said so. 661 tests over 53 files, green at
`TZ=UTC`, at `TZ=Asia/Yekaterinburg` and at `TZ=Europe/Berlin` — the third added because the DST
case this repair turns on is invisible in the first two; `npm run type-check`, `npm run build` and
`make spa-snapshot` clean; `make docs-check` OK. **The review is not finished** — this is one P1
answered, and the reviewer is still working through the import graph, the operator-facing surfaces
and the guards' boundaries.
