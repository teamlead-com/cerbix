# iter-0183 — contract and tenancy seam guards

> **Lifecycle: OPENED 2026-09-19; CLOSED 2026-09-21 BY THE OWNER.** The owner instructed
> implementation after choosing the recommended first block: prevent the two defect classes repaired
> by iter-0181 and iter-0182 from returning, without adding a product feature or widening Cerbix's
> scope. The owner explicitly closed the completed iteration on 2026-09-21 after iter-0188 strengthened
> and reverified its contract and tenancy guards.

## 0. Plan

| Priority | Task | Status |
| --- | --- | --- |
| P0 | Specify contract and tenant conformance | DONE |
| P0 | Register status-page request DTO parity | DONE |
| P0 | Share fake and PostgreSQL store cases | DONE |
| P1 | Make tenant-reference owners executable | DONE |
| P1 | Add killing regressions for every guard | DONE |
| P2 | Synchronize roadmap, status and traceability | DONE |
| P2 | Run repository and scoped live gates | DONE |

## 1. Scope

The iteration implements [`cross-contract-conformance.md`](../specs/cross-contract-conformance.md):

- exact JSON-field parity for the status-page write family;
- one component-create case inventory executed by the API fake and PostgreSQL store;
- one executable inventory for the tenant-bearing references hardened in the preceding two
  iterations.

There is no new endpoint, response, UI, configuration key, migration or metric.

## 2. Role outputs

### Agent A — Implementation

- Replaced the five anonymous/implicit status-page write shapes with named request DTOs and kept the
  handlers' runtime behaviour unchanged.
- Strengthened `fakeStore.CreateComponent` to own the same page, binding, organization, project and
  retained-pair decisions as the PostgreSQL writer.
- Added the executable tenant-reference registry sourced from the canonical cross-spec table.

### Agent B — Tests

- Added `TestRegisteredStatusPageRequestContractsMatchOpenAPI`; removing `service_id` from the Go DTO
  or adding an OpenAPI-only field fails with both property sets printed.
- Added one closed component-create case inventory in `internal/contracttest`, executed by
  `TestFakeStoreComponentCreateContract` and `TestPostgreSQLStoreComponentCreateContract`.
- Added `TestTenantReferenceOwnerInventoryResolves`: deleting a row, changing one of the six keys,
  or removing any named writer/schema/runtime/test token fails the exact-set or resolution check.

### Agent C — Docs + Traceability

- Added the cross-cutting specification and D-0251.
- Opened the iteration in `status.md`, synchronized the roadmap snapshot, and added the
  traceability row before implementation.
- Marked NFR-026 and AC-0183-1…4 DONE only after the implementation and repository gates passed.

### Agent D — Observability + Ops

- Confirmed no new runtime metric is appropriate: these are build/test gates, and target ids must
  not become labels.
- The PostgreSQL half stays opt-in through `CERBIX_TEST_DATABASE_DSN`; default tests remain
  hermetic.
- Ran the shared real-store contract against temporary `postgres:16-alpine` after migrations
  1–106. The first invocation used a database named `cerbix` and was refused by the existing
  `TruncateAll` safety gate; rerunning against `cerbix_test` passed all shared cases. No production
  data path was involved.

## 3. Acceptance evidence

- The Go/OpenAPI registry contains `CreateStatusPage`, `UpdateStatusPage`, `CreateComponent`,
  `UpdateComponent` and `ConversionTarget`; exact property-set equality is checked in both
  directions.
- `node:22` regenerated the TypeScript schema from the unchanged OpenAPI document with no diff.
- Fake and PostgreSQL backends execute the same ten cases: manual, monitor, service, retained
  same-project pair, missing page, missing monitor, missing service, foreign organization,
  page-project mismatch and retained-pair mismatch.
- The owner registry has an exact six-key set and resolves every store writer, schema constraint or
  trigger, scoped runtime reader and behavioural regression named by §5 of the spec.
- No route, OpenAPI field, response, configuration key, migration, UI surface or metric changed.

## 4. Gates

| Gate | Result |
| --- | --- |
| focused `go test ./internal/api ./internal/store ./internal/contracttest -count=1` | PASS |
| `go test ./... -count=1` | PASS |
| `go test -race -count=1 -timeout 40m ./...` | PASS |
| `go vet ./...` | PASS |
| `make build` | PASS |
| `make docs-check` | PASS (184 checker tests + reference checker) |
| `golangci/golangci-lint:v2.12.2 golangci-lint run ./...` | PASS, `0 issues` |
| `node:22 npm run gen:api` + tracked schema diff | PASS, no diff |
| PostgreSQL 16 migrations 1–106 + `TestPostgreSQLStoreComponentCreateContract` | PASS, all ten cases |
| `git diff --check` | PASS |
| Playwright | NOT APPLICABLE: no route, OpenAPI shape, response, UI or runtime behaviour changed |

Implementation and DoD are complete. The owner explicitly closed the iteration on 2026-09-21 after
the iter-0188 audit remediation added required/type/format mutations, behavioural PostgreSQL owner
bindings and the cross-object reference discovery guard.
