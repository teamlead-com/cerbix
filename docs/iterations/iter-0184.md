# Iteration 0184 — audit-log retention

Opened and implemented on 2026-09-19 after owner approval of `ops-audit-log-retention.md` revision 1.

## Plan

1. P0 — add strict `audit.*` configuration and the retention index.
2. P0 — add a database-clock, fenced, bounded purge pass and scheduler lifecycle.
3. P1 — expose low-cardinality metrics, alerts and recovery guidance.
4. P1 — prove config and metrics contracts; run focused repository checks.
5. P2 — record DB/live rollout gates and traceability.

## Role outputs

- **Agent A — Implementation:** added migration 00107, validated config defaults/bounds, dedicated
  advisory lock slot 4, one cutoff per pass and ordered `FOR UPDATE SKIP LOCKED` deletes on the pinned
  connection; scheduler roles `all` and `scheduler` start the loop.
- **Agent B — Tests:** added config boundary/unknown-key coverage and metrics cardinality coverage;
  targeted Go packages pass. PostgreSQL-specific cutoff equality, contention, rollback and migration
  smoke require `CERBIX_TEST_DATABASE_DSN` and were not run in this hermetic environment.
- **Agent C — Docs + Traceability:** approved spec header, status, traceability, decision, example
  config and this report are synchronized.
- **Agent D — Observability + Ops:** added retention counters/gauges, two warning alerts and runbook
  recovery/irreversibility guidance. `LeaderSession.Release` now closes a connection when advisory
  unlock proof fails, preventing a held lock from returning to the pool.

## Verification

- PASS: `GOCACHE=$(pwd)/.cache/go-build GOMODCACHE=$(pwd)/.cache/go-mod go test ./internal/config ./internal/metrics ./internal/scheduler ./internal/store ./internal/cli`
- PASS: `GOCACHE=$(pwd)/.cache/go-build GOMODCACHE=$(pwd)/.cache/go-mod go test ./...`
- PASS: `make build`
- PASS: `make docs-check` (184 unit checks plus living-document reference verification)
- BLOCKED: `make lint` — `golangci-lint` is not installed in this environment (`Error 127`).
- NOT RUN: live PostgreSQL migration/backlog-drain smoke, full race and E2E; they need the opt-in
  development/database environment and remain release gates for this operations feature.

## Remaining rollout gates

Before production rollout, run migration up/down on supported PostgreSQL, prove cutoff equality,
batch continuation, `SKIP LOCKED` contention, rollback and two-node leader fencing against a live
database, then drain a seeded backlog while watching the new metrics.
