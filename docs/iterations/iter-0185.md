# Iteration 0185 — project-level inherited gate policy

Opened on 2026-09-19 by owner instruction approving `func-project-gate-policy.md` revision 1.

## Plan

1. P0 — add project policy persistence, source tuple and tombstone CAS.
2. P0 — resolve service → project → none in the gate snapshot and bind overrides.
3. P1 — expose project CRUD/effective service reads and source observability.
4. P1 — produce and obtain approval for the project-policy SPA mock.
5. P2 — run PostgreSQL, generated-client, frontend and live-browser gates.

## Role outputs

- **Agent A — Implementation:** added migration 00108, project-policy CRUD/CAS/tombstones,
  transactional effective resolution, source-tuple override binding, ledger fields, project routes and
  effective service reads. Inheritance does not materialize service rows.
- **Agent B — Tests:** `go test ./...`, frontend type-check/build and Vitest pass in this
  environment. PostgreSQL migration compatibility, inherited-policy evaluation, source replay and
  override-revocation regressions pass against PostgreSQL 16.
- **Agent C — Docs + Traceability:** approved the backend/spec scope, synchronized status,
  traceability, decision and runbook, regenerated TypeScript from OpenAPI, and recorded this report.
  The review artifact is [`mock-project-gate-policy.html`](../design/mock-project-gate-policy.html);
  the owner approved it on 2026-09-19 and re-approved it after canonical release-gate shell alignment.
  Implemented the project editor in
  [`ProjectGatePolicy.vue`](../../frontend/src/components/settings/ProjectGatePolicy.vue), added it
  to Settings, and made [`ServiceGate.vue`](../../frontend/src/components/ServiceGate.vue) show
  inheritance and correctly create a service-level override.
- **Agent D — Observability + Ops:** decision metric recording accepts bounded
  `policy_source=service|project|none`; runbook documents tuple diagnosis, safe return to inheritance
  and revision-conflict handling.

## Verification

- PASS: `GOCACHE=$(pwd)/.cache/go-build GOMODCACHE=$(pwd)/.cache/go-mod go test ./...`
- PASS: focused `go test ./internal/store ./internal/api ./internal/domain ./internal/metrics`
- PASS: PostgreSQL 16 `TestProjectGatePolicyMigrationBackfillsLegacyDecisionSource` plus inherited
  project-policy snapshot/replay and override-revocation coverage.
- PASS: generated TypeScript parity, full SPA type-check/build and Vitest (`57` files, `689` tests).
- PASS: Dockerized `golangci-lint:v2.12.2` (`0 issues`), `make build`, `make docs-check`, and
  `git diff --check`.
- PASS: scoped live `gate-ui.spec.ts` (`5 passed`), including inherited project policy and service
  override creation from a null service CAS token.

## Outcome

FR-034/NFR-028 delivery gates are complete and the requirement is `DONE`. The iteration lifecycle
remains OPEN pending an explicit owner closure instruction.
