# Iteration 0186 — worst-of-all-windows gate evaluation

> **Lifecycle: OPENED 2026-09-19, CLOSED 2026-09-19 by the OWNER.** The owner approved revision 1 of
> [`func-reliability-gate-all-windows.md`](../specs/func-reliability-gate-all-windows.md). Backend,
> schema and API work is authorized; the backend slice is complete. The canonical-theme artifact
> [`mock-reliability-gate-all-windows.html`](../design/mock-reliability-gate-all-windows.html) was
> approved by the owner on 2026-09-19; SPA implementation is authorized.

## Plan

| Priority | Task | Status |
| --- | --- | --- |
| P0 | Add policy schema-v2 mode and compatibility persistence | DONE — migration `00109`, strict v1/v2 union validation and project/service persistence |
| P0 | Evaluate canonical target inventory in one snapshot | DONE — set-based report/segment/burn/repair reads, standard-window order, batch latches and fixed statement count |
| P1 | Persist all-window evidence and API/OpenAPI unions | DONE — decision/ledger `window_mode` + complete per-window freshness evidence, OpenAPI and generated TypeScript |
| P1 | Add aggregation, migration and tenancy regressions | DONE — clean 108→109 compatibility, inherited policy, no-target UNKNOWN, override revocation, target-race replay and earliest freshness |
| P2 | Produce and approve the SPA artifact mock | DONE — canonical release-gate shell approved by the owner on 2026-09-19 |
| P2 | Synchronize runbook, metrics, traceability and delivery gates | DONE — SPA, PostgreSQL, generated-client, build, lint, docs and live-browser gates pass |

## Role outputs

- **Agent A — Implementation:** added schema-v2 policy persistence, repaired migrations 00107/00108,
  implemented fixed-phase set-based report loading, complete per-window evidence and ledger/API parity.
- **Agent B — Tests:** added pure strict-union/precedence checks plus PostgreSQL 16 migration,
  inherited-policy, no-target, override, snapshot/replay, earliest-freshness, statement-count and
  four-window transaction-budget regressions. Live E2E exposed and then killed a schema-version
  regression in live, replayed and listed all-window decisions.
- **Agent C — Docs + Traceability:** synchronized D-0255, status, traceability, runbook and this report;
  produced the canonical-theme SPA artifact and recorded explicit owner approval.
- **Agent D — Observability + Ops:** added closed `window_mode=one|all|none` decision metrics and
  diagnostics for source tuple, inventory drift, determining reasons, per-window freshness and budget failures.

## Verification

- `go test ./...` — green across the full hermetic Go suite.
- `make build` — green.
- generated TypeScript exactly matches `openapi.yaml`; full SPA type-check/build and `57` files / `689`
  tests are green.
- `golangci/golangci-lint:v2.12.2 golangci-lint run ./...` — `0 issues`.
- `make docs-check` and `git diff --check` — green.
- Temporary PostgreSQL 16: clean migrations `00001`→`00109` green; v1 `00108`→`00109`
  compatibility probe green; all targeted iter-0186 snapshot/performance/inheritance/override cases green.
- Live single-process stack: migration 109, readiness and scoped `gate-ui.spec.ts` green (`5 passed`),
  covering inherited all-window policy, one↔all mode switching, inventory preview, UNKNOWN evidence,
  action-only override, schema-v2 ledger replay and mixed ALLOW/WARN/BLOCK presentation.

## Outcome

FR-035/NFR-029 and every iter-0186 acceptance/DoD row are `DONE`. The owner explicitly closed the
iteration on 2026-09-19; this report is immutable from the closure commit.
