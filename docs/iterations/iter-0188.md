# iter-0188 — post-close audit remediation for iter-0183…0187

**Opened:** 2026-09-20 by the owner request for a full implementation audit.  
**Closed:** 2026-09-21 after every P0/P1/P2 finding had executable closure evidence.  
**Lifecycle:** CLOSED.  
**Audit:** [`2026-09-20-iter-0183-0187-implementation-audit.md`](../checks/2026-09-20-iter-0183-0187-implementation-audit.md).  
**Decisions:** D-0258, D-0259.

## Plan

| Priority | Task | Status |
| --- | --- | --- |
| P0 | Complete and preserve retention DB proof | DONE |
| P1 | Make retention alerts cadence/absence correct | DONE |
| P1 | Make effective policy GET one-snapshot | DONE |
| P1 | Add CLI source and window evidence | DONE |
| P1 | Define and test all-window downgrade | DONE |
| P1 | Fence onboarding loads by workspace epoch | DONE |
| P2 | Strengthen contract and owner guards | DONE |
| P2 | Re-run full delivery and live matrices | DONE |

## Agent A — Implementation

- Audit retention now discards a poisoned dedicated advisory-lock connection instead of returning it
  to the pool. Retention metrics publish the configured purge interval and configured timestamp;
  Prometheus rules derive their thresholds from that cadence and alert when health gauges are absent.
- `EffectiveGatePolicy` resolves service → project → none inside one repeatable-read transaction.
- `cerbix gate check` human output now renders stable `policy_source`, `policy_owner_id`,
  `policy_revision`, `window_mode` and ordered `evaluated_windows` fields without changing JSON output
  or exit semantics.
- Migration 00109 has a Goose-safe preflight block that refuses lossy downgrade while any all-window
  service policy, project policy or decision ledger row exists.
- Dashboard onboarding uses a monotonically increasing load generation across workspace loads,
  candidate-region reads and polling; project switches and unmount invalidate stale continuations.
- Registered request DTOs declare required/format facts used by the conformance test. Tenant-bearing
  reference fields declare registered `tenantref` owners while schema/store guards remain authoritative.

## Agent B — Tests

- Added `TestAuditRetentionPostgreSQLMatrix` with eight isolated PostgreSQL cases: cutoff equality and
  mixed scopes, 251-row batching, row-lock continuation, statement rollback, two-node fencing,
  poisoned-owner connection discard, 2500-row bounded live drain, and migration 107 down/up row
  preservation. The complete matrix passes normally and under `-race`.
- Added `TestEffectiveGatePolicyUsesOneRepeatableReadSnapshot` with a traced concurrent service-policy
  insert between service and project reads.
- Added CLI human-summary goldens for service/project policy source, revision, one/all mode, ordered
  evaluated windows, not-configured and override cases.
- Added `TestGateAllWindowsMigrationRejectsLossyDowngrade`, proving refusal for service, project and
  decision all-window rows, unchanged migration version after refusal, and clean 109→108 success.
- Added Dashboard deferred project-A → project-B and unmount regressions.
- Extended request-contract tests to required/type/format parity and mutation fixtures. Bound all six
  tenant-reference inventory entries to their real PostgreSQL behavioural/direct-SQL regressions and
  added a tag-based discovery guard for unowned cross-object ID fields.

## Agent C — Docs + Traceability

- Published the immutable audit snapshot and D-0258 without editing closed historical iteration files.
- Updated `status.md`, `traceability.md`, `roadmap.md`, `runbook.md`, affected long-lived specs and
  alerts to the implemented contracts.
- D-0259 records that the audit is closed only by executable evidence at the named layer; no local
  session references were added to canonical documents.

## Agent D — Observability + Ops

- `CerbixAuditRetentionStale` and `CerbixAuditRetentionBacklog` now use two configured purge cadences
  across the supported 5m..24h range rather than a fixed two-hour assumption.
- Both alerts have explicit `unless` branches after two cadences when their health gauge never appears.
- Existing low-cardinality result/deletion/backlog metrics remain; no tenant, target or window id became
  a metric label.

## Verification

- PASS: `go test ./...`.
- PASS: `make race` (`-race -count=1 -timeout 40m ./...`).
- PASS: `make build`; `go vet ./...`.
- PASS: Dockerized `golangci-lint:v2.12.2` — `0 issues`.
- PASS: `make docs-check` — 184 unit checks plus living-reference validation.
- PASS: `git diff --check`.
- PASS: PostgreSQL `TestAuditRetentionPostgreSQLMatrix` — 8/8 normally and under `-race`.
- PASS: PostgreSQL `TestEffectiveGatePolicyUsesOneRepeatableReadSnapshot`.
- PASS: PostgreSQL `TestGateAllWindowsMigrationRejectsLossyDowngrade`.
- PASS: PostgreSQL `TestTenantReferenceOwnerBehavioralBindings` — all six owners.
- PASS: focused CLI and API contract suites.
- PASS: SPA — 61 files / 708 tests; `vue-tsc`; Vite production build.
- PASS: rebuilt single-topology Playwright onboarding smoke — 2 tests including auth setup.

## Closure

Every finding in the audit has a root-cause fix and a killing regression. FR-033/NFR-027,
FR-034/NFR-028, FR-035/NFR-029, FR-036/NFR-030 and the strengthened NFR-026 conformance guards are
`DONE`. The dated audit remains unchanged as the record of the opening condition; living documents
carry the corrected state.
