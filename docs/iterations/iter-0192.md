# iter-0192 — overall-status post-close remediation

**Opened:** 2026-09-21
**Closed:** 2026-09-21
**Lifecycle:** CLOSED — post-close review findings remediated and independently reverified.
**Requirements:** FR-038 / NFR-032 hardening
**Canonical contract:** [`func-status-pages-overall-status.md`](../specs/func-status-pages-overall-status.md)
**Decision:** D-0263

## Goal

Restore immutable iteration history and make explicit `summary_state` authoritative for every
status-page hero output, including supporting component truth, without changing backend facts or
incident/component ownership.

## Inputs read

- [`project-description.md`](../project-description.md), [`status.md`](../status.md),
  [`decisions.md`](../decisions.md), [`traceability.md`](../traceability.md), and
  [`roadmap.md`](../roadmap.md)
- [`func-status-pages-overall-status.md`](../specs/func-status-pages-overall-status.md)
- immutable [`iter-0189.md`](iter-0189.md), [`iter-0190.md`](iter-0190.md), and restored
  [`iter-0191.md`](iter-0191.md)
- Independent review findings covering immutable-history drift, active-incident supporting-copy
  authority, exact conflict expectations, shared incident vocabulary, complete matrix coverage, and
  full live disclosure

## Plan

| Priority | Task | Status |
| --- | --- | --- |
| P0 | Restore iter-0191 closure snapshot | DONE |
| P0 | Make summary state fully authoritative | DONE |
| P0 | Require exact conflicting-state copy | DONE |
| P1 | Preserve complete matrix and vocabulary | DONE |
| P1 | Rebuild and verify live SPA | DONE |
| P2 | Synchronize docs and close iter-0192 | DONE |

## Agent A — Implementation

- [`iter-0191.md`](iter-0191.md) is restored to the original closure narrative: its lifecycle,
  selected-test counts, Agent outputs and verification record no longer include post-close work.
- D-0263 assigns every review correction to iter-0192. In
  [`statuspage.ts`](../../frontend/src/lib/statuspage.ts), an explicit `summary_state` now controls
  headline ownership, supporting component truth, visual fallback and all-clear eligibility.
  `summary` is consulted only when state is absent, or as severity detail within explicit
  `impaired` state.
- The incident-owned component-copy branch is isolated in one function, so
  `summary=no_data/state=operational` cannot overwrite the operational truth. The helper continues to
  import `IncidentImpact`, `IMPACT_ORDER` and labels from shared
  [`incident.ts`](../../frontend/src/lib/incident.ts).
- No component row, uptime strip, SLA/SLO, incident lifecycle, feed, webhook, timestamp, backend
  summary, API, database or configuration path changed.

## Agent B — Tests

- [`statuspageOverall.spec.ts`](../../frontend/src/lib/statuspageOverall.spec.ts) retains the complete
  36-case operational/impaired/no-data/empty × zero/one/many × every-impact matrix and now has exact
  expected headline/supporting-copy assertions for contradictory state/summary pairs, including
  `summary=no_data/state=operational`.
- The helper suite contains 46 tests; together with the ten
  [`PublicStatusView.spec.ts`](../../frontend/src/views/PublicStatusView.spec.ts) component tests, the
  focused gate is 56/56. The full frontend suite is 63 files / 764 tests.
- [`statuspages.spec.ts`](../../e2e/tests/statuspages.spec.ts) requires the complete Major supporting
  sentence including one-component unmeasured disclosure on public and authenticated preview at
  desktop and 430 px, plus accessible `h1`, visual/icon parity and no horizontal overflow.

## Agent C — Docs + Traceability

- D-0262 and iter-0191 are restored to their closure scope. D-0263, this report and living status,
  traceability, roadmap, PRD, spec index, canonical spec and changelog own the post-close remediation.
- `docs/status.md` records AC-0192-1 through AC-0192-5 and DoD-0192 as `DONE`, with code/tests and
  metrics marked not applicable where presentation/governance-only.
- Immutable iter-0189 and iter-0190 remain unchanged. No local session, screenshot or user-file path
  is cited. No root analysis artifact was created.

## Agent D — Observability + Ops

- No metric or alert is warranted. The correction changes only deterministic frontend composition;
  it creates no new health fact, threshold, SLI/SLO, readiness verdict, failure mode or recovery
  action.
- Public/authenticated response fields, response size semantics, cache keys, backend summary algebra,
  OpenAPI and persistence remain unchanged. Generated TypeScript stays byte-identical to
  `openapi.yaml`.
- The single-process stack was rebuilt from the corrected tree, migrated at schema version 109, and
  is ready at `http://localhost:8080`. PostgreSQL, RabbitMQ, MariaDB, Keycloak and Mailpit remain
  healthy.

## Verification

- PASS: focused helper and `PublicStatusView` suites — `2` files / `56` tests.
- PASS: full frontend unit suite — `63` files / `764` tests.
- PASS: `vue-tsc --noEmit -p tsconfig.json --composite false` and Vite production build.
- PASS: generated TypeScript API parity; `schema.d.ts` SHA-256
  `d14a26ea3ba77acc6ccc96e8ba94f4c346b93753e624cf63d7a787011e2c3a4e`.
- PASS: `make spa-snapshot`; `frontend/dist` and `internal/web/dist` differ only by the intentional
  embedded `placeholder.txt`.
- PASS: rebuilt-stack focused Playwright public/preview scenario plus setup — `2/2`; desktop and
  430 px require accessible headline, exact full copy, non-green band, alert icon, parity and no
  horizontal overflow.
- PASS: `go test ./...`, `make build`, repository-local-cache `go vet ./...`, full `make race`, and
  Dockerized `golangci-lint:v2.12.2` with `0 issues`.
- PASS: `make docs-check`, `git diff --check`, explicit trailing-whitespace inspection of new
  untracked files, and immutable iter-0189/0190 checks.

## Closure

Iter-0191 is again an immutable closure snapshot, and all review remediation is isolated in
iter-0192. Explicit state cannot be contradicted by summary in headline, supporting copy, visual or
icon output. The local stack remains available at `http://localhost:8080`. No commit was created.
