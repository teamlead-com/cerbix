# Requirement Status

Statuses: `TODO`, `IN_PROGRESS`, `DONE`. Every `DONE` links to code, tests, and metrics.

## Current iteration (iter-0174 — CLOSED 2026-09-03. FR-031/NFR-025a: a rendering claims no more than the facts behind it. Three surfaces that rendered two different states identically — the reliability timeline whose length did not mean duration, the Response time panel whose horizontal position did not mean time, and the /sla objective card whose input did not say whether it was saved. Mock-approved before any frontend code. FR-032 is opened for the fact cerbix does not record. See [iter-0174](iterations/iter-0174.md), D-0235)

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0174-1 | The timeline's axis is CLOCK TIME: cells come from the requested range at the rollup grain, a step with no stored bucket occupies its own width in its own encoding, and inter-tick padding is fixed rather than a share of the tick. | DONE | [`reliabilitygeometry.ts`](../frontend/src/lib/reliabilitygeometry.ts) `buildCells`; 24 tests in [`reliabilitygeometry.spec.ts`](../frontend/src/lib/reliabilitygeometry.spec.ts) and "gives every step of the requested range a cell of its real time width, stored or not" in [`ServiceReliability.spec.ts`](../frontend/src/components/ServiceReliability.spec.ts) — the unaligned 30-day window of the fixture yields 31 cells, not the two the response carries points for. Mutants killed: emitting only steps that have points (5 failures), and dividing the stack by the CLIPPED extent, which overflowed the first cell of a window starting mid-step. This REFINES [218] P1-3 rather than reversing it: bucket count was a sound width proxy inside measured coverage and failed only in a storage hole. |
| AC-0174-2 | Five encodings, none confusable: `unknown` a solid neutral slice (a decided verdict, never a status hue), `not-stored` a hatch with a cell outline (absence, and it may borrow neither a hue nor opacity), `provisional` the sole user of opacity. Height carries QUANTITY, so the short-tick encoding is retired and a cell is a proportional stack. A PROBLEM IS NEVER HIDDEN: an eligible slice is granted the floor where the cell can fund it, the stack totals the cell's height by construction, and a slice the grant could not fund is MARKED non-geometrically and named in the readout. | DONE | `stackSlices` + [`ReliabilityStrip.vue`](../frontend/src/components/ReliabilityStrip.vue); "shows a partly unmeasured step as BOTH its states, and never hides the unmeasured part" and "marks provisional series points and never colours unknown as a status" (asserting the neutral fill and the ABSENCE of every status hue). Mutants killed: removing the floor; paying it in stack order (which zeroed provisional good); the cap limiting the accumulated DEBT rather than the grants (party [180]: 17.6px in a 14px lane); first-come grants instead of proportional; absence removed as a funder; the unfundable slice no longer marked; the marker drawn on every cell. The absolute "never thinner than the minimum" claim is WITHDRAWN: reviewer P1 [184] refused the incoherence between it and the tests, so §5.2 and invariant 6 now state a GRANT bounded by the cell and invariant 6b carries the marker that keeps the promise where height runs out. §12.2 of `func-service-reliability.md` is amended in place to say its own sentence no longer describes the product. |
| AC-0174-3 | Segments are lanes on the shared axis with real width, never floored; a sub-pixel segment gets a non-geometric marker; colliding boundary marks cluster into ONE mark anchored at the cluster's EARLIEST member and carrying its count and members. | DONE | `clusterTransitions`/`transitionsOf`; "marks a sub-pixel segment instead of widening its lane" (the marker's `aria-label` says "not to scale" and the lane draws NO cell) and "collapses colliding boundary marks into ONE mark carrying its count, anchored at the earliest". Mutants killed: anchoring a cluster at its midpoint, and counting a segment's start as a transition. |
| AC-0174-4 | A segment states its STORAGE verdict and quotes no availability while that storage is incomplete, with coverage still printed as its own separately named fraction. | DONE | `storageVerdict` from the payload's own `ReliabilitySegment.buckets`; "withholds a segment's availability while its storage is incomplete, and keeps coverage" — `availability —`, `1440 of 5760 · incomplete`, `coverage 100%` retained, and the explanation asserted to contain NEITHER canonical reason code. Mutant killed: calling storage always complete. This closes a defect in the OLD numbers — a segment could quote `availability 100%` over a range it never materialized — invisible for as long as every lane was normalised to full width. The explanation is display grammar with no code identity, fenced from metrics, audit, API and reliability state (reviewer [176]). |
| AC-0174-5 | The Response time panel draws EVERY fetched heartbeat at its real timestamp, including the zero-latency failures that used to vanish; points only, no stroke and no fill; the timeout stated always and drawn only inside the computed extent; avg and p95 over the drawn series with the population named. | DONE | [`latencypanel.ts`](../frontend/src/lib/latencypanel.ts) + [`MonitorDetailView.vue`](../frontend/src/views/MonitorDetailView.vue); 18 tests (10 library, 8 against the real panel in [`ResponseTimePanel.spec.ts`](../frontend/src/views/ResponseTimePanel.spec.ts)) — 59 dots and 1 baseline mark from 60 fetched checks, zero `<path>` and zero `<polyline>` in the plot, the widest x-delta more than five times the ordinary one, and the header reading "59 of 60 checks with a recorded latency". Mutants killed: reinstating the `filter(v > 0)` (5 failures) and swapping the time axis for an index. |
| AC-0174-6 | Absence on that panel is rendered POSITIVELY by an observation ruler — one tick per recorded check, empty spans carrying one meaning uniformly, no threshold on that meaning, no server classifier, and no claim outside the first and last point. | DONE | The ruler in `MonitorDetailView.vue` + `emptySpans`; "carries the observation ruler: one tick per recorded check, and focusable empty spans" — 60 ticks, 59 focus targets, and a readout saying "not late, not missed, not covered, not anomalous". The 12 px that remains is a keyboard HIT TARGET measured from the rendered width through a `ResizeObserver`, and merging spans changes interaction only; a merged target states its real outer bounds. The panel's prose states the interval between two RECORDED checks and is asserted never to count checks it cannot know were due. |
| AC-0174-7 | The `/sla` objective card is read-only with an explicit Edit and closes on a guarded save; the draft is cleared on success; Save is offered only when it would do something; the context reset covers the card; both writers gate on their load generation. | DONE | [`SlaView.vue`](../frontend/src/views/SlaView.vue); 11 tests in [`SlaView.spec.ts`](../frontend/src/views/SlaView.spec.ts) including two new P0 cases — "does not carry a typed draft across a project switch" and "drops a deferred objective save from the previous project". Four mutants killed: removing the card from the context reset (2 failures), dropping the save writer's generation guard, leaving the draft behind on success, and offering Save when it would do nothing. One regression of this session's own, caught by an existing test: Save disabled for an invalid draft left the operator unable to learn why, so the refusal is met at the field, live. |
| AC-0174-8 | ONE time mechanism: a NAMED RENDERER PER SUBJECT — an instant and a UTC cell's extent at iter-0174, six renderers by 2026-09-04, the rule being the naming and not the count — no generic formatter, the offset resolved AT the instant, and identity kept UTC while presentation is local. | DONE | [`wallclock.ts`](../frontend/src/lib/wallclock.ts); 14 tests in [`wallclock.spec.ts`](../frontend/src/lib/wallclock.spec.ts) — a UTC day rendered as `01.09 05:00 → 02.09 05:00 (UTC+05:00)` and never as the viewer's `01.09`, one zone with two offsets across a DST boundary, BOTH offsets named when a cell spans the change, `+05:30` keeping its minutes, and a source assertion that no product call site passes the test-only zone. Mutant killed: taking the offset from `now` instead of from the instant — which fails exactly the two DST tests and nothing else. |
| DoD-0174 | AC-0174-1…8 DONE with evidence, `go vet` + `make docs-check` + `npm test` + `npm run build` + `make spa-snapshot` + the live browser suite green. | DONE | No Go code and no API change: all four surfaces are the SPA and the storage verdict comes from a field the payload already carried. `go build -buildvcs=false ./...` and `go vet ./...` clean; `make docs-check` green; `vue-tsc` clean; **591 tests / 50 files**; `npm run build` clean; `make spa-snapshot` refreshed (43 assets) and committed with the change that produced it. **Twenty-four mutants planted and killed**, each by the tests that name it, and separately **four product defects the tests caught with nothing planted** — separated because they are different kinds of evidence, and party [177] conflated them into a count of thirteen (reviewer P2 at [178]). Live browser suite: five runs, see [iter-0174](iterations/iter-0174.md) §9. **Reviewed and APPROVED** by the independent reviewer at party [190], range `7cfb2ec..aee68e3`, after six review rounds and eleven findings — [159] figures borrowed rather than derived; [161] availability quoted over unmaterialized storage; [176] the client asserting a canonical payload code; [178] the drawn width floored and the slice floor unfunded; [180] the cap limiting debt rather than grants; [184] the specification promising what the implementation cannot deliver, plus a stale map; [186] the readout recomputed at a nominal height and the lane rendering only labels; [188] the canonical traceability row stale against the repaired invariant map. The reviewer independently re-ran `make docs-check` on the final tree (31 checker tests plus reference and acceptance validation) and confirmed `git diff --check` clean; **the frontend, build, snapshot and live-suite gates stay attributed to this session's own runs**, because that environment has no npm. Recorded as an implementation approval, not a release declaration. Stated as a limit rather than left unsaid: the panel's data-dependent behaviour is pinned by component tests against real fixtures, and what the live run adds for it is proof that this panel is the one inside the embedded SPA. |

## Previous iteration (iter-0173 — CLOSED 2026-09-03. FR-030: a monitor says what it is for. One optional 200-character description, mock-approved before any frontend code, shown under the name on the monitor list, on the dashboard panel and in full on the monitor page; every existing monitor reads back empty and every surface renders exactly as before. See [iter-0173](iterations/iter-0173.md), D-0234)

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0173-1 | The bound is 200 CODE POINTS and the form and the server count it the same way, published rather than remembered. | DONE | `domain.MaxMonitorDescriptionRunes` + `utf8.RuneCountInString`; `TestMonitorDescriptionBoundIsByCodePointOnBothSidesOfTheEdge` (200 accepted, 201 refused naming the field — Latin, Cyrillic, emoji), `TestMonitorBoundsArePublishedForTheClient` writes [`monitor_bounds.json`](../internal/domain/testdata/monitor_bounds.json), and [`monitorBounds.spec.ts`](../frontend/src/lib/monitorBounds.spec.ts) asserts the client mirrors it and counts code points (200 emoji = 400 UTF-16 units must stay legal). |
| AC-0173-2 | Compatibility is a property, not a hope: every monitor that existed before the migration reads back empty and every surface renders as it did. | DONE | Migration 00099 (`NOT NULL DEFAULT ''`); `TestMonitorDescriptionRoundTripsAndReadsEmptyWhenNeverSet`; each SPA surface is a `v-if`, and the live case asserts the ABSENCE — no description element on the list row, the card or the detail page for a monitor without one (`e2e/tests/monitors.spec.ts`). |
| AC-0173-3 | The four surfaces of the approved mock, with the refusal met at the field, and the dashboard card's geometry stated as a measured fact rather than a promise. | DONE | [`NewMonitorView.vue`](../frontend/src/views/NewMonitorView.vue) (textarea under Name, live `n / 200`, `--down` past the limit, Create/Save disabled), [`MonitorsView.vue`](../frontend/src/views/MonitorsView.vue) (one line under the name, ellipsis, full text as tooltip), [`MonitorCard.vue`](../frontend/src/components/MonitorCard.vue) (one line, cut by the panel's width), [`MonitorDetailView.vue`](../frontend/src/views/MonitorDetailView.vue) (in full); `NewMonitorDescription.spec.ts` (3 cases: the live count, the refusal with Create disabled, the trimmed body through the real form submit); `MonitorCardDescription.spec.ts` (one line only, `truncate` present, absent when empty, and the card's height difference measured as exactly one more flex child — plus the pre-existing push-vs-http difference, which is why "the panel keeps its height" was never true of this card). Reviewer P1 [122] found that claim false; the owner kept the mock as drawn and the contract is corrected in the spec, the mock, `notes.md` and D-0234. |
| AC-0173-4 | The write surfaces agree: API, and a bundle that owns the field like every other. | DONE | `openapi.yaml` (`Monitor`, `CreateMonitor`, `UpdateMonitor`, `maxLength: 200`) + regenerated TS schema; `TestMonitorDescriptionAtTheAPI` (400 names the field; omitted on update is unchanged, `""` clears); `TestABundleOwnsTheDescriptionLikeEveryOtherField` (set; identical re-apply is no change; a description-only change IS a change because the canonical hash covers it; the UI cannot write a file-managed monitor at all; dropping the key clears it). |
| AC-0173-5 | The field is presentation, never evaluation, and a description change is audited as a monitor update. | DONE | `internal/domain/execsemantics.go` classifies it `SemanticPresentation`, which `TestMonitorFieldsAreExhaustivelyClassified` demanded before the field could exist; a change rides `monitor.update` (FR-026 §10) and the description's text never reaches an audit target (D13). |
| DoD-0173 | AC-0173-1…5 DONE with evidence, `go vet` + `make docs-check` + the full `-race` suite + `npm test` + `npm run build` + `make spa-snapshot` + the live browser suite green. | DONE | All green on the closing tree: `go vet` clean, `git diff --check` clean, `gofmt` clean; full `-race` **33 packages, exit 0** (`internal/store` 668.1s, on a private database — see iter-0173 §6 for the collision that made the first attempt red and the mechanism behind it); `make docs-check` green; `npm test` **46 files / 514 tests**; `npm run build` clean; `make spa-snapshot` refreshed (42 assets, the field present in all four surface chunks, `internal/web` green through the restored placeholder fixture); `make dev-test` on the rebuilt image **66 passed / 1 skipped / 6.3 min / exit 0**, including the new description case and the FR-026 §10 audit-listing assertion. **Reviewed and APPROVED** by the independent reviewer at party [130], cumulative range `a1617fe..0ded475`, after one finding withdrawn on measurement ([122] → [124]: the dashboard height claim was false of the PROSE, not the code) and one documentation P2 closed ([126] → `0ded475`). The reviewer independently re-ran `make docs-check`, `git diff --check` and hermetic `go test ./internal/domain ./internal/api ./internal/fileprovider`; the frontend, `-race` and live gates stay attributed to this session's isolated runs, which is how the reviewer recorded them. |

## Previous iteration (iter-0172 — CLOSED 2026-09-03. The six residuals of the v0.1.9 arc, closed on the owner's order: explicit R2/R3 review stamps, the monitor-write audit trail (FR-026 §10, D-0233), a live canary through the AMQP canary queue AND a real pull agent on the geo stack, the bounds map enumerated from the source, the final-tree gates, and the CI red diagnosed from its log. See [iter-0172](iterations/iter-0172.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0172-1 | R2 and R3 carry an explicit APPROVED, not an inference from "no new blocker". | DONE | Party [110]: `0555937..72cd2c3` (R2), `72cd2c3..63f4d17` (R3), docs `63f4d17..dc48509` — recorded in the DoD-0168 and DoD-0170 rows. |
| AC-0172-2 | A monitor write says who wrote it, by FR-026's rules and no new ones: three principal doors, no exported unaudited writer, three closed words, a target that names the document and never its contents, and the acknowledged `cleanup.kind: none` of FR-029 invariant 13 visible in the trail. | DONE | [`internal/store/monitoraudit.go`](../internal/store/monitoraudit.go), the unexported writers in [`monitors.go`](../internal/store/monitors.go) reached by tests through `export_test.go`; `TestAMonitorWriteLeavesOneAuditRowInItsTransaction`, `TestAMonitorAuditTargetNamesTheDocumentAndNotItsContents`, `TestAZeroValuedActorRefusesAMonitorWriteBeforeAnyStatement`, `TestTheFileProviderWritesNoMonitorAuditRow`; API `TestTheStoreExportsNoUnauditedMonitorWriter` + fixture; mutation killed (the create door with its hook removed). Reviewer P1 [112]: the delete door took a whole monitor from the caller, so a concurrent region change made the audited region stale and a caller could file the row under another project's organization — the door takes an id now and the row is read in the `FOR UPDATE` statement (`TestTheDeleteAuditNamesTheLockedRowNotTheCallersCopy`, `TestTheUpdateAuditNamesTheStoredSlug`, both mutations killed). FR-026 map rows 17–21 + 17a; FR-029 map row 13 no longer `PARTIAL`. |
| AC-0172-3 | The positive half of FR-029 invariant 6 is proven LIVE on both remote transports. | DONE | `e2e/tests/topology-geo.spec.ts` "a canary is claimed and run by the AMQP worker in geo1 and by the pull agent in geo2" — `make geo-test` 12 passed / 2.3 min on `2686a24`: a STAGE heartbeat from each region, not a `dispatch:` refusal and not silence. |
| AC-0172-4 | The published bounds are enumerated from the source in both directions; the hand count is gone. | DONE | `TestCanaryBoundsArePublishedForTheClient` parses the canary source for `CanaryMax…`/`CanaryMin…` and itself for the identifiers it publishes; a planted `CanaryMaxPhantomThing` fails it by name (D-0228 addendum). Reviewer P2 [112]: the first version parsed a hand-written file list — the hand count again — so the enumeration globs the directory and `TestTheBoundEnumerationSeesAnUnlistedCanaryFile` proves it against a copy carrying a file no list had. |
| AC-0172-5 | The final tree of the capability work has its own full `-race` and live run. | DONE | On `2686a24`: `-race` 33 packages exit 0 (`internal/store` 660.4 s); `make dev-test` 65 passed / 6.7 min. |
| AC-0172-6 | D-0225 is dispositioned with evidence: the failing tests named from the log, the mechanism shown, and the tests corrected without touching the product. | DONE | The job log (302 → download, not 403): `TestGateMaintCrashAfterEveryRemovalStatementConverges/after_drop.commit` and `TestConfirmedAnnulActuallyRewritesTheSealedFacts`, `internal/store` 942.8 s on the runner. The first REPRODUCED with a foreign older transaction (the D10 drop gate defers, correctly); `TestGateMaintADropIsDeferredWhileAnOlderTransactionIsOpen` pins it and the convergence test now injects on the first pass that reaches the drop. The second read from the code (the closing lifecycle write missed a 2 s test slice; production keeps the 60 s lease); `drainRepair` mirrors the recovery. Neither is the candidate D-0225 named — recorded as the wrong guess it said it might be. Not claimed at the time: a green re-run (the token cannot trigger one). **CLOSED 2026-09-04:** the owner reports the hosted TimescaleDB gate green. What this session verified itself: `origin/main` is `721c414` and `67e5aa9` — the commit carrying these test corrections — is an ancestor of it, so a green run there exercises exactly this fix. What it could NOT verify: the run itself, because this environment has neither `gh` nor any GitHub Actions read tool. The green status is the owner's report, not this session's observation, and is recorded as such. |
| DoD-0172 | AC-0172-1…6 DONE with evidence, `go vet` + `make docs-check` + the full `-race` suite + the live browser suite (with the audit-listing assertion) green on the closing tree. | DONE | On the closing tree (the audit doors, the corrected store tests, the geo case and the bounds enumeration): `go vet` clean; full `-race` **33 packages, exit 0**; `make docs-check` green (FR-026 map at 21 rows, FR-029 map with no `PARTIAL`); `make dev-test` on the rebuilt image **65 passed / 1 skipped / 6.7 min / exit 0**, including the new audit-listing assertion in `async-canary.spec.ts`; `make geo-test` 12 passed / 2.3 min. **Reviewed:** the range `2686a24..67e5aa9` drew two findings at party [112] — both accepted, fixed in `a1617fe`, and that range **APPROVED at [116]** (the reviewer independently re-ran `internal/domain`, `internal/api`, `internal/store` and checked the diff); cumulative for this iteration, `2686a24..a1617fe` is approved as an implementation range, which the reviewer states is not a release declaration. |

## Previous iteration (iter-0171 — CLOSED 2026-09-03. FR-029 phase C, invariant 6: the canary's capability announcement. An executor receives a canary ONLY if it announced the workflow kind and version — the last half of FR-029 that iter-0169 named in its own rows rather than implying. See [iter-0171](iterations/iter-0171.md), D-0231)

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0171-1 | The announcement is a SET of `<kind>@<version>` tokens, matched EXACTLY in both directions, and the token a job needs comes from the DOCUMENT rather than from core's constant. | DONE | [`internal/domain/canarycapability.go`](../internal/domain/canarycapability.go); `TestTheCapabilityTokenCarriesKindAndVersion`, `TestAnAnnouncementMatchesExactlyInBothDirections` (a v2 runner does not satisfy a v1 job, a v1 runner does not satisfy a v2 job, and `@11` does not answer for `@1`), `TestTheCarrierIsNamedForTheDocumentsCapability`. |
| AC-0171-2 | Three sources, merged once per tick and only when a canary is due, never crossed; a failed lookup refuses instead of assuming. | DONE | `Scheduler.canaryAnnouncements` over [`store.LiveCanaryAgentCapabilities`](../internal/store/pulljobs.go), [`mqadmin.LiveCanaryJobRegions`](../internal/mqadmin/mqadmin.go) and `WithLocalCanaryRegions`; `TestAnAgentAnnouncementDoesNotSpeakForAnAMQPRegion`, `TestAPullAgentAnnouncementAuthorizesItsOwnRegion`, `TestAFailedCapabilityLookupRefusesRatherThanAssumes`, `TestTheCapabilityLookupIsSkippedWhenNoCanaryIsDue`, `TestTheInProcessExecutorAnnouncesItsOwnRegion`, `TestLiveCanaryAgentCapabilitiesUnionsOnlyLiveAgents` (liveness proven by AGEING the row, because a non-positive window falls back to the function's own default). |
| AC-0171-3 | The two failures are named separately because the operator's fix differs, both bounded per-monitor DOWNs, and the check precedes the in-flight lease. | DONE | `TestARegionThatAnnouncedNothingRefusesTheCanary` (reaches `no_capable_runner` AND asserts the refused run takes NO slot), `TestAVersionSkewIsNamedAsSuchRatherThanAsAnAbsence` (`capability_mismatch`); mutations killed for the gate always passing, for the reason collapsed to one value, and for the resolver made eager. Reviewer P1 [99]: role=all announced its runner for a pull-served `core`, which is executed by agents — the false announcement let a capability-bound row sit unclaimed and the monitor stay SILENT; fixed at resolve time (`TestTheInProcessRunnerDoesNotSpeakForAPullServedRegion`, both builder orders, guard-removal mutation killed). Its credential-side twin — the same override raising core's carrier to generation 3 when core is pull-served, reaching ordinary credentialed monitors — fixed the same way as the follow-up the review asked for (D-0232, `TestTheInProcessExecutorDoesNotRaiseAPullServedRegionsCarrier`). |
| AC-0171-4 | The filter is not treated as the whole barrier: an incapable executor is physically unable to RECEIVE a canary on either transport. | DONE | AMQP — `checks.canary.[v3.]<token>.<region>` bound only by a worker whose runner has the type ([`internal/dispatch/amqp.go`](../internal/dispatch/amqp.go)); `TestASecretlessCanaryRidesThePlainCarrier`, `TestABoundCanaryRidesTheEnvelopeCarrierAndOnlyThat`, `TestTheEnvelopePairingIsRefusedInBothDirections`, `TestLiveCanaryJobRegionsReadsTheAnnouncedTokens`, `TestTheCanaryQueuePrefixMatchesTheDispatcher` (two packages that do not import each other, asserted against the other's source). Pull — migration 00098 + the per-claim `X-Cerbix-Workflow-Kinds` header; `TestAClaimTakesOnlyTheCapabilitiesItDeclared` (the old agent still takes every ordinary job — a barrier that starved the ordinary path would be the worse bug), `TestAClaimReceivesOnlyTheWorkflowsItDeclared`, `TestTheAgentAnnouncementIsBoundedGrammarCheckedAndStored`, `TestTheHeartbeatAnnouncesWhatTheRunnerActuallyHas`, `TestTheClaimDeclaresTheSameCapabilityAsTheHeartbeat`; mutations killed for removing the claim filter and for the handler dropping the declared header. |
| DoD-0171 | AC-0171-1…4 DONE with evidence, the FR-029 invariant map free of the `PARTIAL` row for invariant 6, the runbook's "upgrade the region first" instruction retired, and `go vet` + `make docs-check` + the full `-race` suite + the live browser suite green. | DONE | All green on the final tree: `go vet` clean; full `-race` **33 packages, exit 0** (`internal/store` 660.4s — the earlier 652.8s run predates the one-rule refactor and is not cited); `make docs-check` green with invariant 6 discharged and ONE `PARTIAL` left in the FR-029 map (invariant 13's audit-trail clause, which belongs to no requirement); `make dev-test` on the rebuilt image **65 passed / 1 skipped / 6.5 min / exit 0**, including the new third `async-canary.spec.ts` case — a canary in `pull1`, a declared pull region with no agent, reports exactly `dispatch: no_capable_runner` after 15 s, the scheduler's own decision on the real path. No frontend change, so no snapshot to regenerate. **Reviewed and APPROVED** by the independent reviewer: the capability range at party [102] after one P1 (the pull-served-core announcement, fixed in `5dc4783`), and the credential-side follow-up D-0232 (`4a2c7c8`) at [106] — cumulative `f06f343..4a2c7c8` approved. Release approval was separately withheld on the red TimescaleDB CI gate (D-0225), which is not this iteration's scope. **That gate is green as of 2026-09-04** on the owner's report — see AC-0172-6 above for what was verified here and what was not — so this withholding no longer stands. |

## Previous iteration (iter-0170 — CLOSED 2026-09-03. The dependency sweep (R3): the go-modules and frontend groups, the CI action pin, the `golang` build image, and three of the four frontend MAJORS. TypeScript 7 is declined with a reason that is upstream of this repository. No requirement row changes — this iteration moves versions, not behaviour. Third and last leg of the v0.1.9 arc. See [iter-0170](iterations/iter-0170.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0170-1 | Patch and minor together: the go-modules group, the frontend group, the CI action pin and the `golang` build image, each verified by the gate that owns it. | DONE | `amqp091-go` 1.14.0 + `grpc` 1.83.1 (`go build`, `go vet`, full `-race`); `@types/node` 26.4.1 + `eslint` 10.9.1 + `vue-tsc` 3.3.11 (38 files / 418 tests, clean build); `docker/setup-buildx-action` 4.3.0 SHA-pinned with its tag comment updated; `golang` 1.27.0-bookworm in [`docker/Dockerfile`](../docker/Dockerfile), which does not move `go.mod`'s language version. |
| AC-0170-2 | Each frontend MAJOR alone with its own verification — except where the ecosystem makes that impossible, in which case the coupling is recorded rather than papered over. | DONE | `jsdom` 30.0.1 alone (38/418, build clean); `vite` 8.2.2 alone (38/418, build clean); `vue-router` 5.3.1 on top of Vite 8, because it declares `peerOptional vite@"^7.3.0 \|\| ^8.0.0"` and the install fails with `ERESOLVE` on Vite 6 — **two majors that are one decision**, written up in [iter-0170](iterations/iter-0170.md) §2. |
| AC-0170-3 | TypeScript 7 is disposed of with a verified reason, not skipped. | DONE | NOT TAKEN: `vue-tsc` 3.3.11 cannot drive TypeScript 7 — `npm run type-check` dies in `vue-tsc/index.js` with `ERR_PACKAGE_PATH_NOT_EXPORTED` before checking a file. Upstream of this repository; the dependabot branch stays open on purpose, because closing it loses the reason. |
| AC-0170-4 | `make spa-snapshot` is regenerated and committed with the change that produced it, and the binary embeds what is committed. | DONE | 41 files in [`internal/web/dist`](../internal/web/dist) — every asset hash moved, because Vite 8 hashes differently; `internal/web/web_test.go` passes through the restored `assets/placeholder.txt` fixture; `go build ./...` clean. |
| DoD-0170 | AC-0170-1…4 DONE with evidence, `go vet` + `make docs-check` + the full `-race` suite + `npm run test` + `npm run build` + the live browser suite green. | DONE | All green on the final tree: `go vet` clean; full `-race` **33 packages, exit 0** (`internal/store` 639.8s, which is why the documented timeout is 40 m); `make docs-check` green; vitest 38 files / 418 tests; `npm run build` clean; `make dev-test` on the rebuilt image **62 passed / 1 skipped / 6.4 min / exit 0**, including both `async-canary.spec.ts` cases and `alertmanager.spec.ts`, the receiver path FR-026 rewired. The independent review: R3 code range `72cd2c3..63f4d17` and its docs `63f4d17..dc48509` **APPROVED** by the independent reviewer at party [110] (no new blocker at [74]; the explicit stamp requested and given on 2026-09-03). |

## Previous iteration (iter-0169 — CLOSED 2026-09-03. FR-029/NFR-024 phases B–E: the typed external canary built to the design approved at revision 8 (D-0218) — the contract and the canonical hash, the five-stage executor with its own strict SSRF guard, the in-flight lease and the idempotency key, SSE with the pinned fixture, the per-job pull lease, bundles, metrics and the runbook. Phase F (the typed UI form) is gated on the owner's visual approval of the mock, which now exists, and capability announcement is carried forward — both named in the rows rather than implied. First leg of the v0.1.9 arc the owner ordered as canary → R2 → R3, reviewed once at the end. See [iter-0169](iterations/iter-0169.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0169-1 | The workflow is a TYPE, not a convention: closed unions, no free-form field, both grammars, the body algebra, the ten frozen credential-bearing header names, and every bound with a number — with the D3a residual asserted as a NON-guarantee rather than left unsaid. | DONE | [`internal/domain/canary.go`](../internal/domain/canary.go), [`canarycanonical.go`](../internal/domain/canarycanonical.go), [`canaryexec.go`](../internal/domain/canaryexec.go); 22 test functions in `canary_test.go` including `TestALiteralInAnOrdinaryBodyLeafIsNotDetectable`, `TestBoundsAtTheEdgeAndOnePastIt`, `TestTheCanonicalDocumentCarriesNoProjectSecretName`. |
| AC-0169-2 | The executor runs the five stages and owns the rules `net/http` does not: its OWN strict SSRF guard with no configuration path into it, and a redirect policy that drops EVERY binding-backed header on a host-or-port change. | DONE | [`internal/prober/canary.go`](../internal/prober/canary.go); `TestCanaryDropsEveryBindingHeaderOnACrossHostRedirect`, `TestCanaryKeepsBindingHeadersOnASameOriginRedirect`, `TestCanaryStageFailures`, `TestCanaryResultNeverCarriesSecretsURLsOrIdentifiers`. Tests reach a fixture through an INJECTED DIALER at the seam, never a product option. |
| AC-0169-3 | One run per monitor and four per region, claimed at the scheduler's dispatch decision on BOTH paths — a canary with no binding never reaches the credential path, which the first version of the change missed — with a refused run writing ONE ordinary DOWN carrying a bounded reason. | DONE | Migration 00095 + [`internal/store/canaryinflight.go`](../internal/store/canaryinflight.go); `TestCanaryInflightHoldsOneRunPerMonitorAndBoundsTheRegion`, `TestCanaryInflightExpiresSoACrashedExecutorDoesNotParkAMonitor`, `TestASaturatedCanaryRunReportsDownRatherThanQueueing`, `TestAnUnsaturatedCanaryRunIsDispatchedNormally`. |
| AC-0169-4 | SSE with a pinned content type and bounds, the fixture verified by SHA-256 before a byte is uploaded, admission to `fileSupportedTypes` with a recursive secret guard, two low-cardinality metrics, and the per-job pull claim lease — which closes a defect older than the canary: any pull monitor with a timeout past 30 s was re-claimable mid-probe. | DONE | Migrations 00096; `TestCanarySSETakesTheSuccessEventAsTheResultDocument`, `TestCanarySSEFailureAndMalformedCases`, `TestCanaryMultipartUploadsThePinnedFixture`, `TestCanaryFixtureDigestsArePinned`, `TestCanaryBundleRefusals`, `TestPullLeaseIsAskedForOnlyWhenTheProbeOutlivesTheDefault`. |
| AC-0169-5 | The type is accepted by the DATABASE, not only by Go: `async_canary` is in `monitors_type_check`, and the vocabulary is asserted against the real table so the next type cannot repeat it. | DONE | Migration 00097 + `TestEveryValidMonitorTypeIsAcceptedByTheDatabase`. Found by the live E2E after every Go test passed, because no store test writes to the real `monitors` table — recorded that way in [iter-0169](iterations/iter-0169.md) §1. |
| AC-0169-6 | What is NOT built is named in the rows, the spec banner, the discharge map and the runbook — capability announcement, phase F and the per-kind queue — with the operational consequence stated: upgrade a region's executors BEFORE declaring a canary there. | DONE | FR-029/NFR-024 rows below (`IN_PROGRESS`), the spec's lifecycle banner, the FR-029 map in [`traceability.md`](traceability.md) with two `PARTIAL` rows, [`runbook.md`](runbook.md) §FR-029. **Superseded, kept as the record of iter-0169:** all three gaps this row named are built — capability announcement and the per-kind queue at iter-0171 (D-0231), phase F at D-0226 — the runbook's "upgrade the region first" instruction is retired, and both `PARTIAL` rows are closed. FR-029 and NFR-024 are `DONE`. |
| DoD-0169 | Phases B–E are DONE when AC-0169-1…6 are DONE with evidence, the fourteen §6 invariants have a discharge row each (`PARTIAL` allowed only where it names the gap), and `go vet` + `make docs-check` + the full `-race` suite + the live browser suite are green. FR-029 and NFR-024 stay `IN_PROGRESS` until phase F and the capability announcement land. | DONE | AC-0169-1…6 DONE; fourteen invariants discharged with two `PARTIAL` rows that name what is missing, enforced as a SET by a new `make docs-check` gate; `go vet` clean; full `-race` suite green; `e2e/tests/async-canary.spec.ts` on a live stack. The independent review of the whole arc happens once, at the end, by the owner's instruction. **Superseded, kept as the record of iter-0169:** phase F (D-0226) and the capability announcement (iter-0171, D-0231) both landed, so the condition this row set is met and FR-029 / NFR-024 are `DONE`; the arc's ranges were reviewed and approved one by one rather than once at the end. |

## Previous iteration (iter-0168 — CLOSED 2026-09-03. FR-026/NFR-021: an incident write says who wrote it. The gap D-0171 named and iter-0156 §2.8 proved — incident writes carried no `audit_logs` row for EITHER anchor — closed to the approved design (revision 4, D-0214), with the two behaviour corrections the design carries because its own invariant depends on them. Part of the v0.1.9 release arc the owner ordered as canary → R2 → R3, reviewed once at the end. See [iter-0168](iterations/iter-0168.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0168-1 | Every incident write a PRINCIPAL makes writes exactly one `audit_logs` row, committed by the SAME transaction, through a door that takes the actor as a required parameter — so a forgotten actor is a compile error, not an unaudited write. | DONE | [`internal/store/incidentaudit.go`](../internal/store/incidentaudit.go) (the actor, the five-word typed vocabulary, the tenant resolved in the insert, D5's target builders) and the door split in [`incidents.go`](../internal/store/incidents.go); `TestEveryPrincipalIncidentWriteIsAudited`, `TestARolledBackCreateLeavesNoIncidentAndNoAuditRow`, `TestAnAuditInsertFailureAbortsTheMutation`. |
| AC-0168-2 | The two door guards of D3 hold the surface — no file in `internal/api` calls a system door, and `internal/store` declares exactly the system doors that have machine callers — and BOTH are driven by fixtures that contain the violation. | DONE | [`internal/api/incidentdoors_test.go`](../internal/api/incidentdoors_test.go): `TestTheAPINeverCallsASystemDoor`, `TestTheStoreDeclaresExactlyTheSystemDoorsThatHaveMachineCallers`, each paired with `TestGuardOneFailsOnAFixtureThatViolatesIt` / `TestGuardTwoFailsOnAFixtureThatViolatesIt`. The guard's FIRST version exempted `handlers_alertmanager.go` by name and the exemption hid a real defect — see AC-0168-3. |
| AC-0168-3 | The Alertmanager receiver's create and its resolve are audited, because Alertmanager posts with a project-write token and a token is a PRINCIPAL (D1) — they were wired to system doors and audited nothing. | DONE | [`internal/api/handlers_alertmanager.go`](../internal/api/handlers_alertmanager.go) now uses `CreateIncidentByPrincipal`/`AddIncidentUpdateByPrincipal` with `auditActor(p)`; `TestTheAlertmanagerResolveIsAudited`, `TestEveryAuditedRouteCarriesItsPrincipalToTheStore`. Mutation: routing the resolve back through the system door — CAUGHT. |
| AC-0168-4 | No machine write audits, for each of the NINE writers D1 enumerates — and each case asserts the write HAPPENED before asserting the trail is empty. | DONE | `TestNoMachineWriterAudits` (7 subtests over 9 writers: the reconciler's two doors, the service auto-incident and its resolve, `⚡ Context:`, both `⏸ Suppressed:` writers, `🚀 Changes:` and `🕸 Impact:`). |
| AC-0168-5 | NFR-021's two clauses: nothing that was WRITTEN reaches a row, and incident behaviour is unchanged except for D8a and D8b — a repeated acknowledgement is a genuine no-op, and a concurrent duplicate delivery is ignored with HTTP 200 instead of 500. | DONE | `TestNoBodyEverReachesTheAuditTrail` (a marker planted in five bodies, every row searched whole); `TestARepeatedAcknowledgementIsANoOpAndAuditsNothingTwice`, `TestAnAcknowledgementThatWaitsStillStampsForward`; `TestAConcurrentDuplicateFiringLosesBenignly` (the real partial unique index), `TestADuplicateDeliveryThatLosesTheRaceIsIgnoredNot500` (the receiver's answer, and the human route unaffected). |
| AC-0168-6 | The documents move with the code: the §6 invariants discharged as a SET enforced by `make docs-check`, FR-022's invariant 14 given the number and state of the requirement that closes it, and the operator's own answer to "who resolved this incident". | DONE | The FR-026 map in [`traceability.md`](traceability.md) (16 rows, none `PARTIAL`) with a new checker gate (`FR026_HEADING`, §6 compared as a SET — verified by removing a row and watching it fail); [`func-service-incidents.md`](specs/func-service-incidents.md) invariant 14; [`overview.md`](overview.md); [`runbook.md`](runbook.md) §"Who wrote this incident?". |
| DoD-0168 | FR-026 and NFR-021 are DONE only when AC-0168-1…6 are DONE with evidence, the sixteen §6 invariants are discharged, and `go vet` + `make docs-check` + the full `-race` suite are green. The independent review: R2 code range `0555937..72cd2c3` and its docs `63f4d17..dc48509` **APPROVED** by the independent reviewer at party [110] (no new blocker at [74]; the explicit stamp requested and given on 2026-09-03). | DONE | AC-0168-1…6 DONE; sixteen invariants discharged with named tests and eight killed mutations; `go vet` clean; `make docs-check` green; full `-race` suite green. Two design claims the implementation found wrong are recorded in the spec's banner rather than quietly corrected: the guard exemption, and what the postmortem's `FOR UPDATE` can actually be observed to do. |

## Previous iteration (iter-0167 — CLOSED 2026-09-03, APPROVED by the independent reviewer at party [43]. The synthetic binding editor, built to the mock the owner approved visually on 2026-09-03. Closes the one gap FR-028's rows named: the SPA could show a scenario to a writer and could not DECLARE a binding. No requirement row moves — FR-028/NFR-023 closed at iter-0166 (D-0217) — and a pre-existing defect the tests found is fixed with it: a synthetic monitor could not be created from the SPA at all. See [iter-0167](iterations/iter-0167.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0167-1 | The binding rules live in a pure library that mirrors `domain.ScenarioBindings` and invents no rule of its own; the two things it adds are the two only a client can know — which project secrets exist now, and where in the form each refusal belongs. | DONE | [`frontend/src/lib/scenarioBindings.ts`](../frontend/src/lib/scenarioBindings.ts) + [`scenarioBindings.spec.ts`](../frontend/src/lib/scenarioBindings.spec.ts) (16 cases: the wire shape, the malformed key that used to be ignored, every refusal, the residual hint, the D10 reason). |
| AC-0167-2 | The editor delivers the three properties the mock was approved for — the mapping panel above the steps with no binding name ever shown alone, D7 taught by the control at a credential-bearing header, and "save before test" stated at the button — plus the residual as a hint and never a refusal. | DONE | [`NewMonitorView.vue`](../frontend/src/views/NewMonitorView.vue); [`NewMonitorScenarioBinding.spec.ts`](../frontend/src/views/NewMonitorScenarioBinding.spec.ts) (6 cases on the real component, including the POST body carrying the flat key and the placeholder and no literal). |
| AC-0167-3 | The first E2E anywhere that touches a synthetic monitor, closing part of FR-SYN-3's named coverage gap: declare a binding through the UI, meet the D7 refusal, see the test blocked, save, and find the flat key and the placeholder on the wire with no value anywhere. | DONE | [`e2e/tests/synthetic-bindings.spec.ts`](../e2e/tests/synthetic-bindings.spec.ts) against a live stack; the run is recorded in iter-0167 §4. |
| AC-0167-4 | A synthetic monitor can be created from the SPA at all — the defect the tests found, older than this work: `canSubmit` demanded a target the form deliberately hides for a type whose `NeedsTarget()` is false. | DONE | The scenario is the synthetic's own submit condition (`NewMonitorView.vue`); the case that found it is AC-0167-2's body test, and the reason it survived is the coverage gap AC-0167-3 closes. |
| DoD-0167 | AC-0167-1…4 DONE with evidence, `npm test` + `npm run build` + `make spa-snapshot` byte-consistent + `make docs-check` + the live browser suite green, and the reviewer's disposition of the range. | DONE | vitest 38 files / 416 tests; build clean; snapshot regenerated and committed with the change; docs-check green; `make dev-test` **60 passed / 1 skipped / 6.0 min / exit 0** after the review's P1 corrections (vitest 418 with the two added regressions). **APPROVED at party [43]** for the whole range (`9ab639e` + `3a67106` + `95ee945` + `c321931`), with the reviewer's OWN reproducible run: 24/24 focused, 38 files / 418 tests full, `vue-tsc --noEmit`. For two rounds their verification was a type-check and a diff read only — recorded that way, because this repo's docker builds wrote `frontend/node_modules` as root and stopped vitest starting in their workspace; `make spa-snapshot` now runs as the developer, which is what made the verification repeatable. |

## Previous iteration (iter-0166 — FR-028/NFR-023: a credential inside a synthetic monitor's scenario, in three stages. Opened 2026-09-02 after an operator asked whether a synthetic monitor could carry a bearer token and the answer was "yes, in cleartext, to everyone". Stage 2 APPROVED by the independent reviewer at party [30] after five rounds. See [iter-0166](iterations/iter-0166.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0166-1 | Stage 0: no probe result — heartbeat message, log line or metric label — carries a request URL, for ANY monitor type; a failure is a bounded class plus a host. | DONE | `internal/prober/failure.go`, applied in `prober.go`, `promql.go`, `synthetic.go`, `rabbitmq.go`; `TestProbeResultNeverCarriesTheRequestURL` (per type, through the real `NewRunner`, with a secret planted in the URL, the query, a header and the body), `TestFailureClassNamesTheCause`, `TestFailureClassesDNSAndTimeout`. |
| AC-0166-2 | Stage 1: the scenario is ciphertext at rest, covered by rotation and by an idempotent NON-FATAL startup backfill, and withheld from any principal who cannot write the monitor — with readiness never coupled to the key or the backfill. | DONE | Three classifications in `internal/domain/monitor.go` + reader MODES in `internal/store/monitors.go`; `BackfillMonitorConfigEnc`; `TestScenarioIsEncryptedAtRestAndReadPerMode`, `TestScenarioSurvivesAPartialUpdate`, `TestScenarioWriteRefusedWithoutAnAtRestKey`, `TestBackfillMonitorConfigEncIsIdempotent`, `TestSyntheticScenarioIsWriterOnly`. |
| AC-0166-3 | Stage 2: a DECLARED credential is a named binding resolved from the project inventory, validated by one rule on all four write surfaces, delivered in a body-bound envelope and substituted into the scenario with no copy beside it. | DONE | `internal/domain/syntheticbindings.go` from `Monitor.Validate`; `internal/store/materialize.go`; `internal/dispatch/credentials.go`; `TestScenarioBindingsAcceptsTheDeclaredShape`, `TestScenarioBindingsRefusals`, `TestMalformedBindingReferenceIsRefusedByName`, `TestScenarioBindingIsSubstitutedIntoTheScenario`, `TestScenarioBindingGateFailsClosed` (including the relocated placeholder), `TestSyntheticTestBeforeSaveFailsClosed`, `TestScenarioBindingRidesTheOrdinaryRefPath` (rename, delete and rotation on the ordinary ref path). |
| AC-0166-4 | The binding belongs to a synthetic monitor and to no other type — refused at the write boundary and gated in the store, in both derived key sets and in the dispatch gate — with the pre-rule row's lifecycle pinned rather than assumed. | DONE | D6b; `TestScenarioBindingIsRefusedOnEveryNonSyntheticType`, `TestScenarioBindingIsRefusedOnANonSyntheticMonitorAPI`, `TestScenarioRefsAreContributedBySyntheticMonitorsOnly`, `TestScenarioBindingOnANonSyntheticTypeIsRefused`, `TestAPreFixNonSyntheticBindingRowBehavesAsDocumented`, `TestAPreFixNonSyntheticBindingBlocksEditsUntilTheKeyIsDropped`. |
| AC-0166-5 | The documents that must move with the code moved with it: the two false promises in `func-oncall-synthetic-pull.md` corrected in place, the SYN requirements given the rows they never had, the binding key documented in `openapi.yaml`, an FR-028 runbook section stating what is NOT protected as plainly as what is, plus the traceability row and this report. | DONE | `docs/specs/func-oncall-synthetic-pull.md` (FR-SYN-1, AC-SYN-2), rows FR-SYN-1/2/3 + NFR-SYN-1/2 below, `openapi.yaml` (three monitor config schemas + regenerated TS), `docs/runbook.md` §FR-028, `docs/traceability.md`, `docs/iterations/iter-0166.md`. |
| DoD-0166 | FR-028 and NFR-023 are DONE only when AC-0166-1…5 are DONE with evidence, the sixteen §6 invariants are discharged, `go vet` + `make docs-check` + the full `-race` suite + the live browser suite are green, and the independent reviewer has disposed of every range. | DONE | Reviewer approval at party [30] for `b3c99b6` + `084b49d` + `258adb5` subject to committing the final documentation diff, committed as `900aa1b`. Full `-race` suite green (`internal/store` re-run idle after a load-dependent FR-024 flake that reproduces without this work); live browser suite 59 passed / 1 skipped / 6.2 min / exit 0. The SPA editor and D9 are deferred product scope, named in the rows and not counted as done. |

## Previous iteration (iter-0165 — CLOSED 2026-09-01 (D-0213); FR-025 and NFR-020 are DONE. Opened 2026-08-30. FR-025/NFR-020 Change Intelligence: implementation of the approved design (spec revision 3, D-0209) under the full AGENTS.md workflow — schema, domain and config; the store (phases, timeline, comparison, correlation, retention, the token allow-list); API + limiter + metrics; the CLI verb; the §7 matrix; the docs that must move; the SPA gated on the owner's visual approval of the mock. See [iter-0165](iterations/iter-0165.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0165-1 | Schema, domain, config: migration creating `service_changes` (UNIQUE identity+phase, UNIQUE (id, project_id), composite FK to services CASCADE, CHECKs exactly as §5), `incident_changes` (project_id + both composite FKs), `incidents UNIQUE (id, project_id)` if absent, `api_tokens.actions text[] NULL`; domain `ChangeKind`/`ChangePhase`, the phase-order function, `ValidateChangeText` (NFC, trim, no Cc/Cf, no line separators, code-point lengths), the `🚀 Changes:` marker; the ten `change.*` keys with bounds refused at boot and documented in `docker/config.example.yaml`. | DONE | landed `c6c74dd` (§1.1): migration 00094 with §5's constraints verbatim, the incidents key added only when absent, `ChangeKind`/`ChangePhase`/the phase order/`ValidateChangeText`/the marker, the ten `change.*` keys refused outside their bounds by name; 9 domain/config/migration tests — APPROVED — `c6c74dd` [99] and `5cace76` narrowly [39] in the design party |
| AC-0165-2 | Store: `RecordChangePhase` (per-identity advisory lock, domain order, identical replay by client-owned fields, `phase_exists` naming the field, `kind_mismatch`, `decision_unknown`, actor triple), the timeline over GROUPS (range ≤ 92 d, cursor over (latest_occurred_at, source, external_id), kind/source filters, LIMIT in groups), the group by identity, `serviceReliabilityCompareTx` extending the series owner (parity, withholding words, `pending`), correlation at `opened` delivery (links + note in ONE transaction, anchor immutable, fail-open, own + probable_root), retention by group keys, `authz.Can` intersecting a token's `actions`. | DONE | landed `c6c74dd` + `5cace76` (D-0211) + `fb625ca` (§1.2, §1.4): `RecordChangePhase` under the spelling-independent identity lock, the timeline over groups, `ServiceReliabilityCompare` sharing `reliabilityStepRollupTx` with the series, correlation anchored at the `opened` delivery over rows recorded by then, retention holding the same lock, the allow-list in `Can`/`VisibleScope`; 66 store tests in BOTH storage modes, 17 mutations killed — APPROVED — `c6c74dd` [99], `fb625ca` [29]; the store matrix independently green in BOTH modes (hypertable 616.764 s, declarative partitions 591.406 s) |
| AC-0165-3 | API + limiter + metrics + OpenAPI: record (201/200 replay/409/400), timeline, group, comparison, incident-changes routes; `change:record` action; token create/read with `actions` (400 `action_unknown`, immutable, audited); `change.*` limiter permits-then-bucket with `Retry-After`; the D15 metrics without identity labels; regenerated TS schema. | DONE | landed `7260e68` (§1.3): the four routes in the gate's order, the limiter permits-then-bucket with `Retry-After`, the six D15 metric families, the token `actions` on the wire (`action_unknown`, `action_not_granted` — D-0212), OpenAPI + regenerated TS; 29 tests — the API's EFFECTIVE SLICE approved at [20] (`7260e68` + `2c662a4` + `424edcd` + `a1cfe00` + `4f3a046`); `7260e68` alone NOT approved as submitted — findings [8], [11], [14] |
| AC-0165-4 | CLI `cerbix change record` per D13: env-only credentials, one stdout line (`recorded`/`replayed`), exit 0/2/1, no 429 retry, `--json` verbatim, `--at` default now. | DONE | landed `74ce187` + `25e181a` (§1.5, §1.5a): D13's contract with the exit classes, `flag.Visit` keeping an explicitly empty `--at`/`--decision` verbatim; 26 tests and every exit class proven live in the attestation — the CLI's EFFECTIVE SLICE approved at [24] (`74ce187` + `25e181a`); `74ce187` alone NOT approved as submitted — finding [52] |
| AC-0165-5 | The §7 matrix discharged with named tests and mutations (store, API, CLI, E2E on a live stack for the routes and the CI-token allow-list), the FR-025 acceptance map in `docs/traceability.md` compared as a set by `make docs-check`. | DONE | landed `f8f8cb4` + `42ae4ab` (§1.8, §1.9): `changes.spec.ts` (3 API-shaped tests), `changes-ui.spec.ts` (the card, the horizon, the two hops), and 107 new vitest tests — 32 files / 375 tests; the live proof is the committed [attestation](iterations/iter-0165-live-evidence.md): full suite 58 passed / 1 skipped / exit 0 and every CLI exit class against the running server — APPROVED — the E2E and SPA evidence at [74] (`f8f8cb4` + `42ae4ab`), the CLI-evidence correction slice at [72] (`7c45db0..201f542`); `f27133d` alone NOT approved as submitted, and the historical run is NOT reproduced — the current-head confirmation is `iter-0165-cli-evidence.md` ([72], [77]) |
| AC-0165-6 | The docs that must move (§4) moved with the code: overview, runbook (rows + the CI-token recipe), `func-service-reliability.md` §14.9 and §16.8, `func-incident-context.md`, `sec-authn-authz.md`, README, specs README. | DONE | landed `35ded9a` (§1.7): the FR-025 acceptance discharge (23 invariants + the §7 matrix, `PARTIAL` only where the SPA or the live run is the evidence), the checker comparing §6 as a SET, the runbook's operations section, overview, the sibling specs and the READMEs — the docs/checker EFFECTIVE SLICE approved at [56] (`35ded9a` + `056eff5` + `b320290`); `35ded9a` alone NOT approved as submitted — findings [49], [50], [54]. The roll-up and map approved at [75] |
| AC-0165-7 | SPA per the approved mock — the owner approved `docs/design/mock-change-intelligence.html` visually on 2026-08-30 and the reviewer its static contract (party [29]); the mapping to product surfaces is D-0210; no Vue file before the API changeset exposes the routes. | DONE | landed `d2e0f3c` (§1.6): the card with terminal-only marks on the facts strip, the comparison view, the per-service timeline, the incident's `Preceded by`, the token allow-list form; `make spa-snapshot` regenerated and proven byte-identical to a fresh build — the SPA's EFFECTIVE SLICE approved at [47] (`d2e0f3c` + `0c8a489` + `41b688c` + `3ad656f` + `caf8e6e` + `7bca415` + `673f707` + `381bfd4`); `d2e0f3c` alone NOT approved as submitted — findings [31], [32] |
| DoD-0165 | FR-025 and NFR-020 are DONE only when AC-0165-1…6 are DONE with evidence, every invariant of §6 (23) is discharged in `docs/traceability.md`, and the independent reviewer has approved the implementation; AC-0165-7 may close later without reopening the rest. | DONE | AC-0165-1…7 all DONE (AC-0165-7 did not need the exemption); the 23 invariants and the nine §7 groups discharged with named tests and mutations, no `PARTIAL` row, enforced as a SET by `make docs-check`; the reviewer issued a disposition for EVERY commit in the chain, and four ranges are approved as an effective slice with the original NOT approved as submitted — API [20], CLI [24], SPA [47], docs/checker [56] — plus the live-evidence correction slice [72]. `f27133d` is not approved as submitted and `cee8790` is SUPERSEDED, subject to no standalone approval ([77]). The live run of `f8f8cb4` is NOT reproduced: what exists is a current-head confirmation, and both evidence files say so ([72]). D-0213 |

## Previous iteration (iter-0164 — CLOSED 2026-08-30. Hygiene after FR-024, one landed change each — the alerting panel's draft survives a late prop, a server-side `state` filter on the gate ledger — and the FR-025 Change Intelligence design approved at revision 3 (D-0209). See [iter-0164](iterations/iter-0164.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0164-1 | An operator's unsaved edit in the service alerting panel survives a later `alerting` prop from the parent (re-load, poll, `@saved` echo): the typed value stays, the form stays dirty, Save carries it; a prop change with no unsaved edit still re-initialises the draft. Root cause proven with a test that fails on the unfixed component; `alerting.spec.ts:36` deterministic. | DONE | landed (§1.2 of [iter-0164](iterations/iter-0164.md)): both halves of the cause proven by tests failing on the unfixed files; vitest 26 / 268; `alerting.spec.ts` 3 × 3 and the full suite 54 / 1 on a vite-dev SPA over the live backend; APPROVED at party [18] (independent mutation: removing the draft preservation fails the two loss-of-edit tests); `make dev-test` on the image from `dd83bfa` 54 / 1 |
| AC-0164-2 | `GET /api/v1/projects/{p}/gate/decisions` accepts a repeatable `state` filter (a set, OR; unknown value → 400 `state_invalid`) applied before the limit with unchanged cursor semantics; the SPA's decision history uses it and its client-side hint is gone; OpenAPI + regenerated TS; store, API and vitest evidence. | DONE | `84adac1` (§1.1 of [iter-0164](iterations/iter-0164.md)): store `TestGateLedgerListingStateFilter`, API `TestGateDecisionListStateFilter` + 4 refusals, SPA spec; APPROVED at party [18] |
| AC-0164-3 | The FR-025 specification (a new `func-change-intelligence` spec under `docs/specs/`) exists at revision 1 in the working tree with the FR-024 spec's section shape, bounded by D17 of `func-reliability-gate.md`, with D12's token-scoped CI capability in its principal model, and is on the party for the reviewer's design review; revisions are NOT committed per round — the accepted design phase is committed once, as FR-024's was (D-0203). Design approval is NOT part of this iteration's DoD. | DONE | [`func-change-intelligence.md`](specs/func-change-intelligence.md): revision 1 on the party at [8]; rounds [9] and [13]; design APPROVED at revision 3 ([15]); the owner's seven answers recorded (§11, D-0209); committed once as the accepted design phase (D-0203's rule) |
| DoD-0164 | AC-0164-1 and AC-0164-2 DONE with evidence and reviewer approval of their ranges; AC-0164-3 DONE when the revision-1 spec is on the party for review; `make docs-check` green; no requirement row changes (FR-025 gets its row when its design is approved). | DONE | AC-0164-1/2 APPROVED [18], AC-0164-3 design APPROVED [15] and committed once (35f2851); docs-check green on every commit; FR-025/NFR-020 entered as TODO |

## Previous iteration (iter-0163 — CLOSED 2026-08-29 (D-0208). FR-024/NFR-019 Reliability Gate DONE: schema and store core, the fenced ledger maintenance, API + limiter + metrics + audit, the CLI, the §7 matrix discharged in both storage modes, the docs, and the SPA per the owner-approved mock — every range approved by the independent reviewer ([16], [26]/[30], [44]/[46], [60], [69], [74], [82], [94]). See [iter-0163](iterations/iter-0163.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0163-1 | Schema and domain: migration creating `service_gate_policies` (generation revision, tombstone), `service_gate_overrides` (actor + revoker triples, history index), the daily-partitioned `service_gate_decisions` with `gate_uuid_ms` CHECK and the two listing indexes, the `service_gate_decision_partitions` registry, and the bootstrap partitions `[today, today+7d]` registered — in BOTH storage modes; `LateArrivalGrace` moved to `internal/domain` beside `CanonicalBucket`, `MinSealLag` derived there and asserted as a FORMULA; the ten `gate.*` keys declared, defaulted, range-validated and cross-validated at load. | DONE | `16eecaf` — [`00093_reliability_gate.sql`](../internal/store/migrations/00093_reliability_gate.sql), [`gate.go`](../internal/domain/gate.go), [`config.go`](../internal/config/config.go); tests [`gateschema_internal_test.go`](../internal/store/gateschema_internal_test.go) + [`gateschema_b_internal_test.go`](../internal/store/gateschema_b_internal_test.go), [`gate_test.go`](../internal/domain/gate_test.go) + [`gate_b_test.go`](../internal/domain/gate_b_test.go), [`config_gate_test.go`](../internal/config/config_gate_test.go) + [`config_gate_b_test.go`](../internal/config/config_gate_b_test.go), both storage modes; five mutations killed; reviewer APPROVED 59dc96d..e080699 at party [30]; §1.3 of [iter-0163](iterations/iter-0163.md) |
| AC-0163-2 | Store core: policy PUT/DELETE with `expected_revision` CAS before no-op, revision as a never-reused generation, exhaustive clause validation, override closure on change/delete; override create under the service lock with expired-slot release, revoke by immutable id, read-time `status` function (four rows, table-tested with a precedence mutation); the decision in ONE `REPEATABLE READ` transaction — `evaluated_at` from `statement_timestamp()`, report/latches/incident/coverage consumed through tx-taking variants, the D4 algebra, D7 presence, `facts_fresh_until`'s one formula, UUIDv7 from `evaluated_at`, canonical evidence, retry-once on serialization failure; ledger by-id read pruned to one partition and the keyset listing with `LIMIT+1` and cursor from the last returned row. | DONE | `8cb12d6` + fixes `b7518b9` (three P1: post-lock statement_timestamp, budget bounds BEGIN, audit actor label) + `e080699` (Agent B: 11 tests, 19 mutants) — [`gatedecision.go`](../internal/store/gatedecision.go), [`gatepolicy.go`](../internal/store/gatepolicy.go), [`gateoverride.go`](../internal/store/gateoverride.go), [`gateledger.go`](../internal/store/gateledger.go); tests `gate_*_internal_test.go`, `gate_b_*_internal_test.go`, [`gate_clock_budget_internal_test.go`](../internal/store/gate_clock_budget_internal_test.go), both storage modes; reviewer APPROVED at party [26] and [30]; §1.4–§1.6 of [iter-0163](iterations/iter-0163.md) |
| AC-0163-3 | Ledger maintenance: the pass on its own `LeaderSession` (slot 3), standalone build + ATTACH, `DETACH CONCURRENTLY` behind a `detaching` intent, FINALIZE, deferred snapshot-gated DROP, stage accounting, creation `[0,12s)` / removal `[12s,27s)` clamped per statement, release as a proof under an absolute deadline with hijack-and-close on any unknown, marker-based ownership with `relid` refresh, the four catalog gauges and the maintenance error family; wired as the leader's goroutine off the dispatch loop. | DONE | `a6a9915` + fix `24e901f` ([33]: no time minted past the absolute release deadline) — [`gatemaintenance.go`](../internal/store/gatemaintenance.go), [`scheduler/gatemaintenance.go`](../internal/scheduler/gatemaintenance.go), 33 tests in `gatemaintenance_*_internal_test.go` (both modes + PostgreSQL 15.8 prepared-statement case) and [`gatemaintenance_test.go`](../internal/scheduler/gatemaintenance_test.go); four mutations killed; D-0206; reviewer APPROVED at party [44]/[46]; §1.7, §1.9 of [iter-0163](iterations/iter-0163.md) |
| AC-0163-4 | API, limiter, metrics, audit: the eight policy/override routes, `POST …/gate`, the by-id and listing ledger routes; three `authz.Action`s; in-flight permits then atomically-debited token buckets with `Retry-After = ceil`, never below 1; `cerbix_gate_*` evaluation metrics; audited mutations with the actor triple; `openapi.yaml` and the regenerated TS schema. | DONE | metrics `db16dfa` ([16]); API `229b33e` + fix `24e901f` ([41]: non-object bodies refused) — [`handlers_gate.go`](../internal/api/handlers_gate.go), [`gatelimiter.go`](../internal/api/gatelimiter.go), [`authz.go`](../internal/authz/authz.go), `openapi.yaml`, regenerated `frontend/src/api/schema.d.ts`; 34 handler/limiter tests + 10 authz; two limiter mutations killed; reviewer APPROVED at party [44]/[46]; §1.1, §1.8, §1.9 of [iter-0163](iterations/iter-0163.md) |
| AC-0163-5 | CLI `cerbix gate check`: `CERBIX_URL`/`CERBIX_TOKEN` from the environment only, TLS verified with `CERBIX_CA_FILE`, one stdout line or `--json` verbatim, reasons/diagnostics on stderr, exit 0/2/4/1, no retry on 429 with `Retry-After` printed. | DONE | `d926568` + `92d5a66` (review [13]: `--json` now byte-identical) — [`gate.go`](../internal/cli/gate.go), 23 tests in [`gate_test.go`](../internal/cli/gate_test.go) against `httptest` servers incl. TLS with/without `CERBIX_CA_FILE`, 429 seen exactly once; reviewer APPROVED at party [16]; §1.2 of [iter-0163](iterations/iter-0163.md) |
| AC-0163-6 | The §7 matrix is discharged with named tests and mutations in both storage modes, including the scheduler off-loop test, the fencing test (terminated backend, successor removes exactly once), the release-proof tests, the PostgreSQL 15.8 prepared-statement case RUN on a throwaway container, and E2E on a live stack for the API surface. | DONE | E2E landed — [`gate.spec.ts`](../e2e/tests/gate.spec.ts) (the D15 FOREIGN case is a real second project, before/after unchanged) and [`gate-live.spec.ts`](../e2e/tests/gate-live.spec.ts) (the 300 s seal-lag floor against the live sealer): full suite 51 passed / 1 skipped on a live stack, the CLI exercised against it (exit 4 / 0 / 1), invariant 12's post-delete HTTP read proven, the `pg_dump`/`pg_restore` round trip run and its relid rebinding proven against the live catalog (§1.10 and §1.12 of [iter-0163](iterations/iter-0163.md)); the §7 matrix roll-up per invariant is the FR-024 discharge table in [`traceability.md`](traceability.md) — at that range every row was reached except the SPA clauses, which AC-0163-8 reached later in `e78f4f7` (evidence as of `52db317`, not a current exception); the D15/live/restore range `9073a42..52db317` APPROVED by the reviewer at party [69] ("this closes the D15 live HTTP proof"), earlier ranges at [16], [26]/[30], [44]/[46], [60] |
| AC-0163-7 | The docs that must move (§4) moved in the same change: overview, runbook thresholds and capacity, `func-service-reliability.md` §16.6a/§16.8 invariants 106–107 with traceability rows, README sentence, `docs/specs/README.md`; `make docs-check` green. | DONE | `docs/overview.md`, `docs/runbook.md`, `README.md`, `docs/specs/README.md` in `8cb12d6`; `func-service-reliability.md` §16.6a / §16.8 invariants 106–107 and the FR-024 discharge table in `16839a4`, its intro corrected in `9073a42` and closed to one open gap in `886831f`/`52db317`; `make docs-check` green on every commit of the range (§3 of [iter-0163](iterations/iter-0163.md)) |
| AC-0163-8 | SPA per the approved mock — ONLY after the owner's visual approval of `docs/design/mock-reliability-gate.html`; until then no Vue file is touched and this row says so. | DONE | the owner approved the mock visually on 2026-08-29 (D-0207); the SPA phase runs as iter-0163 §0 task 8 (8a card + editor + override, 8b ledger and override-history views, 8c vitest + live UI E2E + snapshot, 8d docs); 8a/8b landed in `9793758` (backend prerequisite `3fc19e3`, APPROVED [82]); 8c landed with the three review fixes ([86] Discard no longer clears a 409, [88] the cold deep-link keeps `?service=`, [90] the parent service load is guarded): 26 vitest files / 261 tests, `e2e/tests/gate-ui.spec.ts` (3 tests), `make spa-snapshot` regenerated; 8d docs landed with it; the 8c range `9793758..e78f4f7` APPROVED at party [94] (independent mutations killed: restoring the Discard conflict-clear, startup `start(false)`, removing the stale guard); `make dev-test` on the rebuilt image 54 passed / 1 skipped |
| DoD-0163 | FR-024 and NFR-019 are DONE only when AC-0163-1…7 are DONE with evidence, every invariant of §6 is discharged in `docs/traceability.md`, and the independent reviewer has approved the implementation; AC-0163-8 may close later without reopening the rest. | DONE | AC-0163-1…8 DONE with evidence; every invariant of §6 is reached in the FR-024 discharge table (no `PARTIAL —` row); the independent reviewer approved every range, the SPA's at [94]; closed by D-0208 |

## Previous iteration (iter-0162 — CLOSED 2026-08-28. Docs only: the FR-024 design gate (thirteen revisions, approved at [47]), its UI mock (three revisions, static contract approved at [59]), and the owner's decision to defer the requirement (D-0202). No product code. See [iter-0162](iterations/iter-0162.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0162-1 | The FR-024/NFR-019 design is APPROVED as a specification before any code, by an independent reviewer who re-read the working tree rather than the implementer's disposition, with every owner decision recorded. | DONE | [`func-reliability-gate.md`](specs/func-reliability-gate.md) revision 13; party [47]; D-0188, D-0189, D-0190, D-0193, D-0194 (owner), D-0191, D-0192, D-0195 … D-0201 (implementer authority) in [decisions.md](decisions.md). |
| AC-0162-2 | A UI mock exists BEFORE any frontend code, drawn from the approved specification with the repository's design language (tokens verbatim, no new colour), and its static contract — routes, presence, states, RBAC rendering — agrees with the specification. | DONE | [`mock-reliability-gate.html`](design/mock-reliability-gate.html), three revisions; static contract approved at party [59]; the mock's section in [`notes.md`](design/notes.md). Not visually approved by the owner — stated, not hidden. |
| AC-0162-3 | The retired vocabulary of every superseded specification revision is refused by `make docs-check`, in the spec, the FR-024 decision sections and the status rows, and the guard's own scope is fixture-tested. | DONE | [`check-docs-references.py`](../scripts/check-docs-references.py) `GATE_STALE`, [`check_docs_references_test.py`](../scripts/check_docs_references_test.py) (18 cases); §10 of the spec. |
| AC-0162-4 | The owner's deferral is recorded where a reader will meet it — spec banner, status rows, decision, traceability, design notes — with the resume path stated, and WITHOUT widening the three-state status vocabulary. | DONE | D-0202 in [decisions.md](decisions.md); the FR-024/NFR-019 rows below stay `TODO`; [`traceability.md`](traceability.md) FR-024 row; §5 of [iter-0162](iterations/iter-0162.md) records the reverted `DEFERRED` value. |
| AC-0162-5 | The arc's own process defects are corrected on the record, not edited away: per-round commits against the commit policy, the fourth status value, the late "why", and the work having run outside the iteration workflow. | DONE | §5 of [iter-0162](iterations/iter-0162.md); D-0201 and D-0202. |
| DoD-0162 | The design gate is closed as approved, the mock exists, the deferral is written down with its resume path, and every role has a stated output — including "none, by decision". | DONE | §3 of [iter-0162](iterations/iter-0162.md): Agent A none (D-0202), Agent B the guard's 18 fixtures + `docs-check` green, Agent C the canonical docs, Agent D no runtime delta. FR-024/NFR-019 remain `TODO`. |

## Previous iteration (iter-0161 — CLOSED 2026-08-28. A production upgrade failed on PostgreSQL 14, then an adversarial review of FR-022/FR-023 turned into the arc that runs from §7 to §54: incident lifecycle, service escalation, alert routability, and the outbox's delivery order. See [iter-0161](iterations/iter-0161.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0161-1 | PostgreSQL 15+ is ENFORCED and documented instead of assumed. A production upgrade on PG14 applied `00061`…`00069` and died on `00070` with `syntax error at or near "("`; five migrations use the column-list `ON DELETE SET NULL (col)` form from PG15. `Migrate` now reads `server_version_num` before the first file and refuses with the version, the requirement and the fact that nothing was applied — verified on a real `postgres:14-alpine`, which left ZERO tables and no `goose_db_version`. The guard's own first version was buggy (its early returns held the advisory lock); it now runs before the lock is taken. | DONE | [`store/migrate.go`](../internal/store/migrate.go), `TestMigrateRefusesAServerTooOldForItsOwnSchema`; requirement stated in [README](../README.md), [runbook.md](runbook.md) (with the partial-upgrade recovery note for `monitors.slug`) and [overview.md](overview.md) ([iter-0161](iterations/iter-0161.md) §1–§3). |
| AC-0161-2 | A status-page component can be created from a Service. The picker rendered a blank option and carried no value: the view read `sv.name`/`sv.id` from an endpoint answering `ServiceSummary`, behind an `as Service[]` cast that silenced the compiler, and the test fixture repeated the same wrong shape. | DONE | [`StatusPagesView.vue`](../frontend/src/views/StatusPagesView.vue), `StatusPagesView.spec.ts` (fixture corrected to the real shape, +1 test asserting the option's value AND label); reverting the read fails it ([iter-0161](iterations/iter-0161.md) §5). |
| AC-0161-3 | The service page no longer claims availability, the error budget and the burn rate "arrive with the next iteration" — they arrived in iter-0144 and the page renders them; what a fresh service actually lacks is sealed facts and a declared objective. | DONE | [`ServiceDetailView.vue`](../frontend/src/views/ServiceDetailView.vue) ([iter-0161](iterations/iter-0161.md) §4). |
| AC-0161-4 | An incident's status is FORWARD-FLOWING and the store owns it. `resolved` was terminal only against a status the handler had read moments earlier, so two overlapping writers could commit backwards, and nothing at all stopped `identified → investigating` even in sequence. A plain comment carried the status the API had materialized from a stale read. All three now live in ONE guarded write: `CanFollow` in the store's transaction, and an omitted status is a keep-current INTENT rather than a client-supplied value. The CLOCK moved with it — `statement_timestamp()` taken AFTER the incident row lock, one instant across the incident, the timeline entry and the outbox row, so a writer that waited cannot claim it acted before the wait. | DONE | [`store/incidents.go`](../internal/store/incidents.go) (`AddIncidentUpdate`, `AcknowledgeIncident`, `CreateIncident` stamping `resolved_at` when created resolved), [`api/handlers_incidents.go`](../internal/api/handlers_incidents.go), [`IncidentDetailView.vue`](../frontend/src/views/IncidentDetailView.vue) + [`lib/incident.ts`](../frontend/src/lib/incident.ts) (forward-only picker; a comment sends no status); `TestAWriteThatRacedAResolveIsRefusedWithoutATrace`, `TestAnIncidentCannotWalkBackwardsThroughItsLifecycle`, `TestAPlainCommentTakesTheStatusItLandsOnNotTheOneItRead`, `TestAWaitingWriterStampsItsUpdateAfterTheWait`, `TestAWaitingAcknowledgementStampsAfterItsWait`, `TestAnIncidentCreatedResolvedIsStampedAndStaysVisible`; [iter-0161](iterations/iter-0161.md) §9, §19, §20. |
| AC-0161-5 | A service incident is a lifecycle, not a row. It announces both ends like any other incident (webhooks and status-page subscribers, not just the operator's screen), it RESOLVES when the health episode it belongs to ends — including the disown and delete paths, which used to close the alert and leave the incident open — and the postmortem names the declaration that governed the outage rather than the newest one. Snapshotting members verifies revision ownership FIRST and refuses a foreign one, fail-closed rather than saving an empty member list as if it were true. | DONE | [`store/serviceincidents.go`](../internal/store/serviceincidents.go), [`store/servicealertlifecycle.go`](../internal/store/servicealertlifecycle.go); `TestServiceIncidentAnnouncesItsLifecycleLikeAnyOther`, `TestDisowningAServiceEndsItsIncidentAndNotJustItsAlert`, `TestTheIncidentSnapshotNamesTheGoverningDeclaration`, `TestSnapshotRefusesARevisionThatIsNotTheServices`, `TestTheMachineLeavesAHumanIncidentAlone`; [iter-0161](iterations/iter-0161.md) §10–§12, §21. |
| AC-0161-6 | An incident climbs the ladder it STARTED with. The escalation policy is snapshotted at open (migration [`00085`](../internal/store/migrations/00085_incident_escalation_snapshot.sql)) with a tenant-composite FK, so editing a policy mid-outage cannot re-time a page that is already in flight. The backfill for incidents open ACROSS the upgrade would have fired every overdue step at once, because their offsets counted from `started_at` and the attachment history does not exist — so backfilled rows carry `due_base = the migration instant` and the ladder starts PROSPECTIVELY. That is a legacy exception, approved and recorded as one rather than described as the same rule (D-0175). | DONE | [`store/escalation.go`](../internal/store/escalation.go) (`due_base` in selection), [`store/serviceincidents.go`](../internal/store/serviceincidents.go) (`snapshotEscalationPolicyTx`); `TestUpgradingWithAnOpenServiceIncidentKeepsItsLadder`, `TestTheFirstPassAfterAnUpgradeDoesNotEmptyTheLadder` (continuity WITHOUT the retroactive burst — the composition is what catches it), `TestAnEscalationSnapshotCannotCrossTenants`; [iter-0161](iterations/iter-0161.md) §13, §16–§18. |
| AC-0161-7 | Arming requires a route that is really a route, a policy that covers the state the service is IN, and an onset that was actually committed. Routability was decided by COUNTING array entries, so a schedule naming a deleted or disabled channel armed coverage and the member's page was suppressed in favour of a replacement that could notify nobody — the P0 of this arc, and it was live in two places at once. An onset nobody can receive is now WITHHELD rather than latched, counted by reason (`unroutable` vs `no_governing_revision` — different owners, so one number would have sent the wrong person), and coverage begins when the onset commits, not when the route returns (D-0176) — and **narrowed further by AC-0161-12**: committing is not enough, the announcement must have been DELIVERED. | DONE | [`store/alertdelegation.go`](../internal/store/alertdelegation.go), [`store/servicealerteval.go`](../internal/store/servicealerteval.go), [`store/serviceburneval.go`](../internal/store/serviceburneval.go), [`store/servicealertstate.go`](../internal/store/servicealertstate.go); `TestADeadScheduleTargetDoesNotArmAnythingWhenTheProjectHasNoChannels`, `TestAPolicyThatDoesNotCoverTheCurrentStateArmsNothing`, `TestARestoredRouteDoesNotArmBeforeTheOnsetIsCommitted`, `TestACorruptScheduleParticipantIsRefusedAndSurfaced`, `TestAnUnroutableServiceOpensNothingAndDoesNotSwallowTheLaterOnset`, `TestAServiceWithNoGoverningDeclarationOpensNothing`, `TestAnAmbiguousBurnRowArmsNothing`; [iter-0161](iterations/iter-0161.md) §21–§23, §25–§27. |
| AC-0161-8 | An incident's lifecycle events cannot be DISPATCHED out of order, and the guarantee says exactly that and no more. `incidents.event_seq` (migration [`00086`](../internal/store/migrations/00086_incident_event_sequence.sql)) is advanced by every writer inside the transaction that writes the fact; the CLAIM refuses to release an event while an earlier one of the same incident is undelivered, a DEAD predecessor blocks too, and legacy rows tying at zero are broken on `(seq, created_at, id)` so a whole backlog does not go out in one batch (migration [`00087`](../internal/store/migrations/00087_outbox_incident_stream.sql)). The fenced class is enforced by the DATABASE (migration [`00088`](../internal/store/migrations/00088_outbox_fenced_class_owner.sql)), not by whichever binary is inserting, because a rolling upgrade is exactly the window the barrier exists to cover. The first version of all this was a delivery-time gate that dropped openings nobody had received; it is deleted and dissected. Arrival order is NOT claimed: cerbix orders what it controls, and the payload's `(incident.id, seq)` is what lets a receiver do the rest (D-0177). | DONE | [`store/outbox.go`](../internal/store/outbox.go) (`ClaimDueOutbox` as a CTE preserving `was_due`), [`outbox/outbox.go`](../internal/outbox/outbox.go) (the gate GONE), [`domain/webhook.go`](../internal/domain/webhook.go); `TestAnIncidentsEventsAreClaimedInOrder`, `TestADeadPredecessorHoldsTheIncidentStream`, `TestAClaimIsOrderedByWhenTheEventWasDueNotByItsNewLease`, `TestLegacyRowsWithoutASequenceAreStillClaimedOneAtATime`, plus real 87→88 migration tests for a pre-barrier DEAD row and a pre-barrier CLAIMED row; [iter-0161](iterations/iter-0161.md) §24, §28–§32. |
| AC-0161-9 | Upgrades crossing `00088` take ONE stop, and the operator documents say so. The trigger fences rows; it cannot reach a delivery an old worker already has in flight, and the promotion only takes that worker's claim token away. So the outbox owners (`all`/`api`/`scheduler` — the roles with `coreDelivery`; probers and agents keep running) stop before migrating. Skipping it loses no event and corrupts nothing; it risks the out-of-order delivery the migration exists to end, bounded by one ALREADY-CLAIMED batch per old owner — up to 50 rows each, each fanning out to every hook and subscriber, because a worker continues through its batch after losing the claim-token CAS. The first version of this row said "once", which was wrong by three mechanisms at the same time. | DONE | [`docs/runbook.md`](runbook.md) (the procedure, with the role list matching `servicesForRole` exactly and a mandatory one-off NEW-binary `cerbix migrate`), [`docs/overview.md`](overview.md), D-0177; [iter-0161](iterations/iter-0161.md) §33. |
| AC-0161-10 | The screen and the metric speak ONE vocabulary, in two parts: each candidate service is classified by the same clause evaluation, and across several candidates the metric reports the furthest miss — always one of their badge reasons, not necessarily the badge on screen. The badge said which clause of §16.1 was unsatisfied — eleven values, each a different thing to go and fix — while the delivery-time lookup computed the same conjunction and reported `no_active_owner` whatever had failed, which is also what it reports for the majority of monitors no service claims at all. Both now evaluate ONE clause set, so a candidate's badge and the reason computed for that candidate are the same string by construction; `no_owning_service` separates the ordinary case from the real ones. §16.6b carries the vocabulary as a NORMATIVE table with the fix for each value, and a new value needs a decision record. The contract has TWO parts, because "cannot disagree" was too strong: classification is shared per candidate, and SELECTION across several candidates reports the furthest miss — always one of their badges, not necessarily the one on screen. Three operational values (`error`, `record_failed`, `unspecified`) are tabled separately as having no badge counterpart. The burn arm's diagnostic aggregates are scoped to the conjunction's own eligible targets, so a latch on a DISABLED target cannot name the reason. | DONE | [`store/servicealertstate.go`](../internal/store/servicealertstate.go) (`serviceCoverageClausesSQL`, `liveVerdict`/`burnVerdict`), [`store/alertdelegation.go`](../internal/store/alertdelegation.go) (`coverageReasonRank`), [`metrics/metrics.go`](../internal/metrics/metrics.go); `TestDeliveryReportsTheSameReasonTheBadgeShows`, `TestSeveralCandidatesReportTheFurthestMiss` and its burn twin (the alphabetically first row carries the SHALLOWER miss, so a first-row-wins lookup fails), `TestADisabledTargetsLatchDoesNotNameTheReason` (rewritten to expire a lease for real — its first version wrote an unannounced FIRE and asserted the catch-all, codifying the defect D-0178 then fixed), `TestAWithheldBurnOnsetIsDiagnosedAsTheRouteAndThenAsPending`, `TestTheRankOrderIsTheOneTheSpecPublishes`; five mutations — collapsing the cause, `coverageReasonRank => 0`, widening the target eligibility, removing the two burn clause cases, and swapping a rank pair — each failing on its own safety message; invariants 92, 94, 95 and 96, D-0178, [iter-0161](iterations/iter-0161.md) §34, §36 and §37. |
| AC-0161-11 | Corrections of record, kept as corrections. Four claims of mine were wider than the code and are replaced rather than amended: D-0177 closed on "nobody is told an outage began after it ended" AFTER the owner narrowed the guarantee, and cited the mutations of the gate it deletes; "keep the highest `seq`" was called exact ordering when it is a current-state projection that loses what it discards; the runbook's diagnostic excluded an opening that genuinely never arrived; and `TestEveryLifecycleEventCarriesAUniqueMonotonicSequence` claimed every path while exercising only the two HUMAN doors. The product code was right in the last case — the evidence was not, which is how the other half of the writers ships without a sequence one day. | DONE | [`docs/decisions.md`](decisions.md) (D-0177 rewritten, the two receiver strategies stated separately), [`docs/runbook.md`](runbook.md) (both diagnostic candidates; the ordering prose moved OUT of the metric table it was splitting), [`domain/webhook.go`](../internal/domain/webhook.go); `TestServiceAutoIncidentPathsCarryTheSameSequence` covering `OpenServiceIncidentTx` and `ResolveServiceIncidentTx`, both mutations failing with their own safety sentences; invariant 93, [iter-0161](iterations/iter-0161.md) §35. |
| AC-0161-12 | Coverage means somebody was TOLD. D-0176 closed the case of no route AT EVALUATION; the window between the ENQUEUE and the DELIVERY produced the same swallow one step later — channel deleted before the worker ran, zero recipients resolved, terminal success, latch still firing so no further edge, and a restored route re-armed coverage for a page nobody received. `delivered_seq` on both latch tables is credited by the outbox worker only when a delivery resolved ≥1 recipient; arming requires `delivered_seq >= emitted_seq`; the credit is monotonic, sequence-guarded and refused for a CLOSE by the worker AND the store. Re-edging on zero resolved was NOT taken — it needs an episode-identity decision — and is recorded as available; not arming is the safe half and silences nobody. | DONE | Migration [`00089_service_alert_delivered.sql`](../internal/store/migrations/00089_service_alert_delivered.sql) (existing rows seeded `delivered_seq = emitted_seq`, a named legacy exception: a truthful zero would page an entire installation in one minute), [`store/alertdelegation.go`](../internal/store/alertdelegation.go) (`MarkServiceAlertDelivered`, both gates), [`outbox/outbox.go`](../internal/outbox/outbox.go); `TestCoverageNeedsAnAnnouncementSomebodyReceived` + burn twin, `TestMarkServiceAlertDeliveredIsGuardedAndIdempotent`, `TestAnAnnouncementNobodyReceivedIsNotCreditedAsDelivered`, `TestACloseIsNotCreditedAsCoverage`, `TestAnUnrecordedDeliveryFailsOpenRatherThanRetrying`; both gate mutations fail with their own safety message; `-race` green in BOTH storage modes. Two dead queries (`activeLiveDelegationSQL`, `activeBurnDelegationSQL`) that had stopped running at AC-0161-10 were deleted along with the comments pointing at them. Invariants 97/98, D-0179, [iter-0161](iterations/iter-0161.md) §40. |
| AC-0161-13 | A status page, its feed and its subscriber mail agree about which projects the page reports. Three surfaces decided it three ways: the render never seeded the page's OWN project (a manual-only project page showed none of its project's incidents), the feed walked `components → monitors` (a Service-backed component brought nothing), and the subscriber fan-out was that same JOIN with neither half of the axis — so a Service-only page rendered an incident and emailed nobody, while its webhooks fired. ONE owner now: `page.project_id` when project-scoped UNION every non-NULL `components.source_project`, NOT filtered by `source` because a dormant binding still renders. The subscriber query is its exact inverse. | DONE | [`store/statuspageprojects.go`](../internal/store/statuspageprojects.go), [`store/subscribers.go`](../internal/store/subscribers.go), [`api/handlers_feed.go`](../internal/api/handlers_feed.go), [`api/handlers_statuspage.go`](../internal/api/handlers_statuspage.go); `TestAPageProjectSetIsOneAxisInBothDirections` (both lost shapes, neither masked by a monitor component of the same project), `TestADormantBindingKeepsThePageReportingItsProject`; both mutations fail with their own message. D-0021's canonized `subscribers→components→monitors` sentence is marked superseded rather than quietly edited. Invariant 99, D-0180. |
| AC-0161-14 | `repeat_last` on a service is inert, and all three places now say so. The non-goal (no renotify knob for services) STANDS; what was wrong was FR-023 D8's justification — "the policy's own steps (including its repeat) are the repeat mechanism" — which is false, because the repeat branch needs `renotifySeconds > 0` and the service arm passes zero. Nothing was broken; the spec told a reader a mechanism existed that does not, and the form offered a toggle that is inert on a service-attached policy without saying so. | DONE | [`docs/specs/func-service-escalation.md`](specs/func-service-escalation.md) D8 carries a forward-only correction naming the false sentence, [`EscalationView.vue`](../frontend/src/views/EscalationView.vue) states it beside the control, [`store/escalation.go`](../internal/store/escalation.go) unchanged in behaviour; `TestAServiceLadderDoesNotRepeatItsLastStep`, whose FIRST version set the LIVE policy while an open incident climbs the FROZEN ladder (D-0175) and therefore proved nothing — the mutation caught it, and it now asserts the frozen value before proceeding. Invariant 100, D-0181. |
| AC-0161-15 | A search hit brings its tenant, and a detail view follows the URL. `SearchBox` switched the workspace for PROJECT hits only, so a monitor or incident hit from another workspace landed on a page whose reads and authz still ran against the previous tenant — a stranger's services in the successor and convert pickers, and `canWrite` comparing the workspace's org against the loaded subject's project, which hid acknowledge, resolve and postmortem from a legitimate editor. Compounding it, both detail views read `route.params.id` ONCE and loaded on mount while Vue Router reuses the component, so an A→B navigation of the same kind changed the URL and nothing else — with every mutation on the page still pointing at A. | DONE | [`SearchBox.vue`](../frontend/src/components/SearchBox.vue) (every hit type switches), [`App.vue`](../frontend/src/App.vue) (`RouterView` keyed by PATH, not `fullPath`, so `?tab=` state survives), [`MonitorDetailView.vue`](../frontend/src/views/MonitorDetailView.vue) and [`IncidentDetailView.vue`](../frontend/src/views/IncidentDetailView.vue) (reactive id, `watch(() => [id, ws.projectId], load)` as ServiceDetail already did, and a load ticket so a slow response for the previous subject cannot overwrite the current one); `SearchBox.spec.ts` (3) and `DetailViewIdentity.spec.ts` (3, including the stale-response race); three mutations, each failing its own case; 148 frontend tests in 19 files. Invariant 101. |
| AC-0161-16 | The rows the lifecycle fixes could not reach are repaired or reported, never guessed. Earlier commits stopped NEW damage; nothing touched what the old versions had already written, and a resurrected incident is not cosmetic — it occupies the one-open-incident index, so the NEXT outage cannot open one. FIVE classes, split 3 REPAIRED / 2 REPORT-ONLY. Repaired where the value is derivable from the row: resurrected incidents (`resolved_at` set, status walked back) and stranded service incidents (health episode closed, service not firing), each with a `🔧 Repaired:` timeline note guarded for reruns; and `resolved` rows with NO `resolved_at`, stamped from `updated_at` and deliberately given NO note — nothing about that row's history is wrong, and a note would tell a reader otherwise. REPORTED, not repaired: member snapshots that MIGHT name a not-yet-governing revision (the snapshot stores no revision id, and deriving one from `started_at` is the transaction-clock guess the review warned against — the migration prints an upper BOUND and says so) and anchorless auto-incidents. Both carry a runbook query and procedure. | DONE | Migration [`00090_incident_lifecycle_repair.sql`](../internal/store/migrations/00090_incident_lifecycle_repair.sql) with `CHECK ((status = 'resolved') = (resolved_at IS NOT NULL))` added AFTER the repair, [`docs/runbook.md`](runbook.md) for the two reported classes; `TestTheRepairMigrationFixesWhatItCanAndLeavesWhatItCannot` runs the REAL migration over seeded legacy shapes and carries two negative controls — a STILL FIRING service and a HUMAN's incident — because a repair that closes a live outage is worse than the defect. Both mutations fail. `-race` green in BOTH storage modes. Invariant 102, D-0182. |
| AC-0161-17 | The review of the previous six commits found three P1s, all the same failure: a claim at a level the code did not reach. (a) LIVE coverage armed on an announcement about a DIFFERENT state — `emitted_state IS NOT NULL` rather than `= observed_state`, so a delivered DEGRADED covered a service now observing DOWN and still collecting its confirm streak; the canonical §16.1 ALREADY required the stronger form, so the code was behind its own spec. `emitted_seq = 0` also counted as delivered. (b) D-0180's "one owner" was three implementations that happened to agree: the render rebuilt the set inline and the inverse re-typed the rule, and deleting the own-project arm from the render left every API test green. (c) The 00090 test proved half its contract — no suspect snapshot, no anchorless row, no warnings read, and a rerun its comment claimed but did not run; `SELECT 0` on both reporting queries left it green. | DONE | [`store/alertdelegation.go`](../internal/store/alertdelegation.go) (one predicate: firing ∧ emitted = observed ∧ seq > 0 ∧ delivered ≥ emitted), [`api/handlers_statuspage.go`](../internal/api/handlers_statuspage.go) (asks the owner), [`store/statuspageprojects.go`](../internal/store/statuspageprojects.go) (`statusPageReportsProjectSQL` shared with the inverse), [`store/incidentrepair_internal_test.go`](../internal/store/incidentrepair_internal_test.go) (both reported classes seeded, notices captured through `pgx.OnNotice`, 90→89→90 rerun); `TestAnAnnouncementOfAnEarlierStateIsNotCoverage`, `TestAStateWithNoSequenceIsNotAnAnnouncement`, `TestPublicRenderReportsThePagesOwnProject`, `TestAPartialDeliveryStillCountsAsAnAnnouncement`. Four mutations including the reviewer's own `SELECT 0`. Invariants 92–103 moved into §16.8 (they had existed only in the discharge map) and `check-docs-references` now DERIVES the count from the spec — which immediately caught invariant 98's row naming no test. |
| AC-0161-18 | The fix for the swallow contained the swallow. `ChannelDelivery.Resolved` counts channel ROWS that exist and are enabled — set right after `EnabledChannelsByIDs`, before any send — and D-0179's credit read it as "somebody was told". A service whose only channel returned 500 resolved one, delivered none, was credited, and its members went silent for an announcement nobody received; permanently, once the outbox dead-lettered the retry. The partial-delivery test could not see it because the fake had ONE field for both numbers. Separately, §44's clock fix passed the CALLER's `asOf`, which is post-ITS-locks and not post-the-incident-lock, and left `enqueueOutboxTx` in place so the outbox row stayed at transaction start despite invariant 103 naming it. | DONE | [`domain/servicealert.go`](../internal/domain/servicealert.go) (`Requested`/`Resolved`/`Delivered` — three facts, and collapsing any two loses the one that matters), [`notify/notify.go`](../internal/notify/notify.go) (counts successful sends), [`outbox/outbox.go`](../internal/outbox/outbox.go) (credits `Delivered`, still independently of `err` so a real partial credits AND retries), [`store/serviceincidents.go`](../internal/store/serviceincidents.go) (takes the incident `FOR UPDATE` itself, reads `statement_timestamp()` after that wait, `enqueueOutboxAtTx`; the `asOf` parameter REMOVED rather than ignored); `TestATotalSendFailureIsNotAnAnnouncement`, `TestAServiceIncidentResolvesAfterTheWaitItActuallyDid` (a real lock wait polled through `pg_blocking_pids` for the exact waiter/holder backend pair — not a timer, and not an unscoped cluster-wide lock probe — asserting all six timestamps). Five mutations fail, including both of the review's docs mutations against the now SET-comparing `check-docs-references`. Invariants 97 and 103 rewritten. |
| AC-0161-19 | The P0 fix was pinned at the fake and absent at the notifier. Nothing in `internal/notify` had ever called `DeliverChannelsReporting` — the only place `ChannelDelivery.Delivered` is computed — so the outbox tests drove a double whose field a test set by hand, and deleting the production increment left every suite green while the real dispatcher reported zero for each successful send. Three more from the same review: the canonical invariant 103 still described the CALLER's instant after the code had stopped using it (spec wins, so the range formally did not implement the contract); the SET gate folded a duplicate `103.` away before it could be noticed; and the lock-wait proof accepted ANY ungranted tuple lock in the cluster, so a stranger's waiter would have handed it a floor it never earned. | DONE | [`notify/notify_test.go`](../internal/notify/notify_test.go) (`TestChannelDeliveryReportsAskedExistedAndSucceeded` over `httptest` — success, the P0's own 500-with-a-live-channel, a real partial, and §16.4a's deleted recipient — plus `TestChannelDeliveryCountsEmailSendsThatSucceeded` for the SMTP branch's own increment), [`docs/specs/func-service-reliability.md`](specs/func-service-reliability.md) invariant 103 rewritten, [`scripts/check-docs-references.py`](../scripts/check-docs-references.py) (collects a LIST and refuses duplicate numbers by name before the set comparison), [`store/serviceincidents_internal_test.go`](../internal/store/serviceincidents_internal_test.go) (`pg_blocking_pids` between the two captured backends). Four mutations fail. |
| AC-0161-20 | The review returned D-0185…D-0187 with six P1s and no P0. `renotify_seconds` could not be SAVED — `alertPolicyDiff` is the audit line and the no-op gate both, the field was missing from it, and a renotify-only PATCH returned 200 and committed nothing; every test wrote the column with direct SQL, so the feature was pinned everywhere except the write path. A file-managed service could not declare it at all, and those services refuse UI paging edits by design. The delivery budget had three gaps of its own shape: the RECORDING writes sat on the bounded context one layer below the settle, the subscriber confirmation still dialled on `context.Background()`, and the budget subtracted a database timestamp from a worker one. A claim taken and never attempted burned its retry. The BURN arm had no re-announce branch at all. And "not covered: an event that exhausts its retries" did not survive invariant 105's own "no retry owed". | DONE | [`store/servicealertpolicy.go`](../internal/store/servicealertpolicy.go), [`fileprovider/service.go`](../internal/fileprovider/service.go) + [`store/fileapply_alerting.go`](../internal/store/fileapply_alerting.go), [`outbox/outbox.go`](../internal/outbox/outbox.go) (`deliver(ctx, settleCtx, …)`, `ReleaseOutboxClaim`, `condemnDead`), [`mailer/mailer.go`](../internal/mailer/mailer.go) (`SendContext`), [`store/outbox.go`](../internal/store/outbox.go) (the claim returns the DB clock), [`store/serviceburneval.go`](../internal/store/serviceburneval.go); `TestEveryPolicyFieldSurvivesTheWritePath`, `TestServiceAlertingDeclaresTheRenotifyCadence`, `TestTheBudgetBoundsSendingAndNeverRecording`, `TestAnUnattemptedClaimIsGivenBack`, `TestTheLeaseIsSpentAsASpanNotAcrossTwoClocks`, `TestAnUndeliveredBurnFireIsAnnouncedAgainOnceThereIsSomebodyToTell`, `TestAnAnnouncementThatDeadLettersIsCondemned`. Eight mutations fail. Two more fakes had to stop ignoring their context first. Invariants 100, 104 and 105 rewritten; §16.6a gained the field. |
| AC-0161-21 | Three more P1s, each sitting immediately behind a fix from the round before. A PARTIAL delivery was condemned: the credit lands on the first attempt, the event can still fail, and when the retries ran out `condemnDead` fired unconditionally — recording an announcement three people received as heard by nobody, so the evaluator would page all three again. The lease was counted from AFTER `ClaimDueOutbox` returned, though it starts at the database's `now()` inside that statement, so the claim's own duration was handed back as budget the worker does not have. And the burn re-announcement never passed `supersedeReason`, so the shared emission closed its episode as `policy_changed` — a cause that never happened, on the arm written expressly to avoid that. | DONE | [`store/alertdelegation.go`](../internal/store/alertdelegation.go) (`delivered_seq` guard in the condemnation SQL, both arms), [`outbox/outbox.go`](../internal/outbox/outbox.go) (batch clock before the claim), [`store/serviceburneval.go`](../internal/store/serviceburneval.go); `TestADeliveredAnnouncementCannotBeCondemned`, `TestTheClaimsOwnDurationIsChargedToTheLease` (rewritten with a lease longer than `leaseHeadroom`, because the first version's headroom refused the delivery on its own and the mutation went green), and the burn re-announce test extended to the episode identity and close reason. Three mutations fail. Also two stale comments: "four columns" after the fifth arrived, and a note saying `applyServiceAlertingTx` was not yet wired when it had been for several iterations. |
| DoD-0161 | Every correction is applied, gated and recorded. | DONE | The guard, the documentation and the two UI fixes are in `v0.1.5-beta.2`. The review arc (AC-0161-4 … AC-0161-11) is discharged finding by finding, each with mutation evidence and `-race` over all 30 packages, and in BOTH storage modes wherever a migration changed. The independent review ran to its end on 2026-08-27: **PRODUCT IMPLEMENTATION APPROVED at `35de54d`**, with the residual documentation P2 closed under IMPLEMENTER authority at `aec8bf9` — the reviewer explicitly declined to create a new approval-claim for that commit, so the approval names `35de54d` and not HEAD. An earlier version of this row stopped at `4c4bae3` and described the arc as rejected; it was, at that point, and the row was not updated as the arc continued, which is the same staleness this iteration spent itself correcting elsewhere. The E2E gap is closed: `make dev-test` on a stack built from this branch runs **48 passed / 1 skipped** (`file-providers.spec.ts`, the documented idle-provider case), and `dev-up` applied 00089 and 00090 to the dev database — months of real rows — where the repair was correctly a no-op. All five items once recorded as available-but-not-taken are closed: PostgreSQL 14 declined (D-0183), the dormant-binding axis decided as it stands (D-0184), the service renotify cadence built (D-0185), the bounded delivery budget built (D-0186), and the re-announcement of an undelivered onset built (D-0187) — its episode-identity question answered by superseding the episode nobody heard rather than reusing it. **Corrected scope:** an earlier version of this row made the DoD depend on whether one operator upgrades their PostgreSQL — an infrastructure decision that is not cerbix's to hold open. The product question underneath it, whether cerbix should support PostgreSQL 14 at all, is now DECIDED: **no** (D-0183, owner, 2026-08-27). 15 is the floor. The compatibility path would mean emulating column-list `ON DELETE SET NULL` with triggers across five migrations and maintaining them indefinitely, in the code path whose correctness the composite FKs exist to guarantee. No code changes — the version guard and the runbook already implement it — and no document now describes it as pending. |

## Previous iteration (iter-0160 — public positioning reviewed against the code: the product is a service reliability platform, "control plane" bounded rather than banned (D-0174). See [iter-0160](iterations/iter-0160.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0160-1 | The positioning proposal was REVIEWED against the code before any text changed, and one part of it was refused: "Reliability Control Plane" is not the primary noun, because unbounded it promises actuation on the user's infrastructure that cerbix does not perform and implies it sits in the request path, which it does not. The primary noun is "service reliability platform"; control plane survives as a bounded concept in the README; "system of record for service reliability" is the SRE-facing phrase. | DONE | **D-0174**; [README.md](../README.md), [`project-description.md`](project-description.md) ([iter-0160](iterations/iter-0160.md) §1–§2). |
| AC-0160-2 | The README states the NON-GOALS publicly, quoted from the specification rather than softened: no arbitrary time-series queries, no generic telemetry, no downsampling, no query language, no metrics backend, no service catalog — plus no trace/log ingestion and no automatic root-cause analysis, because the product gives correlation CANDIDATES and a heuristic note over a graph the operator declared. | DONE | [README.md](../README.md) "What cerbix is not" and "Where it fits" ([iter-0160](iterations/iter-0160.md) §3). |
| AC-0160-3 | A stale API description is corrected, not merely a marketing line: `ServiceDetail.reliability` said SLO, error budget and burn rate "are phase 2" when phase 2 shipped in iter-0144 and the numbers live on `…/services/{id}/reliability` with their honesty context. TS types regenerated; frontend type-check clean. | DONE | [`openapi.yaml`](../openapi.yaml) ([iter-0160](iterations/iter-0160.md) §4). |
| AC-0160-4 | `v0.1.5-beta.1` is cut with a CHANGELOG that matches reality: a real release section derived from the 242 commits since `v0.1.0-beta.5`, the stale `[Unreleased]` block relabelled as the whole beta train rather than rewritten, and the two foreign version numbers (`v0.35.0`, `v0.10.0`) kept with a note that they match no tag. The one intentional public-output break is named in the notes, not buried. | DONE | [CHANGELOG.md](../CHANGELOG.md) ([iter-0160](iterations/iter-0160.md) §6). |
| AC-0160-5 | `docs-check` was BLIND to markdown link targets — it matched only backticked paths, which is why `CHANGELOG.md` cited a `backend/internal/...` path for weeks. The gate now covers link targets and the CHANGELOG itself, verified by reintroducing that exact path and watching it fail at `CHANGELOG.md:120`. | DONE | [`scripts/check-docs-references.py`](../scripts/check-docs-references.py) ([iter-0160](iterations/iter-0160.md) §6). |
| DoD-0160 | Positioning matches the code, the boundary is public, and the legacy framing is either rewritten or explicitly kept as history. | DONE | §5 of [iter-0160](iterations/iter-0160.md). |

## Previous iteration (iter-0159 — the dependency backlog: seven dependabot branches merged in two classes, none of them stale. See [iter-0159](iterations/iter-0159.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0159-1 | Three low-risk bumps merged and verified by the gates that can see them: `x/crypto` 0.55.0 / `x/net` 0.58.0 / `x/text` 0.41.0 (33 packages with `-race`), the build image's golang 1.26.6 (the IMAGE built, brought up, browser suite against it), pinia 4.0.3 (frontend gates + snapshot + image + browser suite). | DONE | [iter-0159](iterations/iter-0159.md) §1. |
| AC-0159-2 | Four MAJOR frontend bumps merged one at a time, each with `npm ci` from the resolved lock, `vue-tsc`, 136 unit tests and a build — `@vitejs/plugin-vue` 6.0.8, `npm-run-all2` 9.0.3 (which is the build gate itself), `unplugin-auto-import` 21.1.0 (0.x → 21, running inside `vite.config.ts`), `@vueuse/core` 14.4.0. The lock CONFLICTED and was regenerated from `package.json` rather than hand-merged, then read back to confirm the pins — a hand-merged lock describes a tree npm never resolved. | DONE | [iter-0159](iterations/iter-0159.md) §1–§2; browser suite 48 passed / 1 known skip. |
| AC-0159-3 | Two findings recorded rather than smoothed over: NOTHING in this SPA uses `@vueuse/core` — removing it from the auto-import preset leaves type-check and build green, so bump 7 is safe by accident, and whether the dependency should exist is a question this sweep does not answer. And `make spa-snapshot` produced a ZERO diff after all four majors: the emitted bundle is byte-identical, checked rather than reasoned about. | DONE | [iter-0159](iterations/iter-0159.md) §3–§4. |
| DoD-0159 | The dependency backlog is empty of anything worth merging, and what remains unmerged is named with the reason it must stay that way. | DONE | No unmerged remote branch remains. `deps/tailwind-v4` and `deps/ts58-vue-tsconfig-openapi-fetch` carry OLDER versions than `main` and would roll dependencies backwards; `origin/feat/service-reliability` is empty. All three are deletion candidates and deletion is the owner's call ([iter-0159](iterations/iter-0159.md) §6). |

## Previous iteration (iter-0158 — CLOSED: corrections of record after the merge — the flake iter-0157 could not name, and two harness self-inflictions. Merged `main` green with `-race` over 33 packages. See [iter-0158](iterations/iter-0158.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0158-1 | The flake `iter-0157` §4 could not name is identified and fixed at its cause: `TestFenceRefusesAnExhaustedCallerTail` (a phase-1 test, predating the merged branch) asserted a BUDGET refusal through a 5ms wall-clock deadline — 0 failures in 15 idle runs, 2 in 12 with the CPUs busy. The budget half is now deterministic (a one-nanosecond fence budget, generous context, no statement cut); the caller-tail half keeps the 5ms deadline but asserts what it can promise — refused WITHOUT DAMAGE, either error being honest. | DONE | [`backlog0133_internal_test.go`](../internal/store/backlog0133_internal_test.go); 20 `-race` runs under the same CPU load, clean; one mutant fatal; `internal/store` `-race` green ([iter-0158](iterations/iter-0158.md) §1). |
| AC-0158-2 | Two harness self-inflictions recorded as a pair, because they share one cause — a command matching its own pattern: `pkill -f` killed my shell (second time in this arc), and an `until pgrep -f` loop waited on itself for a full five-minute timeout. The rule: never match a process by a pattern the matching command contains — kill by PID, or bound the load with `timeout` and let it expire. | DONE | [iter-0158](iterations/iter-0158.md) §2. |
| DoD-0158 | Corrections of record are discharged; `main` is green with `-race` over every package. | DONE | §3 of [iter-0158](iterations/iter-0158.md); the closing gate is done: 33 packages with `-race` on merged `main`, on a database that applied all 84 migrations from zero, exit 0. |

## Previous iteration (iter-0157 — FR-023 escalation for services: CLOSED (D-0173). The ladder, its fail-closed predicates, the payload, the write path the column never had, observability and the SPA control — against an enforced map of 16 invariants + 19 scenarios. See [iter-0157](iterations/iter-0157.md))

| ID | Acceptance criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-0157-1 | A service with an escalation policy escalates its OWN auto-opened incident: steps fire from the incident's start (with one recorded exception for ladders carried across migration 00085, which start at the upgrade — D-0175, [iter-0161](iterations/iter-0161.md) §17), progress latches on the incident so a step fires once, acknowledgement or resolution ends it, and the step names the SERVICE. The candidates come from a SECOND query — the monitor one is unchanged character for character, which is how NFR-018 is kept structurally rather than by inspection. | DONE | [`store/escalation.go`](../internal/store/escalation.go); `TestAServiceLadderFiresNamesTheServiceAndStopsOnAck`, `TestAServiceLadderIgnoresAHumansIncidentAndAPolicylessService` (a human's incident, a policyless service, and a service DELETED mid-outage) ([iter-0157](iterations/iter-0157.md) §2.1). |
| AC-0157-2 | The ladder FAILS CLOSED on anything it cannot confirm — no state row, an expired lease, ownership withdrawn, or a verdict that says the service recovered. The rule lives in the JOIN, not in a branch, and the asymmetry with delegation's fail-open is deliberate: ambiguity at delivery time means a page EXISTS, ambiguity here would mean a page MULTIPLIES. | DONE | `TestAServiceLadderFailsClosedOnAnythingItCannotConfirm` — four sub-cases, and the first asserts the fixture CAN fire so the rest cannot pass vacuously; four mutants, each killed by the sub-case that owns it, including the "generalized" LEFT JOIN with `COALESCE` defaults that a careless refactor would produce. |
| AC-0157-3 | The service GRAPH does not pause the ladder (FR-023 invariant 7), against the obvious symmetry with the monitor dependency pause — §14 annotates and links, never suppresses. | DONE | `TestTheServiceGraphDoesNotPauseAServiceLadder` builds the exact configuration that pauses a MONITOR and asserts the service ladder fires; the mutant that adds the pause fails it. |
| AC-0157-4 | The step payload names the SERVICE and cannot strand a pre-FR-023 worker: `service_id` and `subject_name` are new, `monitor_name` stays populated with the SUBJECT's name as a documented legacy render field. Fencing the topic was the first answer and was WRONG — pre-fence topics stay legacy because every deployed owner already dispatches them, so fencing this one would stop those workers claiming escalation steps during a rolling upgrade. | DONE | [`domain/outbox.go`](../internal/domain/outbox.go); the test renders the payload through the LEGACY field alone, because the claim is about a binary that cannot be run here ([iter-0157](iterations/iter-0157.md) §2.2). |
| AC-0157-5 | A service's own step skips delivery-time delegation entirely, and attempts no monitor lookup: that step IS the page delegation exists to redirect, and muting it would leave the outage with nobody told. | DONE | [`outbox/outbox.go`](../internal/outbox/outbox.go); `TestAServiceEscalationStepSkipsDelegationEntirely` asserts the monitor case is still suppressed AND that no lookup happened for the service one — the fake now records its lookups. |
| AC-0157-6 | The three statements FR-023 falsified moved in the same change: §16.9's bullet carries a SUPERSEDED note and keeps the bullet, the comment at the refusal site says what is true now, and `overview.md` gained the ladder with both of its rules. | DONE | [`func-service-reliability.md`](specs/func-service-reliability.md) §16.9, [`store/servicealerteval.go`](../internal/store/servicealerteval.go), [`overview.md`](overview.md) ([iter-0157](iterations/iter-0157.md) §2.4). |
| AC-0157-7 | FR-023 has a DISCHARGE MAP: fourteen invariants and seventeen scenarios, each naming a test that exists, enforced by `make docs-check` (now FR-021 91+24, FR-022 16+16, FR-023 14+17). Building it found three defects in my own work — the matrix has SEVENTEEN lines and I had written sixteen from memory; two matrix lines had no test (fail-closed must RESUME when the verdict refreshes, and a read error must write no progress); and one row cited `TestEscalationStepDelivery`, a name that never existed (I invented it from memory), which the gate refused. | DONE | [traceability.md](traceability.md); new cases in `TestAServiceLadderFailsClosedOnAnythingItCannotConfirm` (resume, and a direct-SQL fault injection that renames `service_alert_state` away for one pass) ([iter-0157](iterations/iter-0157.md) §2.5). |
| AC-0157-8 | What the ladder DID is countable, split by the subject that paged: `cerbix_escalation_steps_total{subject}`. Before this the ladder had no metric at all — only a log line — so "how much did the on-call ladder fire this week" was not an answerable question. Attribution takes the service id rather than a boolean, because the anchors are exclusive at the schema and a second flag would be a second thing to keep in sync. | DONE | [`store/escalation.go`](../internal/store/escalation.go), [`metrics/metrics.go`](../internal/metrics/metrics.go), [`scheduler/scheduler.go`](../internal/scheduler/scheduler.go); `TestLeaderPublishesServiceAlertTelemetry` extended with unequal counts (2 monitor, 5 service); two mutants — the publish dropped, and a service step counted as a monitor one ([iter-0157](iterations/iter-0157.md) §2.6). |
| AC-0157-9 | An operator can attach or clear the escalation policy of an EXISTING service, and the change is audited with what moved, inside the mutating transaction. Found while sizing the SPA delta: `services` has carried the column since phase 5 but has NO update endpoint at all, so the field was reachable only at create time or through a file provider — harmless while the column was inert, and a hole the moment FR-023 made it decide who is woken. `PUT …/services/{id}/escalation-policy` is its own route (own transaction, own audit action `service.escalation_policy`), a no-op writes nothing at all, and a foreign policy is a 400 `owner_not_in_project` rather than an FK violation an API could only render as a 500. | DONE | [`store/servicealertpolicy.go`](../internal/store/servicealertpolicy.go), [`api/handlers_servicealerting.go`](../internal/api/handlers_servicealerting.go), [`openapi.yaml`](../openapi.yaml); `TestSetServiceEscalationPolicy` (five sub-cases), `TestSetServiceEscalationPolicyIsAuditedAndRefusesCarefully`; four mutants, all fatal ([iter-0157](iterations/iter-0157.md) §2.7). |
| AC-0157-10 | The escalation-policy write path is proven on a LIVE stack, audit trail included: attach, the service detail agreeing, `service.escalation_policy` present in the org audit with the policy id in its target, then clear with an empty string. Unit tests prove a row is inserted; only a live run proves the action name reaches the surface an operator reads. | DONE | [`e2e/tests/services.spec.ts`](../e2e/tests/services.spec.ts); image rebuilt from this branch, browser suite 48 passed / 1 known skip ([iter-0157](iterations/iter-0157.md) §2.9). |
| AC-0157-11 | The SPA lets an operator attach or CLEAR a service's escalation policy, built to the approved mock: the picker in the paging panel, `— none —` clearing it, its own save button and its own error line (two writes, two audit actions). The control sits OUTSIDE the declaration branch — a declaration that could not be read must not hide a field on a different route, which is what the unit tests caught. It trusts the endpoint's ECHO rather than its own draft, and a project with no policies says so instead of offering a picker that implies one. | DONE | [`ServiceAlerting.vue`](../frontend/src/components/ServiceAlerting.vue), [`ServiceDetailView.vue`](../frontend/src/views/ServiceDetailView.vue); `ServiceAlerting.spec.ts` +6 (136 unit tests green), e2e through the CONTROL — select, save, poll, clear, poll; browser suite 48 passed / 1 known skip; `make spa-snapshot` committed with the change ([iter-0157](iterations/iter-0157.md) §2.10). |
| AC-0157-12 | A pre-existing FLAKE found by the full-suite run is fixed at its root: `leadUntilMetrics` predicates that waited for fewer facts than their test asserted. One waited for the health arm's error and asserted a BURN series, so it could return before the burn arm had run at all; the neighbour waited for three series and asserted sixteen. The wait is now built from the same list the assertions use, so the two cannot drift. Proven not to be FR-023's doing: 20 runs with the new metric calls removed behave identically, and those calls never execute in that test. | DONE | [`scheduler_test.go`](../internal/scheduler/scheduler_test.go); 60 `-race` runs of both tests clean, full 33-package suite green on a dedicated database ([iter-0157](iterations/iter-0157.md) §2.11). |
| DoD-0157 | FR-023 closes when its fourteen invariants and SEVENTEEN scenarios all have a row naming a test that exists, enforced by `make docs-check` as FR-021's and FR-022's are — plus observability for what a service ladder actually did. | DONE | Tasks 5 and 6 of [iter-0157](iterations/iter-0157.md) §0. |

## Previous iteration (iter-0156 — FR-022 service incidents: CLOSED (D-0171). Schema, store, evaluator, correlation, public projection, the member snapshot, observability and the SPA deltas all landed, against an enforced map of 16 invariants + 16 scenarios. See [iter-0156](iterations/iter-0156.md))

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0156-1 | An incident's subject is unambiguous at the schema and stays true after the subject is gone: at most ONE anchor (`monitor_id` / `service_id`) by CHECK — *at most*, so a manual project-level incident with neither keeps working — a COMPOSITE tenant-safe FK whose PG15 column-list `ON DELETE SET NULL (service_id)` clears the anchor and not the tenant key, one OPEN auto-incident per service by partial unique index, and a member snapshot a postmortem can still name after a member is deleted. | DONE | [migration 00084](../internal/store/migrations/00084_service_incidents.sql), [`store/serviceincidents.go`](../internal/store/serviceincidents.go); `TestAnIncidentHasAtMostOneAnchor`, `TestDeletingAServiceClearsTheAnchorAndKeepsTheIncident`, `TestOpeningAServiceIncidentIsIdempotentAndSnapshotsItsMembers`; five mutants, four behavioural and one named as weaker ([iter-0156](iterations/iter-0156.md) §3). |
| AC-0156-2 | A service alert opens its own incident and resolves it, and touches nobody else's. The open rides the SAME transaction as the announcement (so neither can exist without the other), only the LIVE evaluator can reach it, the close RESOLVES the incident it opened, a second failure after a recovery opens a SECOND incident, and `source = 'auto'` in the resolve's WHERE is what keeps a machine from overwriting a person's conclusion. FR-021 invariant 86 is SUPERSEDED — note, discharge row, §16.10 scenario 24 and the rewritten test all moved in the same change. | DONE | [`store/servicealerteval.go`](../internal/store/servicealerteval.go), [`store/serviceincidents.go`](../internal/store/serviceincidents.go); `TestAServiceAlertOpensAndResolvesOnlyItsOwnIncident` (the phase-5 test rewritten, not deleted), `TestTheMachineLeavesAHumanIncidentAlone` (written after a mutation showed that property rested on nothing); four mutants, each fatal with its own message ([iter-0156](iterations/iter-0156.md) §2.3–§2.4). |
| AC-0156-3 | A service incident is correlated through the service graph like any other anchor, and its links name every service but its own (FR-022 invariant 9). Both anchor kinds WITNESS, because counting only monitors would make the relation's content depend on opening order — the asymmetry §14.3's mirror exists to prevent. The self-link is held STRUCTURALLY by `walk` never returning its own start node, not by a guard: no public path can produce one, so the test that proves the line load-bearing plants a cycle by direct SQL. | DONE | [`store/serviceimpact.go`](../internal/store/serviceimpact.go), [`store/serviceincidents.go`](../internal/store/serviceincidents.go), [`store/incidents.go`](../internal/store/incidents.go); `TestAServiceIncidentIsCorrelatedAndNamesEveryServiceButItsOwn`, `TestAServiceIncidentWitnessesJustLikeAMonitorOne`, `TestNoSelfLinkEvenWhenTheStoredGraphHasACycle`; three mutants, each fatal with its own message; the pre-FR-022 correlation suite unchanged and green is the NFR-017 regression ([iter-0156](iterations/iter-0156.md) §2.5). |
| AC-0156-4 | The public projection carries a service incident, and neither its anchor nor any impact link (FR-022 invariant 11). Found a leak already on the branch: `PublicRedacted` cleared `project_id`/`monitor_id`/external key/ack actor and NOT the `service_id` added in slice 1, so every unauthenticated render of a page with a service incident shipped the service's internal UUID. The authenticated render still carries the anchor, which keeps the redaction targeted. The 🕸 timeline note stays public for BOTH anchors, per §14.5's approved scope — pinned so "no impact links" is not misread as silencing it. | DONE | [`domain/incident.go`](../internal/domain/incident.go); `TestPublicRenderCarriesAServiceIncidentAndNoImpactLinks` — a raw unauthenticated JSON regression; the mutant that drops the new line prints the whole leaked body ([iter-0156](iterations/iter-0156.md) §2.6). |
| AC-0156-5 | The incident detail names the members the service had AT OPEN TIME (FR-022 invariant 13). Found by reading the invariants against the code: the snapshot was written by slice 1 and read by NOTHING, so the fact was recorded and invisible. Three answers stay distinguishable in the JSON — absent (no snapshot), `[]` (a service that genuinely had no members), `members_unavailable` (a degraded read, never served as "no members"). `openapi.yaml` gained `service_id` on `Incident` (missing since slice 1), `members`/`members_unavailable` and an `IncidentMember` schema; TS types regenerated, frontend build green, `make spa-snapshot` byte-identical. | DONE | [`api/handlers_incidents.go`](../internal/api/handlers_incidents.go), [`openapi.yaml`](../openapi.yaml); `TestIncidentDetailNamesTheMembersTheServiceHadAtOpenTime` + the store's `TestOpeningAServiceIncidentIsIdempotentAndSnapshotsItsMembers` for the durable half; two behavioural mutants ([iter-0156](iterations/iter-0156.md) §2.7). |
| AC-0156-6 | FR-022 has a DISCHARGE MAP: all sixteen invariants have a row in [traceability.md](traceability.md) naming a test that exists, and `make docs-check` fails if any number is missing or cites a test the tree lacks — the same enforcement FR-021 closed against. Reading the invariants against the code found five unpinned properties and one FALSE invariant. | DONE | New tests: `TestADisarmedServiceOpensNoIncidentAndItsMembersKeepPaging` (5), `TestABurnBreachOpensNoIncidentEver` (6), `TestAnOpenServiceIncidentMovesNoComponentStatus` (10), `TestTheSuppressionNoteKeepsOneHomeWhenBothIncidentsAreOpen` + the extended `TestIncidentContextAttached` (12), `TestAServiceIncidentUsesTheSameOperatorSurfacesAsAMonitorOne` (14, corrected). Invariant 14 was FALSE as written — incident writes are audited for NEITHER anchor — so the spec now states the promise FR-022 can keep and names the audit gap as its own requirement. |
| AC-0156-7 | The counters the evaluator computes are READ: `cerbix_service_incidents_total{action}` publishes what a machine opened and resolved, counted apart from the alert edges because an onset whose incident is already open announces without opening one. They were computed since slice 2 and consumed by nothing — the same dead-fact shape as the member snapshot. | DONE | [`metrics/metrics.go`](../internal/metrics/metrics.go), [`scheduler/scheduler.go`](../internal/scheduler/scheduler.go); `TestLeaderPublishesServiceAlertTelemetry` extended with unequal incident/edge counts, so a wiring that published the edges twice cannot pass; mutant fatal ([iter-0156](iterations/iter-0156.md) §2.8). |
| AC-0156-8 | The SPA says what an incident is an incident OF, on the four surfaces the approved mock draws: a SUBJECT chip on the detail and in the list (one shared component, absent for a project-level incident — the absence is the discriminator), the service page LINKING its open incident rather than embedding a second copy of one timeline, and the public page unchanged (the incident, never the graph). Names resolve once per screen and every lookup is best-effort, so a deleted subject still renders its timeline. | DONE | [`IncidentSubjectChip.vue`](../frontend/src/components/IncidentSubjectChip.vue), [`IncidentsView.vue`](../frontend/src/views/IncidentsView.vue), [`IncidentDetailView.vue`](../frontend/src/views/IncidentDetailView.vue), [`ServiceDetailView.vue`](../frontend/src/views/ServiceDetailView.vue); `IncidentSubjectChip.spec.ts`, `ServiceDetailView.spec.ts` (new), `IncidentDetailView.spec.ts` (+4); three on-disk mutants; the browser suite 47 passed / 1 known skip, covering two of the chip's three states — the SERVICE state is opened only by the evaluator and is unreachable from a browser test ([iter-0156](iterations/iter-0156.md) §2.10). |
| DoD-0156 | FR-022 closes when the requirement does — schema, store, evaluator, correlation, public projection, SPA deltas — or when the remainder is handed back with what blocks it. FR-021 invariant 86 is superseded IN THE SAME CHANGE as the evaluator, not after it. | DONE | [iter-0156](iterations/iter-0156.md) §0 (state per task) and §4 (the supersession obligation). |

## Previous iteration (iter-0155 — the four commissioned candidates: instance audit, project-level SLO objective, `job_id` correlation, and the §16.9 design input. Opened 2026-08-18 after the backlog sweep; see [iter-0155](iterations/iter-0155.md))

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0155-1 | The instance's own audit history is readable by the only role it belongs to. A global admin's actions are stored with `org_id IS NULL` and, until this arc, appeared in no listing: `ListAuditByOrg` is org-scoped by construction. `ListGlobalAudit` is a SEPARATE read (`org_id IS NULL`) rather than the org listing with a widened filter — a filter that can be widened by a parameter is one authz slip away from showing a tenant admin the whole installation — and both listings now share one read shape so they cannot drift. `GET /api/v1/admin/audit`, global admin only, limit clamped server-side. | DONE | [`store/audit.go`](../internal/store/audit.go), [`api/handlers_audit.go`](../internal/api/handlers_audit.go), `openapi.yaml` 0.21.0 + regenerated `schema.d.ts`; `TestGlobalAuditIsSeparateFromEveryOrgListing` (real PG, both directions of the isolation), `TestGlobalAuditIsGlobalAdminOnly`, `TestGlobalAuditLimitIsBounded`; three mutants fatal ([iter-0155](iterations/iter-0155.md) §4). UI: mock approved by the owner and implemented 1:1 — [`InstanceAuditPanel.vue`](../frontend/src/components/settings/InstanceAuditPanel.vue) in the Administration group, global-admin only, with the row grammar extracted into [`AuditRows.vue`](../frontend/src/components/settings/AuditRows.vue) so the org trail and the instance trail share ONE implementation rather than two that agree today; `InstanceAuditPanel.spec.ts` (5) and `MembersPanel.spec.ts` (the regression guard that refactor did not have — written when the gap was noticed), two UI mutants fatal; 116 frontend tests, `vue-tsc` + build green, `make spa-snapshot` refreshed. Browser gate on an image REBUILT from this branch: `make dev-test` 45 passed / 1 known skip, including a live scenario that writes an instance-level entry and then asserts a row renders and the empty state does not (an "either rows or empty" assertion would pass on a panel that renders nothing). |
| AC-0155-2 | The discharge map is true clause by clause, not merely resolvable. Review [425] sampled ~5 rows and found TWO false — the cited test existed, passed, and did not prove the clause — so all 91 invariant rows were re-read against the doc-comment of every test they cite: **73 confirmed, 11 gained a citation for a clause that had none, 2 were false (53, 82), 2 were genuinely unpinned and now have new tests (4, 6), 1 is marked PARTIAL with its gap named (78), 2 were reworded because the claim was never a test's job (28, 46)**. Nothing was broken: every property behind the new tests is correctly implemented, and no mutant required a product fix. | DONE | [iter-0155](iterations/iter-0155.md) §2.2 for the table and the class ("a multi-clause invariant cited by a test that proves ONE clause"); the rows themselves in [traceability.md](traceability.md); `TestProvenanceTellsAnIgnoredMemberFromAnExcludedOne` and `TestThresholdValidationReadsTheDeclaredCardinality`, both mutation-proven. |
| AC-0155-3 | A project can carry an SLO objective, and it cannot page. `sla_targets.project_id` was dormant schema for eleven migrations; it is now writable through `GET` and `PUT /api/v1/projects/{projectID}/sla-target` with `{window, objective}` and nothing else, the objective passing the ONE canonical rule of D-0165. The project SLA report states the objective and its budget for a window that has one and stays silent for a window that does not — burn fields ABSENT rather than `false`, because an omitted field is the honest rendering of "this cannot page". Migration 00083 makes that structural: `sla_targets_project_no_burn_chk` refuses a paging project target whoever writes it, exactly as the service scope was refused until the phase that owned service paging lifted it deliberately. | DONE | UI landed after the owner approved [`mock-project-objective.html`](design/mock-project-objective.html): the card states the promise about the WHOLE beside the mean across its parts, a project without one reads "not set" with NO budget, the client rejects an impossible objective through the same D-0165 rule, and there is no burn control at all — not a disabled one. The mock's Clear demanded `DELETE …/sla-target`, which now answers 404 on a window that has no objective and takes the derived budget with it.  [`store/sla.go`](../internal/store/sla.go), [`api/handlers_sla.go`](../internal/api/handlers_sla.go), [migration 00083](../internal/store/migrations/00083_project_sla_objective.sql), `openapi.yaml` 0.22.0 + regenerated `schema.d.ts`; `TestProjectObjectiveIsStoredAndCannotPage` (real PG, the CHECK asserted by DIRECT SQL), `TestProjectObjectiveIsReportingOnly`, `TestProjectObjectiveAuthzAndAbsence`, `TestProjectReportStatesTheObjectiveOnlyWhenSet`; two mutants fatal ([iter-0155](iterations/iter-0155.md) §2.3). `SlaView.spec.ts` (+3), `TestClearingTheProjectObjectiveTakesTheBudgetWithIt`, two UI mutants fatal; live e2e round-trip and clear; browser suite 46 passed / 1 known skip on a rebuilt image. |
| AC-0155-4 | A scheduled result says which job it answers and cannot predate it. The id and the ISSUE INSTANT come from the database in the statement that materializes the job — so `observed_at >= job_issued_at` compares an executor's clock against the core's — and every executor copies both onto its result through one owner, `dispatch.StampResult`. Beyond `result.allowed_skew` the result is rejected as `observed_before_issue`; inside the tolerance it is ACCEPTED and counted (`cerbix_result_observed_before_issue_total`), because a region trailing the core by seconds is ordinary and dropping those results would lose real measurements; with no identity carried nothing is checked, so push and older executors are untouched. | DONE | [`store/materialize.go`](../internal/store/materialize.go), [`dispatch/dispatch.go`](../internal/dispatch/dispatch.go) (`StampResult`), [`store/monitors.go`](../internal/store/monitors.go) (`ReasonObservedBeforeIssue`), [`metrics/metrics.go`](../internal/metrics/metrics.go); `TestAResultCannotPredateTheJobItAnswers` (all three cases), `TestEveryMaterializedJobCarriesItsOwnIdentity`, `TestStampResultCarriesTheJobIdentity`, `TestProbeErrorHeartbeatNamesItsJob`, `TestPublishedResultCarriesTheJobItAnswers` — the last written after a mutation showed the worker's stamp was covered by NOTHING; six mutants fatal ([iter-0155](iterations/iter-0155.md) §2.4). `-race` 33 packages hypertable + store/api/worker/dispatch declarative. |
| AC-0155-5 | The fourth commissioned item is handed over as a REQUIREMENT, not an amendment: `FR-022`/`NFR-017` with a design-gate input that names the six decisions only the owner can make and the three places the feature can quietly break something that already works (the prefix-keyed incident notes, §14 correlation's lack of a self-link case, and the TOTAL §15.0 precedence table that invariants 66–68 rest on). No code, and none until the spec is reviewed and a mock approved — per **D-0169**, which closed FR-021 with §16.9 as an explicit non-goal so each item would open its own requirement. | DONE | [`func-service-incidents.md`](specs/func-service-incidents.md); the `FR-022`/`NFR-017` rows above (`TODO`); [iter-0155](iterations/iter-0155.md) §2.6. |
| AC-0155-6 | The build gate is a gate, not a habit. `make docs-check` runs in CI and the suite triggers on `push` (a 66-commit branch with no PR had never run it), CI covers BOTH storage modes — `postgres:16-alpine` and `timescale/timescaledb:latest-pg16`, the second being what the dev and prod stacks actually run — and readiness is THREE consecutive successful queries rather than `pg_isready`, which was measured reporting ready for a full second while a real query still failed. | DONE | [`.github/workflows/tests.yml`](../.github/workflows/tests.yml); [iter-0155](iterations/iter-0155.md) §2.7 for the measurement and the reasoning; YAML validated with a real parser and the barrier script run locally against a fresh container of each image. |
| DoD-0155 | Each of the four commissioned items is either delivered with its gates, or handed back with the decision that blocks it named. No item is reported done while its UI surface is unbuilt. | DONE | All four are discharged: AC-0155-1 (instance audit, UI included — the mock was approved and built after iter-0155's own §0 table was written, which is why that table still reads "UI awaits mock approval"; the row above is the current state), AC-0155-3 (project objective, UI included), AC-0155-4 (`job_id` correlation), and the §16.9 design input delivered as its own requirement — FR-022, itself now shipped in [iter-0156](iterations/iter-0156.md). Corrected while auditing stale status claims ([iter-0161](iterations/iter-0161.md) §15). |

## Previous — FR-021 and NFR-016 are CLOSED (iter-0153, closed at `d390e90` and immutable from it; D-0169)

[iter-0154](iterations/iter-0154.md) followed it as a three-sentence arc, closed at `cae4b4f` and immutable from it, opened and closed in one commit: the spec and one code comment still said "until phase 5" and "until phase 4 opts in" after both phases had landed — one having changed nothing, the other having declined. No behaviour moved.

[iter-0153](iterations/iter-0153.md) closed the requirement against the discharge tables in [traceability.md](traceability.md): every one of the spec's 91 acceptance invariants and all 24 §16.10 scenarios name the test that holds them, and `make docs-check` fails if that stops being true. §16.9 is an explicit NON-GOAL of FR-021 — service incidents, escalation policies for services, retroactive alerting, cross-project delegation, per-member severity inside a service page and suppression beyond the three named topics each open their OWN requirement when commissioned. Predecessors [iter-0151](iterations/iter-0151.md) (`475a0d3`) and [iter-0152](iterations/iter-0152.md) (`cc247ce`) are closed and immutable from those commits. The next commissioned work opens `iter-0154`.

**No lifetime review scoreboard lives here, and no list of review rounds either — on purpose.** An
earlier revision kept a cumulative "reviewer findings" total in this document, and every review OF
that total produced a finding that changed it: a recursion that cannot converge while the number is
live. A list of round IDs is the same shape more slowly — it grows with every round while nothing here
forces it to, and it was found stale twice. Round IDs are per-arc evidence and they live in the report
for that arc, which freezes: [iter-0151](iterations/iter-0151.md) §0/§2,
[iter-0152](iterations/iter-0152.md) §0/§2, [iter-0153](iterations/iter-0153.md) §2, and the party
record.

One counter remains, because it is mechanically stable and each term freezes when its report closes:

- **§2 finding sections in the FROZEN reports — 34: 15 in [iter-0151](iterations/iter-0151.md), 10 in
  [iter-0152](iterations/iter-0152.md), 8 in [iter-0153](iterations/iter-0153.md) and 1 in
  [iter-0154](iterations/iter-0154.md).** Named after exactly what its checker counts,
  `grep -c '^### 2\.'` — it is SECTIONS, not individual defects (§2.7 of iter-0152 carries two honesty
  gaps under one heading), and it INCLUDES the review's governance and documentation findings. An OPEN
  report's own count is stated in that report when it closes and added here then, so nothing in this
  bullet moves while an iteration is in progress. All three reports are now closed and this number is
  final until a new iteration opens.

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0154-1 | No statement in this spec or its code is conditioned on an event that has already been decided. Three were: the §12.2 burn card said "until phase 5 owns alerting" (phase 5 came and left the card alone — paging rides the declared `burn_rules` of §16.4), two places said status-page impacts are absent "until phase 4 opts in" (phase 4 came and DECLINED), and `store/servicereport.go` sent a reader looking for the alerting path to the reporting pair. | DONE | [iter-0154](iterations/iter-0154.md) §2.1; the comment-only production diff shown in its §3; invariant 59 still discharged by `TestIncidentImpactsDetailOnlyAndNeverPublic`. |
| DoD-0154 | The arc opens and closes in one commit, because it is three sentences and no reviewer round exists; `make docs-check`, `go build`, `go vet` and `gofmt` are green, and no executable behaviour changed — so no test run is claimed. | DONE | [iter-0154](iterations/iter-0154.md) §3. |
| AC-0153-1 | No living document asserts a contract that has been deleted, and none enumerates this phase's review rounds. The cumulative scoreboard was DELETED at `cc247ce`, not relocated, so a sentence saying where it lives is false wherever it appears; and a list of round IDs in a long-lived document grows with every round while nothing updates it, which is the same recursion at a slower rate — it had already gone stale twice, at different rounds, in the two places it appeared. Round IDs belong to the report for their arc, which freezes. | DONE | [iter-0153](iterations/iter-0153.md) §2.1; the checkers in its §5: no `totals stated once` match in this file or `traceability.md`, and `sed -n '5,20p' docs/status.md \| grep -o '\[[0-9]\{3\}\]'` empty while all three reports stay linked. |
| AC-0153-2 | A CLOSED iteration report is never the writable current iteration, and a frozen report's mistakes are corrected in its successor BY RECORD rather than by edit. Closing a report without opening its successor leaves the changing facts with nowhere legal to go, and they land back in the frozen one — which is how a post-closure commit ended up described in a closed report's header. | DONE | [iter-0153](iterations/iter-0153.md) §2.2 and §3 (the correction of record for iter-0152's three stale lines, which are deliberately NOT edited); `diff` of both predecessors against `475a0d3` and `cc247ce` — empty. |
| AC-0153-3 | This live checklist carries the CURRENT iteration's own atomic items, not only the previous arc's `DONE` rows. A current-iteration header with nothing `IN_PROGRESS` under it is a heading, not a status. | DONE | This table; [iter-0153](iterations/iter-0153.md) §2.3. |
| AC-0153-4 | The living documents and the code agree citation by citation: every path and every `Test*` name they cite resolves in the tree, and the canonical spec does not assert an invariant the code was deliberately changed to break. Twenty-two unresolved citations were found and fixed, one of them a name (`TestPartitionAdoptionFencesConcurrentInserts`) that has never existed in any commit; and invariant 47 of `func-service-reliability.md` still read "a service-scoped `burn_alert_enabled` is rejected" while §16.4 of the same file lifts that rejection, migration 00082 drops the CHECK and the app pages per burn rule. The agreement is now enforced rather than reviewed: `make docs-check` fails the build on a citation a reader cannot follow. | DONE | [iter-0153](iterations/iter-0153.md) §2.5; `scripts/check-docs-references.py` + `make docs-check` (three mutation probes in §5); the reverse direction checked too — every migration, Go file, e2e spec and Vue component is named by some document. |
| AC-0153-5 | No requirement row claims a state the project has moved past. Twenty-eight `AC`/`DoD` rows for iter-0125…iter-0129 were `IN_PROGRESS` — so this document, the one consulted to answer "is anything left?", said the FR-021 phase-1 hardening arc was unfinished while phases 2–5 sit on that code as `DONE`. The arc's verdict is quotable: round 2/2 APPROVE at [123], "no remaining P0/P1/P2 findings in reviewed scope", explicitly closing iter-0125 through iter-0132. After this arc the only `IN_PROGRESS` rows in this file are iter-0153's own. | DONE | [iter-0153](iterations/iter-0153.md) §2.6; [iter-0132](iterations/iter-0132.md) §5 for the verdict and its scope; the section note above the flipped rows; `docs/traceability.md` rework rows re-stated with their outcome. |
| AC-0153-6 | "FR-021 is done" is a checkable claim, not a chain of verdicts: every one of the spec's 91 acceptance invariants (§19 for 1–74, §16.8 for 75–91) and all 24 scenarios of the §16.10 required matrix have a named test, and `make docs-check` fails when a row cites a test the tree lacks or when any of the 116 numbers has no row. Building the map found SEVEN properties that were specified, believed and unpinned — invariants 1, 25, 27, 35, 37, 78 and 86 — and a later per-clause re-read (iter-0155 §2.2, after review [425] found two FALSE rows by sampling) found eleven more clauses with no test named, two unpinned properties and one partial: this row's original claim that every invariant "has a named test" was true of the numbers and not yet of the clauses. None of them a product defect, each now carrying a test that fails behaviourally when the property is broken. | DONE | The discharge tables in [traceability.md](traceability.md); [iter-0153](iterations/iter-0153.md) §2.7 for the seven gaps and their mutations; `go test -race -count=1 ./...` 33 packages green in hypertable mode and `store`/`api`/`scheduler` green in declarative-partition mode, on containers removed after the runs. |
| AC-0153-7 | FR-021 closes against a stated boundary, and no requirement of this spec is untracked: `FR-021` is `DONE` under **D-0169** with §16.9 recorded as an explicit NON-GOAL (each item carrying the owner decision behind it, each opening its own requirement when commissioned), and `NFR-016` — declared in the spec's title line and never present in this file — now has its own row with the invariants that carry each of its clauses. | DONE | [decisions.md](decisions.md) D-0169; the `FR-021` and `NFR-016` rows in this file; [iter-0153](iterations/iter-0153.md) §2.8; the closure row in [traceability.md](traceability.md). |
| DoD-0153 | The closing commit — and nothing before it — adds iter-0153's `CLOSED` line, states that report's final `§2` section count, adds that count to the frozen-report counter above, and flips these rows to `DONE` Until then `make docs-check` passes on every commit of this arc. Until then both predecessors stay byte-identical to their closure commits, every claim in the report is a command in its §5 that a reader can run, and the arc touches no product file, test or generated artifact (`go build` is a guard here, not evidence about the change). | DONE | [iter-0153](iterations/iter-0153.md) §4–§5; last product commit `6613e64`. |

## Previous iteration (iter-0151 + iter-0152 — FR-021 phase 5, alerting ownership: the backend/UI arc and the post-close correction arc. [iter-0151](iterations/iter-0151.md) CLOSED at `475a0d3`, [iter-0152](iterations/iter-0152.md) CLOSED at `cc247ce`, both immutable from those commits; their review rounds are enumerated in those reports, per AC-0153-1)

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0151-1 | Suppression is per SIGNAL, per POLARITY, and only while a replacement is ARMED (§16.1, invariants 75–77): `owns_paging` alone silences nothing; a HOLD, a missing verdict for ANY declared rule, a changed generation or effective revision, a stale DB-clock lease and an unroutable service each dis-arm; a recovery and a burn CLEAR are suppressed NEVER; everything ambiguous FAILS OPEN with a named, counted reason. | DONE | [`store/alertdelegation.go`](../internal/store/alertdelegation.go) (the conjunction, built from named predicates shared with the badge read); regressions for each dis-arming cause incl. the add-rule and replaced-in-place identity gaps, each with its own mutation; [iter-0151](iterations/iter-0151.md) §2.4/§2.7. |
| AC-0151-2 | Both evaluators are BOUNDED, transactional and fenced (§16.7, invariants 80/85): one snapshot per slice, verdict + episode + outbox row in ONE transaction, the whole transaction on the leader's LOCK-OWNING connection, linearized against config writes by locking the writers' rows in the writers' order, and a lost CAS rolls the page back rather than publishing it. | DONE | [`store/servicealerteval.go`](../internal/store/servicealerteval.go), [`store/serviceburneval.go`](../internal/store/serviceburneval.go), [`store/leadership.go`](../internal/store/leadership.go); the deposed-leader barrier on real Postgres (backend identity asserted, then killed mid-transaction) and three linearization barriers; [iter-0151](iterations/iter-0151.md) §2.6/§2.8. |
| AC-0151-3 | Every removal path closes a firing episode in the SAME transaction as the removal, with its named reason, and the close survives what it names (§16.4a, invariant 89). | DONE | [`store/servicealertlifecycle.go`](../internal/store/servicealertlifecycle.go) (one closing path, from the episode's own snapshot, through the FENCED enqueue owner), [`store/servicealertpolicy.go`](../internal/store/servicealertpolicy.go); the deletion close, its atomicity proof (a refused DELETE publishes nothing), and the mixed old/new fleet regression of invariant 83. |
| AC-0151-4 | The write surface is one validator shared by API and MaC, server-owned fields are refused structurally, a file-managed service refuses UI edits with 409, and the file is DECLARATIVE — the same bundle converges to the same database whatever it used to say (§16.6a, §15.2, invariant 87). | DONE | [`api/handlers_servicealerting.go`](../internal/api/handlers_servicealerting.go) (pointer patch merged in the STORE's transaction; `firing` and the generations unknown-field 400s), [`fileprovider/service.go`](../internal/fileprovider/service.go) (an absent `alerting:` block is the DEFAULT, and the declaration is outside the canonical hash so a paging edit creates no revision and no epoch). |
| AC-0151-5 | The operator can read what is ACTUALLY happening: per-signal coverage from the server with its reason, re-read rather than cached, and a delegated monitor keeping its real status pill beside a chip naming who pages instead (§16.6b and the approved mock). | DONE | [`store/servicealertstate.go`](../internal/store/servicealertstate.go) (composed from the delegation lookup's own predicates; the agreement table pins badge == delivery for every cause), [`ServiceAlerting.vue`](../frontend/src/components/ServiceAlerting.vue), [`MonitorDetailView.vue`](../frontend/src/views/MonitorDetailView.vue); 19 component/view tests + `e2e/tests/alerting.spec.ts`. |
| DoD-0151 | Every fix is proven against the code it fixes, and every claim here is backed by the run that was actually made. | DONE | `go test -race -count=1 ./...` — 30 packages, BOTH storage modes, fresh containers behind a real SQL-connection/migration barrier; `go vet` clean; `gofmt -l` empty; 110 frontend tests; `vue-tsc` + SPA build; `make dev-test` 44 passed / 1 known skip on an image built from this branch, including the new alerting spec. Every recorded defect carries a mutation that failed BEHAVIOURALLY; cumulative review totals are stated NOWHERE — a frozen report cannot own a number that keeps changing, and a living one cannot own a number that every review of it changes ([iter-0153](iterations/iter-0153.md) §2.1 is the correction of record); the harness readiness failure recorded as a HARNESS defect, not product evidence. |

## Previous iteration (iter-0150 — DONE at [319]; FR-021 phase 4: the status-page service projection and the composite lifecycle. Design gate cleared [310], mock approved; review round 1/2 [314] REJECTED 2 P0 + 7 P1 + 2 P2 with the contested item disposed [315]/[316]; FINAL round 2/2 [318] REJECTED 2 P0 + 3 P1 + P2 — no third reviewer round, so disposition and closure were the implementer's under owner authority)

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0150-1 | A Service can be what a status-page line reports, and adding that source did not multiply the three inherited statements that reported an UNKNOWN as health. The source is a DISCRIMINATOR (`monitor` / `service` / `manual`) with the replaced binding kept DORMANT for revert, so every read branches on the discriminator — the shipped renderer branched on `monitor_id != ""` and would have kept publishing the OLD monitor of a converted component, while `if ManualStatus != ""` would have let a dormant manual status beat a live service. `no_data` is a public status deliberately OUTSIDE the severity ladder: the summary is worst-of-MEASURED plus an unmeasured COUNT (`summary_state`, `unmeasured_count`), and no operator can type it (CHECK + both write paths refuse). The three §17 changes: a `pending` monitor component, a manual component with no status, and an EMPTY page all stop publishing `operational`. | DONE | Migration [`00081_status_projection.sql`](../internal/store/migrations/00081_status_projection.sql); [`domain/statuspage.go`](../internal/domain/statuspage.go) (`CompNoData`, `Summarize`, `severity()` default moved off operational); [`store/statusprojection.go`](../internal/store/statusprojection.go) (one snapshot, constant statements, total `PublicComponentStatus` precedence); [`api/statusprojection.go`](../internal/api/statusprojection.go) (ONE resolver for the public page and the preview). The two unit tests pinning the OLD answers were REWRITTEN and each also asserts the old answer is gone. 15 store + 8 API + 1 domain table + 12 frontend tests; store PASS in both storage modes; -race zero FAIL; lint 0; [iter-0150](iterations/iter-0150.md), D-0167. |
| AC-0150-2 | Changing what a page line MEANS is explicit, previewed, reversible and CAS-fenced. Preview and confirm share ONE validator (a preview cannot promise what the confirm refuses) and the preview renders through the SAME resolver as the public page (it cannot predict a page the server would not produce). Consent binds `components.revision` AND `status_pages.component_generation` — bumped by ANY component mutation on the page, so a NEIGHBOUR's edit invalidates it — mismatch is 409 `page_configuration_stale`, and confirming without the tokens is 400. Deletion is per case: an ACTIVE service binding is `RESTRICT` with a typed refusal naming the page and the component; a DORMANT one is swept in the same transaction with both counters bumped, which is what makes RESTRICT mean *rendered* rather than *ever mentioned*. | DONE | [`store/componentconversion.go`](../internal/store/componentconversion.go), [`store/services.go`](../internal/store/services.go) (`ErrServiceRendered` + the one-statement dormant sweep), [`api/handlers_statusprojection.go`](../internal/api/handlers_statusprojection.go), [`StatusPagesView.vue`](../frontend/src/views/StatusPagesView.vue) (the Apply control does not exist until a preview does; a stale 409 DISCARDS the preview rather than leaving dead tokens); `TestComponentConversionCASOnBothTokens` incl. the neighbour case, `TestComponentConversionIsReversible`, `TestComponentConversionNoOpIsIdempotent` (zero audit rows), `StatusPagesView.spec.ts` (5). |
| AC-0150-3 | A converted composite stays visible and ACTIVE; retiring is a separate, explicit act (owner decision). The link is ONE stored fact rendered from both ends, and recording it bumps no `execution_revision` — an annotation must not force a re-probe. `retire` is ONE transaction: `retired_at` + `enabled = false` + the config fence + the `state_sequence` advance that makes a queued DOWN stale at delivery + the epoch fan-out for every referencing service + audit; reversible, and refused for a file-managed monitor. Composite→service conversion is one serialized transaction, idempotent on the link column, with a named slug conflict; the `quorum` translation is `degraded_min = n − M + 1` and REFUSES a multi-region composite instead of inventing thresholds that merely look similar. | DONE | [`store/monitorretire.go`](../internal/store/monitorretire.go), [`store/compositeconvert.go`](../internal/store/compositeconvert.go); `TestRetireMonitorEndsLifecycleAndExecution`, `TestCompositeQuorumTranslation` (7 rows), `TestConvertCompositeToService` + idempotent re-confirm, `TestConvertCompositeSlugCollision`, `TestSuccessorLinkClearedOnServiceDelete`; UI: a Retired badge distinct from Paused, a retire confirmation stating BOTH consequences, and "Converted from … still probing" on the service page. |
| AC-0150-4 | The public render is bounded in ROWS, MEMORY and TIME. The unit of work is the PAGE, not the project — an org page spans projects, so the round-1 per-project batch was neither one snapshot nor a constant count: `ServicePageProjections` takes `(project, service)` pairs and costs five statements whatever the page holds, and the monitor half is three set-wise reads instead of three per component. Witnessed by a pgx `QueryTracer` (1 service/1 project vs 11 services/6 projects) and by per-source call counters (exactly ONE call per source for 500 components across 250 projects). The ceiling is LOWERED by every removal and may never exceed `max(50, count)`, so no write creates headroom; above the absolute 500 the page returns `status_page_over_safe_limit` naming both numbers while the authenticated view serves all of them; a 5-second render cache keyed to page + access shape + unlisted token is the RATE bound a ceiling cannot provide. `uptime_90d` and its `withheld_reason` come from `decideServiceWindow` — EXTRACTED from the authenticated report, so the page cannot quote what the report withholds. A failed read carries a PUBLIC `unavailable` marker and increments `cerbix_status_page_component_unreadable_total`. | DONE | [`store/statuspageprojection.go`](../internal/store/statuspageprojection.go), [`store/monitorpageprojection.go`](../internal/store/monitorpageprojection.go), [`api/statuspagecache.go`](../internal/api/statuspagecache.go); `TestServicePageProjectionsStatementCountIsConstant`, `TestServicePageProjectionAgreesWithTheReport`, `TestPublicRenderIsBoundedAtTheCeiling`, `TestPublicRenderRefusesOverTheSafeLimit`, `TestUnreadableServiceIsVisibleAndCountedPublicly`, `TestPublicRenderCacheIsKeyedAndShortLived`, `TestPageComponentCeilingOnlyShrinks`. |
| AC-0150-5 | The schema holds the invariants that no application is on the path for. A DORMANT binding cannot escape tenancy (a composite FK is MATCH SIMPLE, so the rule keys on the PRESENCE of a binding, not on `source`); the page-scope guard is a serialization point, not merely deferred, so two sessions cannot commit a state neither would accept; deleting a monitor a page RENDERS turns its component manual through a `BEFORE DELETE` trigger — which the API path and the D-0150 cascade share — instead of failing on the active-binding CHECK; and both counters are DB-owned so a cascade cannot leave a stale preview confirmable. | DONE | Migration [`00081_status_projection.sql`](../internal/store/migrations/00081_status_projection.sql) (FOLDED, not patched: unreleased and untracked, and `cerbix_test` was recreated so it had to apply from zero); `TestDormantBindingCannotEscapeTenancy`, `TestPageScopeRaceIsSerializedInBothOrders` (both commit orders, with a blocking witness), `TestDeletingARenderedMonitorTurnsItsComponentManual`, `TestProjectCascadeAdvancesTheSurvivingPageGeneration`, `TestComponentMutationsCannotDeadlock`, `TestCountersMoveOnEveryMutation`. |
| AC-0150-6 | Converting a composite ASKS rather than assumes: `sli[]` is required with no default anywhere in the stack (§15.5 forbids a silent "all children"), every live child joins the operational context regardless, a partially-lost composite is refused by name, and the quorum translation is the exact binary mapping. The lifecycle actions are COMPOSITE-only, and file ownership splits by DECLARATION authority — `retire`/`reactivate`/conversion refuse, the annotation is permitted, because it is absent from bundle format 2 and no reapply can restate it. | DONE | [`store/compositeconvert.go`](../internal/store/compositeconvert.go), [`store/monitorretire.go`](../internal/store/monitorretire.go), [`MonitorDetailView.vue`](../frontend/src/views/MonitorDetailView.vue) (the SLI checklist); `TestCompositeConversionRequiresExplicitSLI` (subset honoured, foreign member refused, audit records both counts), `TestCompositeLifecycleScopeAndFileOwnership` (annotation permitted with NO execution-state movement; retire/convert refused), `TestConvertCompositeRefusals` (partial loss), `TestCompositeQuorumTranslation`; §15.1 narrowed in the spec so the matrix no longer contradicts the permitted annotation. |

## Previous iteration (iter-0149 — DONE; two honesty fixes surfaced by looking at the running stack)

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0149-1 | The product no longer says two untrue things: (1) the services list claimed availability/error budget/burn rate "arrive with the next iteration" — false since iter-0140, and the numbers were visible on the adjacent service page; it now states where they live and when they appear, keeping the watermark rationale; (2) the committed `internal/web/dist` snapshot — what `go build` / `go install .../cmd/cerbix@latest` embed — was three weeks stale (no phase 2, no phase 3) and NO gate could have caught it, because the Docker image rebuilds the SPA and discards that directory. Root cause was a manual refresh with no command: `make spa-snapshot` now performs it (restoring the `placeholder.txt` fixture the web test asserts through) and CLAUDE.md states the rule. | DONE | [`ServicesView.vue`](../frontend/src/views/ServicesView.vue) note, [`Makefile`](../Makefile) `spa-snapshot`, refreshed `internal/web/dist` (50 files, now carrying phases 2–3), [`CLAUDE.md`](../CLAUDE.md) rule; `internal/web` 3/3 incl. `TestServesRealAsset`, frontend 76/76, -race 33 ok, lint 0; [iter-0149](iterations/iter-0149.md). |

## Previous iteration (iter-0148 — DONE; FR-021 phase 3 changeset 3: the SPA surface. Review chain [298]→[299]→[301]; four residuals closed under the [263]-delegated disposition, iter-0148 §9. **FR-021 phase 3 COMPLETE**)

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0148-1 | The §14.5 SPA surface implements the APPROVED mock 1:1 (lists and badges, no visual graph): the service detail carries both edge directions with the phase-2 two-layer neighbour health (absent health renders "unknown", never a green pill) and the count against the 20-edge bound; the replace-set editor saves the token it READ WITH THE LIST and renders each rejection as ITSELF from the API's own message — cycle 400, stale `graph_generation` 409 with a Reload that RE-READS instead of overwriting, count bound 400, plus the file-owned notice; a rejected fetch is handled exactly like an error payload and a deferred response from a previous service can never land (load-generation guard); the incident detail carries 🕸 probable-root chips with the stored root-first path for EVERY candidate and affected chips, and renders `impacts_unavailable` as its own state rather than as "no impact"; the incidents list still carries none; the approved mock's file-pin chip is supported end to end by bounded edge provenance (`managed_by`, one correlated subquery inside the existing single-statement read), and a failed candidate list never poses as an empty project. | DONE | New [`ServiceDependencies.vue`](../frontend/src/components/ServiceDependencies.vue) mounted in [`ServiceDetailView.vue`](../frontend/src/views/ServiceDetailView.vue); chips in [`IncidentDetailView.vue`](../frontend/src/views/IncidentDetailView.vue); 11 component regressions in [`ServiceDependencies.spec.ts`](../frontend/src/components/ServiceDependencies.spec.ts) + the browser scenario in [`e2e/tests/services.spec.ts`](../e2e/tests/services.spec.ts) (edge declared through the UI editor, reverse direction from the parent, cycle refused with the editor open, API-verified no-op); 18 `ServiceDependencies` + 4 `IncidentDetailView` component regressions (the latter added at [301] P1-1, where my claimed acknowledge coverage did not exist), 1 store regression (`TestServiceGraphEdgeCarriesOwnership`), 1 e2e scenario; frontend 76/76 across 9 files, e2e 42 passed/1 known skip, -race 33 ok, declarative store+api ok; [iter-0148](iterations/iter-0148.md); review chain [298]→[299]→[301]+§9 disposition. |

## Previous iteration (iter-0147 — DONE; FR-021 phase 3 changeset 2: the API surface. Review chain [288]→[290]→[292]; the [292] residual closed under the [263]-delegated disposition, iter-0147 §9)

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0147-1 | The §14.4 API surface exists on the wire: dedicated `GET/PUT …/services/{id}/dependencies` (one-snapshot read with `graph_generation`; CAS 409 `graph_generation_stale`; cycle/foreign/depth/limit as named 400s; typed actor attribution from the principal); create-with-edges through ONE atomic store path sharing the edge mutator; the service detail gains `dependencies` with neighbour health from EXACTLY ONE batched snapshot call (invariant 60, pinned by a call-count test); the authenticated incident DETAIL carries `impacts` while the shared incident model, the incident LIST and the raw unauthenticated status-page JSON carry none (invariant 59, proven over an incident that HAS links); openapi 0.20.0 + regenerated `schema.d.ts` as the contract of record; edge writes obey OWNERSHIP (a file-owned service is 409 `managed_by_file`, active or orphaned); the request contract is strict and presence-aware with ONE transport UUID fence shared by the PUT and create-with-edges; neighbour health is a CONSTANT four set-wise statements for any neighbour count (pgx tracer proof at 1 vs 30 neighbours + verdict parity with the single-service owner); a failed impact read is `impacts: null` + `impacts_unavailable`, never a lying `[]`. | DONE | [`handlers_servicegraph.go`](../internal/api/handlers_servicegraph.go), [`handlers_incidents.go`](../internal/api/handlers_incidents.go) (`authedIncidentView`), store additions (`applyServiceDependenciesTx` extraction, `CreateServiceWithDependencies`, `ServiceNeighbourHealth`); 11 regressions (6 in [`api_servicegraph_test.go`](../internal/api/api_servicegraph_test.go), 5 real-PG in the store incl. the fixed-statement-count proof and the atomic create rollback); [iter-0147](iterations/iter-0147.md); review chain [288]→[290]→[292]+§9 disposition. |

## Previous iteration (iter-0146 — DONE; FR-021 phase 3 changeset 1: schema/store core. Review chain [276]→[281]→[283]; the [283] residual closed under the [263]-delegated disposition, iter-0146 §9)

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0146-1 | The service impact graph exists as specified (§14.1–14.2, invariants 48–51, 58): `service_dependencies` same-project by SCHEMA (one shared `project_id` under two composite FKs), DAG with the depth cap pinned at 9/10/11 and `max_service_dependencies`=20, every edge-mutating path (replace-set, delete/cascade) under the per-project `service_graph` advisory lock ordered after `service_membership`, edges OUTSIDE the declaration axes (no revision, no epoch, no hash byte), `graph_generation` CAS with first-committer-wins 409, per-delta tenant audit rows in the same transaction, and the desired-edge delete guard (409 naming the provider for a file-pinned target; UI edges never pin). | DONE | Migration [`00080`](../internal/store/migrations/00080_service_impact.sql), [`store/servicegraph.go`](../internal/store/servicegraph.go), DeleteService guard; 8 regressions in [`servicegraph_internal_test.go`](../internal/store/servicegraph_internal_test.go) incl. the concurrent-cycle-writers race, direct-SQL cross-tenant rejection, no-revision/no-epoch proof, and both pin directions. |
| AC-0146-2 | Correlation is the §14.3 contract (invariants 52–57): its OWN outbox topic enqueued in the incident's opening transaction; ONE store transaction (incident rows locked FOR UPDATE ascending, open-state RECHECKED under the lock, links + 🕸 notes atomic); both interleavings converge to the same relation content (incl. the implementation-discovered upward `affected` mirror, disclosed in the spec); canonical endpoint-inclusive root-first paths (diamond lex tie, direct edge = 2 slugs, depth-10 chain = 11); redelivery inserts zero rows, writes zero notes, byte-identical relation; resolved incidents never annotated (resolve-races lose to the lock barrier); membership via `monitors[]`, never `sli[]`. | DONE | [`store/serviceimpact.go`](../internal/store/serviceimpact.go), enqueue in [`store/incidents.go`](../internal/store/incidents.go), worker case in [`outbox/outbox.go`](../internal/outbox/outbox.go), metrics §14.6; 12 regressions in [`serviceimpact_internal_test.go`](../internal/store/serviceimpact_internal_test.go) (both interleavings, redelivery byte-equivalence, resolve/snapshot barriers observed via pg_locks/pg_stat_activity, note-failure rollback, reachable-scoped witness bound + unrelated-service zero-overflow, tenant-scoped list) + 3 worker tests + the bounded 🕸 prose domain test. [iter-0146](iterations/iter-0146.md); review chain [276]→[281]→[283]+§9 disposition. |
| AC-0146-3 | The outbox claim fence holds over the WHOLE lifecycle (invariant 61): `incident_correlation` (and every post-phase-2 topic) enqueues as `pending_fenced` with the immutable `fenced` column; the deployed legacy claim shape selects zero fenced rows and burns zero attempts; a failed capable attempt and BOTH replay paths restore the fenced class; the legacy replay SQL fails closed on `outbox_events_fence_check`; legacy topics keep their exact pre-fence behavior; the producer is pinned to the fenced class. | DONE | Migration 00080 (status CHECK + fence CHECK + fenced column + partial index), [`store/outbox.go`](../internal/store/outbox.go) class-aware enqueue/claim/fail/replay, `domain.FencedTopic(s)` as the one topic→class map; 3 regressions in [`outboxfence_internal_test.go`](../internal/store/outboxfence_internal_test.go) incl. the verbatim legacy claim/replay SQL shapes, + the fenced-topics-dispatchable pin and the overflow-wiring assertion in the worker suite. [iter-0146](iterations/iter-0146.md); review chain [276]→[281]→[283]+§9 disposition. |

## Previous iteration (iter-0145 — DONE at [231]; the iter-0144/0145 UI changeset closed, FR-021 phase 2 complete)

**The `AC-0125`…`DoD-0132` rows in this section are the phase-1 review-hardening arc.** Each was
rejected at its own round and reworked by the next iteration, and the ARC was approved at [123] —
"no remaining P0/P1/P2 findings in reviewed scope", explicitly closing iter-0125 through iter-0132
([iter-0132](iterations/iter-0132.md) §5). Twenty-eight of those rows were still `IN_PROGRESS`,
left there from before that verdict and never flipped when it arrived; they are `DONE` on the
strength of it, and every test they name resolves in the tree (`make docs-check`). Phases 2–5 are
built on the code these rows describe and are themselves `DONE`, which is the second, independent
reason the arc cannot still be open.

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| AC-0145-1 | The two residual honesty gaps of [221] are closed: (1) every request — report, health, sealed series, provisional tail, per-segment series, objective PUT — is caught at ITS OWN boundary, a rejected fetch handled exactly like an HTTP error payload (openapi-fetch rethrows network failures); only the report owns the primary error; an unanswered health request renders PENDING, never a product pill; health is an INDEPENDENT task that neither gates the timeline nor strands as pending when the report fails ([228] P1-1); (2) segment strips never mix same-epoch reconstruction parts: unique-epoch segments slice the fetched GLOBAL series with no extra request (bounded fan-out, [228] P1-2), only same-epoch parts get exact `[from,to)` requests (SQL filters before grouping) through a 4-worker pool with per-completion assignment and explicit pending/failed/ready states, and ticks clip to the segment's own bounds so a repair in one half never masks the other ([228] P1-3). | DONE | Reworked `ServiceReliability.vue` (`guarded()` boundary normalizer, independent health task, `segmentPoints` map, clip); 22 component specs incl. rejected-Promise health/series/PUT tests, health-pending independence, report-error health settlement, the same-epoch regression, repair-clip, deferred-one/fast-one, and the six-segment zero-request bound; [iter-0145](iterations/iter-0145.md). APPROVED, reviews [228] (round 1/2) + [231] (final 2/2, zero findings). |
| AC-0144-1 | The phase-2 SPA surface implements the APPROVED mock (spec §12.2, screens 1–3) over the approved API: two-layer health card (never merged, never a percentage), KPI row with every §11 honesty state (each dash carries the payload's reason; burn verdicts render reasons as operator prose, never 0×), the ∅ spans-revisions banner with per-segment cards and the reconstruction badge, the time-weighted timeline strip with the mock's two fixed encodings (short unknown tick, reduced-opacity provisional), revision/epoch markers, the repair mask and the provisional tail, and the mutable objective editor on the ONE client rule. One disclosed store touch: the nothing-sealed report states its stored objective (current-view parameter, §11.3). | DONE | `ServiceReliability.vue`/`ReliabilityStrip.vue` + component specs (13 at the [219] fix pass; 22 with iter-0145); `TestNothingSealedReportStillStatesItsObjective` (real PG); live-stack e2e (honest empty states + browser-side objective rejection + 99.9 round-trip + 99.99 edit); [iter-0144](iterations/iter-0144.md). Its FINAL 2/2 [221] rejected two residual P1s → owner disposition A closed both in [iter-0145](iterations/iter-0145.md), APPROVED at [231] (zero findings), which closes this AC. |
| AC-0143-1 | The D-0165 objective rule reaches the CLIENT: `lib/objective.ts` mirrors `domain.CanonicalObjective` (raw open-interval bound before rounding, half-up at four decimals, post-round bound), `SlaView.saveSlo` rejects 99.99995/0.00001 with no request and sends the CANONICAL value — closing [203]'s residual P1 (owner option A). | DONE | `objective.spec.ts` (14 cases mirroring the Go table), the focused SlaView wiring test (no PUT on rejection; `body.objective === 99.9999` for 99.99994), 7/7 SlaView suite; [iter-0143](iterations/iter-0143.md). APPROVED, reviews [210] (impl accepted) + [213] (final 2/2, zero findings). |
| AC-0142-1 | SLA objectives live in the OPEN interval (0,100) — D-0165: raw input rejected at >=100 before rounding, the canonical 4-decimal value must stay inside (0,100) (maximum 99.9999), one rule (`domain.CanonicalObjective`) for the monitor and service scopes, and migration 00079 tightens the schema CHECK so EVERY writer meets the same fence — closing [195]'s P0 (objective=100 rode out a total outage at burn 0× with no alert). | DONE | `TestCanonicalObjective` (14 cases incl. both boundary rejections and NaN/±Inf), API 400s for 100/99.99995/100.00004, `TestObjectiveIsCanonicalAndRepresentable` (max value byte-exact through REAL PG + the schema fence by direct INSERT); post-round-bound mutant killed; [iter-0142](iterations/iter-0142.md). APPROVED, reviews [210] (impl accepted) + [213] (final 2/2, zero findings). |
| AC-0141-1 | The reporting API returns the reviewed domain payloads VERBATIM over session-auth project-scoped routes: reliability report (standard windows only, default 30d), the live health signal as its own endpoint (§11.3's separate named thing), bounded typed series (90d cap, hour/day), and the service SLA target whose body is {window, objective} and nothing else (invariant 47 at the decoder); a nonexistent/foreign service is 404 from the store's own tenant check, never a no_sli-shaped 200. openapi.yaml carries the four paths + ten mirroring schemas; the TS schema is regenerated and the SPA production build passes against it. | DONE | `internal/api/handlers_servicereport.go`; `api_servicereport_test.go` (authz/tenancy, parameter validation with store-boundary passthrough asserts, wire-verbatim payloads); store tenant regression re-pinned to ErrNotFound; migrations 00078/00079 with `domain.CanonicalObjective` as the one objective rule (D-0165, open interval (0,100) per iter-0142); mutants M1–M7 killed + one disclosed wrapper survivor (§6); [iter-0141](iterations/iter-0141.md), [iter-0142](iterations/iter-0142.md). APPROVED, reviews [210] (impl accepted) + [213] (final 2/2, zero findings). |
| AC-0140-1 | current_health is a TRUE point evaluation: `reliability.StateAt` (the reducer's own censusAt/aggregate keyed at as_of, right-continuous) — no fixed-width window exists to be split by nanosecond-granular derived freshness deadlines (time.ParseDuration admits them; [178] P1); the live signal and the facts share one member-state semantics by construction. | DONE | `TestServiceHealthNowSurvivesSubMicrosecondDeadlines` (the [178] repro: active_floor=90.0000005s — healthy at T+90s, down one µs later); all prior health regressions unchanged; window-reintroduced mutant killed; [iter-0140](iterations/iter-0140.md). APPROVED, reviews [184] (impl accepted) + [187] (final 2/2, zero findings). |
| AC-0139-1 | HISTORICAL, superseded in part: iter-0139's mechanism for current_health was a 1µs sample-and-hold window — its µs-alignment premise was refuted by [178] (derived freshness deadlines are ns-granular) and iter-0140's point evaluator (AC-0140-1) replaces the window entirely. The parts that STAND from this iteration: carry-in/maintenance/freshness through the materializer's own helpers with post-as_of inputs excluded; burn windows carrying TRUE anchored-window verdict fields under stale/era-short statuses; the spans-revisions label set only when >1 revision is actually stored. | DONE | `TestServiceHealthNowIsTheStateAtAsOf` (same-minute DOWN→UP/UP→DOWN + accepted-future beat, ordinary ingest only), extended burn/continuity asserts; mutants M1–M3; [iter-0139](iterations/iter-0139.md). APPROVED, reviews [184] (impl accepted) + [187] (final 2/2, zero findings). |
| AC-0139-2 | DISCOVERED phase-1 defect fixed with prominence: observationsFor's carry-in DISTINCT ON had NO defined order (trailing ORDER BY bound to the whole UNION) — an arbitrary prior observation held the state for any bucket whose member had ≥2 prior rows; sealed facts may carry wrong pre-first-observation slices, restatable via the audited admin repair. | DONE | Parenthesized carry-in branch in `servicematerialize.go`; `TestCarryInIsTheLatestPriorObservation`; empirical psql repro recorded in [iter-0139](iterations/iter-0139.md) §3. APPROVED, reviews [184] (impl accepted) + [187] (final 2/2, zero findings). |
| AC-0138-1 | The §11 report is computed ONLY from sealed facts over [sealed_through − W, sealed_through): both duration axes, independent storage-continuity and decidable-coverage verdicts (min 0.95 fixed), availability/budget/burn absent — with a named reason — on zero denominators, spans-revisions windows (invariant 43, "not even labelled"), missing objectives, insufficient history (era-anchored, 00071) and stale watermarks; excluded time is in NO denominator; segments are (epoch × reconstruction-part) with only the retroactive backfill part flagged (§6.6); repairing intervals ride the payload; the objective is a stated current-view parameter. | DONE | `internal/store/servicereport.go`; twelve regressions in `servicereport_internal_test.go` (invariants 38–45 + §11.1 confident-lie + mixed-excluded denominators + burn staleness); mutants M1–M8 killed; [iter-0138](iterations/iter-0138.md). APPROVED, reviews [184] (impl accepted) + [187] (final 2/2, zero findings). |
| AC-0138-2 | sla_targets carries service_id as a THIRD exclusive scope (00077) and — as delivered in phase 2 — service burn alerting rejected at the schema AND absent from the app layer (invariant 47, **SUPERSEDED by phase 5/§16.4/D-0168**, which lifts that rejection: `sla_targets_service_no_burn_chk` is dropped by migration 00082 and a service SLO pages per burn rule); hour/day rollups are on-read epoch-keyed exact sums that never merge across an epoch boundary and never mix provisional into sealed; the live signal is a separate categorical two-layer answer (§12.2) where a diagnostic monitor cannot touch the SLI layer; a diagnostics-only declaration moves NO reliability number and no snapshot hash (invariant 42). | DONE | Migration 00077; `TestServiceScopedBurnTargetIsSupported` (the phase-2 rejection regression, rewritten by phase 5 where it inverted), `TestReliabilitySeriesNeverMergesAcrossEpochs`, `TestServiceHealthNowIsTwoSeparateLayers`, `TestReportDiagnosticMonitorChangesNoNumber`; D-0163. APPROVED, reviews [184] (impl accepted) + [187] (final 2/2, zero findings). |
| AC-0137-1 | The adoption row gate is AUTHORITATIVE, not advisory: enforced by an unlocked preflight AND re-counted under the parent's ACCESS EXCLUSIVE lock before the sweep (the unlocked count alone is a TOCTOU); the bound (100k) and the operator-only recovery mode are owned by spec §10.11 + D-0161; recovery for oversize AND persistent under-bound fence failure is ONE shipped artifact — `cerbix adopt-fact-month` (same code path, operator budget, gate off, validated YYYY-MM input, idempotent), tested end-to-end through the real command line; staging FKs are verified by COMPLETE definition (column arrays both sides, actions, MATCH, deferrability + initial state; unvalidated correct-shape keys fail closed). | DONE | `TestOversizeGateIsRecheckedUnderTheParentLock` (deterministic lock-queue interleave), `TestOversizeMonthIsGatedAndOperatorCommandAdopts`, `TestAdoptFactMonthCommandEndToEnd` + `TestAdoptFactMonthValidatesItsInput` (internal/cli), `TestStagingFKVerificationDemandsExactDefinitions` (wrong-columns / initially-immediate / NOT VALID); mutants M1–M6 killed; [iter-0137](iterations/iter-0137.md). APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0137-2 | Evidence harness is honest: the fresh-migration UTC probe uses a UNIQUE per-run database name derived from the parsed DSN, never pre-drops a shared name, drops only what it created, and FAILS (not skips) on privilege errors inside the configured DB gate; the minute-boundary flake class in serviceingest tests is pinned out by construction; no phase-1 work is deferred — the iter-0133 canceller "yet" is closed as a design decision. | DONE | Reworked `TestFreshMigrationUnderNonUTCSessionSeedsUTCBounds`; pinned `TestHistoricalHeartbeatUsesMembershipAsOfItsBucket`, `TestHeartbeatBeforeMembershipDoesNotMark`, `TestHeartbeatIntoASealedBucketBecomesALateArrival`; D-0162. APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0136-1 | Month ownership is EXPLICITLY UTC (spec: "native UTC RANGE partitions in BOTH storage modes") independent of the session TimeZone — recovery truncates the UTC projection, the 00064 seed computes months and bounds with explicit UTC/+00. | DONE | `TestPastRecoveryIsSessionTimezoneProof` (Baku-session rollover: UTC month attached, session month absent), `TestMigrationSeedExpressionsAreUTC`; session-zone mutant killed. APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0136-2 | The cutover holds ONE absolute 5s budget Begin→COMMIT (deadlineTx, no per-statement re-mint), CAPPED by the caller's remaining deadline with an exhausted tail refused before Begin; the copy is progress-resumable from the staging's durable max key with no prefix rewrite; the automatic fence carries the DECLARED 100k bound with the operator-only recovery mode (owned by §10.11/D-0161, enforcement per AC-0137-1); orphan sweeps stop only once both FKs verify by complete definition; past recovery runs before current work; the stuck-month signal is a low-cardinality metric pair floored at tracking start. | DONE | `TestAdoptionCutoverUsesTheDeadlineMechanism`, `TestAdoptionResumesPastTheCopiedPrefix` (xmin), `TestFenceRefusesAnExhaustedCallerTail`, `TestFactMaintenanceStuckSignal` (initial-failure floor); zero-cursor and per-statement-fence mutants killed; [iter-0136](iterations/iter-0136.md), reworked per [152] in [iter-0137](iterations/iter-0137.md). APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0135-1 | DEFAULT-month adoption keeps the parent AUTHORITATIVE at every committed point (copy, never delete, until the fence), resumes an interrupted pass via pg_inherits instead of trusting CREATE IF NOT EXISTS, and its fence is authoritative by construction (DELETE…RETURNING under the parent lock, no watermark logic). | DONE | [iter-0135](iterations/iter-0135.md) §3; `TestAdoptionKeepsFactsVisibleAndResumes`, `TestAdoptionReconcilesConcurrentUpdatesAtTheFence` (mid-fence commit interleave), `TestAdoptionFenceIsBoundedAndAbortsSafely`; mutants: CREATE-first trap, unsound watermark. APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0135-2 | Sampled probes are bounded by ROWS EXAMINED (per-state partial indexes, plan-pinned with residual-filter rejection); UNKNOWN is published synchronously before leadership is visible; lifecycle outcomes require an affected row. | DONE | Migration 00075; `TestReliabilityStatsPlansAreIndexBounded` (all probes), `TestLeadershipPublishesUnknownBeforeLeader`, `TestStepDownJoinsAndClearsThroughRealLead`, `TestSamplerHealthyThenErrorInOneLoop`, `TestALifecycleOutcomeNeedsARow`; mutants: leader-before-unknown, rows-gate, dropped index. APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0134-1 | The archive-race proof blocks AFTER the span read and is judged at the FACT; readiness is ONE answer (/readyz = cerbix_ready) and the wedge is terminal-error-only with lag explicitly excluded. | DONE | `TestArchiveFencesAnInFlightRepairBatch` (late barrier, fact-first quiescence), `TestServiceWedgedGatesReadinessAndClearForgetsIt` (+consistency), `TestOldBackfillLagIsNotWedged`, `serviceWedgeReason`. APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0134-2 | §21 events: rejections and reason-attributed lifecycle outcomes are commit-independent counters (row-affected-gated); epoch fan-out and late arrivals live in a persisted per-kind aggregate bumped IN the owning transaction and exported by the sampler; gauges cover only index-bounded, saturating samples; the sampler and fact maintenance run off the dispatch loop with cancel+join lifecycles. | DONE | `ServiceEventsSink` + sites; `serviceStatsLoop`; `TestServiceEventCountersFireAtTheirSites`, `TestAdoptionReconcilesConcurrentUpdatesAtTheFence` and `TestAdoptionFenceIsBoundedAndAbortsSafely` (this row cited `TestPartitionAdoptionFencesConcurrentInserts`, a name that never existed — see [iter-0153](iterations/iter-0153.md) §2.5); [iter-0134](iterations/iter-0134.md). APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0133-1 | An archived maintenance window never takes effect beyond its cancel instant — a FUTURE window is cut at its own start — in the service axis AND every legacy suppression/SLI/rollup path; archive state is exposed through List/Get, the API and the SPA. | DONE | `maintEffectiveEnd`, archive CASE ([iter-0133](iterations/iter-0133.md) T1); `TestAnArchivedFutureWindowNeverTakesEffect`, `TestLegacySuppressionStopsAtTheCancelInstant`, `TestListExposesTheArchiveState`, SlaView reload test. APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| AC-0133-2 | Maintenance invalidation coalesces (one pending union range per service) and carries the window as origin; cancel-by-origin is proven at the store layer (single-origin only; ambiguous unions carry NULL origin, preserving §10.8); no production canceller for maintenance origins exists in phase 1 BY DESIGN (D-0162 closes the iter-0133 "yet": §10.8 forbids cancelling unfinished historical ranges, and archive already enqueues its own repair). Fact partitions are maintained monthly by the scheduler. Sampled gauges cover only index-bounded, saturating active-state counts and the zero-clamped watermark lag; rejections and reason-attributed lifecycle outcomes are commit-independent counters; epoch fan-out and late arrivals live in the persisted per-kind aggregate; a terminally parked error range fails readiness per §21 and a deposed leader clears its gauges and verdict. | DONE | iter-0133 T2–T4; `TestMaintenanceMutationsCoalesceAndCarryTheirOrigin`, `TestServiceFactPartitionsAreMaintained`, `TestServiceReliabilityStatsSnapshot`, `TestServiceReliabilityGauges`. APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| DoD-0133 | §15.4 acceptance concurrency tests committed (service-owner no-deadlock proven, monitor direction documented); NO TRANSACTION migrations crash-resumable with a re-execution test; derived slugs cannot overflow the shape; declaration/delete/archive audited in-tx; ServiceDetail is one repeatable-read snapshot. Closed-by-inspection items carry their evidence. | DONE | iter-0133 T5–T8, §3, §5. APPROVED, reviews [157] (round 1/2, zero findings) + [160] (final 2/2). |
| DoD-0132 | The accumulator's commit appendix runs under ITS OWN production envelope (fresh deadline, net derived from it); iteration reports are self-contained evidence (tail deadline synced to 100ms, dev-test result recorded verbatim). | DONE | [iter-0132](iterations/iter-0132.md); `TestConsecutiveStatementsCannotAccumulate` commit half. APPROVED, review [123] round 2/2. |
| AC-0131-1 | The claim fixes ONE absolute bound before BEGIN (min(caller deadline, 100ms cap)); the client net derives from it, deadlineTx receives the same instant, and a BEGIN returning past it cannot claim. | DONE | `claimRepairRangeBounded` ([servicerepair.go](../internal/store/servicerepair.go)); `TestAClaimCannotOutliveItsBound` (delayed-Begin dbConn, both stall shapes; kills the root-ctx re-mint mutant). APPROVED, review [123]. |
| AC-0131-2 | An undersized tail runs NEITHER repair nor forward — the guard sits at the RunServiceSlice boundary, so "false" from the repair slice keeps its one meaning (empty queue) and forward cannot jump a pending backlog. | DONE | [leadership.go](../internal/store/leadership.go); `TestAnUndersizedTailRunsNeitherRepairNorForward` (kills the guard-inside-repair mutant). APPROVED, review [123]. |
| DoD-0131 | Every timing assertion passes at budget + max_scheduling_tolerance FLAT; measured environment overhead is a logged diagnostic, never part of a pass threshold; the pack holds over five -race runs. | DONE | [iter-0131](iterations/iter-0131.md) §3, §5. APPROVED, review [123]. |
| AC-0130-1 | Persistence — token, cursor, watermark, lease, and the COMMIT itself — runs through the remainder-derived envelope on all three slice paths; the preview's persistence is O(1) statements via one unnest insert. | DONE | `persistPhase()` + bounded `Commit` in [`deadlinetx.go`](../internal/store/deadlinetx.go); `TestForwardPersistenceIsBoundedAndTheSessionSurvives`, `TestPreviewPersistsItsTokenInsideTheReserve` at normative thresholds ([iter-0130](iterations/iter-0130.md) §2). APPROVED, review [123]. |
| AC-0130-2 | The repair lifecycle — claim, release, complete, fail, park — is bounded BY THE SLICE on the leader path (min(slice deadline, 100ms cap), refused once spent; standalone callers mint the cap), and a missed release falls back to the lease with a durable-cursor resume. | DONE | `claimRepairRangeBounded`, `boundedLifecycleExec`; `TestABlockedClaimIsBoundedOnTheLeaderSession`, `TestABlockedReleaseFallsBackToTheLease`, `TestAFullSliceBlockedAtTheLifecycleWriteHoldsCadence` (flat budget + 25ms on a full RunServiceSlice), `TestALifecycleWriteIsCappedUnderAGenerousDeadline`. APPROVED, review [123]. |
| DoD-0130 | Timing assertions are normative (budget + max_scheduling_tolerance, environment overhead MEASURED per run, never a fixed allowance); mutant claims match what the committed suite proves, hangs counted as kills, unkillable hybrids and belt-and-braces named honestly. | DONE | [iter-0130](iterations/iter-0130.md) §3–4; `TestSlicePathsPersistThroughTheEnvelope` (labelled structural). APPROVED, review [123]. |
| AC-0129-1 | Server bounds are derived from the remainder before every statement, the client deadline is a net behind them, and no slice — forward, repair or preview — can exceed its budget plus tolerance or take the leader's connection with it. | DONE | [`deadlinetx.go`](../internal/store/deadlinetx.go); `TestAPreviewBlockedOnTheMembershipLockIsBounded` (rejects context.DeadlineExceeded by name), `TestForwardSliceCadenceHoldsWhileBlocked` ([iter-0129](iterations/iter-0129.md) §2). Awaiting review. |
| AC-0129-2 | The preview budget covers lock→resolve→gate→project→persist→commit with a persist reserve, so exhaustion still writes the approximate answer down. | DONE | `TestPreviewPersistsItsTokenInsideTheReserve` (blocked projection, committed token, honestly unconfirmable). |
| AC-0129-3 | The affected set is interval-aware: only declaration validity overlapping the requested range counts, and a service joins a past range's set only by first-adoption backfill. | DONE | `TestAffectedSetIsIntervalAware` (ended-before / straddling / began-after); the rebuilt joins-the-set and monitor-binding tests. |
| DoD-0129 | Claims match runs; limits stated; mutants verified to compile before counting. | DONE | `-race` 33 pkgs hypertable + 4 pkgs plain postgres; lint 0; dev-test 40/1; four iter-0128-shape mutants killed, the cushion hybrid recorded as equivalent-with-adapter ([iter-0129](iterations/iter-0129.md) §5). |
| AC-0128-1 | Both sides of a preview reduce the EXACT requested range — clipped per bucket, ungoverned time counted UNKNOWN — and each side conserves to the range's length on both axes. | DONE | `projectBothSides` in [`servicemaintenance.go`](../internal/store/servicemaintenance.go); `store.TestASubMinuteRetroactiveWindowStillNeedsAPreview` (conservation to 15s, excluded→good on annul), `TestPreviewConservesAcrossUngovernedTime` ([iter-0128](iterations/iter-0128.md) §2). Awaiting review. |
| AC-0128-2 | The preview's wall budget bounds the whole operation: per-service wall checks, savepointed projections with remaining-derived bounds, exhaustion delivering the approximate token rather than a 500. | DONE | `store.TestABlockedPreviewDegradesInsteadOfHoldingTheProject` (real table-lock block, 2 services, bounded time, lock released). The wall-skip alone has no independent mutant kill — recorded in [iter-0128](iterations/iter-0128.md) §5, not claimed. |
| AC-0128-3 | A service slice can neither poison nor DESTROY the leader's session: the client deadline is a cushioned net behind the server bounds, proven on the error path of a real LeaderSession with release-and-reacquire. | DONE | Cushions in [`servicematerialize.go`](../internal/store/servicematerialize.go)/[`servicerepair.go`](../internal/store/servicerepair.go); the round-3 fix raced client cancel against server timeout and lost the lock-owning CONNECTION ([iter-0128](iterations/iter-0128.md) §3). |
| AC-0128-4 | The preview is honest about why it cannot answer: retention fails closed at preview time (422 + earliest repairable), and `projected:false` carries a bounded reason through store, OpenAPI and SPA. | DONE | `TestRetroactiveMutationBeyondRawFailsClosed` (moved forward to the preview); reason enum in [`openapi.yaml`](../openapi.yaml) and [`SlaView.vue`](../frontend/src/views/SlaView.vue). |
| AC-0128-5 | The SPA never renders one project's data under another's name: load, mutation and preview flows are generation-guarded on success, error and finally, with a committed component test for the deferred race in both directions. | DONE | [`SlaView.spec.ts`](../frontend/src/views/SlaView.spec.ts) (late list, late mutation error). |
| DoD-0128 | Claims match the runs made, and the limits are stated. | DONE | `-race` 33 pkgs hypertable + store/api/config/scheduler on plain `postgres:16-alpine`; lint 0; `make dev-test` 40/1; mutants vs iter-0127: five killed, one hybrid NOT killed and recorded; two non-compiling mutants caught and re-run ([iter-0128](iterations/iter-0128.md) §5). |
| AC-0127-1 | A retroactive mutation whose window lies entirely inside one bucket is gated, previewed and confirmable exactly like a bucket-aligned one — interval overlap, never boundary membership. | DONE | [`servicemaintenance.go`](../internal/store/servicemaintenance.go) `rangeIntersectsSealed`/`currentAggregate`; `store.TestASubMinuteRetroactiveWindowStillNeedsAPreview` walks create, annul and the tokened path ([iter-0127](iterations/iter-0127.md) §2). |
| AC-0127-2 | The projection, its storage, the API payload and the SPA all carry BOTH conserved axes, and each side of each axis conserves to the range exactly. | DONE | Migration [`00073`](../internal/store/migrations/00073_preview_health_axis.sql); `ReliabilitySplit` in [`openapi.yaml`](../openapi.yaml); `store.TestPreviewCarriesTheHealthAxis` asserts conservation on both sides of both axes. |
| AC-0127-3 | A preview cannot hold the project: the projection has a wall budget and SET LOCAL statement bounds, and exhaustion degrades to `approximate`, which is unconfirmable. Pre-upgrade tokens are expired by migration. | DONE | `previewWallBudget` + `TestPreviewDegradesToApproximateAtItsWallBudget`; 00073 expiry + `TestPreUpgradePreviewTokensAreDead`. |
| AC-0127-4 | No service slice can poison the leader's lock-owning session: timeouts are tx-local on the forward AND repair paths, verified on a real LeaderSession. | DONE | `SET LOCAL` in [`servicematerialize.go`](../internal/store/servicematerialize.go)/[`servicerepair.go`](../internal/store/servicerepair.go); `TestForwardSliceLeavesTheLeaderSessionUnpoisoned` runs real slices, then asserts `SHOW` defaults and a working leadership Check. |
| AC-0127-5 | Starting an era advances the driver's cursor atomically; a recompute over destroyed evidence fails closed with the sealed facts untouched and the range parked terminally; config caps are rejected fail-fast, not reinterpreted. | DONE | `TestStartingAnEraAdvancesTheCursorWithIt` (product path, no fixture-side fix), `TestRecomputeOverDestroyedEvidenceFailsClosed`, `config.TestServiceCapsAreRejectedNotReinterpreted`. |
| DoD-0127 | Every fix is proven against the code it fixes, and every claim in this file is backed by the run that was actually made. | DONE | All eight P0 mutants + two P1 mutants re-applied against iter-0126 code and killed (batch order 3/3 on fresh databases); `-race` 33 packages on hypertable AND store/api/config/scheduler on plain `postgres:16-alpine` — stated as exactly that; `golangci-lint` 0 issues; `make dev-test` 40 pass / 1 known skip. Open items in [iter-0127](iterations/iter-0127.md) §5; FR-021 phase 1 stays IN_PROGRESS. |
| AC-0126-1 | Round 2's nine P0s are fixed, and each carries a regression that fails on the code as it stood. | DONE | Migrations 00070–00072; [iter-0126](iterations/iter-0126.md) §2; every one re-applied as a mutant and killed (§4). |
| AC-0126-2 | An epoch describes the credential the monitor will actually USE: the shared write contract is row → refs → epochs, for every caller. | DONE | `updateMonitorTx`/`updateMonitorTxPreparedWithRefs` ([`monitors.go`](../internal/store/monitors.go)); `store.TestChangingACredentialRefOpensAnEpochDescribingTheNewCredential` asserts the snapshotted GENERATION, which only the refs table supplies. |
| AC-0126-3 | Deleting a monitor obeys the §15.1 matrix and destroys no retained maintenance provenance. | DONE | Ownership check in `retireMonitorFromServicesTx`; `maintenance_windows → monitors` FK dropped (00070); `TestDeletingAMonitorDeclaredByAFileOwnedServiceIsRefused`, `TestDeletingAMonitorKeepsItsMaintenanceProvenance`. |
| AC-0126-4 | A retroactive mutation is PREVIEWED — both sides, both axes, per service — with a token bound to the exact mutation including the window an annul names, and the mutation itself is audited with its actor. | DONE | `projectMutation` runs the same reducer the materializer runs; `TestPreviewProjectsBothSidesOfTheMutation` asserts the promise matches what the confirm produces; `TestAnAnnulTokenIsBoundToTheWindowItNames`, `TestARetroactiveMutationIsAuditedWithItsActorAndToken`. The flow reaches the SPA ([`SlaView.vue`](../frontend/src/views/SlaView.vue)). |
| AC-0126-5 | A service that declared silence can report again, and a referenced owner can still be deleted. | DONE | Contiguity ERA (00071) with the watermark computed inside it; column-list `ON DELETE SET NULL` (00070), reproduced against live PostgreSQL 16 before the fix; `TestAServiceThatDeclaredSilenceCanReportAgain`, `TestDeletingAReferencedOwnerClearsTheReferenceAndNotTheTenant`. |
| DoD-0126 | The pass is verified in both storage modes and on a live stack, and what it did NOT fix is stated. | DONE | `-race` 33 packages (hypertable) + store on a throwaway `postgres:16-alpine`; `golangci-lint` 0 issues; `npm run build` clean; `make dev-test` 40 pass / 1 known skip. Open P1s listed in [iter-0126](iterations/iter-0126.md) §6; FR-021 phase 1 stays IN_PROGRESS. |
| AC-0125-1 | A service declared through the shipped API MATERIALIZES without anyone asking twice: the declaration puts it on the driver's list and the leader slice produces sealed facts and a moving watermark. | DONE | `ensureMaterializationRowTx` + `AdvanceServiceMaterializationOn` + `LeaderSession.RunServiceSlice`, migration [`00067`](../internal/store/migrations/00067_service_materialization_driver.sql); [`servicereachability_internal_test.go`](../internal/store/servicereachability_internal_test.go) may name no materialization entry point and fails on the shipped code ([iter-0125](iterations/iter-0125.md) §2). |
| AC-0125-2 | Durable work survives its worker: an expired claim is reclaimable, a live one is not stealable, and the lease can never outlive the progress it protects. | DONE | Lease in `claimRepairRangeOn`, extended in the cursor's own transaction; `store.TestACrashedLeadersClaimIsReclaimedAfterItsLease` kills the backend mid-claim. |
| AC-0125-3 | A retroactive maintenance mutation is gated ON THE PRODUCT PATH by a token bound to that exact mutation, DELETE archives rather than destroys, and a confirmed change actually rewrites the sealed facts. | DONE | `CreateMaintenanceWindowChecked`/`PreviewMutation`/`Archive`/`Annul` wired into [`handlers_sla.go`](../internal/api/handlers_sla.go); `store.TestAPreviewTokenOnlyAuthorizesTheMutationItWasIssuedFor`, `TestConfirmedAnnulActuallyRewritesTheSealedFacts`, `api.TestRetroactiveMaintenanceIsGatedOnTheProductPath`. |
| AC-0125-4 | Evaluation semantics never move without an epoch: the linearization point holds for the API, the file provider and secret rotation alike. | DONE | Bump moved into `updateMonitorTxPrepared`; rotation fence opens epochs (the secret generation is in the snapshot); `store.TestAFileProviderApplyOpensAnEpochJustLikeTheAPIDoes`. |
| AC-0125-5 | A service configuration change can never break core monitoring, and routing can never point across a tenant boundary. | DONE | Fan-out/member/project caps refused at the write (`enforceDeclarationBoundsTx`); composite owner FKs (migration [`00069`](../internal/store/migrations/00069_service_owner_tenancy.sql)); `store.TestExceedingTheFanOutCapIsRefusedAtTheDeclarationNotAtIngest` asserts the write is refused AND the monitor still ingests. |
| DoD-0125 | Every P0 fix carries a regression that fails on the shipped code. | DONE | All twelve shipped behaviours re-applied as mutants and killed; `-race` 33 packages in both storage modes; `golangci-lint` 0 issues; `make dev-test` 40 pass / 1 known skip ([iter-0125](iterations/iter-0125.md) §5). P1 findings explicitly NOT claimed (§6). |
| AC-0124-1 | A service carries TWO axes: a `definition_revision` (what a human declared availability to mean) and an `evaluation_epoch` (what the system was measuring). A fact references the epoch only, and every revision transaction creates its matching epoch unconditionally. | DONE | [`internal/store/services.go`](../internal/store/services.go) `putServiceDeclarationTx`, [`serviceepochs.go`](../internal/store/serviceepochs.go), migration [`00064`](../internal/store/migrations/00064_service_reliability.sql); `store.TestAddingToContextDoesNotChangeTheSLI` asserts identical snapshot hash AND a distinct epoch AND `revision_count == epoch_count` ([iter-0124](iterations/iter-0124.md) §3). |
| AC-0124-2 | A declaration is prospective: `effective_at = ceil_to_bucket(created_at)` from the DB clock, so sealed history keeps the meaning it was measured with. Only the FIRST revision may be retroactive, and its result is labelled a declared reconstruction. | DONE | `DeclarationOptions.BackfillFrom` (first revision only, floored) in [`services.go`](../internal/store/services.go); API `400 retroactive_not_first_revision`; the wording ships in [`ServiceDeclarationView.vue`](../frontend/src/views/ServiceDeclarationView.vue). |
| AC-0124-3 | `sealed_through` is contiguity-defined — a hole holds it rather than being jumped over — and it is a first-class read surface on both the list and the detail screen. | DONE | `stops` CTE in [`servicematerialize.go`](../internal/store/servicematerialize.go); `ServiceSummary.SealedThrough` on the list endpoint; lag rendering + suppression below the normal seal cadence in [`lib/services.ts`](../frontend/src/lib/services.ts) with 8 unit tests. |
| AC-0124-4 | Phase 1 ships NO availability number. SLO, error budget, burn rate and coverage are absent rather than zero, in the API and in the UI. | DONE | `reliability` is a present-and-null key ([`handlers_services.go`](../internal/api/handlers_services.go)); `api.TestPhase1PayloadCarriesNoReliabilityNumbers` guards the serialized bytes; `e2e/tests/services.spec.ts` asserts no leaf element renders a percentage. |
| AC-0124-5 | A service is declarable as code: bundle **format 2** reconciles services beside the monitors they name, absence orphans rather than deletes, and a file-owned service is read-only through the UI (409, ownership persists while orphaned). | DONE | [`internal/fileprovider/service.go`](../internal/fileprovider/service.go), [`store/fileapply_services.go`](../internal/store/fileapply_services.go), migration [`00066`](../internal/store/migrations/00066_managed_services.sql); `store.TestUIDeclarationOnAFileOwnedServiceIsRefused`, `TestDeletingAFileOwnedServiceIsRefusedEvenWhenOrphaned`, `TestFormat1BundleDoesNotOrphanServices`. |
| DoD-0124 | Phase 1 is verified in both storage modes and on a live stack, and the deferred scope is stated rather than approximated. | DONE | `-race` **33 packages** green (hypertable) and store+scheduler+reliability green on a throwaway `postgres:16-alpine`; `golangci-lint run ./internal/...` **0 issues**; `npm run build` + 25 vitest green; `make dev-test` **40 passed / 1 known skip**; deferred list in [iter-0124](iterations/iter-0124.md) §8. |
| AC-0123-1 | The runbook documents carrier generations as three distinct things — rolling forward, the existential emission gate, and dropping support in a future release — without claiming an operator action that does not exist or a causality the code does not have. | DONE | [`runbook.md`](runbook.md) §"Carrier generations"; the emission gate verified against [`scheduler.go`](../internal/scheduler/scheduler.go) and the consumer rule against [`amqp.go`](../internal/dispatch/amqp.go) ([iter-0123](iterations/iter-0123.md) §4). |
| AC-0123-2 | The drain procedure is executable end to end: symmetric across `pull_jobs` and `pull_tests`, distinguishing live from expired, naming the hourly housekeeping tick, and ending in the same zero-proof migration `00063` enforces. | DONE | Runbook steps 1–5; every published SQL statement executed against the live dev schema before publication ([iter-0123](iterations/iter-0123.md) §5). |
| DoD-0123 | The documentation gap left by iter-0122's Agent C role is disposed of in a canonical iteration report with atomic status items, not in a commit message. | DONE | [iter-0123](iterations/iter-0123.md); iter-0121 and iter-0122 stand as written. |
| AC-0122-1 | A partial monitor update that omits the credential slot preserves the stored credential for BOTH shapes — an inline write-only value and a `*_ref` reference — and never diverges the config from `monitor_secret_refs`. | DONE | Stored reference restored inside the update tx before bindings ([`internal/store/monitors.go`](../internal/store/monitors.go) `storedCredentialTx`); domain rule `SurfaceAPIUpdate` ([`credsettings.go`](../internal/domain/credsettings.go)); regressions `store.TestPartialUpdateKeepsTheStoredCredential` and `TestPartialUpdateKeepsAStoredReference`, both verified to fail when reverted ([iter-0122](iterations/iter-0122.md)). |
| AC-0122-2 | Executor credential readiness degrades on a PERSISTENT key mismatch, identically for AMQP jobs, AMQP test-RPC and pull agents; only "no keyring at all" is immediate, a future envelope generation never degrades, and a success clears the streak. | DONE | One tracker in [`internal/dispatch/health.go`](../internal/dispatch/health.go) shared by [`worker`](../internal/worker/worker.go), its cli test callback and [`agent`](../internal/agent/agent.go); `agent.TestCredentialHealthDegradesAndRecovers`, `agent.TestReadinessDegradesOnPersistentFailureNotTheFirst`, `worker.TestCredentialFailurePublishesTypedProbeError`; live degradation/recovery in `make secret-smoke`. |
| AC-0122-3 | A monitor the authoritative read finds disabled, deleted or non-dispatchable is SKIPPED: no warning, no failure counter, no backoff, and no entry in `cerbix_secret_resolution_failed_total`. | DONE | Early skip path in [`internal/scheduler/scheduler.go`](../internal/scheduler/scheduler.go); `scheduler.TestSkippedMonitorIsNotAFailure` asserts the metric sink directly and fails when the condition is reverted ([iter-0122](iterations/iter-0122.md) §5). |
| DoD-0122 | The round-2 findings against iter-0121 are fixed, and iter-0121's governance gaps are dispositioned in a new report rather than by editing an immutable one. | DONE | `go build`/`vet`/`gofmt` clean; `-race` 32 packages; store `-race` in both storage modes; golangci-lint `--new-from-rev=main` **0 issues**; `make secret-smoke` PASS; disposition and superseded claims in [iter-0122](iterations/iter-0122.md) §1. |
| AC-0118-1 | Non-production single, distributed, and geo stacks have explicit Make build/up/ready/test/down entrypoints; topology conflicts and missing broker-image state fail closed, and teardown never deletes named volumes. | DONE | Fixed-path lifecycle in [`Makefile`](../Makefile); separate fresh-volume pins in [`docker/.env.dev.example`](../docker/.env.dev.example) and [`docker/.env.geo.example`](../docker/.env.geo.example); static + expanded dry-run + network regressions in [`compose_wiring_test.go`](../internal/cli/compose_wiring_test.go); live single/distributed/geo proof in [iter-0118](iterations/iter-0118.md) (D-0158). |
| DoD-0118 | The Make facade is documented and verified without weakening the existing Go test/build targets or the explicit RabbitMQ upgrade boundary. | DONE | `go build`/`vet`/format/diff clean; `make race` green; golangci-lint v2.12.2 `0 issues`; both Compose configs valid; single twice 37 pass/1 intentional MaC skip, distributed 10/1, geo 11/11; README/runbook/traceability/decision synchronized in [iter-0118](iterations/iter-0118.md). |

## Functional Requirements

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| FR-001 | Provide CLI `cerbix serve --role all\|api\|scheduler\|worker\|agent` and `cerbix version`. | DONE | [`internal/cli/cli.go`](../internal/cli/cli.go), [`cli_test.go`](../internal/cli/cli_test.go) |
| FR-002 | Serve operational endpoints `/healthz`, `/readyz`, `/metrics`. | DONE | [`internal/httpsrv/server.go`](../internal/httpsrv/server.go), [`server_test.go`](../internal/httpsrv/server_test.go) |
| FR-003 | Load and strictly validate a YAML config (reject unknown keys, fail fast). | DONE | [`internal/config/config.go`](../internal/config/config.go), [`config_test.go`](../internal/config/config_test.go), [`docker/config.example.yaml`](docker/config.example.yaml) |
| FR-004 | OIDC login (any OpenID Connect issuer — Keycloak/Auth0/Okta/Google/Entra ID) + JIT user provisioning + sessions. | DONE | Provider-agnostic OIDC via discovery (`oidc.issuer`), Authorization Code + PKCE, JIT provision keyed by the OIDC identity `(issuer, subject)` — `UpsertUserByOIDCIdentity`, migration 00056 (D-0143); a legacy `oidc_sub`-only row adopts the current issuer once on next login; public `GET /auth/config` reports `{local, oidc, oidc_button_label}` so the SPA renders a provider-neutral sign-in button (D-0045). [`internal/auth/*`](../internal/auth/), [`handlers.go`](../internal/auth/handlers.go) (`ConfigHandler`), [`internal/store/sessions.go`](../internal/store/sessions.go), [`authflows.go`](../internal/store/authflows.go), mock-OIDC tests [`auth_test.go`](../internal/auth/auth_test.go), [`config_handler_test.go`](../internal/auth/config_handler_test.go) |
| FR-005 | Organization/Project model, membership, and role assignment. | DONE | [`internal/domain/domain.go`](../internal/domain/domain.go), [`internal/store/*`](../internal/store/), API [`internal/api/*`](../internal/api/) |
| FR-006 | Tenant isolation: reads/writes constrained to authorized org/project. | DONE | [`internal/authz/authz.go`](../internal/authz/authz.go), [`internal/api/handlers.go`](../internal/api/handlers.go), tests [`authz_test.go`](../internal/authz/authz_test.go), [`api_test.go`](../internal/api/api_test.go), [`store_integration_test.go`](../internal/store/store_integration_test.go) |
| FR-014 | Embedded schema migrations + `cerbix migrate` + auto-migrate on serve. | DONE | [`internal/store/migrate.go`](../internal/store/migrate.go), [`migrations/00001_init.sql`](../internal/store/migrations/00001_init.sql), [`internal/cli/cli.go`](../internal/cli/cli.go) |
| FR-015 | Default local login (username/password) alongside OIDC, same roles + sessions; bootstrap admin. | DONE | [`internal/auth/{password,local}.go`](../internal/auth/), [`internal/store/users_local.go`](../internal/store/users_local.go), [`migrations/00004_local_users.sql`](../internal/store/migrations/00004_local_users.sql), API `POST /api/v1/me/password` |
| FR-007 | Monitor CRUD + probers (http/tcp/icmp/dns/tls/grpc/websocket/ssh/push + postgres/mysql/redis/promql/rabbitmq/composite/synthetic) with a declarative condition engine. | DONE | [`internal/domain/monitor.go`](../internal/domain/monitor.go), [`internal/prober/*`](../internal/prober/), [`internal/store/monitors.go`](../internal/store/monitors.go), API [`internal/api/handlers_monitors.go`](../internal/api/handlers_monitors.go) |
| FR-008 | Scheduler (advisory-lock leader) + Dispatcher (inproc dev / AMQP prod, per-region queues) + worker pool + ingestion. | DONE | [`internal/scheduler`](../internal/scheduler/), [`internal/dispatch`](../internal/dispatch/), [`internal/worker`](../internal/worker/), [`internal/ingest`](../internal/ingest/). **Efficiency (iter-0031, D-0038):** the leader schedules from an in-memory monitor snapshot refreshed every 15 s (no per-second table scan) and detects push-timeouts via one batched `StalePushMonitors` query (no per-monitor N+1). |
| FR-009 | Heartbeat storage + SLA/SLI over rolling windows + maintenance windows. | DONE | Rolling 24h/7d/30d/90d uptime%, avg **and p95** latency (`percentile_cont(0.95)` on `up` rows, maintenance-excluded — D-0046), SLO error budget. [`internal/sla/sla.go`](../internal/sla/sla.go), [`internal/store/sla.go`](../internal/store/sla.go), [`migrations/00005_sla.sql`](../internal/store/migrations/00005_sla.sql), API [`internal/api/handlers_sla.go`](../internal/api/handlers_sla.go) |
| FR-010 | Notifications (Telegram/Slack/Email/webhook) on status change. | DONE | Per-project notification channels (**webhook/slack/telegram/email**) linked to monitors; on a monitor's down/recover transition the ingest pipeline sends a formatted message to each linked enabled channel via an async best-effort dispatcher ([`internal/notify`](../internal/notify/)). Webhook/Slack/Telegram POST; email via `net/smtp`. All four verified e2e (HTTP receiver + mailpit). **Reliability (iter-0029, D-0036):** transitions are enqueued to a transactional outbox in the same tx as the status change and delivered by a retry/backoff worker ([`internal/outbox`](../internal/outbox/)), so notifications survive restarts and transient failures. |
| FR-011 | Vue SPA (dashboards, monitor detail, members, incidents), served via Docker. | DONE | Full designed surface wired to the API and e2e-verified through the frontend nginx proxy, all built/run in Docker (D-0019): typed `openapi-fetch` client, Pinia `session`+`workspace`+`ui` stores, auth guard + org/project switcher; **Login (provider-neutral, `/auth/config`-driven — D-0045), Dashboard, Monitors, New-monitor, Monitor-detail(+pause/delete), Members, SLA & SLO, Incidents (list/open/detail+timeline+update+resolve+postmortem), Status pages (mgmt + components), the public `/status/:slug` render + RSS/Atom/JSON feeds, a consolidated Settings page (channels/tokens/webhooks) and a self-service org/project create dialog (D-0048), plus a topbar global search box (D-0047)** — every view rebuilt 1:1 against the approved design artifacts with bespoke inline-SVG charts (D-0049) ([`frontend/src/`](../frontend/src/)). **Live SSE (D-0056):** the dashboard and monitor-detail update status in realtime from `GET /api/v1/events`. **Per-control RBAC hiding (D-0057):** action controls (New/Edit/Delete/Invite/Declare/Settings mutations) hide or disable by the caller's effective role, so viewers/editors don't see controls that would 403. The designed SPA surface is complete. |
| FR-012 | Status pages + incidents/postmortems + subscribers/webhooks/feeds. | DONE | **Phase 1 (iter-0011): incidents core** — domain (status `investigating→identified→monitoring→resolved`, impact, source; resolved-terminal; postmortem-requires-resolved), migration `00006`, store (tx: create-with-opening-update, add-update syncs status/resolved_at), REST (`/projects/{id}/incidents`, `/incidents/{id}[/updates][/postmortem]`, editor+ writes, viewer reads, tenant-isolated) — postable via GUI **and** API; `cerbix_incidents_opened_total`. **Phase 2a (iter-0012): status pages + components + rendering** — org-level (opt. project) pages with visibility public/internal/unlisted; components tied to a monitor (status derived) or manual; public UNAUTHENTICATED render (`/api/v1/public/status-pages/{slug}`: public served, unlisted needs `?token=`, internal hidden 404) + authed member render assembling summary + per-component status + 90d uptime + active incidents. **Phase 2b-i (iter-0013): API tokens (service accounts)** — org/project-scoped tokens with a role, hashed at rest, issued/listed/revoked by org admins; `Authorization: Bearer` auth builds a scoped principal so machines post incidents/postmortems per the token's role (`source=api`), tenant-isolated. **Phase 2b-ii (iter-0014): auto-incident** — the ingest pipeline opens an incident (`source=auto`, linked to the monitor) when a monitor transitions to down, and auto-resolves it on recovery; no duplicate while one is open; counted in `cerbix_incidents_opened_total`. **Phase 2b-iii (iter-0015): status-page incident feeds** — JSON Feed v1 + RSS 2.0 + Atom of a page's incidents, served both authed (member) and public (same visibility gate as render: public served, unlisted needs `?token=`, internal hidden), `?format=rss|atom|json`. **Phase 2b-iv (iter-0016): outbound webhooks** — org/project-scoped webhook subscriptions; on every incident lifecycle event (opened/updated/resolved, from both the API handlers and the auto-incident pipeline) a signed (HMAC-SHA256 `X-Cerbix-Signature`) JSON POST is delivered best-effort by an async dispatcher. **Phase 2b-v (iter-0027): Keycloak client-credentials** — machine OAuth2 clients now authenticate via a second bearer path (issuer-trusted JWT, membership-gated), completing FR-013 (D-0034); the incidents/status Vue views were wired in FR-011. **Phase 2b-vi (iter-0029, D-0036): reliable delivery** — incident webhooks are enqueued to a transactional outbox in the same tx as the incident create/update and delivered by a retry/backoff worker (`FOR UPDATE SKIP LOCKED`, multi-replica-safe, dead-letter after max attempts), replacing the best-effort in-memory queue. **Phase 2b-vii (iter-0032, D-0039): dead-letter ops** — global-admin endpoints list and replay dead outbox events (`GET/POST /api/v1/admin/outbox/dead[...]`), so events that exhausted retries are inspectable and recoverable ([`internal/api/handlers_outbox.go`](../internal/api/handlers_outbox.go)). **Closed (iter-0035):** every status-page/incident view rebuilt 1:1 with the design artifacts (management + public render, timelines, postmortems); status pages are now editable and deletable from the UI (`PATCH`/`DELETE /status-pages/{id}`, org-admin, components cascade) and manual incidents can bind an optional affected monitor (D-0050). Backend follow-ups (public per-day availability buckets, public maintenance/incident-history, email subscribers, multi-component incident impact) are tracked as API-gap backlog, not FR-012 scope. |
| FR-013 | API tokens (service accounts) + OIDC client-credentials for API. | DONE | **Service-account tokens** (iter-0013): org/project-scoped, hashed at rest, `Authorization: Bearer cbx_…` → scoped principal ([`internal/store/apitokens.go`](../internal/store/apitokens.go), [`internal/api/handlers_apitokens.go`](../internal/api/handlers_apitokens.go)). **OIDC client-credentials** (iter-0027, D-0034; provider-agnostic per D-0043): a machine OAuth2 client's JWT is verified issuer+signature (audience relaxed, `ccVerifier`), JIT-provisions a user by `sub`, and resolves to a **membership-gated** principal ([`internal/auth/{auth,middleware}.go`](../internal/auth/), [`clientcreds_test.go`](../internal/auth/clientcreds_test.go)). Both converge on the same `authz.Can`. |
| FR-016 | Global tenant-scoped search across monitors, projects, and incidents. | DONE | `GET /api/v1/search` (min 2 chars) runs escaped-`ILIKE` lookups and filters every hit through `Principal.VisibleProject`, so users only see hits in orgs/projects they belong to; surfaced by a debounced topbar search box (D-0047). [`internal/store/search.go`](../internal/store/search.go), [`internal/api/handlers_search.go`](../internal/api/handlers_search.go), [`api_search_test.go`](../internal/api/api_search_test.go), [`search_internal_test.go`](../internal/store/search_internal_test.go), [`frontend/src/components/SearchBox.vue`](../frontend/src/components/SearchBox.vue) |
| FR-017 | Monitoring as Code: tenant-scoped file providers hot-reconcile versioned ProjectBundles into provider-owned monitors without service restart/reload; UI/API monitors coexist. | DONE | Contract-foundation slice (iter-0087): strict static `providers.file` config ([`internal/config/providers.go`](../internal/config/providers.go)) + pure ProjectBundle parser/canonicalizer/validator/planner ([`internal/fileprovider/`](../internal/fileprovider/)); no runtime DB apply yet. Ownership/persistence (iter-0088): migrations 00059 `file_provider_bundles` + 00060 `managed_monitors` (schema-enforced tenant safety via composite FKs; one owner per monitor); provenance model + atomic ownership guard ([`internal/store/managed.go`](../internal/store/managed.go)); typed write-path split (`updateMonitorTx` shared by API + future file apply); API `409 managed_by_file` ([`internal/api/handlers_monitors.go`](../internal/api/handlers_monitors.go)). Atomic bundle apply (iter-0089): `ApplyFileManagedBundle` ([`internal/store/fileapply.go`](../internal/store/fileapply.go)) — tenant resolve + `fileprovider.Plan` + create/update/dependency_update/noop/orphan/restore in ONE tx (monitors + deps + provenance + monotonic generation + `monitor_config_changed` NOTIFY, deterministic lock order, per-project atomic); D-0142 revision-safe (no-op/dep-only never bump), orphan-grace disable (never hard delete), restore reuses DB id + push token; canonical-hash/dep split per D-0146. Filesystem scan & grouping (iter-0090): bounded directory scan ([`internal/fileprovider/scan.go`](../internal/fileprovider/scan.go)) — eligible `.yaml`/`.yml` immediate children, symlink-escape rejection, `max_files`/`max_file_bytes`/`max_total_bytes` bounds; `GroupBundles` duplicate-project freeze + unbound→suspend-orphan (§9.1). Reconcile loop + HA + wiring (iter-0091): `internal/fileprovider/runtime` — per-provider advisory-lock leader (distinct key namespace, same-session liveness/stepdown), poll-watch with debounce + dirty-bit + mandatory resync + directory-replacement recovery, scan→group→`ApplyFileManagedBundle` with whole-project-disappearance orphan honoring `SuspendOrphan`; wired into `--role api`/`all` only ([`internal/cli/cli.go`](../internal/cli/cli.go) `startFileProviders`, fail-fast on missing DB/unreadable dir §4.1). Observability + provenance (iter-0092): `cerbix_file_provider_*` metrics ([`internal/metrics/metrics.go`](../internal/metrics/metrics.go)) wired into the reconcile loop; monitor responses carry the read-only `management` provenance block ([`internal/api/handlers_monitors.go`](../internal/api/handlers_monitors.go)); `orphan_grace_period: 0` now honored as immediate (spec §4.1 pointer fix); runbook HA-invariant + stale-success alert. **No-restart real-binary smoke** (`e2e/mac-smoke.sh`): live create → orphan-disable with the process never restarting (update/LKG/restore/id+token/scheduler-exec were checked interactively but are NOT in the committed script — pending iter-0095 §4). Diagnostics + failover (iter-0093): tenant-aware provider diagnostics API (`GET /api/v1/admin/file-providers` global-admin; `GET /api/v1/organizations/{orgID}/file-providers` org-admin) [`internal/api/handlers_fileproviders.go`](../internal/api/handlers_fileproviders.go); explicit 2-replica leadership-failover race test on real Postgres. UI (iter-0094): source filter + `Managed by file` badge + read-only controls; OpenAPI `management`/diagnostics + regen. Orphan-safety P0 fix (iter-0095): duplicate-project freeze + scan-error provider-wide suspend so invalid/rejected input NEVER orphans LKG (§9.1); TOCTOU byte re-check; `max_monitors_per_bundle` enforced. Leadership fencing (iter-0098): the apply tx runs on the leader's lock-owning connection (`LeaderSession`), so a lost lock aborts the in-flight apply — a former leader can never commit after losing leadership (§17); proven by `store.TestLeaderSessionFencing` (kill backend mid-tx → failover + commit fails). Bounded work (iter-0099): `max_managed_monitors` enforced under a provider-wide advisory lock; YAML depth/node/custom-tag policy; apply-tx `statement_timeout`/`lock_timeout`; process-wide reconcile-concurrency semaphore. Audit + persisted diagnostics (iter-0100): transactional `file_provider.apply` audit row in the apply tx (§9); rejected/errored attempts persist `status`/`last_error`/`attempted_at` (`RecordBundleAttempt`, §15); `last_success` advances only on a clean scan; repeated errors rate-limited. Runtime diagnostics surface (iter-0101): process-local `StatusRegistry` ([`internal/fileprovider/runtime/status.go`](../internal/fileprovider/runtime/status.go)) backs the global-admin diagnostics API with this process's live per-provider view — leadership, last scan, sticky last-success, managed/orphaned counts, last error — **including configured-but-idle providers** (registered before first reconcile); admin response is now `{bundles,providers}`, org response `{bundles}` (§15); OpenAPI + TS schema regenerated. Test matrix (iter-0102): event-storm/dirty-bit backpressure bound on the real `leaderLoop` (281 changes → 2 reconciles), two live `Provider.Run` replicas on real Postgres (single-leader + failover end-to-end), and the full real-binary lifecycle in [`e2e/mac-smoke.sh`](../e2e/mac-smoke.sh) (create → scheduler-exec → in-place update → last-known-good → orphan-disable → restore with same DB id + push token, no restart). P2 UI/API polish (iter-0103): named-provider source filter in the monitor-list UI (§15 `All`/`UI`/named provider) + `?provider=` on the diagnostics API; golangci-lint RUN (MaC packages 0 issues). Reopened by external review 2026-08-08; all items fixed + tested (iter-0104…0109) and closed at acceptance (iter-0110). P0 orphan-safety — absence-trust (`allowAbsence`) is now decided provider-wide BEFORE any apply; an ambiguous scan never orphans/disables a UID absent within a PRESENT project (§9.1, iter-0104). P1: `monitor_config_changed` now has a `ConfigNotifier`→scheduler snapshot-reload listener (§12, iter-0106); orphan/owned/counts errors degrade the reconcile (§16, iter-0104); restore-within-grace with unchanged config no longer bumps revision (§10, iter-0105); the whole apply runs under a 60s deadline (§17, iter-0107); a semantic no-op writes NOTHING and `noop` outcome is reachable (§7/§17, iter-0105); audit fires on dependency-only mutation and `RecordBundleAttempt` is fenced on the leader connection (iter-0105). Enumeration/parse hardening: bounded `ReadDir`+fingerprint, startup readability probe, EvalSymlinks overlap, YAML alias policy, runtime DTO scope fields (iter-0107). Coverage: promtool-validated alert rules + Go guard, browser E2E spec + dev-stack wiring (iter-0108). **Second review (2026-08-08) — six further P1 refinements FIXED + tested (iter-0109):** restore compares actual-vs-desired `enabled` (declarative `enabled:false` no longer bumps revision); bundle diagnostics live on an axis independent of monitor state (stale status cleared on a clean no-op without a generation bump, first empty bundle recorded @gen 1, empty-bundle rename refreshes path); a counts-lookup failure now yields `outcome=error` with gauges preserved; every configured provider registers a zero leader gauge pre-election so the NoLeader alert can fire; an invalid file at a known path marks that bundle row rejected (fenced by-path); audit fires on any persisted state change (D-0148). **Acceptance close-out (iter-0110): DONE** — all §19 criteria evidenced ([iter-0110](iterations/iter-0110.md)); `go test -race ./...` 32 pkgs green; `e2e/mac-smoke.sh` full lifecycle (incl. config-change propagation) PASS; browser E2E `e2e/tests/file-providers.spec.ts` executed 3/3 green on a current-build throwaway stack (diagnostics contract + badge/read-only/named-provider filter); golangci-lint no findings in MaC source; alert rules promtool-validated. Spec [`func-monitoring-as-code.md`](specs/func-monitoring-as-code.md), D-0145/D-0146/D-0147/D-0148. **Third review (2026-08-12) — hardening pass, 7 findings FIXED + tested + independently re-reviewed, re-closed at [iter-0113](iterations/iter-0113.md):** (P0) DB pool sized so file-provider leaders can't starve the reconcile pool ([`internal/store/store.go`](../internal/store/store.go), `pgx`-parser operator-cap detection + DB-gated progress regression); (P1) monitor `target` rejects URL userinfo + known secret query keys + malformed URL-shaped targets (D-0152); (P1) leader loop samples the directory fingerprint BEFORE reconcile so a mid-reconcile write isn't absorbed until resync (§17); (P1) absence is trusted only within the current scope — a same-name scope change quarantines prior-scope rows instead of orphaning them (`ProviderScopeConfig.Includes`, D-0153); (P1→P0) a tenant-bound invalid bundle freezes per-project by its prior last-known-good PATH (`presentPaths` vs `cleanlyApplied`), never orphaning the real prior tenant on a header typo/duplicate/over-limit/apply-error, with duplicate-by-claim + bind-before-policy; (P2) leadership advisory key is FNV-1a-64 namespaced with a startup collision guard (§12); (P2) the warn-throttle map is a bounded LRU plus a fixed-window emission budget (§16). Every destructive-case regression verified to fail when its fix is reverted; DB-backed suites `-race` on both hypertable + declarative storage. Governance/DoD correction in [iter-0114](iterations/iter-0114.md) (iter-0113 left immutable at its close snapshot): changed/MaC scope meets DoD (code approved, `-race` both modes green, `golangci-lint` on MaC packages 0 issues, `e2e/mac-smoke.sh` PASS); the **repo-wide** `golangci-lint run ./...` is RED (exit 1) on 42 pre-existing findings in untouched files, **owner-waived for this branch and tracked as a dedicated cleanup MR (D-0154)** — recorded, not silently marked green. DoD satisfied for the hardening scope. |
| FR-018 | Delete a project: an org admin permanently removes a project and everything it owns (DB cascade — monitors & history, incidents, SLA, escalation/on-call, channels, project-scoped tokens/webhooks/status pages); irreversible, type-slug confirm, refused for file-provider-managed projects. | DONE | Spec [`func-project-deletion.md`](specs/func-project-deletion.md), D-0150. `DELETE /api/v1/projects/{projectID}` → [`internal/store/projects.go`](../internal/store/projects.go) (`DeleteProject` — tenant-scoped single-tx cascade + `409 managed_by_file` guard), [`internal/api/handlers.go`](../internal/api/handlers.go) (`deleteProject`, org-manage/global), [`frontend/src/components/settings/DangerZonePanel.vue`](../frontend/src/components/settings/DangerZonePanel.vue) + [`stores/workspace.ts`](../frontend/src/stores/workspace.ts). Tests: `store.TestDeleteProject` (both DB modes), `api.TestDeleteProjectAuthz`, E2E [`e2e/tests/project-delete.spec.ts`](../e2e/tests/project-delete.spec.ts). |
| FR-019 | Delete an organization: a global admin permanently removes an org and everything under it (DB cascade — all projects & their subtrees, memberships, org-level status pages, org-scoped tokens/webhooks); irreversible, type-slug confirm, refused for orgs owning file-provider-managed projects; `org.delete` audited at instance level (org_id NULL). | DONE | Spec [`func-org-deletion.md`](specs/func-org-deletion.md), D-0151. `DELETE /api/v1/organizations/{orgID}` → [`internal/store/organizations.go`](../internal/store/organizations.go) (`DeleteOrganization` — single-tx cascade + `409 managed_by_file` guard), [`internal/api/handlers.go`](../internal/api/handlers.go) (`deleteOrganization`, global-admin gate first), [`frontend/src/components/settings/OrgDangerZonePanel.vue`](../frontend/src/components/settings/OrgDangerZonePanel.vue) + [`stores/workspace.ts`](../frontend/src/stores/workspace.ts). Tests: `store.TestDeleteOrganization` (both DB modes), `api.TestDeleteOrganizationAuthz`, E2E [`e2e/tests/org-delete.spec.ts`](../e2e/tests/org-delete.spec.ts). |
| FR-020 | Project secret inventory: named write-only credentials (create/edit=rename+rotate/delete) referenced by monitors as `password_ref`; unlocks credentialed monitor types (postgres/mysql/redis/rabbitmq) for Monitoring-as-Code bundles and UI monitors. | DONE | **Re-accepted 2026-08-15 in [iter-0120](iterations/iter-0120.md) after the r7 amendment.** A conformance audit reopened this with three blockers + one P1 ([iter-0119](iterations/iter-0119.md)); the gen3 review then found three more P0s inside that slice; all are fixed with regressions verified to fail when reverted. The amended §9 matrix is fully evidenced, including the parts iter-0119 §8 named as missing: a live auth-less-target fail-open pin (mutation-proven — restoring the hole makes a credential-stripped probe report UP), live generation-3 emission with a mixed-capability barrier, and a browser gate for the Secrets panel. FR-020 now runs enabled in the dev stack: `make dev-test` 38 passed / 1 documented skip. Spec [`func-secret-inventory.md`](specs/func-secret-inventory.md), D-0155, foundation [iter-0115](iterations/iter-0115.md), prior acceptance [iter-0116](iterations/iter-0116.md) (stands as written, superseded — its §9 matrix did not require the tests that would have caught these). Delivered end to end: tenant-scoped inventory/API/UI with transactional audit and reference guards; one domain-owned strict settings gate; normalized `monitor_secret_refs` in API and atomic MaC writes; ciphertext-only reads/snapshot; authoritative bounded materialization; per-region AAD envelopes over physically separated AMQP/pull v2 jobs and tests; capability gating; typed diagnostic-only `probe_error`; rotation fencing and convergent exact-CAS re-encryption; encrypted-by-default PostgreSQL/MySQL/Redis/RabbitMQ target transport; monitor-form reference selector/detail diagnostics; low-cardinality metrics, readiness and promtool-validated alerts. `e2e/secret-inventory-smoke.sh` proves a real DB-less pull agent wrong-key degradation/recovery and PostgreSQL probe. Full `-race` and store `-race` in both storage modes, build/vet/frontend, and branch-new lint are green. |
| FR-021 | Service reliability: an operational unit whose reliability is EXPLICITLY DECLARED — a definition revision (what availability means) and an evaluation epoch (what was measured) as two axes, duration-weighted facts, a contiguity-defined seal, and declarability as code. **CLOSED at iter-0153 under D-0169:** the requirement closes against the discharge map — all 91 acceptance invariants (§19, §16.8) and all 24 §16.10 scenarios named to a test, gated by `make docs-check`; §16.9 (service incidents, escalation policies for services, retroactive alerting, cross-project delegation, per-member severity, suppression beyond the three named topics) is an explicit NON-GOAL of FR-021 and opens its own requirement when commissioned, not a reopening of this one. | DONE | Spec [`func-service-reliability.md`](specs/func-service-reliability.md) (r8 APPROVED, 91 invariants after the phase-5 amendment), D-0159, approved mock [`design/mock-service-reliability.html`](design/mock-service-reliability.html). **Phase 1 (iter-0124):** pure evaluator [`internal/reliability`](../internal/reliability/); evaluation-semantics projection [`domain/execsemantics.go`](../internal/domain/execsemantics.go); schema + declarations/epochs/ingest/materialize/repair/maintenance [`internal/store/service*.go`](../internal/store/) with migrations 00064–00066; project-unique immutable monitor slug (00065); bundle **format 2** [`internal/fileprovider/service.go`](../internal/fileprovider/service.go); REST [`internal/api/handlers_services.go`](../internal/api/handlers_services.go); SPA [`ServicesView`](../frontend/src/views/ServicesView.vue) / [`ServiceDetailView`](../frontend/src/views/ServiceDetailView.vue) / [`ServiceDeclarationView`](../frontend/src/views/ServiceDeclarationView.vue); E2E [`e2e/tests/services.spec.ts`](../e2e/tests/services.spec.ts). **Phase 2 (DONE — compute core at [187], API surface at [213], the UI at [231]):** availability over a window, error budget, burn rate, decidable coverage, reliability timeline, segments/provenance screens — all read from sealed facts only, which is why the watermark shipped first ([iter-0124](iterations/iter-0124.md) §8); the SPA surface is [`ServiceReliability.vue`](../frontend/src/components/ServiceReliability.vue) + [`ReliabilityStrip.vue`](../frontend/src/components/ReliabilityStrip.vue) ([iter-0144](iterations/iter-0144.md), [iter-0145](iterations/iter-0145.md)).. **Phase 3 (DONE — the impact graph and incident correlation, D-0166):** migration 00080 + [`store/servicegraph.go`](../internal/store/servicegraph.go) / [`store/serviceimpact.go`](../internal/store/serviceimpact.go) (same-project DAG outside the declaration axes, `graph_generation` CAS with typed audit, the one-transaction correlation attempt with reachable-endpoint bounded witnesses, the whole-lifecycle outbox claim fence); [`api/handlers_servicegraph.go`](../internal/api/handlers_servicegraph.go) + the authenticated-detail impacts (openapi 0.20.0); SPA [`ServiceDependencies.vue`](../frontend/src/components/ServiceDependencies.vue) + the 🕸 chips in [`IncidentDetailView.vue`](../frontend/src/views/IncidentDetailView.vue), against the approved mock [`design/mock-service-impact.html`](design/mock-service-impact.html) ([iter-0146](iterations/iter-0146.md), [iter-0147](iterations/iter-0147.md), [iter-0148](iterations/iter-0148.md)). **Phase 4 (DONE — the status-page service projection and the composite lifecycle, D-0167):** migration 00081 + [`store/statusprojection.go`](../internal/store/statusprojection.go) / [`store/componentconversion.go`](../internal/store/componentconversion.go) / [`store/monitorretire.go`](../internal/store/monitorretire.go) / [`store/compositeconvert.go`](../internal/store/compositeconvert.go) (a third component source under ONE active discriminator with dormant bindings kept for revert; the SLI layer alone projected into the public vocabulary plus `no_data` outside the severity ladder; sealed-facts-only 90-day strips ending at `sealed_through`; previewed reversible conversion under a two-token structural CAS; retire as one transaction over both the lifecycle statement and the execution switch); [`api/statusprojection.go`](../internal/api/statusprojection.go) — ONE resolver shared by the public page and the preview, which is what keeps a preview from predicting a page the server would not produce; SPA per the approved mock [`design/mock-status-projection.html`](design/mock-status-projection.html) ([iter-0150](iterations/iter-0150.md)). Impact links stay NON-PUBLIC. **Phase 5 (DONE — alerting ownership, D-0168):** migration [`00082`](../internal/store/migrations/00082_alerting_ownership.sql) (owns_paging/page_on/page_on_unknown/confirm_evaluations with DB-owned `alert_config_generation`; the normalized per-rule burn latch; durable episodes with an IMMUTABLE recipient snapshot, a target SNAPSHOT id and the composite tenant FK with column-list `ON DELETE SET NULL`); [`store/servicealerteval.go`](../internal/store/servicealerteval.go) + [`store/serviceburneval.go`](../internal/store/serviceburneval.go) (bounded slices, one snapshot, verdict+episode+outbox in ONE transaction, both FENCED to the leader's lock-owning connection and LINEARIZED against config writes); [`store/alertdelegation.go`](../internal/store/alertdelegation.go) (the arming conjunction — any HOLD dis-arms, a latch for EVERY declared rule); [`store/servicealertlifecycle.go`](../internal/store/servicealertlifecycle.go) (the ONE closing path, from the episode's own snapshot, through the fenced enqueue owner); [`store/servicealertpolicy.go`](../internal/store/servicealertpolicy.go) + [`store/fileapply_alerting.go`](../internal/store/fileapply_alerting.go) (the write surface and the MaC apply, the declaration deliberately OUTSIDE the canonical hash so a paging edit bumps no revision and no epoch); [`api/handlers_servicealerting.go`](../internal/api/handlers_servicealerting.go); [`metrics`](../internal/metrics/metrics.go) §16.6b with scheduler-only readiness; SPA [`ServiceAlerting.vue`](../frontend/src/components/ServiceAlerting.vue) (server coverage re-read on a cadence, and a FAILED refresh stops claiming it rather than leaving a stale green badge) + the delegated-monitor surface in [`MonitorDetailView.vue`](../frontend/src/views/MonitorDetailView.vue), against the approved mock [`design/mock-alerting-ownership.html`](design/mock-alerting-ownership.html); changed-scope browser gate [`e2e/tests/alerting.spec.ts`](../e2e/tests/alerting.spec.ts) ([iter-0151](iterations/iter-0151.md)). |
| FR-026 | An incident write is audited: every incident mutation made by a PRINCIPAL — a session user or an API token — writes exactly one `audit_logs` row in the SAME transaction as the mutation (manual create, the Alertmanager receiver's create and resolve, a status change, a note, an acknowledgement, a postmortem create and update), carrying the organization resolved from the incident's project, the actor triple, an action from a closed five-word vocabulary and a target of ids and transitions. Machine writes are excluded BY DECISION and enumerated. | DONE | [`func-incident-audit.md`](specs/func-incident-audit.md) — design APPROVED at revision 4 (2026-09-01, D-0214) after three review rounds; **IMPLEMENTED at iter-0168 (2026-09-03, D-0219)**. Every principal incident write — the manual create, the Alertmanager receiver's create and resolve, a status change, a note, the first acknowledgement, a postmortem create and update — writes one `audit_logs` row inside the mutating transaction, through a door that takes the actor as a required parameter (`…ByPrincipal`), with the machine doors (`…BySystem`) kept for the nine enumerated machine writers and held by two fixture-driven AST guards. The sixteen §6 invariants are discharged in the FR-026 map in [`traceability.md`](traceability.md), compared as a SET by `make docs-check`. Closes the gap D-0171 named and iter-0156 §2.8 proved. **Amended at iter-0172 (§10, D-0233):** the three monitor writers the API reaches are audited by the same rules — `monitor.create`, `monitor.update`, `monitor.delete`, the validated actor, one row in the transaction, no exported unaudited writer — closing the audit-trail clause FR-029's invariant 13 had left `PARTIAL`; the file provider's machine writes stay unaudited by D1. |
| FR-028 | A credential inside a synthetic monitor's scenario is secret at rest, on read and in the record, in three stages: (0) no probe result carries a request URL — the failure is named by class and host; (1) the scenario is encrypted at rest, covered by rotation and an idempotent backfill, and withheld from a principal who cannot write the monitor; (2) a DECLARED secret inside a scenario is a NAMED BINDING resolved from the project inventory and reaches the executor in the envelope, and a literal is refused wherever a literal is detectable — by header NAME, not by value; a credential in an unlisted header or a body stays legal and is covered by stages 0 and 1 only. | DONE | [`sec-synthetic-secrets.md`](specs/sec-synthetic-secrets.md) — design APPROVED by the reviewer at revision 3 and amended by the owner's blast-radius ruling (D-0216, 2026-09-02) after three review rounds (1 P0 + 5 P1 + 2 P2, then 4 P1 + 2 P2, then 3 corrections and two blockers). **Stage 0 is DONE**: `internal/prober/failure.go` composes every probe failure from a bounded class plus a host, applied to `http`, `promql`, `synthetic` and the RabbitMQ management probe, with the invariant asserted per type through the real Runner (`TestProbeResultNeverCarriesTheRequestURL`) and the vocabulary pinned (`TestFailureClassNamesTheCause`). **Stage 1 is DONE**: the one secret set became three classifications
(`EncryptedMonitorConfigKeys`, `WriteOnlyMonitorConfigKeys`, `WriterOnlyMonitorConfigKeys`) and the
reader an explicit mode named by what it decrypts; the scenario is ciphertext at rest, covered by
`ReencryptSecrets` and by the non-fatal startup backfill `BackfillMonitorConfigEnc`; a viewer receives
no scenario and a writer does, through `GetMonitorForWriter` / `ListMonitorsByProjectForWriter` chosen
after the authorization decision; a partial update carries the stored ciphertext forward; a keyless
instance refuses a scenario WRITE (`ErrNoAtRestKey`) and stays ready. Evidence:
`TestScenarioIsEncryptedAtRestAndReadPerMode`, `TestScenarioSurvivesAPartialUpdate`,
`TestScenarioWriteRefusedWithoutAnAtRestKey`, `TestBackfillMonitorConfigEncIsIdempotent`,
`TestSyntheticScenarioIsWriterOnly`. The materializer trap the review predicted is covered by the
first of those — it failed before the fix. **Stage 2 is DONE except the editor**: a credential in a scenario is a
named binding (`{{secret:<binding>}}` in the document, the inventory name in the flat config key
`scenario_secret_<binding>_ref`), so `repointSecretRefs`, `monitor_secret_refs`, rotation and deletion
work untouched and the scenario-aware repoint the review demanded was never needed. The grammar and
every refusal live in `internal/domain/syntheticbindings.go` and run from `Monitor.Validate`, so all
four write surfaces share one rule; the executor builds one envelope field per binding, substitutes it
into the scenario JSON-escaped, leaves no copy beside it, and refuses fail-closed on a missing
envelope, a generation-1 carrier or an envelope that does not bind the body. The anti-relocation
property comes from making the scenario an EXECUTION BINDING KEY — the digest covers the stored bytes,
so a moved placeholder fails the AEAD before any request; the test asserting that relocation failed
until the key existed. A binding rides the ORDINARY ref path, which is
what the flat key was chosen for and is now proven on a live DB rather than argued:
`TestScenarioBindingRidesTheOrdinaryRefPath` renames a bound secret, finds the flat key re-pointed and
the scenario byte-identical, sees the delete guard count the binding, and rotates the value with no
monitor edit. **Round 5 of the review read the shipped code and found one P0**: the ref key was accepted
on ANY monitor type, so an http monitor could be saved and then refused at dispatch — no credential
escaped (every step fails closed) but a monitor broke for a config its write surface had accepted. The
type is now decided at the write boundary and gated in the store, in both derived key sets and in the
dispatch gate (D6b). The review's follow-up was that calling a PRE-RULE row "inert" overstated the
gates: such a row keeps its monitor probing, and it still counts against deleting its secret and blocks
that monitor's next API edit. No release ever accepted the key, so the state is unreachable — and because
that is a claim about releases rather than about behaviour, two tests seed the row and pin every outcome,
with the detection query and the one-edit repair in `docs/runbook.md` instead of a migration. Evidence: `TestScenarioBindingsAcceptsTheDeclaredShape`,
`TestScenarioBindingsRefusals`, `TestMalformedBindingReferenceIsRefusedByName`,
`TestScenarioBindingIsSubstitutedIntoTheScenario`, `TestScenarioBindingGateFailsClosed`,
`TestSyntheticTestBeforeSaveFailsClosed`, `TestScenarioBindingIsRefusedOnEveryNonSyntheticType`,
`TestScenarioBindingIsRefusedOnANonSyntheticMonitorAPI`,
`TestScenarioRefsAreContributedBySyntheticMonitorsOnly`,
`TestScenarioBindingOnANonSyntheticTypeIsRefused`,
`TestAPreFixNonSyntheticBindingRowBehavesAsDocumented`,
`TestAPreFixNonSyntheticBindingBlocksEditsUntilTheKeyIsDropped`. **The editor landed in iter-0167** (owner-approved mock, 2026-09-03): the mapping panel, the
credential-bearing header as a binding picker, the D10 notice at the Test button, and a
pre-existing defect fixed with it — `canSubmit` demanded a target the form hides for synthetic,
so the type could not be created from the SPA at all. **Not done:** a synthetic monitor still
cannot live in a bundle (D9). |
| FR-029 | A typed external canary runs one async transaction and reports it as an ordinary monitor. Monitor type `async_canary` carries exactly one workflow kind, `async_transaction_v1`, declared in a nested typed schema with closed unions and no free-form fields: it is executed by a capability-announcing executor in the monitor's region, submits with a stable idempotency key, correlates by a declared path, awaits a terminal outcome by SSE or polling, asserts declared fields, validates the cleanup boundary, and returns ONE heartbeat carrying up/down, total latency, the failed stage and a bounded code class. Monitoring-as-Code is its first surface. | DONE | [`func-async-canary.md`](specs/func-async-canary.md) revision 8, **design APPROVED** by the independent reviewer at party [66] after six rounds and seventeen P0s, with the owner's sign-off on the one deviation from the brief (§11.4, binding map) and D-0218. Six phases (§12). **A–E are BUILT (2026-09-03, iter-0169)**: the typed contract, canonicalization and the semantic hash (`internal/domain/canary.go`, `canarycanonical.go`, `canaryexec.go`); the executor's five stages with its OWN strict SSRF guard and the cross-host credential rule the product owns because `net/http` does not (`internal/prober/canary.go`); the per-monitor in-flight lease and per-region cap (migration 00095, `store.CanaryRegionLimit` = 4) claimed on BOTH dispatch paths, with a refused run writing ONE ordinary DOWN carrying a bounded reason; the idempotency key over monitor + execution revision + scheduled window; SSE with a pinned content type and line/event/total bounds; the embedded `small_wav_v1` fixture whose SHA-256 is verified before a byte is uploaded; the per-job pull claim lease (migration 00096), which also closes a defect older than the canary — any pull monitor with a timeout past 30 s was re-claimable mid-probe; admission to `fileSupportedTypes` with a recursive inline-secret guard; `async_canary` added to the `monitors_type_check` DB constraint (migration 00097, found by an E2E after every Go test passed because no store test writes to the real table); `cerbix_canary_stage_total` and `cerbix_canary_dispatch_refused_total` with no monitor id, URL or correlation id; and the runbook section. The fourteen §6 invariants are discharged in the FR-029 map in [`traceability.md`](traceability.md), where **two rows are `PARTIAL` and say what is missing**. **Capability announcement is BUILT (2026-09-03, iter-0171, D-0231)**, and with it the per-workflow-kind AMQP queue this row used to name as missing: an executor announces `<kind>@<version>` from the runner it actually has — a pull agent in its heartbeat and on every claim, an AMQP worker by consuming `checks.canary.[v3.]<token>.<region>`, `--role=all` by construction — and the scheduler dispatches only into a region that announced the token the DOCUMENT needs. A region with none writes one bounded DOWN reason `no_capable_runner`, a version skew `capability_mismatch`, and a failed lookup refuses rather than assuming. The filter is not the whole barrier and is not treated as one (D-0160: a capability check does not stop a consumer from consuming) — the AMQP canary queue is one an old worker never binds, and on the pull transport `pull_jobs.workflow_kind` (migration 00098) plus the per-claim `X-Cerbix-Workflow-Kinds` header keep the old agent in a MIXED fleet from taking a canary it cannot run. **Phase F is BUILT (2026-09-03, D-0226)**: the typed form on `NewMonitorView.vue` with the rules mirrored in [`canaryWorkflow.ts`](../frontend/src/lib/canaryWorkflow.ts), built to the mock the owner approved (D-0224) — bindings as stage 0, the five stages, every refusal met AT THE FIELD, and no JSON editor on the create view or the read view (asserted as a mechanism: no textarea, no control holding the document). 23 library cases, 12 real-component cases, `e2e/tests/canary-ui.spec.ts` on a live stack, four killed mutations. It also closed two seams the form exposed: the API write surface never canonicalized the document, so D3e was FALSE of it and a MaC re-apply over an API-created canary read as CHANGED forever (fixed in `Monitor.Normalize()`, both surfaces proven to AGREE); and `async_canary` was never in `openapi.yaml` at all — API-reachable and undocumented, the same class FR-028 paid for. A canary is declared in a bundle, through the API, or in the SPA form. **Status DONE at iter-0171**: every clause of this row is built and discharged in the FR-029 map. Its last `PARTIAL` — invariant 13's audit-trail clause — closed at iter-0172 by FR-026 §10 (D-0233), and the evidence gap it also carried — a canary through a real pull agent proven only by unit tests — closed the same day on the geo stack (`topology-geo.spec.ts`, both remote transports, 12 passed). Closes against the same standard FR-024 did: the discharge map, with residuals named. Independent review: phase F approved at [97], the capability range at [102], the D-0232 follow-up at [106] — cumulative `f06f343..4a2c7c8` approved. |
| FR-030 | A monitor carries an optional description: plain text, at most 200 characters counted as Unicode code points identically on the form and the server, refused at the field and with a 400 naming the field when longer; shown on the monitor list (one line under the name), on dashboard panels (one line, cut by the panel's width) and on the monitor page (in full); editable on the create and edit forms with a live count; declarable in a Monitoring-as-Code bundle. Existing monitors, bundles and API clients are unaffected: the column defaults to the empty string and no surface changes when it is empty. Not shown on public status pages, in notifications or in search. | DONE | [`func-monitor-description.md`](specs/func-monitor-description.md), D-0234; the mock [`docs/design/mock-monitor-description.html`](design/mock-monitor-description.html) **approved by the owner 2026-09-03** with the two open answers: the limit is 200, search does not match on it. Implementation follows the closing of the v0.1.9 residuals (iter-0172), on the owner's order of sequence. **Built at iter-0173 (2026-09-03)**: migration 00099, `domain.MaxMonitorDescriptionRunes` counted as code points and published to the client through the bounds fixture, the column appended at the END of `monitorColumns`/the scan, the optional bundle key folded into the canonical hash, the API on create and update, and the four SPA surfaces of the approved mock. Compatibility is asserted as the ABSENCE of an element on every surface for a monitor without one. Reviewed: cumulative `a1617fe..0ded475` **APPROVED** at party [130]. |
| FR-031 | A rendering claims no more than the facts behind it, and can distinguish the states it claims to distinguish. The reliability timeline's axis is clock time with unstored time drawn as absence; a cell is a proportional stack of five mutually legible encodings, with a capped presentation floor on `bad`/`unknown`/`excluded` and none on provisional good time; segments are lanes of real width, a sub-pixel one marked rather than widened, and colliding boundary marks clustered at their earliest member; a segment quotes no availability while its storage is incomplete, with coverage still printed as its own fraction. The monitor's Response time panel draws every fetched heartbeat at its real timestamp — the zero-latency failures included — with no stroke and no fill, absence rendered positively by an observation ruler, the timeout stated always and drawn only inside the computed extent, and its avg and p95 over the population it drew. The `/sla` objective card is read-only with an explicit Edit, closes on a guarded save, clears its draft, offers Save only when it would act, and resets its own draft/error/busy state on a context change. Not in scope, permanently: a query builder, a time-range picker on these panels, drag-zoom, panel composition, a second metric or an overlaid series. | DONE | [`func-truthful-rendering.md`](specs/func-truthful-rendering.md), D-0235; the mock [`docs/design/mock-truthful-rendering.html`](design/mock-truthful-rendering.html) **approved by the owner 2026-09-03**, after the independent reviewer's design gate at party [173] and six P1 findings recorded in D-0235 rather than quietly fixed. **Built at iter-0174 (2026-09-03)**: no Go code and no API change — [`reliabilitygeometry.ts`](../frontend/src/lib/reliabilitygeometry.ts), [`latencypanel.ts`](../frontend/src/lib/latencypanel.ts), [`wallclock.ts`](../frontend/src/lib/wallclock.ts), [`ReliabilityStrip.vue`](../frontend/src/components/ReliabilityStrip.vue), [`ServiceReliability.vue`](../frontend/src/components/ServiceReliability.vue), [`MonitorDetailView.vue`](../frontend/src/views/MonitorDetailView.vue), [`SlaView.vue`](../frontend/src/views/SlaView.vue). It closes a defect in the OLD numbers as well: a segment could quote `availability 100%` over a range it never materialized, invisible for as long as every lane was normalised to full width. Twenty-four mutants planted and killed and four defects caught with nothing planted; 591 frontend tests. See AC-0174-1…8 above. |
| FR-032 | cerbix records that a run was EXPECTED, so a surface can distinguish an interval nothing was due in from an interval whose due run never happened. The durable facts a solution must carry: materialized due windows; issue, claim and terminal-outcome timestamps; the interval, retry count and timeout in force for THAT run rather than the monitor's current fields; and retention semantics saying what a surface may claim about a span whose ledger rows have been dropped. | TODO | [`func-expected-run-ledger.md`](specs/func-expected-run-ledger.md) — **problem statement only, NOT DESIGNED**: no schema, no API, no retention rule, with volume, ownership and the DB-less `worker`/`agent` roles named as open questions. Opened by D-0235 at iter-0174 after two designs for deriving contiguity from existing facts were rejected: a run may occupy `(retries+1) × timeout`, `result.allowed_skew` bounds a clock test rather than delivery, and `job_issued_at` is not a column of `heartbeats`, so two received heartbeats bound observed spacing and nothing more. Only this requirement's acceptance criteria may ever permit a connecting stroke on the Response time panel. Needs its own design review and a data-model migration plan. |
| NFR-024 | Nothing the canary touches leaks, and nothing it does can take the product down. Secrets reach the executor only through the existing envelope, live in memory for one execution and are wiped after it; no request or response body, header, secret value, correlation id, object path, presigned URL or raw target URL reaches a heartbeat, log line, error, audit row or metric label. A region with no capable executor, a capability mismatch, an unreachable target and a crashed executor are each a NORMALIZED, bounded, per-monitor outcome — never an indefinite pending state, never a readiness flip, never an effect on another monitor. | DONE | [`func-async-canary.md`](specs/func-async-canary.md) §8, §10, invariants 9, 11, 12 — all discharged in the FR-029 map. Nothing the canary emits carries a URL, a body, a header value, a secret, the correlation id or the object path (`TestCanaryResultNeverCarriesSecretsURLsOrIdentifiers`, with distinctive values planted in every one of those positions and all outputs searched; the live assertion is in `e2e/tests/async-canary.spec.ts`). The URL policy is strict and has NO setting that relaxes it — tests reach a fixture through an injected dialer at the seam, never through a product option. Every bound is asserted at the edge and one past it. The four shortage cases the requirement names are each a bounded, per-monitor, normalized outcome: `region_saturated` and `already_in_flight` from the lease (iter-0169), and `no_capable_runner` and `capability_mismatch` from the capability announcement (iter-0171, D-0231) — the version skew that used to answer `unsupported monitor type` now says which fix it needs, and the check precedes the in-flight lease so an incapable region cannot park a slot it can never release. **Status DONE at iter-0171** with FR-029: the last of the four shortage cases is a bounded outcome, and nothing in the announcement path carries a URL, a body or a secret — the announcement is a token, and the refusal is a reason. |
| NFR-025 | No timestamp is rendered without naming its zone. **(a)** the one time mechanism exists and every timestamp on the FR-031 surfaces goes through it — a named renderer per subject, no generic formatter, the offset resolved AT the instant, identity kept UTC while presentation is local. **(b)** the five pre-existing `toLocaleString` call sites in four files (`SlaView.vue`, `EscalationView.vue`, `AdminOutboxView.vue`, `SettingsView.vue` ×2) are substituted onto that mechanism. **(c)** the HAND-ROLLED date surface — 46 matches on 26 lines across 13 product files rendering a `Date` through `toISOString().slice`, `getUTC*` or `get*`, counted in the ratchet's own unit of MATCHES — is brought under the same contract, each site decided rather than renamed. | IN_PROGRESS | [`func-truthful-rendering.md`](specs/func-truthful-rendering.md) §8 and §9. **(a) DONE at iter-0174**: [`wallclock.ts`](../frontend/src/lib/wallclock.ts), one mutant killed (the offset from `now`, which fails exactly the two DST tests). **(b) DONE 2026-09-04**: all five sites call the mechanism, which gained `instantLabelShort` and `instantRangeLabel` — both instant-specific, the second naming BOTH offsets across a DST change; a source guard forbids the `toLocaleString` family in any product file and reverting one site fails it BY NAME (mutation verified); a surface assertion in `SlaView.spec.ts` reaches what an operator reads. **(c) TODO** — reviewer P1 at party [195]. This session reported (b) as closing the whole requirement, repeating a clause the specification itself carried from iter-0174 — *"so no rendered timestamp anywhere in the SPA is missing its zone"* — which substituting the `toLocaleString` sites does not achieve. §9 now enumerates (c) by kind: bare UTC dates shown as facts, a UTC date that is identity and should say so, a compact unlabelled clock, two sites already honest in their own words, and the sites that must STAY bare (`datetime-local` input values, day-grid map keys, day-boundary comparisons). A RATCHET in `wallclock.spec.ts` counts the idiom per file against an enumerated list with a reason each and fails on a new file, a new call, or a shrunk count — both directions mutation-verified — so the remainder is bounded rather than described. 23 tests in [`wallclock.spec.ts`](../frontend/src/lib/wallclock.spec.ts). |
| FR-SYN-1 | Type `synthetic`: a scenario of steps with extract/assert, normalized and validated in `domain`, with the scenario and its secrets in `config` encrypted like the other types. | DONE | The scenario half shipped with the type ([`internal/domain/synthetic.go`](../internal/domain/synthetic.go), [`internal/prober/synthetic.go`](../internal/prober/synthetic.go)); the ENCRYPTION half was false of the code from the day the requirement was written until FR-028 stage 1 (D-0216), which is why this row exists at all — the SYN requirements had no rows here, and that is how the false promise survived. Evidence for the half that was missing: `TestScenarioIsEncryptedAtRestAndReadPerMode`, `TestBackfillMonitorConfigEncIsIdempotent`. A DECLARED credential is now a binding resolved from the inventory (FR-028 stage 2); an undeclared literal in an unlisted header or a body is still legal and is protected by encryption and read scoping only. |
| FR-SYN-2 | A synthetic probe yields ONE heartbeat — up/down, the latency of the whole scenario, and a message naming the failed step or assert. | DONE | [`internal/prober/synthetic.go`](../internal/prober/synthetic.go) (`stepLabel`, one `Result` per scenario); `TestSyntheticScenarioAssertFails`, `TestSyntheticMissingExtractFails`, `TestSyntheticScenarioSuccess`, `TestSyntheticSubstAndJSONPath`. FR-028 stage 0 changed the TRANSPORT branch of that message to a bounded class plus a host and kept the step label: `TestProbeResultNeverCarriesTheRequestURL`. |
| FR-SYN-3 | Synthetic executes on a regular worker, including geo, region-aware like every other active type. | DONE | True by construction — `synthetic` is an ordinary entry in the prober registry and carries no region rule of its own ([`internal/prober/prober.go`](../internal/prober/prober.go)). **The remaining gap is named rather than hidden:** since iter-0167 an E2E does create and edit a synthetic monitor on the CORE region ([`e2e/tests/synthetic-bindings.spec.ts`](../e2e/tests/synthetic-bindings.spec.ts)) — which is also what exposed that the SPA could not create the type at all — but nothing puts one on a GEO worker, so AC-SYN-2's "reuses the geo E2E" is still undischarged and says so in that spec. |

## Non-Functional Requirements

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| NFR-001 | Config is fail-fast and strictly validated before runtime. | DONE | [`internal/config/config.go`](../internal/config/config.go), [`config_test.go`](../internal/config/config_test.go) |
| NFR-002 | Runtime does not self-heal, downgrade, or silently ignore invalid config. | DONE | [`internal/cli/cli.go`](../internal/cli/cli.go), [`docs/decisions.md`](decisions.md) |
| NFR-003 | Structured logging via slog; level/format from config; no secret logging. | DONE | [`internal/logging/logger.go`](../internal/logging/logger.go), [`logger_test.go`](../internal/logging/logger_test.go), [`.golangci.yml`](../.golangci.yml) |
| NFR-004 | Metrics use `cerbix_` prefix and low-cardinality labels. | DONE | [`internal/metrics/metrics.go`](../internal/metrics/metrics.go), [`metrics_test.go`](../internal/metrics/metrics_test.go) |
| NFR-005 | Graceful shutdown on SIGINT/SIGTERM. | DONE | [`internal/cli/cli.go`](../internal/cli/cli.go) |
| NFR-006 | Tenant isolation enforced (domain + HTTP) + regression-tested. | DONE | schema FK + `authz` + API handlers; tests in `authz_test.go`, `api_test.go`, `store_integration_test.go` |
| NFR-007 | API tokens stored as hashes only; never logged. | DONE | Service-account tokens are stored only as a SHA-256 hash (`HashToken`) and matched by hash on presentation; the plaintext `cbx_…` is shown once at issuance and never persisted or logged ([`internal/store/apitokens.go`](../internal/store/apitokens.go), [`internal/auth/middleware.go`](../internal/auth/middleware.go) `principalFromToken`). Client-credentials JWTs are bearer tokens verified but never stored. |
| NFR-009 | DB-gated readiness + `cerbix_database_up`; fail-fast on DB init error. | DONE | [`internal/cli/cli.go`](../internal/cli/cli.go) (`pingDatabase`), [`internal/metrics/metrics.go`](../internal/metrics/metrics.go) |
| NFR-010 | Local-login passwords hashed (argon2id), never logged, rate-limited. | DONE | argon2id + uniform 401 + no-secret-logging ([`internal/auth/password.go`](../internal/auth/password.go), [`local.go`](../internal/auth/local.go)); **per-IP sliding-window rate limit** ([`internal/auth/ratelimit.go`](../internal/auth/ratelimit.go), `login_rate_limit_per_minute` default 10, 429 over limit) — D-0031. **Proxy trust (D-0143):** `server.trusted_proxy_cidrs` makes the client-IP key honor `X-Forwarded-For` only when the direct peer is inside a trusted network (supersedes the hop-count `trusted_proxy_count` when set), closing the dual-path XFF-spoof. |
| NFR-011 | Probers guarded against SSRF: validate the resolved connect IP; block cloud-metadata/link-local by default. | DONE | `prober.Guard` validates the **resolved** connect IP via a custom `Transport.DialContext` (covers HTTP redirects + DNS rebinding by pinning the checked IP), shared by the TCP prober; ICMP checks the resolved IP. `prober.allow_private_ips` default **true** (internal tool), `allow_metadata_ips` default **false** (block `169.254.0.0/16` + `fe80::/10` + explicit IMDS incl. IPv6 `fd00:ec2::254`) ([`internal/prober/guard.go`](../internal/prober/guard.go), [`guard_test.go`](../internal/prober/guard_test.go), [`internal/config/config.go`](../internal/config/config.go)) — D-0035, D-0042. |
| NFR-012 | Raw heartbeats bounded by retention; long-range availability preserved. | DONE | `heartbeats` is RANGE-partitioned by `ts` into daily partitions + a DEFAULT (migration `00017`); the scheduler leader pre-creates upcoming partitions and DROPs those older than `heartbeats.retention_days` (default 30) ([`internal/store/retention.go`](../internal/store/retention.go), [`internal/scheduler/scheduler.go`](../internal/scheduler/scheduler.go)). The same `retention_days` bounds the rollup recompute window so long-range availability (frozen `heartbeats_daily` rows) is never wiped — D-0037. |
| NFR-013 | Secrets encrypted at rest (webhook secrets, channel credentials). | DONE | `secret.Cipher` (AES-256-GCM, `enc:v1:` tag, legacy-plaintext passthrough) encrypts `webhooks.secret` and `notification_channels.config` values at the store boundary; `security.encryption_key` (base64 32B, empty = off) ([`internal/secret/secret.go`](../internal/secret/secret.go), [`internal/store/{webhooks,notifications}.go`](../internal/store/), [`internal/config/config.go`](../internal/config/config.go)) — D-0040. **Key rotation (iter-0034, D-0041):** the cipher is a keyring (encrypt primary, decrypt try-all); `security.previous_keys` + the `cerbix reencrypt` command migrate data to a new key with zero downtime — D-0041. |
| NFR-014 | Monitoring as Code reconciliation is tenant-scoped, atomic per project bundle, HA-safe, bounded, no-op idempotent, last-known-good preserving, and never auto-deletes history or accepts inline secrets. | DONE | Core delivered (iter-0087…0095): strict/bounded parsing, scope matrix, inline-secret rejection, canonical no-op, per-project atomic apply, advisory leadership, D-0142 revision-safe updates, LKG on invalid, orphan/disable with no history loss, orphan-safety §9.1 fix, runtime diagnostics surface (iter-0101), storm/two-replica/full-real-binary test matrix (iter-0102: backpressure bound + HA single-leader/failover + full real-binary lifecycle). P2 UI/API polish (iter-0103): named-provider filter (UI + diagnostics API), golangci-lint RUN (MaC packages 0 issues). Two external reviews (2026-08-08) reopened a P0 + P1 set; all FIXED + tested (iter-0104…0109) and the browser E2E executed green (iter-0110). See the FR-017 row for per-item disposition. **Acceptance close-out (iter-0110): DONE** — all §19 criteria evidenced; `-race` 32 pkgs + `mac-smoke` + browser E2E (3/3) green; alert rules promtool-validated. **Third-review hardening (iter-0113):** pool-starvation, target inline-secret (userinfo + secret query keys, D-0152), mid-reconcile absorb, scope-change over-orphaning (D-0153), tenant-bound over-suspension (path-keyed per-project freeze), leader-key collision, and unbounded warn-throttle all FIXED + independently re-reviewed; NFR-014 invariants (tenant-scoped, atomic, HA-safe, bounded, LKG-preserving, no inline secrets) reaffirmed under dual-storage `-race`. Spec §§2, 7–18; D-0145/D-0146/D-0147/D-0148/D-0152/D-0153. |
| NFR-015 | Secret values exist in plaintext only in the core materializer transiently, the prober at probe time, and on the wire to the monitored target (encrypted-by-default egress); everywhere else AES-256-GCM ciphertext under two separate keyrings (at-rest master never leaves core; per-region dispatch keys); never in APIs/logs/audit/diagnostics/hashes; never cross-project; fail-closed without keys. | DONE | **Re-accepted 2026-08-15 in [iter-0120](iterations/iter-0120.md).** The credential-LESS fail-closed rule holds and is demonstrated, not merely asserted: the structural gate refuses a stripped, truncated, empty or carrier-mismatched credential before any connection to the target, and against a live auth-less Redis a credential-stripped probe never reports UP — restoring the pre-r7 hole makes it report UP, on demand. The execution binding (`body_digest`) stops a retargeted probe. Threat scope is stated narrowly in spec §2 (option A: carrier routing metadata is trusted, with option B's cost recorded). D-0155/D-0160. Previously evidenced by [`func-secret-inventory.md`](specs/func-secret-inventory.md) §2, D-0155 and [iter-0116](iterations/iter-0116.md): executor roles reject the master and own no generic control-plane consumers; inventory ciphertext is AAD-bound to `(project_id,secret_id)`; dispatch ciphertext is AAD-bound to version/region/key/monitor/revision/field/job; no-decrypt DTOs and tests prove inline/ref plaintext absence from snapshots and persisted v2 pull payloads; v1 claims/queues cannot receive v2; transplant, future-version, previous-key, wrong-key/degraded-readiness and stale-ingest matrices are covered. APIs/audit/logs/metrics expose names or bounded reasons only. Live smoke additionally scans core/agent logs for the test value. |
| FR-022 | A Service can own an incident: seen, acknowledged, annotated and resolved through the same surfaces a monitor incident uses, with its impact links and postmortem. | DONE | Closed in [iter-0156](iterations/iter-0156.md) against an ENFORCED discharge map (**D-0171**): all SIXTEEN invariants of [`func-service-incidents.md`](specs/func-service-incidents.md) §6 and all sixteen §7 scenarios have a row in [traceability.md](traceability.md) naming a test that exists, and `make docs-check` fails if one is missing or cites a test the tree lacks. Building that map found five unpinned properties and one FALSE invariant (14, corrected in the spec with its number kept). The obligation FR-022 carried — superseding **FR-021 invariant 86** with its note, its discharge row, §16.10's scenario 24 and a REWRITTEN test — was discharged in the same change that made it necessary. |
| NFR-017 | Adding a second incident anchor changes no answer the product already gives about the first: a monitor incident's lifecycle, escalation progress, notes, status-page rendering and postmortem are byte-identical before and after FR-022, proven by regression. | DONE | The pre-FR-022 suite unchanged and green, plus the surfaces FR-022 could plausibly reach pinned BY NAME (FR-022 invariant 8's row in [traceability.md](traceability.md)), plus the browser suite on a live stack built from this branch — 47 passed, 1 known skip. The named-surfaces half rests on my judgement of where the change could reach and is stated as such; the browser gate does not. |
| FR-023 | A Service with an escalation policy escalates its OWN auto-opened incident: steps on the policy's schedule, durable progress, acknowledgement stops it, every step names the SERVICE. | DONE | Closed in [iter-0157](iterations/iter-0157.md) against an ENFORCED discharge map (**D-0173**): SIXTEEN invariants and NINETEEN scenarios each name a test that exists, and `make docs-check` fails if one is missing or cites a test the tree lacks. Two invariants (15, 16) were ADDED during implementation, because D1 said the API accepts the policy when it accepts it only at create time — an existing service had no write path at all. The ladder FAILS CLOSED where delegation fails open, and the service GRAPH does not pause it (§14 annotates, never suppresses). |
| FR-024 | Reliability Gate: a pipeline asks whether a service's error budget allows a release and receives a deterministic, machine-readable decision — `ALLOW`/`WARN`/`BLOCK`/`UNKNOWN`/`NOT_CONFIGURED` — with every matching reason and the evidence it rests on, derived from facts cerbix has already sealed and evaluated; a privileged, time-bounded, audited override; a CLI verb for any CI/CD. Change Intelligence is FR-025 and is not part of this. | DONE | [`func-reliability-gate.md`](specs/func-reliability-gate.md) — revision 13 after twelve review rounds (revision 1: 4 P0, 9 P1, 3 P2; revision 2: 2 P0, 5 P1, 4 P2; revision 3: 4 P1, 4 P2; revision 4: 3 P1, 5 P2; revision 5: 3 P1, 3 P2; revision 6: 5 P1, 2 P2; revision 7: 1 P0, 7 P1, 3 P2; revision 8: 8 P1 and a P2 set; revision 9: 6 P1 and a P2 set; revision 10: 7 P1, 2 P2; revision 11: 4 P1, 3 P2; revision 12: 2 P1, 3 P2). **Design APPROVED at revision 13 (party [47], 2026-08-28; D-0201)** — design only at that date; the mock gate and the implementation followed in iter-0163 (the closure at the end of this cell). Revisions 10–13 were committed together as one accepted design phase (owner, 2026-08-28). Round 5 re-sized the rate bounds to what a deploy gate is (process 60/min default, 600 hard max) with the capacity they imply written beside them, made the ledger a RANGE-partitioned table aged out by partition removal so purge outruns ingest by construction (daily partitions since revision 7, owner D-0194; readable until detach (`retention + <1 day + ≤ purge_every`) and on disk until the deferred drop (`… + ≤ 2 × purge_every`) under a healthy pass, beyond that backlog with gauges; a crash-consistent registry (create/attach/drop atomic with their registry write, `detaching` intent for the one non-transactional DDL) whose ownership is a `COMMENT` marker rather than an OID; creation budgeted nearest-horizon-first and removal counted in stage-ops with load-time convergence checks; the id's day DB-CHECKed against `evaluated_at` with a per-partition unique index so no read can meet two rows; a paginated, range-bounded listing route for the SPA history with matching `(project_id[, service_id], evaluated_at DESC, id DESC)` indexes; the eight policy/override routes with `expected_revision` as a never-reused per-service generation (tombstone row), strict full-document bodies, no client `action`, revoke-by-immutable-id and a by-id/history read for any override; the listing cursor encoded from the last returned row, an Append-with-matching-child-index EXPLAIN contract and a per-item presence contract equal to the by-id response; release as a proof under its own 3 s deadline with hijack-and-close on any unknown; creation and removal slices enforced per statement by wall-bound clamps (`statement_timeout` = remaining slice, `lock_timeout` ≤ min(2 s, that), context deadline at the boundary) inside one 30 s lifecycle (work ≤ 27 s, cleanup ≤ 3 s); override `status` a table-tested read-time function over semantic facts (mismatch ranks above a recorded expiry, so housekeeping never moves a status), with all closure fields always present and attribution present for `manual` only under the typed-token contract; ledger pagination stated as live keyset — a returned key never repeats, pre-traversal committed rows appear once, concurrent commits and detaches explicitly outside the guarantee; acquisition inside the 30 s timeline with an absolute cleanup deadline; the history list an indexed `(service_id, created_at DESC, id DESC)` window over unbounded storage; 400 codes for every new parameter; session `SET`s always `RESET` before a pinned connection returns to the pool, hijacked when a reset cannot be proved; creation time-reserved at 12 s of the pass; the drop predicate inclusive; standalone build + `ATTACH` and `DETACH … CONCURRENTLY` so maintenance never takes `ACCESS EXCLUSIVE` on the parent; drop deferred a cadence and gated on open snapshots for PostgreSQL 15.x < 15.9; no DEFAULT partition, a lead of created-ahead days and a writable-horizon gauge instead; the pass fenced by its own pinned `LeaderSession` — every statement on the lock-owning connection; an ownership registry `service_gate_decision_partitions` validated by OID before every act; key `(evaluated_at, id)` with a UUIDv7 id built from `evaluated_at` so the id-only read prunes to one partition; byte bounds on evidence, the revision digest being the complete surviving evidence), scoped the stale-spelling guard to the normative schema fence, the status rows and FR-024 decision sections with fixture tests, and pinned limiter acquisition order and `Retry-After`. Round 4 fixed the floor's arithmetic (the formula now produces 300 s and the test asserts the formula), gave the resource bounds eight keys with numbers and a token-bucket algorithm, chose the decision-constraining set for `facts_fresh_until`, completed the revoker's typed triple, and added a docs-check guard against the retired spellings four rounds kept finding. Round 3 corrected the seal-lag floor to a DERIVED 300 s (bucket 60 s + grace 120 s + headroom — revision 3's 1 m was unreachable on a healthy pipeline), gave `facts_fresh_until` one formula over the seal horizon and the leases, added rate bounds beside the concurrency caps because concurrency alone does not bound write amplification, and stores an immutable `actor_label` for overrides because a token principal's typed attribution reads as `NULL + true`. Round 2 added the seal-lag bound (`max_seal_lag_seconds`, default 900, budget clauses unavailable past it), an override that changes only `action` and never the observed `state`, an expired override releasing its slot, a project-scoped ledger route that survives service deletion, an explicit presence table for the response, and a process-wide plus per-principal load bound. Revision 2 had: the policy names its SLO window, the budget threshold is a consumed percentage, `state` and `action` are separate, one `REPEATABLE READ` snapshot per decision, a decision LEDGER rather than audit rows, override bound to the policy revision, central `authz.Action`s, gate policy writable on file-managed services (D-0188 … D-0201). **RESUMED by the owner on 2026-08-29 (D-0204) as iter-0163** after one day deferred (D-0202) and **CLOSED the same day (D-0208)**: the implementation followed the approved design (revision 13) with three forward corrections (D-0205) and three maintenance deviations toward D10's intent (D-0206); every invariant of §6 and the §7 matrix were discharged with named tests and mutations in both storage modes (the FR-024 discharge table in [`traceability.md`](traceability.md), 21 rows, none `PARTIAL`); the owner approved the UI mock visually on 2026-08-29 (D-0207) and the SPA was built to it — `3fc19e3` (the service detail's `sla_targets` inventory), `9793758` (the `Release gate` card, the policy editor, the override panel, the ledger views, the snapshot), `e78f4f7` (261 vitest tests, `e2e/tests/gate-ui.spec.ts`, the three design-pass fixes [86]/[88]/[90]); `make dev-test` on the rebuilt image 54 passed / 1 skipped. The independent reviewer approved every range — [16], [26]/[30], [44]/[46], [60], [69], [74], [82]/[84], [94] — and the close-out at the range that follows `e78f4f7`. |
| FR-025 | Change Intelligence: a pipeline records a change event for a service — kind ∈ {deploy, rollback, flag}, phase ∈ {started, succeeded, failed, cancelled}, an external identity `(source, external_id)` under which phases are append-only, idempotent and domain-ordered — with an optional bounded `ref`/`url` and the gate `decision_id` it rested on; the service timeline (a bounded `[from, to)` read of change groups with a cursor); incident↔change links written at the auto-incident's `opened` delivery for the own service and its `probable_root` upstream within a bounded window, one `🚀 Changes:` note through the marker guard, "preceded" never "caused", fail-open; before/after SLI over sealed canonical buckets from the series owner, withheld with the page's reasons, `pending` until sealed; `cerbix change record`; a token `actions` allow-list intersected with its role (a CI token narrower than a role). | DONE | [`func-change-intelligence.md`](specs/func-change-intelligence.md) — **IMPLEMENTED — iter-0165 closed 2026-09-01 (D-0213)**; design approved at revision 3 (party [15]; D-0209), the mock approved (D-0210) — landed so far: schema/domain/config + the store (c6c74dd, adversarial fb625ca), D-0211/D-0212 forward corrections, the API (7260e68), the CLI (74ce187), the SPA per the mock (d2e0f3c); the acceptance instrument is the FR-025 discharge table in [`traceability.md`](traceability.md). |
| NFR-019 | The gate never disagrees with the screen: budget, burn and coverage in a gate response are the values the service page and the alerting badge would show for the SAME snapshot instant, because they are produced by the same code paths — the report path for the policy's window (`BurnedPercent`, with `decideServiceWindow` deciding withholding and `seal_lag`), that window's burn latches, the open-incident predicate, and `serviceCoverageClauses` as evidence — inside ONE `REPEATABLE READ` transaction. The gate computes no reliability fact of its own. (Revision 1 of this row named `decideServiceWindow` as the budget owner; it owns withholding, not budget.) | DONE | [`func-reliability-gate.md`](specs/func-reliability-gate.md) D1, invariant 1. |
| NFR-020 | A change never rewrites a fact, and the comparison is the page's own number: recording mutates no reliability fact, incident, policy or decision; correlation and comparison are reads over facts other owners sealed, produced by the same store functions as the reliability page (`serviceReliabilitySeriesTx` extended, never re-implemented), withheld wherever the page would withhold; correlation is fail-open; a CI token's authority is bounded by a per-token allow-list checked only in `authz.Can`. | DONE | [`func-change-intelligence.md`](specs/func-change-intelligence.md) §2, D8, D12, invariants 10–12, 16–17, 22–23 — all discharged with named tests in the FR-025 map. The comparison is `serviceReliabilityCompareTx`, an extension of the series owner's ONE rollup query, never a second implementation; correlation is fail-open in both directions (the notification cannot skip it, it cannot fail the notification); a token's authority is bounded in `authz.Can` with `VisibleScope` as its query mirror. DONE with iter-0165 (D-0213). |
| NFR-018 | A MONITOR's ladder is byte-identical before and after FR-023 — candidates, steps, progress, delivery-time suppression and payload. | DONE | The monitor candidate query is unchanged character for character (a SECOND query serves services), and its five ladder tests are unchanged in behaviour while now asserting `MonitorSteps` specifically — so a monitor step miscounted as a service one fails them. Browser suite 48 passed / 1 known skip on a live stack built from this branch. |
| NFR-016 | Reported service reliability is EXPLAINABLE and REPRODUCIBLE over its declared inputs: every fact references the epoch that produced it, which resolves to exactly one definition revision; identical observations, epoch snapshot and declared exclusions reproduce identical output; a declared-input change is a RECORDED change with an audited repair of the affected range, never a silent rewrite and never a silent no-op; a window is reported only when it passes both coverage axes of §11.2; and the absence of reliability inputs is never rendered as perfect reliability. | DONE | The discharge tables in [traceability.md](traceability.md) — reproducibility is invariants 1, 7, 16, 18 and 20 (`TestTheReducerReadsNoClock`, `TestEachBucketResolvesItsOwnEpoch`, `TestReducerMatchesOracle`, `TestSealedFactIsNotRewrittenByOrdinaryMaterialization`), explainability after retention is 27 and 35 (`TestHeartbeatRetentionLeavesDerivedFactsAndTheirProvenance`, `TestProvenanceCauseSetsAreBoundedAndCountTheirOverflow`), the honesty axes are 39–41 (`TestReportChecksContinuityAndCoverageIndependently`, `TestReportNeverManufacturesNumbers`, `TestReportWithoutSLIHasNoSLO`). This requirement had NO row here until iter-0153 — the spec declared it and the live checklist never tracked it (§2.8). |
| NFR-021 | The trail says what happened, never what was written, and widens nothing: no note body, postmortem body or alert annotation reaches `audit_logs`; the read surface, its RBAC and the instance/organization split are untouched; and incident behaviour is unchanged except for the two corrections FR-026 carries because its own invariants depend on them — a repeated acknowledgement stops rewriting `updated_at`, and a concurrent duplicate Alertmanager delivery is ignored with a stated HTTP contract instead of answering 500. | DONE | [`func-incident-audit.md`](specs/func-incident-audit.md) §2, D5, D8a, D8b, D9, invariants 10–16 — all discharged in the FR-026 map. No body reaches a row (`TestNoBodyEverReachesTheAuditTrail`, a marker planted in five bodies and every row searched whole); the read surface is untouched (`ListAuditByOrg` shows the rows, `ListGlobalAudit` shows none); the two intentional behaviour changes are D8a (a repeated acknowledgement no longer rewrites `updated_at`) and D8b (a concurrent duplicate Alertmanager delivery answers 200 with `ignored=1` instead of 500), each with its own invariant and its own killed mutation. DONE with iter-0168 (D-0219).
| NFR-023 | The definition stays visible to whoever owns it, and nothing degrades silently: the SAFE reader and every scheduler snapshot decrypt no credential plaintext, the authenticated writer reads the scenario only after `ActionProjectWrite`, and READINESS is never coupled to the at-rest key or to a migration — a scenario that cannot be protected fails its own write, one that cannot be opened fails its own probe. | DONE | [`sec-synthetic-secrets.md`](specs/sec-synthetic-secrets.md) §2, §10 and invariants 4–7. The readiness clause is the owner's ruling of D-0216, which withdrew the migration gate revision 3 carried: a service must not go down for an unset variable or one misconfigured monitor. Stage 0 and stage 1 carry no readiness coupling: the backfill logs
and continues, and a keyless instance reports ready while refusing the write it cannot protect. |
| NFR-SYN-1 | The time budget of the whole scenario is bounded by `timeout_seconds` — a hard deadline on the chain, not per step. | DONE | True by construction: [`internal/prober/prober.go`](../internal/prober/prober.go) wraps the whole probe in `context.WithTimeout(ctx, m.Timeout())`, so the step chain shares one deadline and a slow step cannot extend the budget. **No test names the whole-scenario budget** — the synthetic tests cover success, assert failure, missing extract and substitution, none of them the deadline. Stated so the row is not read as evidence it does not have. |
| NFR-SYN-2 | The step-by-step result does not leak secrets into `heartbeat.msg`. | DONE | Held for the ASSERT path from the start and NOT for the transport path until FR-028 stage 0 (D-0216): a failed connection returned `err.Error()`, and `net/http` embeds the request URL, so a step URL's query string reached the message and the record. Every probe result is now composed from a bounded failure class plus a host, for every type: [`internal/prober/failure.go`](../internal/prober/failure.go), asserted per type through the real runner with a planted secret in the URL, the query, a header and the body (`TestProbeResultNeverCarriesTheRequestURL`) and with the vocabulary pinned (`TestFailureClassNamesTheCause`, `TestFailureClassesDNSAndTimeout`). |

## Acceptance Criteria (iter-0001)

| ID | Criterion | Status |
| --- | --- | --- |
| AC-0001-1 | `go build ./...` and `go test ./...` pass at the repo root. | DONE |
| AC-0001-2 | `cerbix serve` responds 200 on `/healthz`, `/readyz`; `/metrics` emits `cerbix_*`. | DONE |
| AC-0001-3 | `cerbix version` prints injected version/commit as JSON. | DONE |
| AC-0001-4 | Invalid/unknown config keys cause fail-fast (non-zero exit). | DONE |
| AC-0001-5 | `docs/` foundation + `AGENTS.md` (cerbix-adapted) present. | DONE |
| AC-0001-6 | `docker-compose` dev stack defined (pg+timescale, rabbitmq, keycloak, cerbix). | DONE |

## Acceptance Criteria (iter-0002)

| ID | Criterion | Status |
| --- | --- | --- |
| AC-0002-1 | Migrations create organizations/projects/users/memberships with FK + role/scope constraints. | DONE |
| AC-0002-2 | `cerbix migrate --config` applies migrations; fails fast without a DSN or on unreachable DB. | DONE |
| AC-0002-3 | `serve` with a DSN migrates, connects, and gates readiness on live DB (`/readyz` 503 + `cerbix_database_up 0` when DB down). | DONE |
| AC-0002-4 | `serve` without a DSN keeps scaffold mode (ready, no `cerbix_database_up`). | DONE |
| AC-0002-5 | Tenant-isolation queries verified: org-level vs project-level members see only permitted orgs/projects. | DONE |
| AC-0002-6 | Cross-org project membership rejected by composite FK. | DONE |
| AC-0002-7 | Coverage ≥ 70% with the DB service; store integration tests run in CI, skip locally without DSN. | DONE |

## Acceptance Criteria (iter-0003)

| ID | Criterion | Status |
| --- | --- | --- |
| AC-0003-1 | OIDC login (`/auth/login`) starts Authorization Code + PKCE with state/nonce stored in `auth_flows`. | DONE |
| AC-0003-2 | `/auth/callback` verifies the ID token (sig/iss/aud/nonce), JIT-provisions the user, creates a session, sets the cookie. | DONE |
| AC-0003-3 | `admin_emails` promotes matching users to global admin on login. | DONE |
| AC-0003-4 | `RequireAuth` resolves the session cookie to a Principal or returns 401; `/auth/logout` revokes the session. | DONE |
| AC-0003-5 | authz matrix + `Can` unit-tested; global-admin bypass; scope rules enforced. | DONE |
| AC-0003-6 | API enforces isolation: non-members get 404 (hidden), insufficient in-org role gets 403; list endpoints filter by visibility. | DONE |
| AC-0003-7 | OIDC flow verified hermetically via a mock provider; store sessions/auth-flows integration-tested. | DONE |
| AC-0003-8 | Coverage ≥ 70% with the DB service; default `go test ./...` stays hermetic. | DONE | coverage 74.7%.

## Acceptance Criteria (iter-0004)

| ID | Criterion | Status |
| --- | --- | --- |
| AC-0004-1 | Monitor CRUD API with authz (ProjectRead/Write) and cross-project isolation. | DONE |
| AC-0004-2 | HTTP + TCP probers with timeout/retries; declarative condition engine. | DONE |
| AC-0004-3 | Scheduler elects a single leader (advisory lock) and publishes due jobs; passive monitors skipped. | DONE |
| AC-0004-4 | Worker pool executes jobs; ingestion writes heartbeats + updates status + `cerbix_checks_total`. | DONE |
| AC-0004-5 | End-to-end: `serve --role=all` drives a real monitor to `up` with heartbeats recorded. | DONE |
| AC-0004-6 | Coverage ≥ 70% with the DB service; default `go test ./...` hermetic (mock-OIDC + inproc, DB tests skip). | DONE | coverage 75.7%.

## Acceptance Criteria (iter-0005)

| ID | Criterion | Status |
| --- | --- | --- |
| AC-0005-1 | `users.oidc_sub` nullable + `password_hash`; OIDC and local users coexist. | DONE |
| AC-0005-2 | argon2id hashing with random salt + constant-time verify; salted (distinct hashes). | DONE |
| AC-0005-3 | `POST /auth/local/login` issues a session on valid credentials; uniform 401 otherwise. | DONE |
| AC-0005-4 | Bootstrap creates a global admin on an empty system from config; never generates/logs a password. | DONE |
| AC-0005-5 | `POST /api/v1/me/password` changes a local user's password (verifies current, enforces min length). | DONE |
| AC-0005-6 | `auth.New` builds OIDC only when configured; cerbix runs local-only (no OIDC issuer). | DONE |
| AC-0005-7 | Real-binary e2e: bootstrap admin → local login → authenticated `/api/v1/me`. | DONE |
| AC-0005-8 | Coverage ≥ 70% with the DB service; default `go test ./...` hermetic. | DONE | coverage 75.7%.

## Acceptance Criteria (iter-0006)

| ID | Criterion | Status |
| --- | --- | --- |
| AC-0006-1 | SLI (uptime%) computed over rolling 24h/7d/30d/90d windows per monitor and per project. | DONE |
| AC-0006-2 | SLO targets (per monitor+window) with error budget (allowed/actual/remaining/burned/met). | DONE |
| AC-0006-3 | Maintenance windows (monitor- or project-scoped) exclude heartbeats from SLI math. | DONE |
| AC-0006-4 | SLA/target/maintenance endpoints enforce authz + tenant isolation. | DONE |
| AC-0006-5 | Real-binary e2e: heartbeats → SLA windows → SLO error budget → maintenance exclusion drops totals to 0. | DONE |
| AC-0006-6 | Coverage ≥ 70% with the DB service; default `go test ./...` hermetic. | DONE | coverage 76.0%.

## Acceptance Criteria (iter-0007)

| ID | Criterion | Status |
| --- | --- | --- |
| AC-0007-1 | `openapi.yaml` (OpenAPI 3.0.3) documents the current API (paths + schemas) and parses. | DONE |
| AC-0007-2 | Embedded SPA served from `embed.FS`; real assets served, unknown routes fall back to index.html. | DONE |
| AC-0007-3 | Route precedence: `/healthz`, `/readyz`, `/metrics` and `/api`, `/auth` win over the SPA catch-all. | DONE |
| AC-0007-4 | Real-binary: `GET /` serves the shell, `/orgs/x` falls back, `/api/v1/me` still 401, `/healthz` ok. | DONE |
| AC-0007-5 | Default `go test ./...` stays hermetic; build embeds the placeholder dist. | DONE |

## Acceptance Criteria (iter-0035)

| ID | Criterion | Status |
| --- | --- | --- |
| AC-0035-1 | OIDC is provider-agnostic (any issuer via discovery); no Keycloak-specific code paths. Config/comments name Keycloak only as one example (D-0043). | DONE |
| AC-0035-2 | User identity column is `oidc_sub` (original migrations rewritten, no rename migration — D-0044); no `keycloak_sub` remains in code/migrations/API. | DONE |
| AC-0035-3 | Public `GET /auth/config` returns `{local, oidc, oidc_button_label}`; the SPA renders a provider-neutral sign-in button labeled by `oidc.button_label` (default "Continue with SSO") — D-0045. | DONE |
| AC-0035-4 | Monitor & project SLA expose `p95_latency_ms` (`percentile_cont(0.95)`, maintenance-excluded) alongside avg — D-0046. | DONE |
| AC-0035-5 | `GET /api/v1/search` returns only hits the caller can see (`VisibleProject`); <2-char queries return empty; LIKE wildcards escaped — D-0047. | DONE |
| AC-0035-6 | Settings page (channels/tokens/webhooks) and role-gated org/project create dialog are wired to existing APIs — D-0048. | DONE |
| AC-0035-7 | Every SPA view rebuilt 1:1 with the approved design artifacts; charts are bespoke inline SVG (no TanStack Query/uPlot) — D-0049. | DONE |
| AC-0035-8 | `go build ./...` + `go test ./internal/web` green; frontend type-checks and builds; embedded `dist` refreshed with the `placeholder.txt` fixture. | DONE |

## Definition of Done (per iteration)

| ID | Item | Status (iter-0001) |
| --- | --- | --- |
| DoD-1 | Target `status.md` items `DONE` with evidence links. | DONE |
| DoD-2 | Required tests green (`go test ./...`, `-race`). | DONE |
| DoD-3 | Docs synchronized (PRD/status/decisions/traceability/runbook/iteration). | DONE |
| DoD-4 | Config strictly validated before business logic. | DONE |
| DoD-5 | No self-healing/fallback for invalid config. | DONE |
| DoD-6 | Iteration report written. | DONE |

## iter-0036 consolidation (sync with code, D-0050…D-0086)

Delivered and `DONE` (details — `decisions.md`, tests — `traceability.md`, report — `iterations/iter-0036.md`):

| Area | Status | Main Ds / code |
| --- | --- | --- |
| Check catalog (15 types, SSRF guard, secret encryption) | DONE | `internal/prober/*` |
| Alert reliability: confirmations / maintenance-suppression / re-notify / tags | DONE | D-0076/0077/0078 |
| SLO error-budget + **burn-rate alerts** | DONE | D-0079 (`sla`, `store.EvaluateBurnAlerts`) |
| Scheduled weekly **SLA reports** | DONE | D-0080 (`store.EnqueueDueSLAReports`) |
| Incidents (auto/manual/API) + postmortems + feeds | DONE | status pages + `feed`/`subscribe` |
| Incoming **Alertmanager receiver** | DONE | D-0081 (`api/handlers_alertmanager.go`) |
| 2FA/TOTP + self-service password reset | DONE | D-0064 + reset |
| **OIDC from the UI** (live provider reconfig) | DONE | D-0082 (`auth`, `oidc_settings`) |
| **Instance settings** (branding/auth-policy/alerting/monitor-defaults) | DONE | D-0083 (`settings`, `instance_settings`) |
| **SMTP group** + live mailer | DONE | D-0084 |
| Secrets-at-rest AES-256-GCM + rotation; global silence | DONE | `secret`, D-0083 |
| **Geo probers** (region-aware pools, `--region`, per-region queues, TTL) | DONE | D-0085 (`dispatch/amqp`, migration 00033) |
| **Live region picker** (RabbitMQ mgmt) + **Test connection** | DONE | D-0086 (`mqadmin`, `api`) |
| **Region-aware Test connection** (RPC, strictly no core fallback) | DONE | D-0087 (`dispatch/amqp` RunTest/ServeTests) |
| **"Region without a worker" alert** (edge-triggered + grace 90s) | DONE | D-0088 (`store/regionalerts.go`, migration 00034) |
| **On-call / escalations** (policies+rotations+ack, phase A) | DONE | D-0089 (`domain/escalation.go`, `store/escalation.go`, migration 00035) |
| **Synthetic checks** (scripted multi-step HTTP, phase B) | DONE | D-0090 (`domain/synthetic.go`, `prober/synthetic.go`, migration 00037) |
| **HTTP pull agent** (geo without a broker, phase C) | DONE | D-0091 (`internal/agent`, `store/pulljobs.go`, `api/handlers_agent.go`, migration 00036) |
| Repo sanitization (module `git.example.com`, embedded SPA without nginx, CI) | DONE | D-0081..D-0086 period |

Period verification: full `-race` — 28 packages green; `vue-tsc`+`vite build`; OpenAPI↔`schema.d.ts`
in sync; the image is self-contained (the SPA is built and embedded); E2E geo-affinity/live-picker/test-connection/
region-RPC-test/region-worker-alert (fires after grace + recovery). **Deploy:** run `cerbix migrate`
as a one-off before starting the roles (roles auto-migrate and race for a new migration — see overview §2.2).

## iter-0085 result-protocol + hardening backlog (sync with code, D-0142…D-0144)

Delivered and `DONE` (contract — `specs/func-result-protocol.md`; details — `decisions.md` D-0143/D-0144;
tests — `traceability.md`; report — `iterations/iter-0085.md`). Clears the iter-0084 §5 deferred list.

| Area | Status | Main Ds / code |
| --- | --- | --- |
| **P0a** result timestamp hygiene + typed origins (scheduled/push/backfill) | DONE | D-0142/D-0143 (`RecordScheduledResult`/`RecordPushResult`/`RecordHistoricalResults`, `ingest.Reconciler`, migration 00054) |
| **P0b** `execution_revision` config-generation gate + typed dead-man | DONE | D-0142/D-0143 (`RecordDeadmanResult`, migration 00055, `result.revision_mode`) |
| **P1** readiness-gated push-token backfill + pull ACK/lease correctness | DONE | D-0143 (`store/pulljobs.go`, `internal/agent`) |
| **P2** resource caps / strict env-expand / TruncateAll guard / migrate lock / fail-closed RNG / public-DTO redaction / atomic reset / TOTP / outbox purge+CAS / dep-graph lock | DONE | D-0143 (#1/#3/#4/#5/#7/#8/#9/#10/#11/#13) |
| **#14** rate-limit `server.trusted_proxy_cidrs` (CIDR trust model) | DONE | D-0143 (`auth/ratelimit.go`, `config`) |
| **#12** OIDC identity keyed by `(issuer, subject)` | DONE | D-0143 (`UpsertUserByOIDCIdentity`, migration 00056) |
| **#2** outbox `state_sequence` delivery-staleness gate | DONE | D-0143 (`MonitorTransition.Seq`, migration 00057) |
| **Push-watermark** preserve liveness on edit + dead-man re-arm on re-enable | DONE | D-0144 (`monitors.push_armed_at`, migration 00058) |

Period verification: full `go test -race` — **30 packages** green in **both** storage modes (timescale +
throwaway `postgres:16-alpine`); migrations 00054–00058 apply on both; image rebuilt; **E2E 34/34** on the
live `single`+`sso` stack (goose → 58). `observe` remains a temporary migration mode with a hard removal
window (D-0142, ≤ iter-0089).
