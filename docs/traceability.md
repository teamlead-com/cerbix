# Traceability

Mapping from requirements to code, tests, and metrics. Updated every iteration.

| Requirement | Code | Tests | Metric / Endpoint | Rule owner |
| --- | --- | --- | --- | --- |
| FR-021 round-4 rework — 2 P0 + 6 P1 (iter-0128, DONE — reviewed in the arc that [123] approved as a whole) | `store/servicemaintenance.go` (`projectBothSides`: clipped both-sides reduction, ungoverned time UNKNOWN, per-service wall checks + savepoints + remaining-derived bounds, retention fail-closed at preview, reason enum), `store/servicematerialize.go`+`servicerepair.go` (client deadline cushioned behind server bounds — the round-3 fix could destroy the lock-owning connection on one blocked bucket), `api/handlers_sla.go`+`openapi.yaml` (reason, earliest_repairable, 422 on preview), `frontend/src/views/SlaView.vue` (guarded load, reason labels) + `SlaView.spec.ts`, `docker/config.{example,dev}.yaml` (services caps surfaced) | `TestASubMinuteRetroactiveWindowStillNeedsAPreview` (conservation to the requested 15s), `TestPreviewConservesAcrossUngovernedTime`, `TestABlockedPreviewDegradesInsteadOfHoldingTheProject` (real lock wait, bounded, lock released), `TestPreviewShowsAHealthOnlyChange` (GOOD-but-DEGRADED: availability identical to the microsecond, health flips), `TestRetroactiveMutationBeyondRawFailsClosed` (fail-closed moved to the preview), `TestPreUpgradePreviewTokensAreDead` (executes the migration’s own statements), `TestForwardSliceLeavesTheLeaderSessionUnpoisoned` (error path + release/reacquire), `TestBarrieredOppositeOrderBackfillsCannotDeadlock` (writers provably parked before release), `SlaView.spec.ts` (2). Mutants vs iter-0127: unclipped, gap-drop, no-savepoint, cross-wired, retention-open killed; wall-skip hybrid NOT killed (bounds deliver the contract alone) — stated, not claimed. `-race` 33 pkgs hypertable + store/api/config/scheduler plain; lint 0; dev-test 40/1 | preview payload: `reason`, `earliest_repairable`; preview 422 documented | domain (conserved clipped projection) / infra (budgets that bound operations, sessions that survive their slices) / transport (honest refusals) | §10.8/§10.9/§10.10 |
| FR-021 round-3 rework — 8 P0 + 5 P1 (iter-0127, DONE — rejected at its review round 2/2 and reworked by iter-0128–0132, the arc approved at [123]: the preview projection reduces whole buckets instead of the clipped requested range, and the per-service aggregate runs under the project lock past the wall budget) | migration 00073 (health axis, pre-upgrade token expiry); `store/servicemaintenance.go` (interval overlap, wall budget + SET LOCAL, DB-clock raw floor from retention, evidence-gone previews), `store/servicematerialize.go` + `store/servicerepair.go` (tx-local timeouts, ErrEvidenceGone terminal parking), `store/services.go` (era advances the cursor), `store/heartbeats.go` (globally-ordered raw batch), `store/sla.go` (unchecked create unexported, hard delete removed), `internal/domain` + `internal/config` (caps owned by domain, rejected fail-fast), `api/handlers_sla.go` + `openapi.yaml` `ReliabilitySplit`, `frontend/src/views/SlaView.vue` (tenant reset, stale-response generation guard, annul flow, health axis) | `serviceround3_internal_test.go`: `TestASubMinuteRetroactiveWindowStillNeedsAPreview`, `TestPreviewCarriesTheHealthAxis` (conservation on both sides of both axes), `TestPreviewDegradesToApproximateAtItsWallBudget`, `TestPreUpgradePreviewTokensAreDead`, `TestForwardSliceLeavesTheLeaderSessionUnpoisoned` (real LeaderSession, SHOW defaults, live Check), `TestStartingAnEraAdvancesTheCursorWithIt`, `TestRecomputeOverDestroyedEvidenceFailsClosed`, `TestTheLastServiceSlotAdmitsExactlyOneWriter` (concurrent UI vs MaC), `TestOverlappingBackfillsInOppositeOrdersDoNotDeadlock` (fresh rows per round); `config.TestServiceCapsAreRejectedNotReinterpreted`; eight P0 mutants + two P1 mutants were run against iter-0126 code and killed, but four of the committed proofs are weaker than that claim (the health-axis test moves availability too, the token-expiry test never runs the real 00072→00073 migration, the leader-session test exercises only the success path, and the opposite-order runs are not reproducible from the committed suite) — recorded per review round 2/2; `-race` 33 pkgs hypertable + store/api/config/scheduler on plain `postgres:16-alpine`; `make dev-test` 40 pass / 1 known skip | `services:` config block (validated, not clamped); preview payload carries `ReliabilitySplit` both sides | infra (session hygiene, lock budgets, evidence retention) / domain (caps, conserved axes) / transport (fail-fast config, honest approximate) | §10.5/§10.8/§10.9/§10.10/§15.4 |
| FR-021 round-2 rework — 9 P0 (iter-0126, DONE — rejected at its review round 1/2, reworked onward, the arc approved at [123]) | migrations 00070–00072 (column-list owner FK, maintenance provenance retained, materialization era, preview target + projection); `store/monitors.go` (row→refs→epochs contract, §15.1 matrix on delete), `store/servicematerialize.go` (whole-slice deadline, era-scoped watermark, both-axis recompute audit), `store/servicemaintenance.go` (projection through the real reducer, target-bound token, mutation audit, DB-clock expiry), `store/serviceingest.go` (global key order for batches), `store/services.go` + `store/fileapply_services.go` (one cap owner for both writers), `config` `services:` block, `api/handlers_sla.go` + `frontend/src/views/SlaView.vue` (preview/confirm flow) | `TestChangingACredentialRefOpensAnEpochDescribingTheNewCredential`, `TestForwardMaterializationHonoursItsDeadlineWhileBlocked`, `TestPreviewProjectsBothSidesOfTheMutation`, `TestAnAnnulTokenIsBoundToTheWindowItNames`, `TestARetroactiveMutationIsAuditedWithItsActorAndToken`, `TestARecomputeThatMovesOnlyHealthIsStillAudited`, `TestDeletingAMonitorDeclaredByAFileOwnedServiceIsRefused`, `TestDeletingAMonitorKeepsItsMaintenanceProvenance`, `TestDeletingAReferencedOwnerClearsTheReferenceAndNotTheTenant`, `TestTheProjectServiceCapBindsTheBundleToo`, `config.TestServiceCapsAreRejectedNotReinterpreted`, `TestAServiceThatDeclaredSilenceCanReportAgain`. Eight of the nine re-applied as mutants and killed (the retained-provenance one asserts row survival, NOT the delete→recompute result it needs); `-race` 33 pkgs on hypertable and `internal/store` only on plain postgres — NOT 33 packages in both modes, as an earlier revision of this row claimed; `make dev-test` 40 pass / 1 known skip | `POST /api/v1/projects/{id}/maintenance/preview` (before/after per service), `POST /api/v1/maintenance/{id}/annul`, `DELETE /api/v1/maintenance/{id}` (archive) | infra (durability, lock order) / domain (declared discontinuity, audited restatement) / transport (actionable 4xx, previewed mutation) | §10.5/§10.8/§10.9/§10.10/§15.1/§15.4 |
| FR-021 phase-1 review rework — 12 P0 (iter-0125, DONE — rejected at its review round 2/2, reworked onward, the arc approved at [123]) | `store/servicematerialize.go` (forward driver + recompute mode + recompute audit), `store/servicerepair.go` (lease, tx enqueue, origin scoping), `store/services.go` (materialization row, write-time caps, owner tenancy, membership lock order), `store/serviceingest.go` (late-data repair), `store/heartbeats.go` (historical backfill in the handshake), `store/monitors.go` (epoch on the shared write contract, monitor-deletion matrix), `store/secrets.go` (rotation opens epochs), `store/servicemaintenance.go` (bound preview token), `store/fileapply_services.go` (owner persisted), `api/handlers_sla.go` (+ preview/annul endpoints, archive on DELETE), migrations 00067–00069 | `store/servicereachability_internal_test.go` (product-path only: `TestDeclaringAServiceMakesItMaterializeWithoutAnyoneAskingTwice`, `TestProgressWalksPastAGapTheWatermarkRefusesToCross`, `TestHistoricalBackfillGoesThroughTheSealHandshake`, `TestLateDataBehindTheWatermarkQueuesItsOwnRepair`, `TestExceedingTheFanOutCapIsRefusedAtTheDeclarationNotAtIngest`, `TestDeletingADeclaredMonitorRetiresItAsASystemRevision`); `TestACrashedLeadersClaimIsReclaimedAfterItsLease`, `TestASameBoundaryDeclarationDoesNotDiscardUnrelatedWork`, `TestConfirmedAnnulActuallyRewritesTheSealedFacts`, `TestAPreviewTokenOnlyAuthorizesTheMutationItWasIssuedFor`, `TestDeletingAnAffectedServiceInvalidatesThePreview`, `TestAServiceCannotBorrowAnotherProjectsOwner`, `TestBundleOwnerIsActuallyPersisted`, `TestAFileProviderApplyOpensAnEpochJustLikeTheAPIDoes`; `api.TestRetroactiveMaintenanceIsGatedOnTheProductPath`. All twelve shipped behaviours re-applied as mutants and killed; `-race` 33 pkgs both storage modes; `make dev-test` 40 pass / 1 known skip | `POST /api/v1/projects/{id}/maintenance/preview`, `POST /api/v1/maintenance/{id}/annul`, `DELETE /api/v1/maintenance/{id}` (now archive) | infra (reachability, durability) / domain (declared change is recorded) / transport (gate on the product path) | §10.4/§10.5/§10.7/§10.8/§15.1/§15.4 |
| FR-021 service reliability, phase 1 (iter-0124, DONE) | `internal/reliability/` (piecewise reducer, two conserved axes, duration-weighted µs); `internal/domain/{service,execsemantics}.go` (policies + shared validator, exhaustive field classification, `EpochSnapshotHash`, `FreshnessPolicy` JSON codec); `internal/store/{services,serviceepochs,serviceingest,servicematerialize,servicerepair,servicemaintenance,fileapply_services}.go` + migrations 00064–00066; `internal/scheduler/scheduler.go` (`LeaderSession` fencing, service sub-tick); `internal/fileprovider/service.go` + format-2 gates in `parse.go`; `internal/api/handlers_services.go` + `openapi.yaml`; `frontend/src/views/Service{s,Detail,Declaration}View.vue` + `frontend/src/lib/services.ts` + AppShell nav | `reliability.*` reducer suite; `domain.TestMonitorFieldClassificationIsExhaustive` (zero value fails the guard), `TestFreshnessFloorIsADurationStringOverJSON` (+ integer back-compat), `TestServicePoliciesRoundTrip`; store: `TestAddingToContextDoesNotChangeTheSLI`, `TestServiceDeletionCascadesTheWholeDomain`, `TestListServiceSummariesIsOneQueryAndKeepsBothCounts`, `TestUIDeclarationOnAFileOwnedServiceIsRefused`, `TestDeletingAFileOwnedServiceIsRefusedEvenWhenOrphaned`, `TestStoreRefusesAMalformedServiceSlug`, `TestFormat1BundleDoesNotOrphanServices`; api: `TestServicesAuthzMatrix`, `TestAServiceWithNoDeclarationReportsNothingRatherThanPerfect`, `TestPhase1PayloadCarriesNoReliabilityNumbers`, `TestConcurrentDeclarationsConflictRatherThanMerge`, `TestServicesListCarriesTheWatermarkAndBothCounts`; frontend `lib/services.spec.ts` (8); E2E `e2e/tests/services.spec.ts` (2). `-race` 33 pkgs in hypertable mode + store/scheduler/reliability on plain `postgres:16-alpine`; `golangci-lint ./internal/...` 0 issues; `make dev-test` 40 pass / 1 known skip | `GET/POST /api/v1/projects/{id}/services`, `GET/DELETE .../services/{id}`, `PUT .../services/{id}/declaration` | domain (two axes, declared meaning) / infra (seal handshake, contiguity watermark, durable repair) / presentation (no number without a fact) | §5/§6/§10/§11/§15/§21 |
| FR-001 (CLI roles/version) | `internal/cli/cli.go`, `cmd/cerbix/main.go` | `internal/cli/cli_test.go` | — | transport |
| FR-002 (health/ready/metrics) | `internal/httpsrv/server.go` | `internal/httpsrv/server_test.go` | `/healthz` `/readyz` `/metrics` | transport |
| FR-003 (strict config) | `internal/config/config.go` | `internal/config/config_test.go` | — | infra |
| FR-017 / NFR-005 corrective close-out (iter-0117) | `internal/cli/cli.go` (cancel/drain-before-close lifecycle); `docker/docker-compose.yml` (single+distributed read-only provider mount); `internal/api/handlers_fileproviders.go`; `openapi.yaml`; generated frontend schema | `cli.TestCleanupServeResourcesCancelsAndDrainsBeforeClosingInfrastructure`, `cli.TestComposeFileProviderMountMatchesOwningRoles`, `api.TestFileProviderDiagnosticsEmptyCollectionsAreArrays`; live missing-root fail-fast and single/distributed stack checks | `/readyz`; global diagnostics always `{bundles:[],providers:[]}`, org diagnostics always `{bundles:[]}` | application (lifecycle) / infra (deployment mount) / transport (stable JSON collections) |
| FR-008 / D-0157 RabbitMQ 4.3 compatibility and safe upgrade boundary (iter-0117) | `internal/dispatch/amqp.go` (durable auto-delete v1/v2 test queues, synchronous consumer barrier, mandatory RPC publish); `docker/docker-compose{,.geo,.prod}.yml` (required persisted image selector); `docker/.env.dev.example`, `docker/.env.geo.example`, `docker/.env.prod.example`; `docs/runbook.md` | opt-in real-broker `dispatch.TestAMQPRoundTrip` covers physical v1/v2 job isolation plus v1/v2 test RPC on RabbitMQ 4.3; single/distributed live-stack checks | `cerbix_broker_up`; queue durability/consumer/depth checks and per-hop rollback procedure | infra (broker/queue lifecycle) / transport (v1/v2 physical queues) / ops (staged upgrade) |
| AC-0118-1 / D-0158 non-production topology Make facade | `Makefile` (fixed base/geo env+Compose paths; build/up/ready/test/down; conflict and migration barriers); `docker/.env{.dev,.geo}.example`; `e2e/run.sh` + lockfile; `e2e/tests/{auth.setup,helpers}.ts` + `e2e/tests/topology-geo.spec.ts` | `cli.TestMakefileNonProductionStackFacade`, `TestMakefileDryRunTopologyExpansion`, `TestGeoComposeKeepsRemoteRolesOnIntendedNetworks`; missing-env and retained-volume init refusal; hostile CLI/shell Compose override rejection; both Compose configs; live single twice 37 pass/1 intentional MaC skip, distributed 10/1, geo 11/11 including scheduled AMQP+pull; per-role internal `/readyz` | `/readyz` for every role; hard-coded `localhost:8080`/`:8082` E2E endpoints | tooling/ops (fixed orchestration) / test infra (dedicated tenant, topology proof) |
| FR-017 (Monitoring as Code file provider; DONE, D-0145/0146/0147/0148) — delivered iter-0087…0110; two external reviews' P0+P1 fixed (iter-0104…0109), browser E2E executed green (iter-0110); §19 evidence table in iter-0110 | contract: `docs/specs/func-monitoring-as-code.md`; code across `internal/{config,fileprovider,fileprovider/runtime,store,api,cli}`, `frontend/src/views/MonitorsView.vue` | full parser/reconcile/store/watcher/HA/API/UI/E2E matrix (spec §18); `go test -race ./...` 32 pkgs + `e2e/mac-smoke.sh` + `e2e/tests/file-providers.spec.ts` (3/3) green | `cerbix_file_provider_*`; provider diagnostics API (`{bundles,providers}`, `?provider=`); alert rules (promtool) | application (reconcile/ownership) / domain (monitor invariants) / infra (watch+persistence+leadership) / transport (strict YAML+API provenance) |
| FR-020 / NFR-015 secret inventory and encrypted credential dispatch (**DONE** — re-accepted in iter-0120 after the r7 amendment; reopened 2026-08-15 by spec-conformance audit (pull-claim blindness, missing execution binding, envelope stripping, publish backoff) plus three further P0s found by the gen3 review, all fixed with revert-verified regressions; live fail-open pin, live generation-3 mixed-capability barrier and browser gate close the §9 matrix; spec r7 + D-0155/D-0160; the audit's non-acceptance P1/P2 remainder closed in iter-0121; iter-0115/0116/0119 stand as written) | contract `docs/specs/func-secret-inventory.md`; `internal/secret/aad.go`; `internal/config/secrets.go`; migrations `00061`/`00062`; `internal/store/{secrets,monitors,fileapply,materialize,pulljobs,pulltests,reencrypt}.go`; `internal/domain/credsettings.go`; `internal/dispatch/{credentials,amqp}.go`; `internal/{scheduler,worker,agent,ingest,api,cli,prober,metrics}`; OpenAPI/TS; `frontend/src/components/settings/SecretsPanel.vue`, monitor create/detail views; `docker/alerts/secret-inventory.rules.yml`; `e2e/secret-inventory-smoke.sh` (self-provisioning, `make secret-smoke`); `e2e/tests/secrets.spec.ts`; `docker/config.dev.yaml` (FR-020 on by default); `internal/dispatch/execbinding.go`; `frontend/src/lib/{monitorCredentials,secretErrors}.ts` | AAD context-transplant/rotation/malformed matrix; role/key/trust and master-less worker wiring matrix; tenant CRUD/quota/FK/rename-apply/rotation-audit-NOTIFY tests; strict per-type settings/TLS/ref-hash tests; API no-store/never-echo/authz; MaC ref/apply/LKG tests; authoritative before/after-read, region/cadence/disable/backoff tests; v1/v2 physical claim and AMQP queue barriers; pull persisted-payload plaintext absence; worker/agent previous/wrong/future-key outcomes and readiness; probe-error stale/SLA/live-clear matrix; 101-row reencrypt convergence + rotate CAS; UI tenant-switch/viewer and monitor-ref helper tests; live pull-agent wrong-key→recovery→PostgreSQL UP smoke; full `-race` + dual-storage store `-race` | `secret.*` audit (names only); `cerbix_secret_resolution_failed_total{reason}`, `cerbix_executor_probe_error_total{reason}`, `cerbix_dispatch_shared_trust`; `/readyz`; monitor-detail `last_probe_error`; three Prometheus rules | domain (typed settings/ref invariants) / infra (tenant-safe persistence, crypto, key ownership) / application (authoritative materialization, fencing/capability) / transport (v2 barrier, API/UI hygiene) / ops (readiness, rotation, alerts) |
| FR-017 hardening pass — third review, 7 findings (iter-0113 + governance/DoD correction iter-0114, DONE; D-0152/D-0153) | `internal/store/store.go` (pool sizing + `pgx`-parser `pool_max_conns` detection, `pool_integration_test.go`), `internal/fileprovider/parse.go`+`scan.go` (target userinfo/secret-query-key rejection D-0152; tenant-bound per-project freeze; duplicate-by-claim; bind-before-policy; string-scalar tenant), `internal/fileprovider/runtime/provider.go` (fingerprint-before-reconcile §17; `presentPaths`/`cleanlyApplied` path-keyed orphan freeze §9.1; `ProviderScopeConfig.Includes` scope quarantine D-0153; FNV-1a-64 leader key + `AssertDistinctLeaderKeys` §12; bounded warn-throttle LRU + emission budget §16), `internal/config/providers.go` (`Includes`), `internal/store/fileapply.go` (`TenantRef.SourcePath`) | pool progress + too-small-cap regressions (both storage modes); target secret/leak + duplicate-by-claim + numeric-tenant-unbound; mid-reconcile-absorb blocking-apply loop test; four path-protection regressions (typo/apply-failure/duplicate-competing-path/over-limit) — each verified to fail when its fix is reverted; scope Includes matrix + out-of-scope-not-orphaned; leader-key FNV-32-collision-pair + fail-fast guard; warn-throttle bound/hot-key/emission-budget/eviction; `go test -race` on both hypertable + declarative | `cerbix_file_provider_*` unchanged; new `file_provider_warnings_suppressed`, `file_provider_owned_out_of_scope`, `file_provider_owned_path_rejected` logs | infra (pool/leadership/persistence) / application (reconcile orphan-safety) / transport (strict target secret policy) |
| FR-017 observability + provenance + live proof (iter-0092, DONE) | `internal/metrics/metrics.go` (`cerbix_file_provider_{leader,reconcile_total{outcome},reconcile_duration_seconds,last_success_timestamp_seconds,managed_monitors,orphaned_monitors,bundle_errors}`, bounded provider label), `internal/fileprovider/runtime/provider.go` (metrics sink wired: outcome/duration/status + `store.FileProviderCounts`), `internal/api/{api.go,handlers_monitors.go}` (`management` provenance block on monitor list/detail via `MonitorProvenanceBatch`), `internal/config/providers.go` (`ResyncInterval`/`OrphanGracePeriod` → `*Duration` so explicit `0` grace = immediate disable §4.1), `docs/runbook.md`, `e2e/mac-smoke.sh` | `metrics.TestFileProviderMetrics`, `api.TestMonitorManagementProvenance`, `config.TestProviderOrphanGraceZeroIsImmediate`; `e2e/mac-smoke.sh` real-binary no-restart smoke (create + orphan-disable, process never restarts; update/LKG/restore not in the committed script — iter-0095); `go test -race ./...` 32 pkgs green | `cerbix_file_provider_*` gauges/counters | transport (provenance) / infra (metrics) / application (grace semantics) |
| FR-017 reconcile loop + HA + wiring (iter-0091, DONE) | `internal/fileprovider/runtime/provider.go` (`Provider.Run` per-provider advisory-lock leadership via `TryBecomeLeader` with a namespaced `leaderKeyFor` key + same-session `check`/stepdown; `leaderLoop` poll-watch = debounce + single dirty-bit + mandatory resync + directory-replacement recovery; `reconcile` = scan→group→`ApplyFileManagedBundle` per valid bundle + whole-project-disappearance orphan gated on `SuspendOrphan`; degraded-on-unreadable-dir keeps LKG), `internal/store/fileapply.go` (`FileProviderProjects`), `internal/cli/cli.go` (`startFileProviders`: api/all-only, fail-fast on missing DB / unreadable dir §4.1, runs under the shutdown WaitGroup) | `runtime.TestReconcileAppliesValidBundles`/`TestReconcileOrphansDisappearedProject`/`TestReconcileSuspendsOrphanOnUnbound`/`TestReconcileDegradedOnUnreadableDir` (hermetic, temp dirs + fake applier); `runtime.TestLeaderKeyDerivation` (distinct/namespaced keys §12); advisory-lock mutual-exclusion primitive reused from `store.TestLeadershipMutualExclusion`; `go test -race ./...` 32 pkgs green | `monitor_config_changed` NOTIFY (from apply); `cerbix_file_provider_*` in iter-0092 | application (leader-elected reconcile loop) / infra (advisory leadership + poll watch) / transport (role wiring) |
| FR-017 filesystem scan & grouping (iter-0090, DONE) | `internal/fileprovider/scan.go` (`ScanDirectory`: non-recursive eligible `.yaml`/`.yml` regular immediate children, dotfile/subdir/special-file skip, `EvalSymlinks` escape rejection, `max_files`/`max_file_bytes`/`max_total_bytes` enforcement, deterministic order, directory-unreadable → top-level error = keep LKG not deletion; `GroupBundles`: decode+group by resolved tenant, duplicate-project freeze §6, unbound→`SuspendOrphan` §9.1) | `fileprovider.TestScanDirectoryEligibility`/`TestScanDirectorySizeBounds`/`TestScanSymlinkEscapeRejected`/`TestGroupBundlesValidAndDuplicate`/`TestGroupBundlesUnboundSuspendsOrphan` (hermetic, temp dirs) | — | infra (bounded filesystem contract) / application (grouping + fault isolation) |
| FR-017 atomic bundle apply (iter-0089, DONE; D-0146) | `internal/store/fileapply.go` (`ApplyFileManagedBundle`: one-query tenant resolve, `readManagedSet`, `fileprovider.Plan`, per-entry apply via `insertMonitorTx`/`updateMonitorTx`/`getMonitorTx`, provenance + monotonic generation, dependency edges under the graph advisory lock, orphan-grace disable, `monitor_config_changed` NOTIFY — all in one tx), `internal/fileprovider/canonical.go` (dependency UIDs excluded from the semantic hash per D-0146) | `store.TestApplyFileManagedBundle` (real PG, both storage modes: create → canonical no-op with NO revision bump / no `updated_at` change → semantic update bumps → dependency-only no bump → first-absence orphan → grace-disable no delete → restore reuses DB id + push token; tenant-not-found rejects) | `monitor_config_changed` NOTIFY (scheduler wake); metrics in iter-0091 | application (atomic reconcile orchestration) / infra (single-tx persistence + NOTIFY) / domain (reused monitor validation + D-0142 revision) |
| FR-017 ownership & persistence (iter-0088, DONE) | `internal/store/migrations/00059_file_provider_bundles.sql` + `00060_managed_monitors.sql` (composite FKs `(project_id,org_id)→projects` and `(monitor_id,project_id)→monitors`; `monitor_id` PK = one owner), `internal/store/managed.go` (`MonitorProvenance`/`MonitorProvenanceBatch`, `assertNotFileManagedTx`, `ErrManagedByFile`), `internal/store/monitors.go` (`UpdateMonitor`/`DeleteMonitor` → tx + row-lock ownership guard; extracted shared `updateMonitorTx` config-write helper), `internal/api/{api.go,handlers_monitors.go}` (`rejectIfManaged` → `409 managed_by_file` + provenance) | `store.TestFileManagedOwnershipGuard` (real PG, both modes: unmanaged edits, ownership rejects update+delete atomically, provenance, tenant FK), `api.TestFileManagedMonitorReadOnly` (409 + body; unmanaged still 200); migrations apply on hypertable + `postgres:16-alpine`; `go test -race ./...` 31 pkgs green | none yet | infra (schema tenant safety + atomic guard) / application (typed write split) / transport (409 provenance) |
| FR-017 contract foundation (iter-0087, DONE) | `internal/config/providers.go` (strict static `providers.file`: name/scope/duration/limit/overlap validation + per-entry defaults), `internal/fileprovider/{bundle,parse,canonical,validate,plan}.go` (strict format-1 decode, scope→tenant matrix §5, UID/format checks, duration→whole-seconds, inline-secret + unsupported-type/field rejection, canonical semantic hash §7, in-bundle dependency DAG, pure deterministic plan create\|update\|dependency_update\|noop\|orphan\|restore + immutable-type guard), `docker/config.example.yaml`, `docs/overview.md` §1.2 | `config.TestProvidersFileValid`/`TestProvidersFileRejections`, `fileprovider.TestDecodeValidAndDefaults`/`TestDecodeStrictRejections`/`TestScopeMatrix`/`TestCanonicalHashSemantics`/`TestDependencyDAG`/`TestPlanCreateNoopUpdate`/`TestPlanDependencyOnlyUpdate`/`TestPlanOrphanRestore`/`TestPlanTypeChangeRejected`; `go test ./...` (31 pkgs), `go vet`, `git diff --check` — green | none yet (metrics wired with the reconciler, iter-0090) | transport (strict YAML/config) / domain (monitor invariants reused) / application (pure plan) |
| NFR-014 (safe declarative reconciliation; DONE, D-0145/0146/0147/0148) — delivered iter-0087…0110; two external reviews' P0+P1 fixed + verified (iter-0104…0110) | contract: `docs/specs/func-monitoring-as-code.md`; enforced across `internal/{fileprovider,fileprovider/runtime,store}` | negative tenant/ownership/LKG/no-op/resource-bound/race tests (spec §18) — incl. unbound-absence non-destructive guard, no-op idempotence (no write), fencing, event-storm bound, two-replica failover, apply-tx deadline, both storage modes | degraded/LKG status + fleet alerts (runbook + promtool-validated rules) | cross-layer; tenant isolation remains domain-owned |
| NFR-001 (fail-fast config) | `internal/config/config.go` (`Validate`, `KnownFields`) | `internal/config/config_test.go` | — | infra |
| NFR-002 (no self-healing) | `internal/cli/cli.go` (`runServe` aborts on load error) | `internal/cli/cli_test.go` (`TestServeFailsOnMissingConfigFile`) | — | infra |
| NFR-003 (logging, no secrets) | `internal/logging/logger.go` | `internal/logging/logger_test.go` | — | infra |
| NFR-004 (`cerbix_` metrics) | `internal/metrics/metrics.go` | `internal/metrics/metrics_test.go` | `cerbix_build_info`, `cerbix_up`, `cerbix_ready`, `cerbix_uptime_seconds` | infra |
| NFR-005 (graceful shutdown) | `internal/cli/cli.go` (`signal.NotifyContext`) | smoke (iter-0001 report) | — | application |
| FR-005 (org/project/membership model) | `internal/domain/domain.go`, `internal/store/{organizations,projects,users,memberships}.go` | `internal/domain/domain_test.go`, `internal/store/store_integration_test.go` | — | domain (rules) / infra (persistence) |
| FR-006 / NFR-006 (tenant isolation) | `internal/store/organizations.go` (`ListOrganizationsForUser`), `projects.go` (`ListProjectsForUser`), `migrations/00001_init.sql` (composite FK) | `store_integration_test.go` (`TestTenantIsolation`, `TestProjectMembershipMustBelongToOrg`) | — | domain |
| FR-014 (migrations) | `internal/store/migrate.go`, `migrations/00001_init.sql`, `internal/cli/cli.go` (`runMigrate`) | `internal/cli/cli_test.go` (`TestMigrate*`), `store_integration_test.go` | — | infra |
| NFR-009 (DB-gated readiness) | `internal/cli/cli.go` (`pingDatabase`), `internal/metrics/metrics.go` (`SetDatabaseUp`) | `internal/metrics/metrics_test.go` (`TestDatabaseUpOnlyExportedWhenEnabled`); serve smoke (iter-0002 report) | `cerbix_database_up`, `/readyz` | application |
| FR-004 (provider-agnostic OIDC login/JIT/sessions; D-0043/D-0044/D-0045) | `internal/auth/*` (`oidc.NewProvider` discovery — any issuer), `internal/auth/handlers.go` (`ConfigHandler`), `internal/store/{users,sessions,authflows}.go` (`UpsertUserByOIDCSub`, `users.oidc_sub`), `migrations/{00001_init,00002_sessions,00004_local_users}.sql`, `internal/config/config.go` (`OIDCConfig.ButtonLabel`) | `internal/auth/auth_test.go` (mock OIDC), `auth/config_handler_test.go` (`/auth/config` payload), `config_test.go` (`TestOIDCButtonLabelDefault`), `store_integration_test.go` (`TestSessionLifecycle`, `TestAuthFlowConsumedOnce`, `TestLocalUsersCoexistWithOIDC`) | `/auth/login`, `/auth/callback`, `/auth/logout`, public `GET /auth/config` → `{local, oidc, oidc_button_label}` | application (flow) / infra (persistence) / transport (config discovery) |
| FR-005 (org/project/member API) | `internal/api/*` | `internal/api/api_test.go` | `/api/v1/organizations*`, `/api/v1/projects/{id}` | transport (shape) / domain (rules) |
| FR-006 / NFR-006 (authz + isolation) | `internal/authz/authz.go`, `internal/api/handlers.go`, `internal/auth/middleware.go` | `authz_test.go`, `api_test.go`, `store_integration_test.go` | — | domain |

| FR-007 (monitors + probers + conditions) | `internal/domain/monitor.go`, `internal/prober/*`, `internal/store/{monitors,heartbeats}.go`, `internal/api/handlers_monitors.go`, `migrations/00003_monitors.sql` | `prober/*_test.go`, `api/api_test.go`, `store_integration_test.go` | `/api/v1/projects/{id}/monitors`, `/api/v1/monitors/{id}` | domain / infra / transport |
| FR-008 (scheduler/dispatch/worker/ingest) | `internal/scheduler`, `internal/dispatch`, `internal/worker`, `internal/ingest`, `internal/store/leadership.go` | `scheduler/scheduler_test.go`, `dispatch/inproc_test.go`, `worker/worker_test.go`, `ingest/ingest_test.go`, `store_integration_test.go` (`TestCheckingPipelineEndToEnd`, `TestLeadershipMutualExclusion`) | `cerbix_checks_total` | application |

| FR-015 (default local login) | `internal/auth/{password,local}.go`, `internal/store/users_local.go`, `migrations/00004_local_users.sql`, `internal/api/handlers.go` (`changePassword`) | `auth/{password,local}_test.go`, `api/api_test.go` (`TestChangePassword`), `store_integration_test.go` (`TestLocalUsersCoexistWithOIDC`), `config/config_test.go` (local rules) | `/auth/local/login`, `/api/v1/me/password` | application / infra / transport |
| NFR-010 (password hashing/uniform errors; local-login rate limiting, D-0031) | `internal/auth/password.go`, `local.go`, `internal/auth/ratelimit.go` (`loginLimiter`, per-client-IP sliding window, `login_rate_limit_per_minute`) | `auth/password_test.go`, `auth/local_test.go`, `auth/ratelimit_test.go` | — | application |

| FR-009 (SLA/SLI + maintenance; p95 D-0046) | `internal/sla/sla.go`, `internal/store/sla.go` (avg + `percentile_cont(0.95)` `p95_latency_ms`, maintenance-excluded), `migrations/00005_sla.sql`, `internal/api/handlers_sla.go` (`P95LatencyMS`) | `sla/sla_test.go`, `api/api_test.go` (SLA/maintenance), `store_integration_test.go` (`TestSLAAndMaintenanceExclusion`, `TestSLATargetUpsert`, `TestMaintenanceWindowLifecycleIsArchiveNotDelete`) | `/api/v1/monitors/{id}/sla`, `/api/v1/projects/{id}/sla` (`p95_latency_ms`), `/api/v1/*/maintenance` | domain (compute) / infra (aggregation) / transport |

| FR-011 (SPA serving + API contract) | `internal/web/web.go`, `internal/web/dist/`, `openapi.yaml`, `internal/cli/cli.go` (mount) | `web/web_test.go` | serves `/` and static assets | transport |
| FR-011 (SPA views + typed client; all views 1:1 with design artifacts, D-0049) | `frontend/src/` — `api/` (generated from `openapi.yaml`), `stores/{session,workspace,ui}.ts`, `router/`, `components/{AppShell,SearchBox,CreateDialog,StatusPill,UptimeBar,Sparkline,MonitorCard,Kpi}.vue`, `views/{Login,Dashboard,Monitors,NewMonitor,MonitorDetail,Members,Sla,Settings,Incidents,IncidentDetail,NewIncident,StatusPages,PublicStatus}View.vue` (of these `MembersView.vue` was later deleted by D-0102), bespoke inline-SVG charts (no TanStack Query/uPlot) | Docker image build (`vue-tsc` type-check + `vite build`) + e2e through the nginx proxy | consumes `/api/v1/*`, `/auth/*` | presentation (types single-sourced from the contract) |

| FR-012 (incidents + timeline + postmortems, phase 1) | `internal/domain/incident.go`, `internal/store/incidents.go`, `migrations/00006_incidents.sql`, `internal/api/handlers_incidents.go`, `openapi.yaml` | `domain/incident_test.go`, `api/api_incidents_test.go` (lifecycle/authz/isolation/terminal/postmortem-gate), `store_integration_test.go` (`TestIncidentLifecyclePersistence`) | `/api/v1/projects/{id}/incidents`, `/api/v1/incidents/{id}[/updates][/postmortem]`; metric `cerbix_incidents_opened_total` | domain (lifecycle/enums) / infra (tx persistence) / transport (authz+isolation) |

| FR-012 (status pages + components + rendering, phase 2a) | `internal/domain/statuspage.go`, `internal/store/statuspage.go`, `migrations/00007_status_pages.sql`, `internal/api/handlers_statuspage.go` (+`PublicRouter`, mounted unauthenticated in `cli.go`), `openapi.yaml` | `domain/statuspage_test.go`, `api/api_statuspage_test.go` (mgmt authz/isolation, monitor-org guard, public visibility gate incl. internal-404/unlisted-token), `store_integration_test.go` (`TestStatusPageAndComponentPersistence`) | `/api/v1/organizations/{id}/status-pages`, `/api/v1/status-pages/{id}[/render][/components]`, `/api/v1/components/{id}`, unauthenticated `/api/v1/public/status-pages/{slug}` | domain (visibility/derive/summary) / infra (persistence) / transport (authz + visibility gate) |

| FR-012 (API tokens / service accounts, phase 2b-i) | `internal/domain/apitoken.go`, `internal/store/apitokens.go`, `migrations/00008_api_tokens.sql`, `internal/auth/{context.go,middleware.go}` (Bearer path), `internal/api/handlers_apitokens.go`, `authz` (`Principal.ViaToken`), `openapi.yaml` | `domain/apitoken_test.go`, `auth/apitoken_middleware_test.go` (bearer→principal, 401), `api/api_apitokens_test.go` (mgmt authz + incident-via-token `source=api` + viewer-403 + cross-org-404), `store_integration_test.go` (`TestApiTokenPersistence`) | `/api/v1/organizations/{id}/tokens`, `/api/v1/tokens/{id}`, `Authorization: Bearer` on all `/api/v1/*` | domain (scope/role) / infra (hashed persistence) / transport (bearer auth + role-driven authz) |
| FR-008 scheduler efficiency (iter-0031) | `scheduler/scheduler.go` (in-memory snapshot refreshed every `refreshEvery`, `pruneNextRun`, `checkStalePush` batched), `store/monitors.go` (`StalePushMonitors` — single indexed query), dropped per-tick scan + per-push `LatestHeartbeat` N+1 | `scheduler_test.go` (schedules from snapshot; publishes downs for the batched stale set), `store/pushstale_internal_test.go` (stale/never-reported returned; fresh/down/non-push excluded — DB-gated), real-binary smoke (leader healthy across both cadences) | scheduler DB load | application (cadence) / infra (indexed staleness query) |
| NFR-013 key rotation (iter-0034) | `internal/secret/secret.go` (keyring: `New(keys...)`, primary-encrypt, try-all-decrypt), `config` (`previous_keys`, `Keys()`), `store/reencrypt.go` (`ReencryptSecrets`), `cli` (`reencrypt` subcommand + serve keyring) | `secret/secret_test.go` (`TestKeyringRotation`, no-match error), `store/reencrypt_internal_test.go` (A→[B,A] reencrypt → readable by B not A — DB-gated), `config_test.go` (`previous_keys` validation), real-binary rotation e2e (reencrypt [B,A]; B-only ok, A-only fails) | secret key lifecycle | infra (keyring + reencrypt) / config (keys) / cli (rotation cmd) |
| NFR-013 (secrets encrypted at rest, iter-0033) | `internal/secret/secret.go` (AES-256-GCM Cipher, `enc:v1:` + passthrough), `store/{store,webhooks,notifications}.go` (`WithCipher`; scan/collect→methods decrypt; Create* encrypt), `config` (`security.encryption_key` + `EncryptionKeyBytes` validation), `cli` (wire) | `secret/secret_test.go` (round-trip, legacy passthrough, wrong-key GCM fail, nil identity, key-length), `store/encryption_internal_test.go` (raw column `enc:v1:`, reads plaintext, legacy readable — DB-gated), `config_test.go` (key validation), real-binary e2e (webhook via API → DB `enc:v1:`, plaintext round-trip) | webhook/channel secret columns | infra (column-boundary crypto) / config (key) |
| NFR-012 (heartbeat retention + partitioning, iter-0030) | `migrations/00017_partition_heartbeats.sql` (RANGE-by-ts daily partitions + DEFAULT, in-place convert), `store/retention.go` (`EnsureHeartbeatPartitions`, `PurgeOldHeartbeats`), `scheduler/scheduler.go` (`maintainPartitions`, `WithRetentionDays`, rollup window = retention), `config` (`heartbeats.retention_days`), `cli` (wire) | `store/retention_internal_test.go` (partition auto-create + insert routed off default; old dropped, recent kept — DB-gated), `scheduler_test.go` (`TestSchedulerLeaderMaintainsPartitions`, `TestWithRetentionDaysClampsLow`), `config_test.go` (default/validation), real-binary e2e (migration 00017 + leader pre-created today..+2) | heartbeats storage/retention | infra (partitions + drop) / application (scheduler cadence + retention↔rollup coupling) |
| FR-012 outbox dead-letter ops (iter-0032) | `store/outbox.go` (`ListDeadOutbox`, `ReplayDeadOutbox`, `ReplayAllDeadOutbox`), `domain/outbox.go` (`OutboxEventView`), `api/handlers_outbox.go` (global-admin endpoints), `api/api.go` (routes + Store iface), `openapi.yaml` 0.19.0 | `store/deadoutbox_internal_test.go` (list dead only; replay→pending/0/due; non-dead→ErrNotFound; replay-all count — DB-gated), `api/api_outbox_test.go` (global-admin only 403/200; replay 204/404; replay-all), real-binary e2e (login→list→replay→worker delivers→list empty; no-cookie 401) | `/api/v1/admin/outbox/dead[...]` | transport (global-admin authz) / infra (dead→pending reset) |
| FR-010 / FR-012 reliable delivery (Transactional outbox, iter-0029) | `migrations/00016_outbox_events.sql`, `store/outbox.go` (`enqueueOutboxTx`, `ClaimDueOutbox` SKIP LOCKED + lease/backoff, `MarkOutboxDelivered`, `FailOutbox`), `store/{incidents,monitors}.go` (in-tx enqueue), `domain/outbox.go` (`MonitorTransition.ShouldNotify`), `internal/outbox/outbox.go` (worker), `notify`/`webhook` `Deliver(...) error`, `internal/cli/cli.go` (wire), `metrics` (`cerbix_outbox_delivered_total`/`_dead_total`) | `store/outbox_internal_test.go` (in-tx enqueue on incident + transition; claim/lease; delivered/dead — DB-gated), `outbox/outbox_test.go` (dispatch, policy skip, deleted-monitor drop, retry→dead, nil-deliverer/unknown-topic, Run cancel), real-binary e2e (seeded event pending→delivered + metric) | outbound webhook + notification delivery | infra (in-tx enqueue + claim) / application (delivery policy in the worker) |
| NFR-011 (SSRF guard, iter-0028) | `internal/prober/guard.go` (`Guard.checkIP`, `dialContext` — resolve+pin checked IP; `resolveChecked` for ICMP), `prober.go` (`NewRunnerWithGuard`: guarded HTTP transport + TCP dialer, `Proxy=nil`), `icmp.go` (guarded resolve), `internal/config/config.go` (`ProberConfig`, defaults private=on/metadata=off), `internal/cli/cli.go` (wire), `docker/config.example.yaml` | `prober/guard_test.go` (`checkIP` matrix; literal-IP + hostname dial rejected pre-connect; full `Run` path blocks metadata HTTP + locked-down loopback TCP) | probe targets on the worker | infra (dial-time IP policy; single owner in `prober`) |
| FR-013 / NFR-007 (OIDC client-credentials, iter-0027; provider-agnostic per D-0043; service-token hashing) | `internal/auth/auth.go` (`ccVerifier`, `SkipClientIDCheck`), `internal/auth/middleware.go` (`principalFromJWT`, JWT bearer fallthrough after service-token `ErrNotFound`), `store.UpsertUserByOIDCSub` (JIT by `sub`), `store.HashToken` (service tokens hashed at rest) | `auth/clientcreds_test.go` (minted client-creds JWT → 204 + `ViaToken` principal + JIT by `sub` + no memberships until granted; invalid bearer → 401), `auth/apitoken_middleware_test.go` (hashed service-token bearer) | `Authorization: Bearer <OIDC-JWT>` on all `/api/v1/*` (issuer-trusted, membership-gated) | transport (bearer verification) / infra (JIT provisioning + hashed token persistence) / authz (`Can` unchanged — one model for cookie/token/JWT) |

| FR-012 (auto-incidents from monitor transitions, phase 2b-ii) | `internal/ingest/ingest.go`, `internal/store/{incidents.go,monitors.go}` (prev-returning `SetMonitorStatus`, `FindOpenAutoIncidentByMonitor`, `incidents.monitor_id`), `migrations/00009_incident_monitor.sql`, `openapi.yaml` (Incident.monitor_id) | `ingest/ingest_test.go` (`TestAutoIncidentOpenAndResolve` — open/dedup/resolve + counter), `store_integration_test.go` (`TestMonitorStatusTransitionAndAutoIncident`) | ingest side-effect; metric `cerbix_incidents_opened_total` | application (transition→incident) / infra (prev-status + open lookup) |

| FR-012 (status-page incident feeds, phase 2b-iii) | `internal/feed/feed.go` (JSON Feed/RSS/Atom), `internal/api/handlers_feed.go` (+authed & public routes), `openapi.yaml` | `feed/feed_test.go` (3 formats marshal/parse + content-types), `api/api_feed_test.go` (public gate + format content-types + authed isolation) | `/api/v1/status-pages/{id}/feed`, unauthenticated `/api/v1/public/status-pages/{slug}/feed` (`?format=rss\|atom\|json`) | domain-free rendering / transport (visibility gate reused) |

| FR-012 (outbound webhooks, phase 2b-iv) | `internal/domain/webhook.go` (Webhook + `IncidentEvent`), `internal/store/webhooks.go`, `migrations/00010_webhooks.sql`, `internal/webhook/webhook.go` (dispatcher + HMAC sign), `internal/api/handlers_webhooks.go` (emit on incident events; the `IncidentSink` seam of this row was later replaced by the outbox's `WebhookDeliverer` when delivery moved in-transaction, D-0036), `internal/ingest/ingest.go` (emit on auto-incident), `cli.go` (wire + Run), `openapi.yaml` | `domain/webhook_test.go`, `webhook/webhook_test.go` (sign + delivery via httptest + failure tolerance), `store_integration_test.go` (`TestWebhookScopeSelection`), `api/api_webhooks_test.go` (mgmt authz + events emitted), `ingest/ingest_test.go` (auto-incident emits) | `/api/v1/organizations/{id}/webhooks`, `/api/v1/webhooks/{id}`; signed `X-Cerbix-Signature` deliveries | domain (event/validate) / infra (persistence + async delivery) / transport (mgmt authz) |

| NFR (distributed dispatch) | `internal/dispatch/amqp.go` (RabbitMQ Dispatcher), `internal/cli/cli.go` (role-aware pipeline), `go.mod` (amqp091-go) | `dispatch/amqp_test.go` (opt-in `CERBIX_TEST_RABBITMQ_URL` v1/v2 round-trip); real-binary 3-process e2e (scheduler+worker+api over the broker → monitor up; 2nd worker scales) | durable regional `checks.jobs.<region>` / `checks.jobs.v2.<region>` and shared `checks.results`; roles `api`/`scheduler`/`worker` | infra (transport) / application (role wiring) |

| FR-010 (notifications on status change) | `internal/domain/notification.go`, `internal/store/notifications.go`, `migrations/00011_notifications.sql`, `internal/notify/notify.go` (dispatcher + per-type render), `internal/ingest/ingest.go` (`reconcileTransition` fires notify), `internal/api/handlers_notifications.go`, `cli.go`, `openapi.yaml` | `domain/notification_test.go`, `notify/notify_test.go` (render 3 types + delivery), `store_integration_test.go` (`TestNotificationChannelPersistence`), `api/api_notifications_test.go` (channel authz + monitor linking), `ingest/ingest_test.go` (transition notifies) | `/api/v1/projects/{id}/notification-channels`, `/api/v1/monitors/{id}/notifications`; webhook/slack/telegram delivery on down/recover | domain (validate) / infra (persist + async deliver) / application (transition→notify) |

| FR-monitoring (push / dead-man's-switch prober) | `internal/domain/monitor.go` (PushToken + validate), `migrations/00012_push_token.sql`, `internal/store/monitors.go` (`GetMonitorByPushToken`), `internal/scheduler/scheduler.go` (`evaluatePush` liveness), `internal/api/handlers_push.go` (+`ResultSink`), `cli.go`, `openapi.yaml` | `domain/monitor_test.go` (push validate), `store_integration_test.go` (`TestPushMonitorToken`), `scheduler/scheduler_test.go` (`TestSchedulerPushLiveness`), `api/api_push_test.go` (push endpoint + token) | unauthenticated `POST /api/v1/public/push/{token}`; scheduler-driven timeout | domain (validate) / infra (token + liveness) / transport (push endpoint) |

| Monitor edit (PATCH) | `internal/store/monitors.go` (`UpdateMonitor`), `internal/api/handlers_monitors.go` (`updateMonitor` partial), `openapi.yaml`, `frontend/src/views/NewMonitorView.vue` (create/edit), `router` (`monitor-edit`), `MonitorDetailView` (Edit) | `store_integration_test.go` (`TestUpdateMonitor`), `api/api_monitors_test.go` (partial + authz + immutable type + 404), frontend build + proxy e2e | `PATCH /api/v1/monitors/{id}` | infra (update) / transport (partial merge + authz) / presentation (shared form) |

| Add member by email + login rate-limit (NFR-010) | `internal/store/users.go` (`GetUserByEmail`), `internal/api/handlers.go` (`addMember` email path), `internal/auth/ratelimit.go` + `local.go` (429), `config.go` (`login_rate_limit_per_minute`), `openapi.yaml` | `api/api_members_test.go` (email 201/400 + authz), `auth/ratelimit_test.go` (window/per-key/disabled/clientIP), `store_integration_test.go` (`TestGetUserByEmail`); real-binary e2e (add-by-email, 10→429) | `POST …/members` (email), `POST /auth/local/login` (429) | infra (lookup) / transport (email resolve + limiter) |

| Daily-availability rollup + endpoints (D-0017 realized) | `migrations/00015_heartbeats_daily.sql`, `internal/store/rollup.go` (`RollupDailyAvailability`, `MonitorDailyAvailability`, `ProjectDailyAvailability`), `internal/scheduler/scheduler.go` (leader rollup tick), `internal/api/handlers_availability.go`, `openapi.yaml`, `frontend/src/views/DashboardView.vue` (90-day strip) | `store/rollup_internal_test.go` (rollup + maintenance-exclusion + project), `api/api_availability_test.go`, real-binary e2e (yesterday rollup 83.3% + today live) | `GET /monitors/{id}/availability`, `/projects/{id}/availability` (`?days=`) | infra (recompute-range job) / application (scheduler tick) / transport+presentation | D-0033 |

| FR-016 (global tenant-scoped search, iter-0035) | `internal/domain/search.go` (`SearchHit`), `internal/store/search.go` (`Search`, `escapeLike` — escaped `ILIKE` over projects/monitors/incidents, un-scoped by design), `internal/api/handlers_search.go` (`VisibleProject` filter — single enforcement point), `internal/api/api.go` (route + Store iface), `openapi.yaml`, `frontend/src/components/SearchBox.vue` | `api/api_search_test.go` (o1 viewer sees only o1; outsider 0; 1-char query 0), `store/search_internal_test.go` (`gate`→monitor+incident, `api`→project, `%`→0 escaping — DB-gated) | `GET /api/v1/search?q=` (min 2 chars) | infra (escaped LIKE) / transport (tenant filter in handler) / presentation (debounced box) | D-0047 |
| FR-018 (delete project, iter-0111; D-0150) | contract: `docs/specs/func-project-deletion.md`; `internal/store/projects.go` (`DeleteProject` — tenant-scoped single-tx cascade + file-ownership guard), `internal/api/{handlers.go,api.go}` (`deleteProject`, `DELETE /projects/{projectID}`, `ActionOrgManage`), `openapi.yaml` (+ members-delete path drift fix → TS regen), `frontend/src/{stores/workspace.ts,components/settings/DangerZonePanel.vue,views/SettingsView.vue}` | `store.TestDeleteProject` (both storage modes: cascade + tenant scoping + `ErrManagedByFile`), `api.TestDeleteProjectAuthz` (204 org/global-admin · 403 project-admin/viewer · 404 outsider/unknown · 409 managed), `e2e/tests/project-delete.spec.ts` (live-stack happy path + cascade); frontend `vue-tsc`+`vite` build | `DELETE /api/v1/projects/{projectID}` (204/403/404/409) | transport (RBAC + 409) / application (tenant-scoped delete) / infra (DB cascade) |
| FR-019 (delete organization, iter-0112; D-0151) | contract: `docs/specs/func-org-deletion.md`; `internal/store/organizations.go` (`DeleteOrganization` — single-tx cascade + file-ownership guard), `internal/api/{handlers.go,api.go}` (`deleteOrganization`, `DELETE /organizations/{orgID}`, `ActionGlobalManage`, instance-level `org.delete` audit), `openapi.yaml`, `frontend/src/{stores/workspace.ts,components/settings/OrgDangerZonePanel.vue,views/SettingsView.vue}` | `store.TestDeleteOrganization` (both storage modes: two-level cascade + tenant survival + `ErrManagedByFile`), `api.TestDeleteOrganizationAuthz` (204 global-admin · 403 org-admin/member/outsider · 404 unknown · 409 managed), `e2e/tests/org-delete.spec.ts` (live-stack happy path + org→project→monitor cascade); frontend `vue-tsc`+`vite` build | `DELETE /api/v1/organizations/{orgID}` (204/403/404/409) | transport (global-admin RBAC + 409) / application (cascade delete) / infra (DB cascade) |
| Status-page edit/delete + incident→monitor link + dashboard KPI parity (iter-0035, D-0050) | `store/statuspage.go` (`UpdateStatusPage`, `DeleteStatusPage` — components cascade), `api/handlers_statuspage.go` (`updateStatusPage`/`deleteStatusPage`, org-admin), `api/handlers_incidents.go` (`createIncident` optional `monitor_id`, project-scoped validate), `api/api.go` (routes + Store iface), `openapi.yaml` (PATCH/DELETE + `UpdateStatusPage` + `CreateIncident.monitor_id`), `frontend/src/views/{StatusPages,NewIncident,Dashboard}View.vue`, `frontend/src/components/{MonitorCard,StatusPill}.vue` | `api/api_statuspage_test.go` (`TestUpdateStatusPageAuthz`, `TestDeleteStatusPageAuthz` — viewer 403 / outsider 404 / admin 200·204 / gone-after-delete) | `PATCH`/`DELETE /api/v1/status-pages/{id}`; `POST …/incidents` (`monitor_id`) | infra (cascade delete) / transport (org-admin authz + project-scoped monitor check) / presentation (dashboard error-budget/p95/pulse) | D-0050 |
| Member identity + role change / removal (D-0051) | `domain/domain.go` (`Member` = membership + email/display_name/last_active_at), `store/memberships.go` (`ListOrgMembers` JOIN users + last session, `GetMembership`, `UpdateMembershipRole`, `DeleteMembership`, `CountOrgAdmins`), `api/handlers.go` (`memberAccess`, `updateMember`/`removeMember`, last-admin guard), `api/api.go` (routes + Store iface), `openapi.yaml` (`Member`/`UpdateMember` + PATCH/DELETE), `frontend/src/views/MembersView.vue` (deleted by D-0102 → `frontend/src/components/settings/MembersPanel.vue`) | `api/api_members_test.go` (`TestUpdateMemberAuthz`, `TestRemoveMemberAuthz`, `TestLastOrgAdminGuard`), `store` `TestOrgMembersEnrichmentAndMutation` (enrichment + last-active + role update + delete + admin count, over Postgres) | `PATCH`/`DELETE /api/v1/organizations/{orgID}/members/{membershipID}`; enriched `GET …/members` | infra (join enrichment) / transport (org-admin authz + scope-valid role + lockout guard) / presentation (role select + remove) | D-0051 |
| Public status page enrichment (D-0052) | `api/handlers_statuspage.go` (`writeStatusPageRender`: per-component `daily` via `MonitorDailyAvailability`, `maintenance` active/upcoming, `recent_incidents` resolved-90d), `openapi.yaml` (`ComponentDay` + `ComponentView.daily` + render `recent_incidents`/`maintenance`), `frontend/src/views/PublicStatusView.vue` (per-day strip, maintenance card, past-incidents) | `api/api_statuspage_test.go` (`TestPublicRenderEnriched` — daily strip, active vs resolved split, upcoming maintenance) | `GET /api/v1/public/status-pages/{slug}` (enriched render) | transport (render assembly) / presentation (strip + history) | D-0052 |
| Monitor method + push grace + channel linking (D-0053) | `store/migrations/00003_monitors.sql` (`method`, `grace_seconds` cols), `domain/monitor.go` (`Method`/`GraceSeconds` + `Normalize`/`Validate`), `store/monitors.go` (columns/scan/insert/update + `StalePushMonitors` grace), `prober/prober.go` (issue method), `api/handlers_monitors.go` (accept+normalize), `openapi.yaml`, `frontend/src/views/NewMonitorView.vue` (method select, grace input, Notifications checkboxes over `/monitors/{id}/notifications`) | `domain` `TestMonitorMethodAndGrace`, `prober` `TestHTTPProberUsesMethod`, `store` `TestStalePushGrace`, `api` `TestCreateMonitorMethodAndGrace` | `POST/PATCH monitors` (`method`,`grace_seconds`); `POST/DELETE /monitors/{id}/notifications` | domain (normalize/validate) / infra (columns + grace liveness) / transport (accept) / presentation (form) | D-0053 |
| Structured postmortem (D-0054) | `frontend/src/lib/postmortem.ts` (serialize/parse sections over markdown `body`, legacy fallback to Summary), `frontend/src/views/IncidentDetailView.vue` (section editor + styled render + edit-existing upsert), `frontend/src/views/IncidentsView.vue` (section render) | frontend type-check + build (no FE test harness; pure lib) | `PUT /api/v1/incidents/{id}/postmortem` (body unchanged — markdown) | presentation only (backend contract unchanged) | D-0054 |
| Status-page email subscribers (D-0055) | `store/migrations/00007_status_pages.sql` (`subscribers`), `domain/statuspage.go` (`Subscriber`), `store/subscribers.go` (create/confirm/delete + `ConfirmedSubscriberEmailsForProject`), `config` (`MailConfig`), `internal/mailer`, `internal/subscribe` (outbox fan-out notifier), `api/handlers_subscribe.go` (+public routes, `WithMailer`), `cli` (`incidentFanout` composite, mailer wiring), `openapi.yaml` (`Subscribe` + 3 public paths), `frontend/src/views/PublicStatusView.vue` (subscribe form + confirm/unsubscribe) | `mailer` `TestLiveMailerResolvesPerSend`, `subscribe` `TestNotifierFansOut`, `store` `TestSubscriberLifecycleAndFanout`, `api` `TestSubscribeConfirmUnsubscribe`/`TestSubscribeDisabledWithoutMailer`, `config` mail validation | public `POST /status-pages/{slug}/subscribers`, `POST /subscriptions/{token}/confirm`, `DELETE /subscriptions/{token}` | infra (subscribers + SMTP + outbox fan-out) / transport (public double opt-in) / presentation (form) | D-0055 |
| Realtime status via SSE (D-0056) | `internal/events` (in-process broker), `internal/ingest/ingest.go` (`WithEvents`, publish on transition), `api/handlers_events.go` (`GET /api/v1/events`, visibility filter), `api/api.go` (`EventSource`, `WithEvents`, `ListProjectsForUser` on Store, route), `cli` (broker wiring to ingest + api), `frontend/src/stores/live.ts` (EventSource singleton), `DashboardView`/`MonitorDetailView` (live patch), `session.ts` (disconnect on logout) | `events` `TestBrokerPublishSubscribe`/`TestBrokerDropsOnFullBuffer`, api `TestEventsDisabledWithoutSource`/`TestEventsStreamFiltersByVisibility` | `GET /api/v1/events` (SSE, tenant-filtered) | infra (pub/sub) / application (ingest publish) / transport (SSE + visibility) / presentation (EventSource) | D-0056 |
| Per-control RBAC hiding (D-0057) | `frontend/src/stores/session.ts` (`canProjectWrite` getter mirroring ActionProjectWrite), gating in `views/{Dashboard,Monitors,MonitorDetail,Sla,Incidents,IncidentDetail,Settings,StatusPages,NewMonitor,NewIncident}View.vue` | frontend type-check + build (presentation-only; backend authz tests remain the enforcement guarantee) | — (UI reflects existing `authz.Can`; server still enforces 403/404) | presentation only — defense-in-depth over the transport/authz layer | D-0057 |
| DNS + TLS-cert probers (Phase 6 batch 1, D-0058) | `domain/monitor.go` (`MonitorDNS`/`MonitorTLS` + Active/Valid), `store/migrations/00014_icmp_monitor.sql` (type CHECK), `prober/dns.go`, `prober/tls.go` (guarded dial + `InsecureSkipVerify` leaf-cert read), `prober/conditions.go` (`[CERT_EXPIRY]` + `Values.CertExpiryDays`), `prober/prober.go` (Result field, registry, judge), `openapi.yaml` (type enums), `frontend/src/views/NewMonitorView.vue` (chips/labels/icons/defaults) | `prober` `TestDNSProber`/`TestTLSProber`, `domain` `TestDNSAndTLSMonitorTypesActive` | monitor types `dns`,`tls`; `[CERT_EXPIRY]` condition | domain (types) / infra (probers via SSRF guard) / presentation (form) | D-0058 |
| gRPC health prober (Phase 6 batch 2, D-0059) | `domain/monitor.go` (`MonitorGRPC` + Active/Valid), `store/migrations/00014_icmp_monitor.sql` (type CHECK), `prober/grpc.go` (guarded dial + `grpc.health.v1` Check), `prober/prober.go` (registry), `go.mod` (`google.golang.org/grpc`), `openapi.yaml` (type enums), `frontend/src/views/NewMonitorView.vue` (chip/label/icon) | `prober` `TestGRPCProber` (SERVING up / NOT_SERVING down / closed down, real health server) | monitor type `grpc` | domain (type) / infra (grpc health via SSRF guard) / presentation (form) | D-0059 |
| Audit log of access changes (Phase 7, D-0060) | `store/migrations/00018_audit_logs.sql`, `domain/audit.go` (`AuditEntry`), `store/audit.go` (`RecordAudit`/`ListAuditByOrg` + actor join), `api/handlers_audit.go` (`audit` helper + `listAudit`, org-admin), recording in `api/handlers.go` (member add/role/remove) + `api/handlers_apitokens.go` (token create/delete), `api/api.go` (route + Store iface), `openapi.yaml` (`AuditEntry` + path), `frontend/src/views/MembersView.vue` (audit section; that view was deleted by D-0102 → `components/settings/MembersPanel.vue`) | `store` `TestAuditRecordAndList`, `api` `TestAuditRecordedAndListed`/`TestAuditListAuthz` | `GET /api/v1/organizations/{orgID}/audit` | infra (append + join) / transport (org-admin authz + best-effort record) / presentation (list) | D-0060 |
| Composite (group) monitor + `monitors.config` (D-0061) | `store/migrations/{00003_monitors,00014_icmp_monitor}.sql` (config jsonb + type CHECK), `domain/monitor.go` (`MonitorComposite`, `Config`, `NeedsTarget`, `ChildIDs`/`CompositeMode`, target-less validate), `store/monitors.go` (config scan/insert/update, `MonitorStatuses`), `prober/composite.go` + `prober/prober.go` (`ChildStatusFunc`/`WithChildStatus`, registry), `cli` (`runner.WithChildStatus(st.MonitorStatuses)`), `api/handlers_monitors.go` (config in body + `compositeChildrenOK` in-project check), `openapi.yaml` (enum + `config`), `frontend/src/views/NewMonitorView.vue` (Group type + members picker) | `prober` `TestCompositeProber`/`TestCompositeWithoutLookup`, `domain` `TestCompositeMonitorValidation`, `store` `TestMonitorConfigAndStatuses`, api `TestCreateCompositeMonitor` | monitor type `composite`; `POST/PATCH monitors` `config` | domain (types/config) / infra (child-status lookup) / transport (in-project validation) / presentation (picker) | D-0061 |
| Encrypted config + PostgreSQL prober (D-0062) | `domain/monitor.go` (`MonitorPostgres`, `SecretMonitorConfigKeys`, `Redacted`), `store/monitors.go` (scanMonitor/marshalConfig → cipher-aware methods, secret encrypt/decrypt), `store/migrations/00014` (type CHECK), `prober/postgres.go` (pgx + guarded DialFunc + query), `prober/prober.go` (registry), `api/handlers_monitors.go` (redact responses + preserve-on-empty secret), `openapi.yaml` (enum), `frontend/src/views/NewMonitorView.vue` (Connection section, write-only password) | `domain` `TestPostgresTypeAndRedaction`, `store` `TestMonitorConfigPasswordEncrypted`, api `TestPostgresMonitorRedactionAndPreserve`, `prober` `TestPostgresProber` (DSN-gated) | monitor type `postgres`; encrypted `config.password` | domain (secret keys/redact) / infra (column-boundary crypto + pgx via guard) / transport (write-only secret) / presentation (form) | D-0062 |
| MySQL + Redis + PromQL probers (Phase 6 complete, D-0063) | `domain/monitor.go` (`MonitorMySQL`/`MonitorRedis`/`MonitorPromQL` + Active/Valid), `store/migrations/00014` (CHECK), `prober/{redis,mysql,promql}.go`, `prober/prober.go` (registry + mysql guarded dialer), `prober/conditions.go` (`[RESULT]` + `compareFloat` + `Values.Result`), `go.mod` (`go-sql-driver/mysql`), `openapi.yaml` (enums), `frontend/src/views/NewMonitorView.vue` (Redis/PromQL sections, DB form shared, `[RESULT]` placeholder) | `prober` `TestRedisProber`/`TestPromQLProber`/`TestMySQLProberClosedPort`, `domain` `TestMySQLRedisPromQLTypes` | monitor types `mysql`,`redis`,`promql`; `[RESULT]` condition | domain (types) / infra (RESP + guarded driver dial + prom HTTP) / presentation (form) | D-0063 |
| Status page past-incidents accordion (D-0072) | `api/handlers_statuspage.go` (`recentIncidentView` = incident + updates + postmortem; enrich in `writeStatusPageRender`), `openapi.yaml` (`RecentIncident` allOf, renamed to `IncidentDetail` later in the same decision), `frontend/src/views/PublicStatusView.vue` (accordion: duration + postmortem-or-timeline via `renderSections`) | api `TestPublicRenderEnriched` (updates + postmortem surface) | `recent_incidents[].updates`, `.postmortem` on `GET /api/v1/public/status-pages/{slug}` | infra (per-incident fetch) / presentation (accordion) | D-0072 |
| Self-service password reset (D-0068) | `store/migrations/00023_password_reset_tokens.sql`, `store/users_reset.go` (`CreatePasswordResetToken`/`ConsumePasswordResetToken`), `auth/context.go` (Store iface +3), `auth/auth.go` (`WithMailer`/`resetEnabled`), `auth/handlers_reset.go` (request/confirm), `auth/handlers.go` (routes + `password_reset` in config), `cli/cli.go` (`authn.WithMailer`), `web/web.go` (SPA cache headers), `openapi.yaml` (2 endpoints + config field), `frontend/src/views/{ForgotPasswordView,ResetPasswordView}.vue` (NEW), `frontend/src/router/index.ts` (`/forgot`,`/reset`), `frontend/src/views/LoginView.vue` (conditional "Forgot?" + reset banner) | `auth` `TestResetRequestCreatesToken`/`TestResetRequestDisabledWithoutMailer`/`TestResetConfirm`, `store` `TestPasswordResetTokens` | `POST /auth/local/reset/{request,confirm}`, `password_reset` on `/auth/config` | infra (single-use hashed tokens) / transport (no-enumeration + rate-limit) / presentation (forgot/reset views) | D-0068 |
| SPA wiring of dormant backend features (D-0067) | `frontend/src/views/SettingsView.vue` (change-password form in Security tab → `POST /api/v1/me/password`), `frontend/src/views/AdminOutboxView.vue` (NEW dead-letter admin → `GET /api/v1/admin/outbox/dead`, `POST …/{id}/replay`, `POST …/replay-all`), `frontend/src/router/index.ts` (`/admin/outbox` route + `globalAdmin` guard), `frontend/src/components/AppShell.vue` (global-admin nav link), `frontend/src/views/LoginView.vue` (dead "Forgot?" link → honest hint), `frontend/src/views/SlaView.vue` (SLO editor re-seed + error surfacing) | `vue-tsc` type-check + `vite build` + `internal/web` embed test (backend unchanged — endpoints pre-existed) | `POST /api/v1/me/password`, `GET/POST /api/v1/admin/outbox/dead[...]` | presentation (wiring existing transport) | D-0067 |
| Re-notify while down (D-0077, Tier 1) | `store/migrations/00025_monitor_renotify.sql`, `store/monitors.go` (`last_notified_at` in `RecordCheckStatus`, `EnqueueRenotifyReminders`), `domain/outbox.go` (`MonitorTransition.Reminder`+`ShouldNotify`), `domain/monitor.go` (`RenotifySeconds`), `scheduler/scheduler.go` (leader tick), `api/handlers_monitors.go`, `openapi.yaml`, `frontend/src/views/NewMonitorView.vue` (Re-notify field) | `store` `TestRenotifyReminders`, `domain` `TestMonitorTransitionShouldNotify`, api `TestCreateMonitorFailureThreshold` | `renotify_seconds` on monitor; leader re-enqueues reminder transitions | infra (atomic claim+bump) / application (leader cadence) / presentation (field) | D-0077 |
| Alert confirmations + maintenance suppression (D-0076) | `store/migrations/00024_monitor_failure_threshold.sql`, `store/monitors.go` (`RecordCheckStatus`, columns), `ingest/ingest.go` (use it; gate incident on `suppressed`), `domain/monitor.go` (`FailureThreshold`+Normalize), `api/handlers_monitors.go`, `openapi.yaml`, `frontend/src/views/NewMonitorView.vue` (Confirmations field) | `store` `TestRecordCheckStatusConfirmationsAndMaintenance`, `ingest` `TestConfirmationsBeforeDown`/`TestMaintenanceSuppressesIncident`, api `TestCreateMonitorFailureThreshold` | `failure_threshold` on monitor create/update; down-flip/incident gating | domain (threshold) / infra (atomic flip + maintenance check) / transport (config) / presentation (field) | D-0076 |
| Optional auto-incidents per monitor (D-0066) | `domain/monitor.go` (`Monitor.AutoIncident`), `store/migrations/00022_monitor_auto_incident.sql` (column, default true), `store/monitors.go` (`monitorColumns` + scan/insert/update), `ingest/ingest.go` (gate open on `mon.AutoIncident`, resolve unconditional), `api/handlers_monitors.go` (create default-true + PATCH), `openapi.yaml` (Monitor/CreateMonitor/UpdateMonitor), `frontend/src/views/NewMonitorView.vue` (toggle), `frontend/src/views/MonitorDetailView.vue` (state row) | `ingest` `TestAutoIncidentDisabledSkipsOpen` + updated `TestAutoIncidentOpenAndResolve`, `store` `TestMonitorCRUDAndHeartbeats`, api `TestCreateMonitorAutoIncident` | `auto_incident` on monitor create/update/read | domain (field) / infra (persist + gate) / transport (default-true) / presentation (toggle) | D-0066 |
| RabbitMQ + WebSocket + SSH probers (extended catalog, D-0065) | `domain/monitor.go` (`MonitorRabbitMQ`/`MonitorWebSocket`/`MonitorSSH` + Active/Valid/NeedsTarget), `store/migrations/00020_rabbitmq_ws_ssh_monitors.sql` (CHECK), `prober/{ssh,websocket,rabbitmq}.go`, `prober/prober.go` (registry wiring), `openapi.yaml` (type enums), `frontend/src/views/NewMonitorView.vue` (3 type chips + labels/hints + RabbitMQ mode/mgmt form) | `prober` `TestSSHProber`/`TestWebSocketProber`/`TestRabbitMQProberAMQP`/`TestRabbitMQProberManagement`, `domain` `TestRabbitMQWebSocketSSHTypes` | monitor types `rabbitmq`,`websocket`,`ssh` | domain (types) / infra (guarded dial: SSH banner, RFC 6455 handshake, AMQP header + mgmt HTTP) / presentation (form) | D-0065 |
| Two-factor auth / TOTP for local login (Phase 7, D-0064) | `internal/totp/totp.go` (RFC 6238 generate/URI/validate + skew), `store/migrations/00019_totp.sql` (`users.totp_secret`+`totp_enabled`, `totp_recovery_codes`), `store/users_totp.go` (Set/Get/Enable/Disable/ReplaceRecoveryCodes/ConsumeRecoveryCode), `store/users_local.go` (`LocalCredential.TOTP*`, decrypt on lookup), `auth/context.go` (`ConsumeRecoveryCode` iface), `auth/local.go` (`secondFactorOK` + `totp` body field + `totp_required` 401), `api/handlers_totp.go` (enroll/enable/disable + recovery codes), `api/handlers.go` (`me` → `totp_enabled`), `api/api.go` (routes + Store iface), `openapi.yaml` (paths + schemas), `frontend/src/stores/session.ts` (`totpEnabled`), `frontend/src/views/LoginView.vue` (second-factor field), `frontend/src/views/SettingsView.vue` (Security tab) | `totp` `TestTOTP*`, `store` `TestTOTPSecretAndRecoveryCodes`, `auth` `TestLocalLoginWithTOTP`, `api` `TestTOTPEnrollEnableDisable`/`TestTOTPEnrollRejectsTokenPrincipal` | `POST /api/v1/me/totp/{enroll,enable,disable}`; `totp` field on `/auth/local/login`; `totp_enabled` on `/api/v1/me` | domain (RFC TOTP) / infra (encrypt secret + hash recovery codes) / transport (login second factor, uniform 401) / presentation (Security tab + login step) | D-0064 |
| Self-service creation + Settings UI (iter-0035) | `frontend/src/stores/ui.ts` (`createKind`), `frontend/src/stores/workspace.ts` (`createOrg`/`createProject`), `frontend/src/stores/session.ts` (`isOrgAdmin`), `frontend/src/components/{CreateDialog,AppShell}.vue` (role-gated entries), `frontend/src/views/SettingsView.vue` (channels/tokens/webhooks tabs) — all over existing APIs (`POST /organizations`, `POST /organizations/{id}/projects`, notification-channels/tokens/webhooks) | frontend `vue-tsc` + `vite build`; e2e create org→project through the proxy | `POST /api/v1/organizations`, `.../projects`, `.../notification-channels`, `.../tokens`, `.../webhooks` | presentation (role gating in UI over existing transport) | D-0048 |

> **iter-0035 (docs-sync + 1:1 UI):** provider-agnostic OIDC (D-0043) + `keycloak_sub`→`oidc_sub` rename (D-0044) + `/auth/config` & configurable button label (D-0045); **p95 in SLA** (D-0046); **global search FR-016 `DONE`** (D-0047); self-service org/project creation + Settings page (D-0048); every SPA view rebuilt 1:1 with bespoke inline-SVG charts (D-0049).
>
> **FR-013 + NFR-007 `DONE`** (iter-0027, D-0034: OIDC client-credentials — issuer-trusted JWT, JIT-by-`sub`, membership-gated — completing the service-account tokens of iter-0013; service tokens hashed at rest); **FR-010 `DONE`** (webhook/slack/telegram/email — D-0027/D-0029); prober catalog: HTTP/TCP/Push/**ICMP `DONE`** (D-0032, needs NET_RAW/ping_group_range); daily-availability rollup + 90-day dashboard timeline `DONE` (D-0033); monitor edit (PATCH) `DONE` (D-0030); add-member-by-email + **NFR-010 rate-limit `DONE`** (D-0031); FR-011 (all designed SPA screens) is `DONE`; distributed dispatch (RabbitMQ) `DONE`; **NFR-011 SSRF-guard `DONE`** (iter-0028, D-0035: resolved-connect-IP validation, private allowed / metadata blocked by default); **transactional outbox `DONE`** (iter-0029, D-0036: webhook/notification delivery enqueued in-tx and delivered with retry/backoff/dead-letter, `SKIP LOCKED` multi-replica-safe); **NFR-012 heartbeat retention `DONE`** (iter-0030, D-0037: daily RANGE partitions + leader-maintained drop, retention_days also bounds the rollup window); **scheduler efficiency `DONE`** (iter-0031, D-0038: in-memory monitor snapshot + batched push-liveness, per-second full scan + per-push N+1 removed); **outbox dead-letter ops `DONE`** (iter-0032, D-0039: global-admin list/replay of dead events); **NFR-013 secrets-at-rest `DONE`** (iter-0033, D-0040: AES-256-GCM for webhook/channel secrets, opt-in via `security.encryption_key`; **key rotation** iter-0034, D-0041: keyring + `cerbix reencrypt`). Rows added as implemented.
| Monitor tags/labels + filtering (Tier 2, D-0078) | `store/migrations/00026_monitor_tags.sql` (`monitors.tags text[]` + GIN `monitors_tags_idx`), `domain/monitor.go` (`Monitor.Tags` + `normalizeTags`, called from `Normalize`), `store/monitors.go` (`monitorColumns`/`scanMonitor`/create/update carry `tags`, nil→`{}`), `api/handlers_monitors.go` (create/update `tags` field), `openapi.yaml` (`tags` on Monitor/Create/Update), `frontend/src/views/NewMonitorView.vue` (chip input), `frontend/src/views/MonitorsView.vue` (`activeTags`/`allTags`/`shown` AND-filter + filter chip row) | `domain` `TestNormalizeTags`, `store` `TestMonitorCRUDAndHeartbeats` (tags round-trip), api `TestCreateMonitorTags` | `tags` array on `POST/PATCH` monitor bodies | domain (normalize: trim/dedupe/cap) / infra (text[] + GIN) / presentation (chip input + AND filter) | D-0078 |
| SLO burn-rate alerts (Tier 3, D-0079) | `store/migrations/00027_sla_burn_alerts.sql` (burn cols + outbox topic whitelist), `sla/sla.go` (`BurnRate`), `domain/sla.go` (`SLATarget.Burn*`), `domain/outbox.go` (`SLOBurnAlert`+`Message`, `TopicSLOBurnAlert`), `store/sla.go` (`EvaluateBurnAlerts` edge-triggered tx, `UpsertMonitorSLATarget` burn flag), `scheduler/scheduler.go` (`burnEvery` tick), `outbox/outbox.go`+`notify/notify.go` (`DeliverText`), `api/handlers_sla.go` (`burn_alert` on set + windowSLA), `frontend/src/views/SlaView.vue` (toggle + 🔥) | `sla` `TestBurnRate`, `store` `TestEvaluateBurnAlerts`, `outbox` `TestDeliversBurnAlert`, api `TestMonitorSLAAndTarget` | `burn_alert` on `PUT sla-target`; `burn_alert`/`burn_firing` on SLA read | domain (burn math) / infra (edge-trigger latch + tx enqueue) / application (leader tick) / presentation (SLA editor toggle) | D-0079 |
| Scheduled weekly SLA reports (Tier 3, D-0080) | `store/migrations/00028_project_sla_reports.sql` (report cols + topic whitelist), `domain/outbox.go` (`SLAReport`+`Message`, `TopicSLAReport`), `store/sla.go` (`EnqueueDueSLAReports`, `SetProjectSLAReport`, `ProjectSLAReportEnabled`), `store/notifications.go` (`ListEnabledChannelsByProject`), `scheduler/scheduler.go` (`reportEvery` tick), `outbox/outbox.go`+`notify/notify.go` (`DeliverProjectText`), `api/handlers_sla.go` (`setProjectSLAReport`+`projectSLA` flag), `frontend/src/views/SlaView.vue` (header toggle) | `store` `TestEnqueueDueSLAReports`, `outbox` `TestDeliversSLAReport`, api `TestProjectSLAReportToggle` | `PUT /api/v1/projects/{id}/sla-report`; `sla_report_weekly` on project SLA read | infra (7d watermark + tx claim) / application (leader tick) / domain (report render) / presentation (header toggle) | D-0080 |
| Incoming Alertmanager webhook receiver (Tier 4, D-0081) | `store/migrations/00029_incident_external_key.sql` (external_key + partial unique open-index), `domain/incident.go` (`Incident.ExternalKey`), `store/incidents.go` (columns/scan/insert external_key, `FindOpenIncidentByExternalKey`), `api/handlers_alertmanager.go` (payload parse, open/resolve, severity→impact), `api/api.go` (route + Store iface), `openapi.yaml` (`AlertmanagerPayload` + path + Incident.external_key) | `store` `TestFindOpenIncidentByExternalKey`, api `TestAlertmanagerWebhook` | `POST /api/v1/projects/{id}/alerts/alertmanager` (bearer token) | transport (webhook parse + auth) / domain (impact mapping) / infra (fingerprint dedup index) | D-0081 |
| OIDC configurable from Settings (D-0082) | `store/migrations/00030_oidc_settings.sql` (singleton), `domain/oidc.go` (`OIDCSettings`), `store/oidc.go` (Get/Upsert, secret encrypt/decrypt), `store/reencrypt.go` + `store/store.go` (rotation + truncate cover it), `auth/auth.go` (`atomic.Pointer[oidcRuntime]`, `resolveEffective`, `buildRuntime`, `SyncOIDC`, `RunOIDC` retry (named `StartOIDC` when this row was written)), `auth/handlers.go`+`middleware.go` (runtime-driven login/callback/config/cc-verifier, 503 when inactive), `auth/context.go` (Store.GetOIDCSettings), `api/handlers_oidc.go` (`GET/PUT /settings/oidc`, redaction, preserve-on-blank), `api/api.go` (routes + `OIDCController` + `WithOIDC`), `cli/cli.go` (st!=nil gate + `StartOIDC`), `openapi.yaml` (`OIDCSettingsView`/`SetOIDCSettings`), `frontend/src/views/SettingsView.vue` (Authentication tab) | `store` `TestOIDCSettingsRoundTrip`, api `TestOIDCSettingsReadWrite`/`TestOIDCSettingsSecretPreserveAndReloadError`, `auth` `TestConfigHandler` + OIDC flow tests | `GET/PUT /api/v1/settings/oidc` (global-admin) | infra (encrypted singleton + rotation) / application (atomic reload + retry) / transport (global-admin API, secret redaction) / presentation (Authentication tab) | D-0082 |
| Generalized instance settings (D-0083) | `store/migrations/00031_instance_settings.sql` (singleton, JSONB per group), `domain/settings.go` (4 group types + Validate/helpers), `store/instance_settings.go` (Get + per-group Upsert), `store/store.go` (TruncateAll), `settings/service.go` (atomic snapshot, resolver DB→config→defaults, refresh), `api/handlers_settings.go` (per-group GET/PUT + public branding), `api/api.go` (routes + WithSettings), `auth/auth.go`+`local.go`+`handlers.go` (PolicySource: session TTL, TOTP-required, allowed domains), `outbox/outbox.go` (WithSilence gate), `api/handlers_monitors.go` (createMonitor defaults), `cli/cli.go` (wire + Start), `openapi.yaml` (Branding/AuthPolicy/Alerting/MonitorDefaults/PublicBranding), `frontend/src/stores/branding.ts` + `App.vue` + `AppShell.vue` (theming + announcement) + `views/SettingsView.vue` (4 tabs) | `domain` `TestBrandingValidate`/`TestAuthPolicyValidateAndHelpers`/`TestAlertingSilenced`/`TestMonitorDefaultsValidate`, `settings` `TestResolveBootstrapThenOverride`, `store` `TestInstanceSettingsRoundTrip`, `outbox` `TestSilenceSuppressesAlerts`, api `TestBrandingSettings`/`TestAuthPolicyAndAlertingAndDefaults` | `GET/PUT /api/v1/settings/{branding,auth-policy,alerting,monitor-defaults}` (global-admin); `GET /api/v1/public/branding` | infra (singleton JSONB + snapshot) / domain (per-group Validate) / application (resolver + consumers) / presentation (Settings tabs + live theming) | D-0083 |
| SMTP settings group + live mailer (D-0084) | `store/migrations/00032_instance_settings_mail.sql` (mail jsonb), `domain/settings.go` (`MailSettings`+Validate/Deliverable/Port), `store/instance_settings.go` (decrypt on get, `UpsertMail` encrypt), `store/reencrypt.go` (mail password via jsonb_set), `settings/service.go` (`Mail()`/`SaveMail`/bootstrap), `mailer/mailer.go` (`NewLive`+resolver+`Enabled`), `auth/auth.go` (`resetEnabled` dynamic), `cli/cli.go` (live mailer from settings), `api/handlers_settings.go` (`GET/PUT /settings/mail` redact+preserve), `api/api.go` (routes), `openapi.yaml` (`MailSettingsView`/`SetMailSettings`), `frontend/src/views/SettingsView.vue` (Email tab) | `domain` `TestMailSettingsValidateAndHelpers`, `store` `TestMailSettingsRoundTrip`, `mailer` `TestLiveMailerResolvesPerSend`, api `TestMailSettings` | `GET/PUT /api/v1/settings/mail` (global-admin) | infra (encrypted JSONB field + rotation) / application (per-send resolver) / transport (redaction + preserve) / presentation (Email tab) | D-0084 |
| Region-aware worker pools + region-picker + E2E (D-0085/D-0085a) | `store/monitors.go` `ListRegions`, `api` `GET /api/v1/regions` + `<datalist>` picker, `docker/docker-compose.yml (distributed profile)`, `docker/config.worker.yaml`, `store/migrations/00033_monitor_region.sql` (region col+idx), `domain/monitor.go` (`Region`+`DefaultRegion`+Normalize/Validate, composite→core), `store/monitors.go` (columns/scan/create/update), `dispatch/amqp.go` (`jobsQueueForRegion`, per-region publish+declare+TTL, `WithJobRegion` consume), `cli/cli.go` (`--region` flag + wiring), `api/handlers_monitors.go` (region create/update), `openapi.yaml` (region on Monitor/Create/Update), `frontend/src/views/NewMonitorView.vue` (Region field), `docs/overview.md` §2.4, `docs/specs/func-geo-worker-pools.md` | `domain` `TestMonitorRegionNormalizeAndValidate`, `dispatch` `TestJobsQueueForRegion`, `store` `TestMonitorCRUDAndHeartbeats`, api `TestCreateMonitorRegion` | `region` on monitor create/update; `cerbix --role worker --region <r>` | domain (region normalize/validate + composite pin) / infra (per-region AMQP queues + TTL) / application (scheduler routes via monitor.Region) / presentation (Region field) | D-0085 |
| Live-worker region picker + Test connection (D-0086) | `internal/mqadmin/mqadmin.go` (mgmt client, FromAMQP derive, LiveJobRegions), `config` (`rabbitmq.management_url`), `api/handlers_monitors.go` (`listRegions` merge live, `testMonitor`), `api/api.go` (`LiveRegionSource`/`WithLiveRegions`, `ProbeRunner`/`WithProber`, routes — both renamed to `RegionTester`/`WithTester` by D-0087, the next row), `cli/cli.go` (`rabbitManagementClient` wire + `WithProber(runner)`), `openapi.yaml` (regions `[{name,live}]` + `/monitors/test`), `frontend/src/views/NewMonitorView.vue` (region combobox + Test-connection), `MonitorDetailView.vue` (Region+Tags rows) | `mqadmin` `TestFromAMQPDerivesManagement`/`TestLiveJobRegions`, api `TestListRegions`/`TestTestMonitor` | `GET /api/v1/regions` → `[{name,live}]`; `POST /api/v1/projects/{id}/monitors/test` | infra (RabbitMQ mgmt lookup) / application (one-off probe via runner) / presentation (combobox + test result) | D-0086 |
| Region-aware Test connection (RPC, no core fallback) (D-0087) | `dispatch/amqp.go` (`testsQueueForRegion`, `RunTest` publisher-side RPC via exclusive reply queue + TTL, `ServeTests` worker-side consumer→reply to `ReplyTo`, `testsOnce`), `api/api.go` (`ProbeRunner`→`RegionTester` `RunTest(ctx,m)(hb,err)`, `WithProber`→`WithTester`), `api/handlers_monitors.go` (`testMonitor` routes by `m.Region`, 502 on tester error, accepts `region` in body), `cli/cli.go` (`localTester` inproc adapter, `WithTester` by dispatcher type, `ServeTests(runner.Run)` on worker role), `frontend/src/views/NewMonitorView.vue` (`region` in test body, `selectedRegionLive` warning), `openapi.yaml` (test description + 502 response), `docs/overview.md` §2.4 | api `TestTestMonitor` (up/push-400/composite-400/viewer-403/**502 no-worker**); dispatch build/vet; **E2E compose** (region=geo1 probed by geo1 worker; stop worker-geo1 → 502; region=core → result) | `POST …/monitors/test` region-routed; `checks.tests.<region>` RPC | infra (per-region test RPC over AMQP direct reply-to, auto-delete queue) / application (region-aware tester, strict no-fallback) / presentation (region in test + non-live warning) | D-0087 |
| Region-worker-missing alert (edge-triggered + grace) (D-0088) | `store/migrations/00034_region_worker_alerts.sql` (state table + outbox topic whitelist), `domain/outbox.go` (`TopicRegionWorkerAlert` + `RegionWorkerAlert`+`Message`), `store/regionalerts.go` (`EvaluateRegionWorkerAlerts`: needed-vs-live diff, grace, latch, per-project enqueue), `store/store.go` (TruncateAll), `outbox/outbox.go` (deliver via `DeliverProjectText` + silence), `scheduler/scheduler.go` (`LiveRegionSource`/`WithLiveRegions`, `regionWorkerEvery`/grace tick, Store iface), `cli/cli.go` (hoist mgmt client, wire scheduler role) | `store` `TestEvaluateRegionWorkerAlerts` (first-observe/grace/fire/latch/recover), `outbox` `TestDeliversRegionWorkerAlert` (delivery + silence), `domain` `TestRegionWorkerAlertMessage`; **E2E** (geo1 monitor, no worker-geo1 → tracking row in a tick, alert after grace, recovery on return) | outbox topic `region_worker_alert` → project channels; scheduler `region_worker_alerts_evaluated` | infra (mgmt lookup + state table) / application (edge-triggered eval + grace + latch) / delivery (per-project text + silence) | D-0088 |
| On-call / escalations (phase A) (D-0089) | `domain/escalation.go` (Policy/Step/Target/Schedule + Validate + `OnCall`), `domain/incident.go` (ack fields), `domain/outbox.go` (`TopicEscalationStep`+`EscalationStepAlert`+Message), `store/migrations/00035_escalation.sql` (policies/schedules/incident ack+escalation_step, monitors.escalation_policy_id, topic whitelist), `store/escalation.go` (CRUD + `AdvanceEscalations` edge-triggered), `store/incidents.go` (`AcknowledgeIncident`+scan ack), `store/monitors.go` (escalation_policy_id column/scan/insert/update, renotify excludes policy monitors), `store/store.go` (TruncateAll), `notify/notify.go` (`DeliverChannels`+`EnabledChannelsByIDs`), `outbox/outbox.go` (`escalation_step` deliver + flat-down suppression for policy monitors), `scheduler/scheduler.go` (`AdvanceEscalations` tick), `api/handlers_escalation.go` + `api/handlers_monitors.go` (escalation_policy_id) + `api/api.go` (routes/Store), `openapi.yaml`(+schema.d.ts), `frontend/src/views/EscalationView.vue` + `NewMonitorView.vue` (policy selector) + `IncidentDetailView.vue` (Acknowledge) + router/AppShell | `domain` `TestEscalationPolicyValidate`/`TestOnCallScheduleResolve`/`TestEscalationStepAlertMessage`; `store` `TestAdvanceEscalations`/`TestAdvanceEscalationsRepeatLast`/`TestAcknowledge…`; `outbox` `TestDeliversEscalationStep`/`TestEscalationPolicySuppressesFlatDown`; api `TestEscalationPolicyAPI`/`TestOnCallScheduleAPI`/`TestAcknowledgeIncidentAPI`; **E2E** (down→auto-incident→escalation_step→ack stops) | policies/schedules CRUD; `POST incidents/{id}/acknowledge`; outbox `escalation_step`→channels | domain (ladder+rotation invariants, pure OnCall) / infra (JSONB policies + latch on incident) / application (edge-triggered AdvanceEscalations + target resolution) / delivery (per-channel + silence, flat-suppression) / presentation (Escalation view + Ack + selector) | D-0089 |
| Synthetic (scripted multi-step HTTP) (D-0090) | `domain/synthetic.go` (Scenario/Step/Extract/Assert + ParseScenario/Validate), `domain/monitor.go` (`MonitorSynthetic`, Active/NeedsTarget/Valid, Validate branch), `prober/synthetic.go` (sequential steps, `{{var}}` subst, extract json/header/status/body, assert status/body_contains/json/latency_ms, redacted msg) + registry, `store/migrations/00037_synthetic_type.sql` (type whitelist), `openapi.yaml` (type enum), `frontend/src/views/NewMonitorView.vue` (synthetic type + JSON scenario editor) | `domain` `TestParseScenarioValidate`; `prober` `TestSyntheticScenarioSuccess`/`AssertFails`/`MissingExtract`/`SubstAndJSONPath`; E2E (2-step synthetic → up) | monitor `type:synthetic`, `config.scenario` | domain (scenario invariants) / execution (multi-step guarded prober + redaction) / presentation (scenario editor) | D-0090 |
| HTTP-pull agent transport (D-0091) | `store/migrations/00036_pull_agent.sql` (pull_jobs + agent_heartbeats), `store/pulljobs.go` (EnqueuePullJob/ClaimPullJobs SKIP-LOCKED/PurgeExpired/RecordAgentHeartbeat/LiveAgentRegions), `store/store.go` (TruncateAll), `config/config.go` (`PullConfig`), `scheduler/scheduler.go` (`WithPullRegions` + pull branch + purge tick), `api/handlers_agent.go` (`AgentRouter` jobs/results/heartbeat + bearer middleware) + `api/api.go` (`WithAgentToken`/Store), `internal/agent/agent.go` (poll→prober→post loop + heartbeat), `cli/cli.go` (`--role agent`/`runAgent`, mount AgentRouter, `liveRegionsUnion`), `docker/config.agent.yaml` + `config.dev.yaml` pull | `store` `TestPullJobsClaimLeaseAckReclaimAndTTL`/`TestAgentHeartbeatLiveRegions`; api `TestAgentEndpoints`/`DisabledWithoutToken`; **E2E** (pull1 monitor → pull_jobs → agent claim/probe/post: heartbeats + region live) | `GET/POST /api/v1/agent/{jobs,results,heartbeat}`; `--role agent`; `pull.regions`/`pull.token` | infra (DB pull queue + SKIP-LOCKED claim + heartbeat liveness) / transport (agent role, outbound-only HTTPS) / application (scheduler pull branch, results→ingest) | D-0091 |
| Refinements: per-region agent token · on-call overrides · synthetic builder (D-0092) | `config/config.go` (`PullConfig.Agents`/`PullAgent`), `api/api.go`+`handlers_agent.go` (`WithAgentRegionTokens`, `agentAuthorized` region-scoped), `cli/cli.go` (per-region token map); `store/migrations/00038_oncall_overrides.sql`, `domain/escalation.go` (`OnCallOverride`+`OnCall` honors overrides), `store/escalation.go` (override CRUD + load with schedule), `store/store.go` (TruncateAll), `api/handlers_escalation.go` (current/overrides CRUD) + routes + Store, `frontend/src/views/EscalationView.vue` (on-call-now + overrides UI); `frontend/src/views/NewMonitorView.vue` (visual scenario builder), `openapi.yaml`(+schema) | api `TestAgentPerRegionTokenScope`; domain `TestOnCallOverrideWins`; store `TestOnCallOverrideStore`; api `TestOnCallOverrideAPI`; `vue-tsc`+build | `pull.agents`; `GET …/oncall-schedules/{id}/current`+`/overrides`; `DELETE /oncall-overrides/{id}` | transport (per-region agent auth) / domain (override precedence, pure OnCall) / presentation (overrides + scenario builder) | D-0092 |
| Pull-transport hardening P0–P3 (D-0093) | P0: `store/pulljobs.go` `PullQueueStats`, `metrics/metrics.go` `PullStat`+gauges, `scheduler/scheduler.go` sample tick+WARN, `cli` `WithPullMetrics`. P1: `store/migrations/00039_heartbeats_unique.sql`, `store/heartbeats.go` `InsertHeartbeat`(ts=probe,ON CONFLICT)+ the bulk path now named `RecordHistoricalResults`, `api/handlers_agent.go` `agentBackfill`, `internal/agent/agent.go` ring buffer+flush+split goroutines. P2: `store/pulljobs.go` `pg_notify`, `store/pullnotify.go` `PullNotifier`(LISTEN+hub), `api` long-poll `agentJobs`+`WithPullWaiter`, `cli` notifier wire. P3: `store/monitors.go` `MonitorRegions`, `api/handlers_agent.go` `enforceRegionScope`+`?region=`, `store/migrations/00040_agent_tokens.sql`+`store/agenttokens.go`+`domain/agenttoken.go`, `api` `agentAuth` DB fallback+`WithAgentDBTokens`+admin CRUD, `cli` wire | store `TestPullQueueStats`/`TestInsertHeartbeatsBulkIdempotent`/`TestPullNotifierWakesOnEnqueue`/`TestAgentTokenLifecycle`; metrics `TestPullStatsGauges`; agent `TestEdgeBufferFlush`/`TestBufferRingDropsOldest`; api `TestAgentJobsLongPoll`/`TestAgentBackfill`/`TestAgentResultsRegionScope`/`TestAgentDBTokens` | metrics `cerbix_pull_jobs_pending`/`_agent_lag_seconds`; `POST /agent/backfill`; long-poll `GET /agent/jobs`; `?region=` scope; `/api/v1/agent-tokens` CRUD | infra (gauges, LISTEN/NOTIFY, unique idempotency, DB tokens) / transport (long-poll, edge buffer, region scope) / application (historical bulk bypass, per-region auth) | D-0093 |
| Test connection over pull transport (D-0094) | `store/migrations/00041_pull_tests.sql` (pull_tests table+index), `store/pulltests.go` (EnqueuePullTest+`pg_notify`/ClaimPullTest SKIP-LOCKED region-scoped/SavePullTestResult/GetPullTestResult atomic DELETE-RETURNING/PurgeExpiredPullTests), `store/store.go` (TruncateAll), `scheduler/scheduler.go` (purge tick), `api/handlers_agent.go` (`GET /agent/tests`+`POST /agent/test-results` under region-scoped agentAuth) + `api/api.go` (Store: ClaimPullTest/SavePullTestResult), `internal/agent/agent.go` (`testLoop`→claim→runner.Run→post, own goroutine, `testPollEvery=1s`), `cli/cli.go` (`pullTester` RegionTester + `regionRoutedTester` pull-region routing, wired when AMQP+pull.regions+store) | store `TestPullTestLifecycle` (enqueue/claim-region-scoped-once/save/get-consumed-once/expired-not-claimed/purge); api `TestAgentTestEndpoints` (claim/empty/post-204); **E2E** (live geo2 agent claims `GET /agent/tests` → probes → posts `POST /agent/test-results` → `up:true code:200`, was 502) | `GET /api/v1/agent/tests`; `POST /api/v1/agent/test-results`; test-connection for `pull.regions` | infra (pull_tests queue + SKIP-LOCKED claim + atomic result fetch) / transport (agent test loop, region-scoped) / application (region-routed tester, strict no-core-fallback) | D-0094 |
| Mail delivery robustness: timeouts · TLS 465 · SMTPUTF8 · async subscribe (D-0095) | `mailer/mailer.go` (`sendMailTimeout` — `net.DialTimeout` 10s + session deadline 30s; implicit-TLS on port 465; `mailFrom` — MAIL FROM without SMTPUTF8, keeps BODY=8BITMIME, CR/LF guard), `domain/outbox.go` (`TopicSubscriberConfirm`+`SubscriberConfirm`), `store/outbox.go` (`EnqueueOutbox` standalone), `store/migrations/00042_subscriber_confirm_topic.sql` (topic whitelist CHECK), `outbox/outbox.go` (`MailSender`+`WithMailer`+`subscriber_confirm` case, silence-exempt), `api/api.go` (Store: `EnqueueOutbox`), `api/handlers_subscribe.go` (queue instead of inline send + clear 503) | mailer build/vet; api `TestSubscribeConfirmUnsubscribe` (queued not sent; confirmed re-subscribe queues nothing)/`TestSubscribeDisabledWithoutMailer`; outbox `TestDeliversSubscriberConfirm` (deliver via mailer / silence-exempt / no-mailer→fail); `-race` 30 pkgs; **E2E** (465 SMTP TLS handshake 0.56s; subscribe→202 in ~5ms) | `POST /api/v1/public/status-pages/{slug}/subscribers` (202 pending, async); outbox topic `subscriber_confirm` | infra (timeout-bounded SMTP, implicit TLS, SMTPUTF8-free MAIL FROM, outbox topic) / transport (async confirm via outbox worker) / presentation (fast 202 + clear 503) | D-0095 |
| Heartbeats: adaptive hypertable + compression (D-0096) | `store/migrations/00043_heartbeats_hypertable.sql` (NO TRANSACTION guarded DO: twin→create_hypertable 1d→unique→copy→swap; compress segmentby=monitor_id orderby=ts DESC + policy 7d; down→declarative schema 00017), `store/store.go` (`timescale` field + `detectTimescale` at Open), `store/retention.go` (`EnsureHeartbeatPartitions` no-op / `PurgeOldHeartbeats` via `drop_chunks` in hypertable mode) | both modes of the full store suite (timescale cerbix_test + plain PG :5433=CI); `TestHypertableRetention` (no-op ensure / chunk on-demand / idempotency / drop_chunks); `TestHypertableCompressionRoundTrip` (read+conflict+insert through compress); `-race` 30 pkgs; **E2E** (live dev DB: 44006 rows in 412ms, 17.6× compression, live smoke role-all writes into an auto-chunk) | `timescaledb_information.hypertables/jobs`; retention `heartbeats.retention_days` via the maintain tick | infra (guarded migration, mode detection, drop_chunks) / application (retention branching without signature changes) | D-0096 |
| Incident context — heuristic RCA (D-0097, iter-0037) | `domain/incidentcontext.go` (`ClassifyProbeError` 8 classes, `IncidentContext`+`Render` with the `⚡ Context:` marker, `Empty`), `store/incidentcontext.go` (`IncidentContext` — ±5m window cap 2000, aggregation of classes/names/region; `AppendIncidentContext` idempotent system update), `outbox/outbox.go` (Store += 2 methods; attach after successful delivery of `incident.opened`+auto, best-effort) | domain `TestClassifyProbeError`/`TestIncidentContextRender`; store `TestIncidentContextAndAppend` (window/class/region/idempotency); outbox `TestIncidentContextAttached` (re-delivery/manual/webhook-fail/ctx-fail); `-race` 30 pkgs; **E2E** (3 tcp monitors → 3 auto-incidents, one summary each with the correct class/region) | system entry `⚡ Context:` in `incident_updates` (timeline UI/feeds) | domain (pure classifier) / infra (bounded window query, idempotent insert) / application (best-effort attach in the outbox) | D-0097 |
| Multi-window burn-rate rules (D-0098, iter-0038) | `store/migrations/00044_burn_rules.sql` (jsonb + legacy→page-rule port + drop of old columns), `domain/sla.go` (`BurnRule`/`Key`/`DefaultBurnRules`/`ValidateBurnRules`, `SLATarget.BurnRules`+`AnyBurnFiring`), `domain/outbox.go` (`SLOBurnAlert` severity+short window, `Message` with [page]/[ticket] and "1h ∧ 5m"), `store/sla.go` (upsert with latch carry-over by Key; `EvaluateBurnAlerts` per-rule window pairs, edge/latch per rule), `api/handlers_sla.go` (+`burn_rules` in PUT with validation; windowSLA.burn_rules), `openapi.yaml` (`BurnRule` schema) + regen schema.d.ts, `frontend/src/views/SlaView.vue` (rule editor per the mockup: severity/threshold/windows/state, Reset to SRE defaults, 🔥/⚠️ badges) | store `TestBurnRulesMultiWindow` (AND, payload attribution, latch carry-over/reset, defaults) + `TestEvaluateBurnAlerts`; api validation 400; `-race` 30 pkgs; vue-tsc+build; **E2E** (migration 44 live, PUT round-trip 200, invalid 400, GET rules) | `PUT/GET …/sla-target|/sla` with `burn_rules`; outbox `slo_burn_alert` with severity | domain (rule invariants, pure defaults) / infra (jsonb latches, per-rule eval in one transaction) / presentation (inline editor per the approved mockup) | D-0098 |
| Probe confirm phase (D-0099, iter-0039) | `store/migrations/00045_confirm_interval.sql`, `domain/monitor.go` (`ConfirmIntervalSeconds`+`ConsecutiveFailures`, Normalize clamp, `ConfirmConfigured`/`InConfirmPhase`), `store/monitors.go` (columns/scan/CRUD + `pg_notify('monitor_confirm')` from RecordCheckStatus in the same tx), `store/confirmnotify.go` (`ConfirmNotifier` LISTEN hub), `scheduler/scheduler.go` (`WithConfirmSignals`, confirmFast with expiry, refresh fallback, effective interval + short pull TTL, `confirmCapPerRegion=50`), `cli` (notifier for all/scheduler), `api/handlers_monitors.go` (+field on create/update), `openapi.yaml`+schema.d.ts, `frontend` (Reliability-block field per the mockup; `StatusPill` label; "Confirming N/M…" in MonitorsView) | domain `TestConfirmIntervalNormalize`; store `TestRecordCheckStatusConfirmNotify` (LISTEN live); scheduler `TestConfirmSignalAcceleratesProbe`/`TestEnterConfirmCap`; `-race` 30 pkgs; vue-tsc+build; **E2E: time-to-down 25s vs ~180s** | `confirm_interval_seconds` in the monitors API; NOTIFY `monitor_confirm`; WARN `confirm_phase_capped` | domain (clamp/phase predicates) / infra (same-tx NOTIFY, LISTEN hub) / application (non-target, non-postponing nextRun; cap; TTL) / presentation (form+pill per the mockup) | D-0099 |
| Dependency graph + cascade suppression (D-0100, iter-0040) | `store/migrations/00046_monitor_dependencies.sql`, `domain/monitor.go` (`DependsOn`), `domain/incidentcontext.go` (`SuppressionMarker`, RootCause is populated), `store/dependencies.go` (`ReplaceMonitorDependencies` DAG validation CTE cap 10 / `DownAncestors` down∪open-auto-incident / `AppendSuppressionNote` idempotent), `store/monitors.go` (depends_on as an aggregate in monitorColumns), `store/escalation.go` (AdvanceEscalations skips down ancestors), `outbox/outbox.go` (`dependencySuppressed` fail-open; down+burn-firing suppressed, recovery always), `api` (`depends_on` create/update, typed 400s, create rollback), `openapi`+schema.d.ts, `frontend` (multiselect with cycle-check, "⏸ suppressed by" badge, Dependencies block) | store `TestMonitorDependencies`; outbox `TestDependencySuppression`; `-race` 30 pkgs; vue-tsc+build; **E2E: parent+2 children → 1 alert, 2× suppressed, ⏸ notes, root cause, cycle→400** | `depends_on` in the monitors API; log `alert_suppressed_by_dependency`; ⏸ note in the timeline | domain (role = edge, DAG) / infra (CTE traversals, idempotent notes) / application (fail-open delivery suppression, escalations freeze) / presentation (per the mockup) | D-0100 |
| Multi-region quorum via composite (D-0101, iter-0041) | `domain/monitor.go` (`CompositeMode`+quorum, `CompositeQuorum` safe parsing, Validate 1≤M≤N), `prober/composite.go` (down when ≥M non-up, missing=down vote, msg-tally), `frontend/src/views/NewMonitorView.vue` (All/Any/Quorum segment + M field; Multi-region set wizard: live-region chips, M=majority, plan, N+1 calls with rollback), `frontend/src/views/MonitorsView.vue` (quorum M/N badge); no migrations | domain `TestCompositeQuorumValidation`; prober `TestCompositeQuorum`; `-race` 30 pkgs; vue-tsc+build; **E2E: 1/3 down→up, 2/3 down→down `2/3 children down (quorum 2)`, M>N→400** | composite `config: {mode: quorum, quorum: M}`; naming `name @ region` | domain (M invariants) / execution (aggregation without a network probe) / presentation (wizard sugar per the mockup, rollback on error) | D-0101 |
| Members → Settings, "Administration" group (D-0102, iter-0042) | `frontend/src/components/settings/MembersPanel.vue` (self-loading extraction of MembersView: invite/table/matrix/audit; action errors no longer hide the table), `frontend/src/views/SettingsView.vue` (tab `members` scope=org; `scopeLabels.instance`→"Administration"; group order Project→Organization→Administration→Account; `?tab=` deep links), `frontend/src/router/index.ts` (`/members` → redirect `/settings?tab=members`), `frontend/src/components/AppShell.vue` (Manage section removed), `MembersView.vue` deleted; no backend changes | vue-tsc+build; **E2E** (Playwright on the live stack: no Manage in the sidebar, redirect lands on the tab, Administration label global-admin only, member row + role select + audit entry after adding testuser) | `/settings?tab=members` deep link | presentation only (IA move, display rename, panel extraction pattern) | D-0102 |
| Instance-wide Users administration (D-0103, iter-0043) | `store/migrations/00047_admin_users.sql` (audit_logs.org_id nullable), `store/audit.go` (empty org → NULL), `store/users_admin.go` (`ListAllUsers` user-keyed aggregation + auth_type + last_active, `DeleteUser`, `CountGlobalAdmins`), `domain/adminuser.go` (`AdminUser`/`AdminUserMembership`), `api/handlers_users.go` (GET/PATCH/DELETE `/api/v1/admin/users*` behind `requireGlobalAdmin`; self-change/self-delete 400, last-admin 400; audit `user.global_admin`/`user.delete` with NULL org), `openapi.yaml`+schema.d.ts, `frontend/src/components/settings/UsersPanel.vue` (tab `users` first in Administration; orphan badge, pills/chips, add-to-org panel with org-scope roles, locked self row) | store `TestListAllUsers`/`TestDeleteUserCascades`/`TestCountGlobalAdminsAndNullOrgAudit`; api `TestAdminUsers*` (403/400/404 matrix); `-race` 30 pkgs; vue-tsc+build; **E2E** (orphan testuser visible → grant/revoke → add-to-org → delete; curl guards; NULL-org audit rows) | `GET/PATCH/DELETE /api/v1/admin/users*`; audit actions `user.global_admin`, `user.delete` | domain (user-keyed DTO) / infra (nullable-org audit, cascade delete, aggregation) / application (anti-lockout guards) / presentation (per the mockup; popover→panel fix) | D-0103 |
| Audit gaps: docs & config example (D-0106, iter-0044) | `docker/config.example.yaml` (+`local.login_rate_limit_per_minute`), `docs/overview.md` (config table: `bootstrap_admin_emails`+`post_logout_redirect_url`, security row owns admin bootstrap keys, `rabbitmq.management_url`, new `pull` row; CLI: `agent` role, `serve` in the worker example) | strict `config.Load` parses the example (throwaway test); keys cross-checked against `yaml:` tags | — | docs only | D-0106 |
| Audit gaps: expired-row purge (D-0107, iter-0045) | `internal/scheduler/scheduler.go` (maintain tick += `DeleteExpiredSessions`/`DeleteExpiredAuthFlows`, Store interface += both), `internal/api/api.go` (−`ListMembershipsByOrg`), fakes updated | `TestSchedulerLeaderMaintainsPartitions` asserts both purges run on leadership; `-race` scheduler+api | logs `expired_sessions_purged`/`expired_auth_flows_purged` | application (leader tick), infra (existing store fns) | D-0107 |
| Audit gaps: live min password length (D-0108, iter-0046) | `internal/api/api.go` (`effectiveMinPasswordLen`), `internal/api/handlers.go` (password change uses it), `internal/auth/handlers_reset.go` (reset-confirm uses `authPolicy()`) | `TestResetConfirmUsesLivePolicyMinLength` (10 chars → 400, 12 → 204 under policy 12); `-race` auth+api; **E2E** (policy 16 live → 12-char change 400) | `PUT /api/v1/settings/auth-policy` now fully effective | application (live resolver at check time) | D-0108 |
| Audit gaps: SPA display (D-0109, iter-0047) | `internal/api/handlers_incidents.go` (`acknowledged_by_name`), `internal/api/handlers_apitokens.go`+`handlers_webhooks.go` (`resolveUserEmails` → `created_by_email`), domain field additions, `openapi.yaml`+schema.d.ts; frontend: `MonitorDetailView` (method + threshold/confirm/renotify/grace/escalation/updated), `IncidentDetailView` (acked-by, monitor link, external_key), `SettingsView` (Created by columns), `AppShell`+`stores/live.ts` (reconnect chip, `started` flag), org-switcher skeleton (`ws.loading`) | `-race` api+domain; vue-tsc; **E2E** (config rows live, Created by = admin@cerbix.local, ack → "by Administrator" + monitor link) | enriched fields in Incident/ApiToken/Webhook responses | application (best-effort enrichment) / presentation (per the approved mock) | D-0109 |
| Audit gaps: branding logo (D-0110, iter-0048) | `frontend/src/stores/branding.ts` (`logoUrl`), `SettingsView` (Logo URL input), `AppShell`/`LoginView`/`PublicStatusView` + `frontend/src/components/BrandMark.vue` (img + glyph fallback; login uses productName) | vue-tsc; **E2E** (data-URI SVG saved → renders in all three placements; empty → fallback) | `logo_url` end-to-end via existing branding API | presentation (per the approved mock) | D-0110 |
| Audit gaps: push onboarding (D-0111, iter-0049) | `frontend/src/views/MonitorDetailView.vue` (Push endpoint panel: URL+Copy, cron example, interval+grace note) | vue-tsc; **E2E** (create push monitor → POST heartbeat 200 → panel renders URL/note/Copy feedback) | uses existing `push_token` + `POST /api/v1/public/push/{token}` | presentation only (per the approved mock) | D-0111 |
| Audit gaps: global silence until (D-0112, iter-0050) | `frontend/src/views/SettingsView.vue` (Until datetime-local, full-object PUT, banner with the resume moment, local↔RFC3339 conversion) | vue-tsc; API E2E (until round-trips, no wipe on toggle); browser E2E (field + banner) | existing `PUT /api/v1/settings/alerting` | presentation + data-loss fix (per the approved mock) | D-0112 |
| Audit gaps: webhook/channel toggles (D-0113, iter-0051) | `store/webhooks.go`+`notifications.go` (Set*Enabled), `api/handlers_webhooks.go`+`handlers_notifications.go` (PATCH handlers, audit `webhook.toggle`), routes+Store iface+fakes, `openapi.yaml` (`ToggleEnabled`+2 paths)+schema.d.ts, `SettingsView` (switches in both tables with fallback text) | `TestWebhookToggle`/`TestChannelToggle`; `-race` api+store; vue-tsc; **E2E** (create → disable → enable → delete) | `PATCH /api/v1/webhooks/{id}`, `PATCH /api/v1/notification-channels/{id}` | infra (setters) / application (guards+audit) / presentation (per the approved mock) | D-0113 |
| Audit gaps: status-page preview + component fields (D-0114, iter-0052) | `api/handlers_statuspage.go` (`componentView.description`), `openapi.yaml`+schema.d.ts, `StatusPagesView` (token/preview link, Group datalist + Description + Position in the form), `PublicStatusView` (authed-render fallback + internal banner, description subtext) | api tests; vue-tsc; **E2E** (group heading + description render; internal hidden publicly, `?preview=` renders with banner) | authed `GET /status-pages/{pageID}/render` finally consumed | application/presentation (per the approved mock) | D-0114 |
| Audit gaps: agent tokens UI (D-0115, iter-0053) | `frontend/src/components/settings/AgentTokensPanel.vue` (list/issue/revoke, show-once banner), `SettingsView` (tab `agenttokens` in Administration) | vue-tsc; **E2E** (issue → banner+row → revoke → pill) | existing `/api/v1/agent-tokens*` endpoints finally consumed | presentation only (per the approved mock) | D-0115 |
| Audit gaps: subscribers view (D-0116, iter-0054) | `store/subscribers.go` (`ListSubscribersByPage`, `DeleteSubscriber` page-scoped), `api/handlers_statuspage.go` (list/delete behind `statusPageAccess`, audit `subscriber.remove`), routes+Store iface+fake, `openapi.yaml` (`Subscriber` schema + 2 paths)+schema.d.ts, `StatusPagesView` (Subscribers card: counts, confirmed/pending pills, remove-with-confirm) | full `-race` suite; vue-tsc; **E2E** (2 real dev subscribers render with pending pills + counter) | `GET/DELETE /api/v1/status-pages/{pageID}/subscribers*` | infra/application/presentation (per the approved mock) | D-0116 |
| Audit gaps 2: docs & dead-scan cleanup (D-0117, iter-0055) | `docs/overview.md` (agent/RabbitMQ, serve flags, reencrypt dsn, mail keys, agent command), `internal/config/config.go`+`docker/config.example.yaml` (scheme-based mgmt-URL comment), `internal/domain/oidc.go`+`internal/store/oidc.go` (−UpdatedAt), test-support doc comments on 4 store fns | build + OIDC store tests; claims cross-checked against cli.go/mqadmin | — | docs/cleanup | D-0117 |
| Audit gaps 2: feed links (D-0118, iter-0056) | `StatusPagesView.vue` (`feedHref`), `PublicStatusView.vue` (`feed` respects internal preview) | vue-tsc; **E2E** (authed feed 200 with session / 401 anon) | authed `GET /status-pages/{pageID}/feed` finally consumed | presentation (link targets only, no mock needed) | D-0118 |
| Audit gaps 2: audit paging (D-0119, iter-0057) | `MembersPanel.vue` (`auditLimit` 30→100→500, Show-more button, `auditHasMore`) | vue-tsc; live check (`?limit=500` honored; button hidden under-window) | existing `GET /organizations/{orgID}/audit?limit=` | presentation (per the approved mock) | D-0119 |
| Audit gaps 2: escalation progress (D-0120, iter-0058) | `domain/incident.go` (+2 fields), `store/incidents.go` (columns+scan), `openapi.yaml`+schema.d.ts, `IncidentDetailView` (⛑ pill gated on open/unacked/step>0) | `-race` store+api; vue-tsc; **E2E** (staged step=2 → API fields + pill render) | `escalation_step`/`last_escalated_at` in Incident responses | domain/infra/presentation (per the approved mock) | D-0120 |
| Audit gaps 2: monitor defaults (D-0121, iter-0059) | `api/handlers_monitors.go` (`retries`/`renotify_seconds` → `*int`; `effectiveMonitorDefaults`), route in `api.go`, `openapi.yaml`+schema.d.ts, `NewMonitorView` (`applyInstanceDefaults` on create) | `-race` api; vue-tsc; **E2E** (defaults 120/15/2 → omitted-create inherits, explicit-0 create keeps zeros) | authed `GET /api/v1/monitor-defaults` | application (pointer semantics) / presentation (prefill, per mock) | D-0121 |
| Audit gaps 2: editable escalation (D-0122, iter-0060) | `EscalationView.vue` (startEdit*/reset* + POST↔PUT branch, confirm-delete blocks with monitor/override counts, project monitors loaded for the counter) | vue-tsc; **E2E** (create → edit/rename via PUT → confirm-delete flow) | existing `PUT /escalation-policies/{id}`, `PUT /oncall-schedules/{id}` finally consumed | presentation (per the approved mock) | D-0122 |
| Audit gaps 2: TOTP honesty (D-0123, iter-0061) | `domain/settings.go` (scope doc), `SettingsView` (expanded hint), `LoginView` (`totp_setup_required` guidance) | vue-tsc+vet; **E2E** (policy=all → discriminator returned, message rendered; restored) | — | domain contract + presentation (per the approved mock) | D-0123 |
| Full E2E suite (D-0124, iter-0062) | `e2e/` (playwright.config.ts with setup-project storageState, tests/{auth,monitors,incidents,escalation,settings,admin,statuspages,oidc}.spec.ts + auth.setup.ts + helpers.ts, run.sh docker runner, .gitignore), `CLAUDE.md` (E2E section) | two consecutive full runs 23/23 green (~12s) incl. Keycloak OIDC round-trip | `./e2e/run.sh` | test infrastructure | D-0124 |
| E2E coverage expansion (D-0125, iter-0063…0066) | `e2e/tests/{alertmanager,sla,outbox,probers,mail,timeflows}.spec.ts`, oidc skip-race fix, `docker/docker-compose.yml` (mailpit profile `mail`, 10.5.0.15), CLAUDE.md counts | full suite 34/34 green twice (~2min); confirm-phase ~25s vs 3min flat; TOTP RFC6238 in-test | `./e2e/run.sh` (+ `--profile mail`) | test infrastructure | D-0125 |
| E2E-load findings: delete race + token cleanup (D-0126, iter-0067) | `store/heartbeats.go` (FK 23503 → ErrNotFound), `ingest/ingest.go` (INFO `heartbeat_for_deleted_monitor`), `store/migrations/00048_heartbeats_fk_name.sql` (guarded rename), `e2e/tests/settings.spec.ts` (api_token.id + assertion) | `TestInsertHeartbeatForDeletedMonitor`; `-race` store+ingest; **live race**: 0 ERROR / 2 INFO; E2E 34/34 | log `heartbeat_for_deleted_monitor` | infra (error taxonomy) / test | D-0126 |
| Operational logging (D-0127, iter-0068/0069) | `internal/logging/access.go`(+test), cli mux wiring, `api/handlers.go` (`logEvent`, canceled→DEBUG), lifecycle events across api handlers, `ingest` (`monitor_status_changed`), `outbox` (`outbox_delivered`) | middleware unit test; `-race` suite; E2E 34/34; live log inspection (access line + full create→flip→delete narrative) | `http_request`, `monitor_status_changed`, `outbox_delivered`, `settings_saved`, `*_issued/_revoked/_toggled` events | infra (middleware) / application (events) | D-0127 |
| Transport resilience: SSE watchdog (D-0128, iter-0070) | `api/handlers_events.go` (comment → `event: ping`), `frontend/src/stores/live.ts` (lastSeen + 75s watchdog, cleanup in disconnect) | wire check (`event: ping` ≤26s); vue-tsc+build | — | infra/presentation | D-0128 |
| Transport resilience: AMQP supervisor (D-0129, iter-0071) | `dispatch/amqp.go` (supervise/redial, current() RWMutex, resilient consume/serveTests, declare-cache reset), `scheduler/scheduler.go` (`jobs_publish_failed` aggregated + 10s throttle) | `TestAMQPSupervisorRecovers` (live broker, exactly-one lost/reconnected); **live distributed E2E** (kill/start rabbitmq → 1×lost, sparse reconnecting, 10s-throttled publish warns, 1×reconnected, probing resumes, no restarts); `-race` suite; E2E 34/34 | logs `broker_lost`/`broker_reconnecting`/`broker_reconnected`/`jobs_publish_failed` | infra (supervisor) / application (aggregation) | D-0129 |
| Hardening: notify SMTP timeout (D-0130, iter-0072) | `internal/mailer/mailer.go` (export `SendMailTimeout`), `internal/notify/notify.go` (`sendMailFunc` → it) | build; notify+mailer tests green | — | infra (bounded SMTP in the outbox path) | D-0130 |
| Hardening: AMQP channel-level resilience (D-0131, iter-0073) | `internal/dispatch/amqp.go` (channelRetryBackoff leg in consume/ServeTests, consumeOnce declares its queue, Close under locks, nil-logger default) | `TestAMQPConsumerSurvivesChannelDeath` (queue DELETE on live conn → resubscribe, no broker_lost); `-race` dispatch+scheduler | logs unchanged | infra (transport resilience) | D-0131 |
| Hardening: backfill FK race + status ErrNotFound (D-0132, iter-0074) | `store/heartbeats.go` (InsertHeartbeatsBulk pre-filters live monitors), `ingest/ingest.go` (RecordCheckStatus ErrNotFound → INFO) | `TestInsertHeartbeatsBulkSkipsDeletedMonitor`; `-race` store+ingest | log `status_for_deleted_monitor` | infra (batch), application (quiet race) | D-0132 |
| Hardening: agent region-scope + channel-secret redaction (D-0133, iter-0075) | `api/handlers_agent.go` (region required on results/backfill/test-results; auth drops region-less path; enforceRegionScope 400 on empty), `store/pulltests.go` (SavePullTestResult scoped by region), `api/handlers_notifications.go` + `domain/notification.go` (`Redacted()`/`SecretChannelConfigKeys`, list+create redacted), `internal/agent/agent.go` (test-results `?region=`) | `TestAgentPerRegionTokenScope`, `TestAgentTestEndpoints`, `TestAgentResults/Backfill` (region required + cross-region rejected), `TestPullTestLifecycle` (wrong-region write ignored), `TestNotificationChannelSecretsRedacted`; `-race` suite | — | infra (transport region boundary) / application (secret redaction) | D-0133 |
| Hardening: maintenance mutes delivery not the record (D-0134, iter-0076) | `store/monitors.go` (RecordCheckStatus always enqueues flip + stamps last_notified_at), `store/sla.go` (`MonitorInMaintenance`), `outbox/outbox.go` (down-notify muted at delivery when in-maintenance, fail-open, recovery exempt; Store iface +1) | `outbox.TestMaintenanceSuppression` (down/reminder muted in-window, recovery + post-window delivered, fail-open), `store.TestMaintenanceDownEnqueuesAndStamps` (event enqueued + stamp set in-window → renotify eligible); `-race` suite; E2E 34/34 | log `alert_suppressed_by_maintenance` | application (delivery-time suppression) | D-0134 |
| Hardening: leadership watchdog + health gauges (D-0135, iter-0077) | `store/leadership.go` (TryBecomeLeader returns check(); pg_locks liveness probe on held conn), `scheduler/scheduler.go` (5s watchdog → step-down + re-contend, leader gauge), `metrics/metrics.go` (`SetSchedulerLeader`/`cerbix_scheduler_leader`, `SetBrokerUp`/`cerbix_broker_up`), `dispatch/amqp.go` (`WithBrokerState` off supervisor transitions), `cli/cli.go` (wiring) | `scheduler.TestLeadershipWatchdogStepsDown`, `metrics.TestSchedulerLeaderGauge`/`TestBrokerUpGauge`, `store.TestLeadershipMutualExclusion` (check held); `-race` suite; E2E 34/34; live `/metrics` scheduler_leader 1 | gauges `cerbix_scheduler_leader`/`cerbix_broker_up`; logs `scheduler_leadership_lost`/`_check_failed`/`scheduler_leader_lost_recontending` | infra (leader election + observability) | D-0135 |
| Hardening: region affinity in shipped wiring (D-0136, iter-0078) | `cli/cli.go` (all-role `WithPullRegions`/`WithPullMetrics`; test-connection router layers pull routing over either base tester), `docker/config.worker-core.yaml` (new: worker + DB), `docker/docker-compose.yml` + `docker/docker-compose.geo.yml` (core worker uses it + depends_on postgres), `docker/config.dev.yaml` (NOTE) | live: all-mode pull1 monitor → pull_jobs, 0 local heartbeats; distributed core worker evaluates a composite (`1/2 up`, down) not "unavailable"; `-race` suite; E2E 34/34 | — | infra (transport routing + core-worker DB) | D-0136 |
| Hardening: pool sizing + SLA deadlock + cadence timeouts (D-0137, iter-0079) | `store/store.go` (ParseConfig + MaxConns floor/lifetimes/health-check), `store/sla.go` (`projectSLI` on `sliQuerier`; EnqueueDueSLAReports computes on tx), `scheduler/scheduler.go` (`withTimeout` per sub-cadence, subCadenceTimeout/maintainTimeout) | `store.TestEnqueueDueSLAReportsNoSelfDeadlock` (deadlocks pre-fix on MAX-1 pool, passes post-fix); `-race` suite; E2E 34/34; live 5 conns within 12 cap | — | infra (pool + leader-loop robustness) | D-0137 |
| Hardening: escalation/incident lifecycle (D-0138, iter-0080) | `store/escalation.go` (AdvanceEscalations `AND m.enabled`), `store/migrations/00049_one_open_auto_incident.sql` (dedup + partial unique index), `store/incidents.go` (23505 → `ErrAlreadyOpen`), `store/store.go` (`ErrAlreadyOpen`), `ingest/ingest.go` (bounded retry + benign already-open) | `store.TestAdvanceEscalationsSkipsDisabledMonitor`, `store.TestCreateIncidentRejectsSecondOpenAuto`, `ingest.TestAutoIncidentOpenRetries`/`TestAutoIncidentAlreadyOpenIsBenign`; `-race` suite; live goose 49 + index; E2E 34/34 | log `auto_incident_open_retry`/`auto_incident_open_failed(escalation)` | application (lifecycle) / infra (unique index) | D-0138 |
| Hardening: auth surface (D-0139, iter-0081) | `auth/ratelimit.go` (trusted-hop clientIP + idle-key sweep), `config` + `auth/auth.go` (`trusted_proxy_count`), `api/tenant_scope.go` + escalation/monitor handlers (same-project refs), `store/sessions.go` (`DeleteSessionsByUser`) + `api/handlers.go` change-password keep-current + `auth` session-token context, `domain/incident.go`+`domain/sla.go` (`PublicRedacted`) + `api/handlers_statuspage.go` public flag | `auth.TestClientIP`/`TestLoginLimiterEvictsIdleKeys`, `api.TestCrossTenantEscalationRefsRejected`, `store.TestDeleteSessionsByUser`, `api.TestPublicRenderRedactsInternalIDs`; `-race` suite; E2E 34/34 (regression found+fixed) | log `password_changed(sessions_invalidated)` | infra (rate limit) / application (tenant scope, session invalidation, public redaction) | D-0139 |
| Consilium-driven security & reliability hardening (D-0141, iter-0083) | `auth/local.go`+`auth/auth.go` (decoy-verify timing), `auth/handlers.go`+`domain/settings.go` (OIDC allowlist/email_verified + TOTP fail-closed), `api/handlers_notifications.go` (PATCH `Redacted()`), `api/handlers_monitors.go`+`domain/monitor.go` (`WithoutPushToken`/`principalCan`), `prober/guard.go`+`mailer`+`notify`+`webhook`+`cli/cli.go` (SSRF egress client/dialer; **P0.2/iter-0084:** separate `config.notification_egress` policy default deny-private via `cli.notificationEgressGuard`, distinct from allow-private prober; **OIDC is NOT guarded — operator-trusted bounded client**), `store/search.go`+`authz/authz.go`+`api/handlers_search.go` (SQL tenant scope), `store/outbox.go`+`migrations/00050` (claim-token CAS), `store/monitors.go`+`ingest/ingest.go`+`migrations/00051` (RecordResult single-tx + freshness), `store/pulljobs.go`+`api/handlers_agent.go`+`agent/agent.go`+`scheduler/scheduler.go`+`migrations/00052` (pull lease/ack), `dispatch/amqp.go` (publisher reopen + poison dead-letter), `api/handlers.go`+`api/api.go` (MaxBytesReader), `store/monitors.go`+`store/reencrypt.go`+`migrations/00053` (push-token encrypt-at-rest + blind index), `Dockerfile` (`npm ci`), `.github/dependabot.yml` (`/backend`→`/`), `go.mod`/`.ci` (Go 1.25.12) | `auth` decoy-timing + allowlist/TOTP fail-closed; `store.TestSearchScopeBeforeLimit`, `TestRecordScheduledResultPipeline`, `TestPullJobsClaimLeaseAckReclaimAndTTL`, `TestReencryptRotatesToPrimary` (pw+TOTP+push at-rest); `dispatch.TestAMQPPublisherReopensChannel`/`TestAMQPDeadLettersPoison`; `api.TestPushTokenHiddenFromViewer`/PATCH redaction/`TestPublicBodyCap`/`TestAgentEndpoints` ack; `config.TestEgressPolicyDefaults` + `cli.TestNotificationEgressGuardWiring` (P0.2/iter-0084); `-race` both storage modes; E2E 34/34; live migrations 50–53 | queue `checks.dead`; log `publish_channel_reopened`/`dispatch_dead_letter_failed`; col `monitors.last_result_ts`/`push_token_{hash,enc}` | security (auth/SSRF/tenant/at-rest) + reliability (result tx, pull lease, publisher) + supply-chain | D-0141 |
| Hardening: remaining correctness sweep (D-0140, iter-0082) | `domain/monitor.go` (push threshold=1), `prober/icmp.go` (echo seq/payload/id match), `prober/guard.go` (CGNAT 100.64/10), `auth/auth.go`+`auth/handlers.go` (bounded OIDC client + off-boot sync), `cli/cli.go` (WaitGroup shutdown drain), `store/{organizations,projects}.go`+`store/store.go` (`ErrConflict`/23505), `api/handlers.go`+`api/handlers_statuspage.go` (409 + uniform monitor-not-found) | `domain` push pin, `prober.TestCheckIP`(CGNAT)/`TestEchoReplyMatches`, `api.TestDuplicateSlugConflict`, `api.TestComponentAuthzAndMonitorOrgCheck` (no oracle); `-race` suite; E2E 34/34; live clean drain | log `background_drain_timeout` (warn) | infra (ICMP/SSRF/OIDC/shutdown) / application (push, 409, oracle) | D-0140 |
| Result-protocol impl + P1/P2 backlog (D-0143, iter-0085) | **P0a:** `internal/store/monitors.go` (`RecordScheduledResult`/`RecordPushResult`/`RecordHistoricalResults`, outcome reasons), `internal/ingest/{reconciler,pushrecorder}.go`, `internal/store/migrations/00054_heartbeat_observed_at.sql`, `internal/config/config.go` (`ResultConfig`). **P0b:** `migrations/00055_execution_revision.sql`, `RecordDeadmanResult`, revision gate under `FOR UPDATE`, `WithResultPolicy`/`WithResultRevisionMode`. **P1:** readiness-gated push-token backfill, `internal/store/pulljobs.go`+`internal/agent` (ack malformed → re-deliver, bounded claim). **P2:** `internal/domain/monitor.go` (resource caps #3), `internal/config/config.go` (`expandEnvStrict` #11), `internal/store/store.go` (`TruncateAll` guard #8), `internal/store/migrate.go` (advisory lock #10), `internal/secret` (fail-closed RNG #13), `internal/domain/{incident}.go` (`PublicRedacted` #9), `internal/auth/handlers_reset.go` + `internal/store/users_reset.go` (atomic reset #7 / TOTP #6), `internal/store/outbox.go` (`PurgeDeliveredOutbox` #4 / CAS-applied #1), `internal/store/dependencies.go` (graph lock #5). **#14:** `internal/config/config.go` (`server.trusted_proxy_cidrs`, `TrustedProxyNets`) + `internal/auth/ratelimit.go` (`clientIPByCIDR`). **#12:** `migrations/00056_oidc_issuer.sql` + `internal/store/users.go` (`UpsertUserByOIDCIdentity`) + `internal/auth/{handlers,middleware}.go` (thread `iss`). **#2:** `migrations/00057_state_sequence.sql` + `internal/store/monitors.go` (`bumpStateSequenceTx`) + `internal/domain/outbox.go` (`MonitorTransition.Seq`) + `internal/outbox/outbox.go` (delivery staleness gate) | `store.{TestRevisionGate,TestRecordDeadmanResult,TestRecordScheduledResultPipeline,TestRecordPushResult,TestUpsertUserByOIDCIdentity,TestStateSequenceIncrementsOnTransition}`, `outbox.TestStaleTransitionSuppressed`, `auth.TestClientIPByCIDR`, `config.TestValidateRejectsBadTrustedProxyCIDR`; `-race` both storage modes; E2E 34/34 (incl. `oidc` round-trip); live goose → 57 | `result_*` outcome metrics; `POST /api/v1/public/push/{token}`; log `transition_superseded` | infra (ingest spine / migrations) / application (dead-man, delivery gate) / security (proxy trust, OIDC identity) | D-0143 |
| Push liveness watermark & dead-man re-arm (D-0144, iter-0085) | `internal/store/migrations/00058_push_armed_at.sql` (`monitors.push_armed_at`), `internal/store/monitors.go` (`UpdateMonitor` type-aware `last_result_ts` reset + `push_armed_at`/`pending`/counter/`state_sequence` re-arm on `disabled→enabled`; `StalePushMonitors` + `RecordDeadmanResult` freshness `COALESCE(GREATEST(push_armed_at, last_result_ts), created_at)`); **lifecycle:** `internal/store/escalation.go` (`AdvanceEscalations` gates `enabled AND status='down'`), `internal/ingest/reconciler.go` (resolve auto-incident on any `→up`, `cur==up && prev!=up`) | `store.TestPushUpdatePreservesLiveness` (live-push edit not stale + `last_result_ts` preserved; re-enable → pending + `push_armed_at`~now + old real ping kept + fresh window → stale; dead-man applies DOWN after the window), `store.TestAdvanceEscalationsRequiresDownStatus` (pending no-page, down resumes), `ingest.TestReconcilerClosesIncidentOnPendingToUp`, `store.TestReEnableSupersedesQueuedDown` (re-enable bumps `state_sequence` past a queued DOWN, silently); both storage modes; E2E 34/34 | dead-man freshness predicate; escalation gate | infra (re-arm epoch) / application (false-DOWN + re-enable lifecycle) | D-0144 |
| FR-017 diagnostics API + failover (iter-0093, DONE) | `internal/store/fileapply.go` (`FileProviderDiagnostics(orgID)`, org filter in SQL), `internal/api/handlers_fileproviders.go` (`GET /admin/file-providers` global-admin; `GET /organizations/{orgID}/file-providers` org-admin `InOrg`+`ActionOrgManage`), `internal/api/api.go` (routes + Store iface) | `api.TestFileProviderDiagnostics` (global all / non-admin refused / org-admin own-org only / foreign-org denied), `runtime.TestProviderLeadershipFailover` (real PG: mutual exclusion + failover, §12) | diagnostics endpoints | transport (tenant-aware authz) / infra (SQL org filter + advisory failover) |
| FR-017 UI + OpenAPI (iter-0094, SUPERSEDED — acceptance close-out WITHDRAWN, see iter-0095) | `openapi.yaml` (`MonitorManagement`/`FileProviderDiagnostic` schemas + `/admin/file-providers` + `/organizations/{orgID}/file-providers` paths), `frontend/src/api/schema.d.ts` (regen), `frontend/src/views/MonitorsView.vue` (source filter + file badge), `frontend/src/views/MonitorDetailView.vue` (read-only "Managed by file" panel, controls hidden), `internal/fileprovider/runtime/provider.go` (`fingerprint`) | `runtime.TestFingerprintDetectsChanges`; frontend `vue-tsc`+`vite build`; `go test -race ./...` 32 pkgs green; the iter-0094 '§19 all met' table was overstated and is corrected in iter-0095 | `cerbix_file_provider_*` | presentation (badges/read-only/filter) / transport (OpenAPI contract) |
| FR-017 review corrections / orphan safety (iter-0095, DONE) | `internal/fileprovider/scan.go` (`GroupResult.Frozen`; duplicate → freeze not provider-wide suspend; TOCTOU actual-byte re-check), `internal/fileprovider/runtime/provider.go` (skip `Frozen` keys; `len(scanErrs)>0` → suspend orphan provider-wide; enforce `max_monitors_per_bundle`), `docs/decisions.md` D-0147 (poll watcher) | `fileprovider.TestGroupBundlesDuplicateFreezesNotSuspend`, `runtime.TestReconcileDuplicateDoesNotOrphan`, `runtime.TestReconcileScanErrorSuspendsOrphan`; `go test -race ./...` 32 pkgs green | — | application (orphan safety §9.1) / infra (bounded scan) | D-0147 |
| FR-017 review refinements (iter-0096, DONE) | `internal/fileprovider/scan.go` (`io.LimitReader` bounded read = memory-safe byte limit, TOCTOU fully closed), `docs/specs/func-monitoring-as-code.md` §11 (normative poll+resync, chmod-by-resync), `docs/status.md`/`docs/traceability.md` (smoke-claim contradiction fixed; iter-0094 §19 claim withdrawn) | `fileprovider.TestScanTotalBytesBound`, `fileprovider.TestReadBounded` + `fileprovider.TestScanRejectsOversizeFileByStat`, `runtime.TestReconcileRejectsOversizedBundle`; `go test -race ./...` 32 pkgs green | — | infra (bounded resource read) / docs | D-0147 |
| FR-017 bounded-reader test + wording (iter-0097, DONE) | `internal/fileprovider/scan.go` (`readBounded` extracted for direct, deterministic testing) | `fileprovider.TestReadBounded` (oversized source → exactly cap+1 bytes, never fully loaded; within-cap exact; cap-0 overflow), `TestScanRejectsOversizeFileByStat` (renamed — stat early-reject), `TestScanTotalBytesBound` (bounded reader via remaining total budget); `go test -race ./...` 32 pkgs green | — | infra (memory-safe bounded read) / docs |
| FR-017 leadership fencing (iter-0098, DONE) | `internal/store/leadership.go` (`LeaderSession` + `TryBecomeLeaderSession`: lock on a pinned conn), `internal/store/fileapply.go` (`applyBundleTx` extracted; pool + `LeaderSession` wrappers — apply on the lock-owning conn), `internal/fileprovider/runtime/provider.go` (`Applier`→`LeaderSession` for the mutating apply; `NewStoreApplier` adapter), `internal/cli/cli.go` (`NewStoreApplier(st)`) | `store.TestLeaderSessionFencing` (kill backend mid-tx → failover + former-leader commit fails), `runtime.TestProviderLeadershipFailover` (session mutual-exclusion + failover); `e2e/mac-smoke.sh` on the fenced path; `go test -race ./...` 32 pkgs green | — | infra (fencing: lock+tx share a connection) / application (reconcile via session) | §17 |
| FR-017 bounded work (iter-0099, DONE) | `internal/store/fileapply.go` (`max_managed_monitors` under `pg_advisory_xact_lock('file_provider_quota',provider)`; `SET LOCAL statement_timeout/lock_timeout`), `internal/fileprovider/parse.go` (`checkYAMLPolicy`: depth 32 / 10000 nodes / core-tag allowlist), `internal/fileprovider/runtime/provider.go` (`WithReconcileLimiter` semaphore), `internal/cli/cli.go` (shared `maxConcurrentReconciles=4`) | `store.TestApplyBundleMaxManagedMonitors` (real PG, serialized quota), `fileprovider.TestDecodeYAMLPolicy` (custom tag/depth), `runtime.TestReconcileConcurrencyGate`; both storage modes; `go test -race ./...` 32 pkgs green | — | infra (quota lock + YAML/tx bounds) / application (concurrency gate) | §4/§14/§17 |
| FR-017 audit + persisted diagnostics (iter-0100, DONE) | `internal/store/fileapply.go` (in-tx `file_provider.apply` audit on changed; `RecordBundleAttempt` persists status/last_error/attempted_at), `internal/fileprovider/runtime/provider.go` (`persistAttempt` on reject/apply-error; `warnThrottled` 30s rate-limit; `last_success` only when `Rejected==0`) | `store.TestApplyWritesAudit`, `store.TestRecordBundleAttempt`, `runtime.TestLastSuccessOnlyOnCleanScan`; both storage modes; `go test -race ./...` 32 pkgs green | audit `file_provider.apply`; `cerbix_file_provider_last_success_timestamp_seconds` | infra (tx audit + persisted status) / application (rejection persistence + log throttle) | §9/§15/§16 |
| FR-017 second-review P1 refinements (iter-0109, DONE; D-0148) | `internal/store/fileapply.go` (restore actual-vs-desired enabled; bundle-row diagnostics axis: read status/last_error/path up front, first-empty gen 1, stale-clear no-bump, direct path compare; audit on `stateChanged`; `RecordBundleAttemptByPath` fenced), `internal/fileprovider/runtime/provider.go` (counts read before outcome → error on counts-fail; `persistAttemptByPath` for known-path decode/scan errors), `internal/metrics/metrics.go` (`SetFileProviderReconcileStats` preserves counts), `internal/cli/cli.go` (register zero leader gauge pre-election) | `store.TestApplyRestoreEnabledSemantics`/`TestApplyBundleDiagnosticsLifecycle`/`TestRecordBundleAttemptByPath`, updated `TestApplyRestoreNoConfigChangeNoRevisionBump`, `runtime.TestReconcileCountsErrorNoFalseSuccess`/`TestReconcilePersistsInvalidByPath`, `metrics.TestFileProviderLeaderZeroRegisteredBeforeElection`/`TestSetFileProviderReconcileStatsPreservesCounts`; `-race` 32 pkgs green; mac-smoke PASS | leader gauge always present; `reconcile_total{outcome=error}` on counts-fail | infra (idempotent diagnostics + fenced by-path) / application (restore/counts semantics) / observability (alert reliability) | §9/§10/§13/§15/§16, D-0148 |
| FR-017 alert rules + browser E2E surface (iter-0108, DONE) | `docker/alerts/monitoring-as-code.rules.yml` (3 real Prometheus alerts), `internal/metrics/alerts_test.go` (drift guard vs emitted metrics), `e2e/tests/file-providers.spec.ts` (diagnostics contract + badge/read-only/named-provider filter), `docker/config.dev.yaml` + `docker/docker-compose.yml` + `docker/monitoring.d/` (representative provider wiring) | `promtool check rules` SUCCESS (3 rules, docker); `metrics.TestMonitoringAsCodeAlertRules`; `e2e/tests/file-providers.spec.ts` runs clean (skips on pre-FR-017 stack; full UI assertions require a current-build stack + provider — not executed here); `go test -race ./...` 32 pkgs green; golangci-lint no MaC findings | `cerbix_file_provider_*` alerts | infra (alerting) / presentation (UI surface E2E) | §15/§16 |
| FR-017 apply-tx deadline + enumeration/parse hardening (iter-0107, DONE) | `internal/store/fileapply.go` (60s `applyBundleTimeout` around both apply wrappers), `internal/fileprovider/scan.go` (`ReadDirBounded`/`readDirBoundedN` batched+capped, truncation → scan rejection), `internal/fileprovider/runtime/provider.go` (`fingerprint` bounded + truncation marker), `internal/cli/cli.go` (startup open+ReadDir readability probe), `internal/config/providers.go` (EvalSymlinks overlap), `internal/fileprovider/parse.go` (`checkYAMLPolicy` follows `AliasNode.Alias`), api/cli/openapi/TS (runtime DTO `scope_org`/`scope_project`) | `fileprovider.TestDecodeYAMLPolicy` (alias bomb), `fileprovider.TestReadDirBoundedTruncates`, `config.TestProvidersFileSymlinkOverlapRejected`; `-race` fileprovider+config+store+cli green; `e2e/mac-smoke.sh` PASS | runtime DTO scope fields | infra (bounded enumeration + tx deadline) / transport (canonical overlap, alias policy) | §4/§14/§17 |
| FR-017 scheduler config-change listener (iter-0106, DONE) | `internal/store/confignotify.go` (`ConfigNotifier` LISTEN `monitor_config_changed`), `internal/scheduler/scheduler.go` (`configCh`/`WithConfigSignals`; select case zeroes `lastRefresh` → next-tick reload), `internal/cli/cli.go` (`configSignals()` wired into all/scheduler roles), `e2e/mac-smoke.sh` (target-change → revision bump + fresh heartbeat) | `store.TestConfigNotifierWakesOnConfigChange` (real NOTIFY→wake), `scheduler.TestConfigSignalForcesSnapshotReload` (signal forces reload < refreshEvery); `e2e/mac-smoke.sh` config-change propagation step; `-race` store+scheduler+cli green | `monitor_config_changed` now consumed (scheduler wake) | infra (LISTEN/NOTIFY) / application (prompt reschedule) | §12 |
| FR-017 idempotent apply + restore/dep semantics + fenced attempts (iter-0105, DONE) | `internal/store/fileapply.go` (`ApplyResult.NoChange`; execChanged/depChanged/stateChanged/pathChanged; conditional bundle write; `ActionRestore` split from `ActionUpdate`; `ActionDependencyUpdate` split from `ActionNoop`; audit on exec\|dep, NOTIFY on exec; `recordBundleAttempt` over `rowExecer` + fenced `(*LeaderSession).RecordBundleAttempt`), `internal/fileprovider/runtime/provider.go` (`persistAttempt` via session; persist frozen duplicates; `Applied` only when `!NoChange`; `RecordBundleAttempt` moved to `LeaderSession` iface) | `store.TestApplyFileManagedBundle` (no-op NoChange + no generation bump), `store.TestApplyRestoreNoConfigChangeNoRevisionBump`, `store.TestApplyDependencyChangeAudited`, `runtime.TestReconcileNoopNotCountedApplied`, `runtime.TestReconcilePersistsDuplicateRejection`; `-race` store+fileprovider+api+cli green; `e2e/mac-smoke.sh` PASS | audit `file_provider.apply` (incl dep); `reconcile_total{outcome="noop"}` reachable | infra (idempotent write + fencing) / application (restore/dep semantics) | §7/§9/§10/§15/§17 |
| FR-017 P0 orphan-safety + error accounting (iter-0104, DONE) | `internal/store/fileapply.go` (`allowAbsence` on `ApplyFileManagedBundle`/`applyBundleTx`: `ActionOrphan` + grace-disable suppressed when false), `internal/fileprovider/runtime/provider.go` (`suspendOrphan`/`allowAbsence` computed BEFORE apply; owned/orphan/counts errors degrade), `internal/fileprovider/runtime/status.go` (`updateNoCounts` preserves last-known counts) | `store.TestApplyBundleAbsenceGuard` (present-project UID kept under `allowAbsence=false`, then orphaned+disabled when trusted), `runtime.TestReconcileAbsenceGuardThreadedToApply` (ambiguous scan → present-project apply with allowAbsence=false), `runtime.TestReconcileOwnedLookupErrorDegrades`/`TestReconcileCountsErrorNoFalseSuccess`; `-race` store+fileprovider+api+cli green | `cerbix_file_provider_last_success_timestamp_seconds` (no false advance) | application (absence-trust ordering) / infra (non-destructive apply) | §9.1/§16 |
| FR-017/NFR-014 P2 close-out + acceptance (iter-0103, ~~DONE~~ SUPERSEDED — reopened iter-0104) | `frontend/src/views/MonitorsView.vue` (source filter `All`/`UI`/named-provider, value `file:<provider>`), `internal/api/handlers_fileproviders.go` (`?provider=` narrows bundles + runtime status), `openapi.yaml` + `frontend/src/api/schema.d.ts` (query param), `internal/api/api.go` (gofmt), `docs/runbook.md` (diagnostics API + full-lifecycle smoke) | `api.TestFileProviderDiagnostics` (named-provider filter: platform keeps all, nope→none); frontend `npm run build` (vue-tsc); `go test -race ./...` 32 pkgs green; `e2e/mac-smoke.sh` PASS; **golangci-lint RUN (docker): `internal/fileprovider/...` 0 issues** (other findings pre-existing, unrelated); §19 all 10 acceptance criteria evidenced in iter-0103 | diagnostics API `?provider=` | presentation (named-provider filter) / transport (API filter) / docs (runbook) | §15/§19 |
| FR-017 test matrix close-out (iter-0102, DONE) | `internal/fileprovider/runtime/provider_test.go` (`TestLeaderLoopEventStormBounded`: real `leaderLoop`, storm of size-changing rewrites → bounded reconciles), `internal/fileprovider/runtime/leadership_integration_test.go` (`TestTwoLiveReplicasSingleLeaderAndFailover`: two real `Provider.Run` on real PG → never two leaders + survivor takeover), `e2e/mac-smoke.sh` (full §18.4 lifecycle) | `runtime.TestLeaderLoopEventStormBounded` (281 changes → 2 reconciles), `runtime.TestTwoLiveReplicasSingleLeaderAndFailover` (DB-gated); `e2e/mac-smoke.sh` PASS — create + scheduler-exec (heartbeat for file-managed monitor) + in-place update (gen bump, same id) + last-known-good (invalid input no-ops) + orphan-disable (no hard delete) + restore (same DB ids + same push token), NO restart; `go test -race ./internal/fileprovider/runtime/` green | — | application (backpressure bound) / infra (HA single-leader + failover) / e2e (real-binary lifecycle) | §11/§12/§17/§18.4 |
| FR-017 runtime diagnostics surface (iter-0101, DONE) | `internal/fileprovider/runtime/status.go` (process-local `StatusRegistry`/`ProviderStatus`: register-at-`WithStatus` so configured-but-idle providers surface, `setLeader`, sticky-`last_success` `update`, sorted `Snapshot`), `internal/fileprovider/runtime/provider.go` (`WithStatus`; `publishMetrics` updates registry + no longer nil-early-returns; `ReconcileReport.LastError` on every reject path), `internal/api/handlers_fileproviders.go` (`FileProviderRuntimeStatus` DTO + `FileProviderStatusSource`; admin body `{bundles,providers}`, org body `{bundles}`), `internal/api/api.go` (`WithFileProviderStatus`), `internal/cli/cli.go` (`fpStatusAdapter` bridge + `.WithStatus` per provider), `openapi.yaml` + `frontend/src/api/schema.d.ts` (response shapes + `FileProviderRuntimeStatus` schema, regen) | `runtime.TestStatusRegistryConfiguredButIdle`/`TestStatusRegistryUpdatePreservesScopeAndLeader`/`TestStatusRegistrySnapshotSorted`, `api.TestFileProviderDiagnostics` (runtime `providers` present for global admin; both endpoints key diagnostics under `bundles`); `go test -race ./...` 32 pkgs green | admin diagnostics API `providers[]` (leader/last-scan/last-success/counts) | transport (global-admin runtime surface) / application (process-local status registry) | §15 |
| FR-021 phase 2, changeset 3 — the UI against the approved mock (iter-0144, DONE: its final-2/2 [221] residuals closed by iter-0145, APPROVED at [231]) | `frontend/src/components/ServiceReliability.vue` (health card, KPI honesty states with burn reasons as prose, ∅ banner, segment cards with range, tenant-context load generation, provisional-tail request, repair mask, mutable objective editor on `lib/objective.ts`), `frontend/src/components/ReliabilityStrip.vue` (short-unknown/reduced-opacity-provisional motif, bucket-weighted geometry, repairing work encoding, revision/epoch markers), `ServiceDetailView.vue` mount, `internal/store/servicereport.go` (`reportObjective` on the nothing-sealed path — disclosed) | `ServiceReliability.spec.ts` (13 specs at the [219] fix pass: honesty states, deferred A→B context, unequal-weight SVG, repair mask, objective edit), `TestNothingSealedReportStillStatesItsObjective` (real PG), e2e reliability scenario incl. the 99.9→99.99 edit; frontend suite 45/45 at [219] | (no metrics surface) | application (SPA honesty rendering) / docs (mock §12.2 as the binding design) | §11, §12.1–12.2, D-0165 |
| FR-021 phase 2 — the [221] honesty-gap closure (iter-0145, DONE at [231], final 2/2 zero findings; completes FR-021 phase 2) | `frontend/src/components/ServiceReliability.vue`: `guarded()` normalizes rejected fetches into the `{error}` shape at EVERY request's own boundary (report owns the primary error alone; health/series/PUT own their states); health is an INDEPENDENT generation-gated task (neither gates the timeline nor strands as pending on report failure, [228] P1-1); health-pending rendering (no product pill for an unanswered request); `segmentPoints` — unique-epoch segments slice the fetched global series with NO request (bounded fan-out, [228] P1-2), same-epoch reconstruction parts fetched by exact `[from,to)` (server filters before grouping) via a 4-worker pool with per-completion assignment and pending/failed/ready states; segment ticks clipped to `[max(stepStart,from), min(stepStart+step,to))` ([228] P1-3) | `ServiceReliability.spec.ts` grown to 22: rejected-Promise health/series/PUT tests, health-pending INDEPENDENCE (timeline+strips render), report-error health settlement, the same-epoch two-reconstruction-part regression (exact half-day ranges pinned; one rect per strip), repair-clip (one half masked, the other keeps `bad`), deferred-one/fast-one, six-segment zero-request bound; frontend suite 54/54 | (no metrics surface) | application (SPA transport honesty) | §11.3, §12.1–12.2, reviews [221]+[228] |
| FR-021 phase 2, changeset 2 — the API surface (iter-0141…0143, DONE — APPROVED [210]+[213]; the UI changeset pending) | `internal/api/handlers_servicereport.go` (4 session-auth project-scoped routes returning the reviewed domain payloads verbatim), `internal/api/api.go` (Store interface +4), `openapi.yaml` (+4 paths, +11 schemas: 10 response + 1 request) + regenerated `frontend/src/api/schema.d.ts`, `internal/store/servicereport.go` (ErrNotFound for nonexistent/foreign services — no existence oracle; one-snapshot series), `internal/domain/sla.go` (`CanonicalObjective`, D-0165 open interval), migrations 00078 (numeric(7,4)) + 00079 (CHECK objective in (0,100)), `frontend/src/lib/objective.ts` (the D-0165 client mirror, iter-0143) + SlaView canonicalization | `internal/api/api_servicereport_test.go` (3 regressions: authz/tenancy incl. 404 on all endpoints; parameter validation with store-boundary passthrough; wire-verbatim payloads incl. burn verdict fields and the reconstruction label); store tenant regression re-pinned to ErrNotFound; mutants M1–M7 killed (window validation, span cap, unknown-field tolerance, store existence check, trailing-JSON tolerance, uuid gate, canonical rounding) + the post-round objective bound (iter-0142) + one disclosed two-snapshot wrapper survivor; `objective.spec.ts` 14 mirrored cases + the focused SlaView wiring test (iter-0143) | (no metrics surface) | application (HTTP contract) / docs (openapi source of truth) | §11.2–11.3, §12.1, §13 inv 47 |
| FR-021 phase 2, changeset 1 — reporting compute core (iter-0138…0140, DONE — APPROVED [184]+[187]; API/UI changesets pending) | `internal/store/servicereport.go` (`ServiceReliabilityReport` window/segments/budget/burn over sealed facts, `ServiceReliabilitySeries` on-read epoch-keyed rollups, `ServiceHealthNow` two-layer live signal, `UpsertServiceSLATarget`), `internal/domain/servicereport.go` (payload types + fixed reason strings), `internal/store/migrations/00077_service_sla_scope.sql` (three-way exclusive scope, schema-rejected service burn) | 18 regressions in `internal/store/servicereport_internal_test.go` (exact grep at iter-0140, incl. `TestServiceHealthNowSurvivesSubMicrosecondDeadlines` — the [178] sub-µs repro) + `TestCarryInIsTheLatestPriorObservation` in `servicematerialize_internal_test.go` (the discovered phase-1 carry-in fix, iter-0139) + `cerbix enqueue-service-repair` operator artifact with 2 cli tests + `reliability.StateAt` point evaluator (iter-0140) — one per owned §19 phase-2 invariant (38–45, 47) + §11.1 confident-lie + mixed-excluded denominators + burn staleness + epoch-never-merge series + two-layer health; mutants across iter-0138…0140 killed on discriminating asserts (window-at-now, both excluded-denominator leaks, aggregate-across-revisions, reconstruction-flag drop, burn-staleness drop, provisional-merge, dead min-coverage threshold, every-partial-quotes, burn-ignores-continuity, live-evaluation-removed, snapshot-escape, pending-as-healthy, undefined carry-in, whole-bucket/left-limit/future-instant evaluation, fixed-width-window-reintroduced); `-race` both storage modes | (reporting only — no metrics surface; §13 forbids service burn alerts) | application (report semantics) / docs (spec §11.3 amendments, D-0163, D-0164) | §11, §12.1–12.2, §13, §6.6, §10.2 |
| FR-021 phase-1 backlog sweep + evidence/§21/adoption hardening (iter-0133…iter-0137, phase 1 DONE — APPROVED [157]+[160]; FR-021 phase 2: compute core DONE [187], API surface DONE [213], UI pending; phases 3+ unstarted) | `internal/store/servicemaintenance.go` (future-window archive cut + generation bump + audit-in-tx), `internal/store/sla.go`/`rollup.go`/`monitors.go` (`maintEffectiveEnd`, half-open `>` at both live gates), `internal/store/servicerepair.go` (coalescing invalidation + unambiguous-origin merge), `internal/store/retention.go` (`EnsureServiceFactPartitions` + DEFAULT-month adoption with the under-lock authoritative gate, `AdoptServiceFactMonthOperator`, `ServiceReliabilityStats` with zero-clamped lag), `internal/cli/cli.go` (`cerbix adopt-fact-month`, D-0161), `internal/store/services.go` (slug-suffix trim, declaration/delete audit, `ServiceDetail` REPEATABLE READ snapshot), `internal/store/migrations/00064/00065` (crash-resume guards, backfill trim), `internal/metrics/metrics.go` (service gauges, wedged component readiness, HA clear), `internal/scheduler/scheduler.go` (partition + stats cadence, wedged rule, step-down clear), `internal/api/handlers_statuspage.go`, `internal/domain/sla.go`, `openapi.yaml`+TS, `frontend/src/views/SlaView.vue` | `internal/store/backlog0133_internal_test.go` (38 regressions — exact `grep -c '^func Test'` — plus 2 in `internal/cli/cli_adopt_test.go` (command end-to-end + input validation) — incl. the LATE-barrier archive fence judged at the fact, DEFAULT-month adoption + concurrent-insert fence, real 00065 Up path, exact-boundary tx, §21 event-counter sites), `metrics_test.go` (wedged readiness + /readyz-vs-cerbix_ready consistency + HA clear), `scheduler_test.go` (lag-is-not-wedged, fact-partition wiring), `SlaView.spec.ts` (archived reload); migrations 00074 (event aggregate + error/watermark indexes) + 00075 (per-state sampler indexes) + 00076 (month-leading fact index; 00064 seed made explicitly UTC); mutants killed across iter-0133…0137 (thirty-three named): archive-CASE, raw-ends_at predicate, raw-insert invalidation, unguarded CREATE, unbounded slug suffix, untrimmed 00065 backfill, `>=` boundary, latest-origin merge, no-bump archive (at the stale FACT), ready-without-wedge composite, CREATE-first Ensure trap, unsound watermark fence, rows-gate removal, leader-before-unknown, dropped pending index, staging-without-lifecycle, recovery-probe removal, CHECK-only-at-creation, dropped month index, session-zone recovery, zero-cursor copy, per-statement fence, no-oversize-gate, uncapped fence tail, FK-name squatter, no last-success floor, session-local seed (killed by the fresh-Baku migration probe), under-lock-recheck removal (TOCTOU), operator-path-keeps-gate, FK-columns-not-compared, FK-initial-state-ignored, FK-validation-not-required, operator-month-not-normalized; `-race` both storage modes | `cerbix_service_*` gauges/counters, `cerbix_service_wedged` gating `/readyz` | application (effective-end rule, generation fence) / infra (partitions, readiness) / docs (runbook §FR-021) | §10.8/§15.4/§21 |
| FR-021 phase 3, changeset 1 — impact-graph + correlation schema/store core (iter-0146, DONE — review chain [276] 1 P0+5 P1+1 P2 → fix pass [281] → FINAL 2/2 [283] with the residual witness-scoping P1 closed under the [263]-delegated disposition, iter-0146 §9; API/UI changesets pending) | Migration `00080_service_impact.sql` (service_dependencies with shared-project composite FKs both ways; services.graph_generation; incidents UNIQUE(id,project_id) + composite monitor anchor with column-targeted SET NULL; incident_service_impacts path text[] 2..11; outbox fenced column + widened status CHECK + outbox_events_fence_check + fenced partial index + incident_correlation whitelist), `internal/store/servicegraph.go` (one-snapshot Get with graph_generation; replace-set under the per-project service_graph advisory lock: CAS 409, same-project/cycle/depth-9-10-11/limit-20 validation, typed GraphActor audit in canonical columns, per-delta rows in-tx; orphan-inclusive file-pin delete guard), `internal/store/serviceimpact.go` (the ONE-transaction correlation attempt: membership→graph advisory snapshot locks, reachable-endpoint witness selection SQL-capped at 5 oldest by (started_at,id) with exact aggregate overflow, ascending FOR UPDATE incident locks + open recheck, canonical endpoint-inclusive root-first shortest/lex paths, links + 🕸 notes atomic; tenant-scoped ListIncidentImpacts), `internal/store/outbox.go` (class-aware enqueue/claim/fail/replay from the immutable fenced column), `internal/store/incidents.go` (opening-transaction fenced correlation enqueue), `internal/domain/serviceimpact.go`+`outbox.go` (FencedTopic map, roles, caps, bounded RenderImpactNote), `internal/outbox/outbox.go` (incident_correlation case + overflow wiring), `internal/metrics/metrics.go`, `internal/api/handlers_services.go` (pin→409) | 25 store regressions (10 graph incl. concurrent-cycle-writers, depth 9/10/11 pin, direct-SQL cross-tenant, no-revision/no-epoch, orphan pin, actor attribution, token↔set parity race; 12 impact incl. both interleavings, redelivery byte-equivalence, pg_locks/pg_stat_activity-observed resolve+snapshot barriers, note-failure rollback, reachable-scoped witness bound + unrelated-service zero-overflow, tenant-scoped list; 3 fence incl. verbatim legacy claim/replay SQL) + 4 worker tests + 1 domain + 1 API; -race 33 ok solo; declarative 7 packages ok; e2e 41 passed/1 known skip on the rebuilt image | `cerbix_service_impact_links_total{role}` (insert-only), `cerbix_service_impact_correlation_failures_total`, `cerbix_service_impact_witness_overflow_total` (unlabeled) | application (graph/correlation semantics, claim fence) / infra (migration 00080) / docs (spec §14 as amended, D-0166) |
| FR-021 phase 3, changeset 2 — the §14.4 API surface (iter-0147, DONE — review chain [288] 1 P0+5 P1+1 P2 → fix pass [290] → FINAL 2/2 [292] with the residual create-path transport fence closed under the [263]-delegated disposition, iter-0147 §9; the SPA changeset pending) | `internal/api/handlers_servicegraph.go` (GET/PUT `/services/{id}/dependencies`: one-snapshot read with `graph_generation`, presence-aware CAS body, the shared `validDependsOn` transport fence, health-enriched edge views, typed `graphActor`, `writeServiceGraphError`), `internal/api/handlers_services.go` (`depends_on` on create through the same fence; the `dependencies` block on the detail), `internal/api/handlers_incidents.go` (`authedIncidentView` with `impacts` + `impacts_unavailable`), `internal/api/api.go` (+2 routes, Store +5), `internal/store/servicegraph.go` (file-ownership guard on the UI path; `applyServiceDependenciesTx` as the single normalizing/capping mutator), `internal/store/services.go` (`CreateServiceWithDependencies` atomic; `createServiceCoreTx`), `internal/store/servicereport.go` (`serviceNeighbourHealthTx`: four set-wise reads + the shared pure evaluator), `internal/authz/authz.go` + `internal/auth/{auth,middleware}.go` (trusted `AuditLabel`), `openapi.yaml` 0.20.0 (+1 path, +5 schemas) + regenerated `frontend/src/api/schema.d.ts` | 6 API regressions (round-trip incl. token↔set and health; write errors incl. viewer 403 / stale 409 / cycle 400 / outsider 404; create-with-edges; detail block with the exactly-one-batched-call pin; impacts detail-only + never-public over an incident that HAS links; the strict request contract 5-case set; the create-path UUID fence; actor attribution human vs token; disclosed impacts degradation) + 5 real-PG store regressions (file-owned refusal active+orphaned, atomic create rollback/success with audit counts, shared normalization/cap, fixed statement count 1 vs 30 neighbours via a pgx tracer, batched-vs-single-owner verdict parity); -race 33 ok solo; declarative 9 packages ok; frontend build + 54/54; e2e 41 passed/1 known skip | (no new metric surface; degraded reads are logged events) | application (HTTP contract, ownership, transport format) / docs (openapi as source of truth, spec §14.4 amendments) | §14.2, §14.4, §14.7, invariants 59–60 |
| FR-021 phase 3, changeset 3 — the SPA impact surface (iter-0148, DONE — review chain [298] 4 P1+2 P2 → fix pass [299] → FINAL 2/2 [301] with four residuals closed under the [263]-delegated disposition, iter-0148 §9; **completes FR-021 phase 3**) | `frontend/src/components/ServiceDependencies.vue` (both edge directions with BOTH health layers, the file-pin chip, the edge count, and the replace-set editor: candidate loading/error states gating Save, the three honesty states with the stale-409 Reload that re-reads, per-open operation sequencing and a synchronous draft reset on context change/unmount), `frontend/src/views/ServiceDetailView.vue` (mount + owner passthrough), `frontend/src/views/IncidentDetailView.vue` (`AuthedIncident`, 🕸 probable-root chips with the stored root-first path, affected chips, the explicit `impacts_unavailable` state, and the acknowledge MERGE that preserves the enrichment the base response lacks), plus the backend the mock required: `store.ServiceEdge.ManagedBy` (one correlated subquery inside the existing single-statement read) → `api.serviceEdgeView.managed_by` → `openapi.yaml` | 18 `ServiceDependencies.spec.ts` + 4 `IncidentDetailView.spec.ts` component regressions (incl. acknowledge preserving populated impacts AND `impacts_unavailable`, divergent SLI/diagnostics layers, absent health as unknown on BOTH layers upstream and downstream, candidate-failure ≠ empty project with Save disabled and no PUT, cancel→reopen out-of-order candidate discard, context-change draft reset, the file-pin chip on both directions) + `TestServiceGraphEdgeCarriesOwnership` (real PG: the chip predicts the real 409) + the e2e scenario (edge declared through the UI editor, reverse direction with an asserted health value, cycle refused with the editor open, API-verified no-op); frontend 76/76 across 9 files; -race 33 ok; declarative store+api ok; e2e 42 passed/1 known skip | (no new metric surface) | application (SPA honesty, edge provenance) / docs (the approved mock as the binding design) | §14.2, §14.4, §14.5, §14.7 |
| FR-021 phase 5 — alerting ownership, per-signal ARMED delegation (iter-0151, DONE — backend landed and gated, NOT declared complete: every review round is enumerated in the report for its arc and not in this row, which would go stale at each new round and twice did: [iter-0151](iterations/iter-0151.md) §0/§2 for the backend rounds, [iter-0152](iterations/iter-0152.md) §0/§2 for the post-close ones, [iter-0153](iterations/iter-0153.md) for the reconciliation. All findings corrected, none contested, and no cumulative total is stated anywhere; the post-close arc is [iter-0152](iterations/iter-0152.md): live coverage refresh AND its error path (a failed refresh stops claiming ARMED), per-signal reasons with `last_error`, the delegated-MONITOR screen of the approved mock, `e2e/tests/alerting.spec.ts` over the changed scope, and the canonical close-out. Browser suite 44 passed/1 known skip on a stack built from this branch; DONE under the implementer authority, no third round by contract) | Migration `00082_alerting_ownership.sql` (services owns_paging/page_on/page_on_unknown/confirm_evaluations + DB-owned `alert_config_generation` trigger; `sla_targets.alert_generation` bumping on burn_rules too, which is what makes an added rule dis-arm; the phase-2 service-burn rejection lifted; `service_alert_state`; `service_burn_alert_state` normalized per (service, project, target, rule_key); `service_alert_episodes` with an IMMUTABLE recipient snapshot, `target_snapshot_id`, the composite tenant FK with column-list `ON DELETE SET NULL (service_id)` and the open-episode uniqueness keyed by target AND rule; the outbox whitelist restored to CUMULATIVE), `internal/domain/servicealert.go` (policy + Canonical/Validate/Pages, the close-reason vocabulary, the pure edge rule, the payload with SLATargetID), `internal/domain/serviceburn.go` (the HOLD matrix as a pure decision), `internal/domain/sla.go` (lossless canonical `BurnRule.Key()`, non-finite thresholds refused), `internal/store/serviceburnmath.go` (ONE burn-math owner, whole slice batched in one statement), `internal/store/servicealerteval.go` + `serviceburneval.go` (both bounded slices, one snapshot, verdict+episode+outbox in ONE transaction, the CAS acted on rather than discarded, both LINEARIZED against config writes by locking the writers' rows in the writers' order), `internal/store/alertdelegation.go` (the arming conjunction: any HOLD dis-arms, a latch for EVERY declared rule, the fence taking the whole payload), `internal/store/servicealertlifecycle.go` (the ONE closing path, from the episode's own snapshot, through the FENCED enqueue owner), `internal/store/servicealertpolicy.go` (the write surface + read, audit in-tx, file-managed refusal, clock after the locks), `internal/store/fileapply_alerting.go` (MaC apply, lock-free reconcile), `internal/fileprovider/service.go` (`alerting:` block, deliberately OUTSIDE the canonical hash), `internal/store/leadership.go` (both arms fenced to the lock-owning connection), `internal/scheduler/scheduler.go` (leader-only sub-cadences + telemetry + readiness), `internal/metrics/metrics.go` + `internal/store/servicealertstats.go` (§16.6b families and the two sampled gauges), `internal/api/handlers_servicealerting.go` + `openapi.yaml` + regenerated `frontend/src/api/schema.d.ts` | Store regressions across arming, both evaluators, episodes, lifecycle closes, the write surface and the MaC apply, each carrying a MUTATION that fails with its own safety sentence — target-scoped fence (`sequence 4, want 11`), open-episode identity (23505 on the second onset), coverage conjunction, latch cardinality vs the target generation (two mechanisms, each with its own mutation), the deletion close and its ATOMICITY (a refused DELETE must publish nothing), the fenced claim class + the exact pre-fence claim SQL seeing neither onset nor close (invariant 83), the deposed-leader BARRIER on real Postgres (backend identity asserted, then killed mid-transaction), three linearization barriers, the post-wait clock, the five P1-3 evidence gaps (page_on_unknown, both signals disowned at once, audit actor COLUMNS, semantic no-op/reorder, foreign+malformed on both arguments of both methods), and the file-apply pin that a paging edit creates ZERO definition revisions and ZERO epochs; plus scheduler wiring/cadence/readiness, metrics label-cardinality, API strict-DTO and 14 HTTP regressions, and the fileprovider parse/hash suite. `go test -race -count=1 ./...` green across all 30 packages in BOTH storage modes on fresh containers behind a real SQL-connection/migration readiness barrier | `GET|PATCH /api/v1/projects/{p}/services/{s}/alerting`, `PUT …/sla-target/burn-alerting`, `alerting` on the service detail; `cerbix_service_alert_evaluations_total{signal,outcome}`, `…_emitted_total{signal,edge}`, `…_active{signal}`, `…_backlog{signal}`, `…_last_success_seconds{signal}`, `…_lag_seconds{signal}`, `cerbix_service_alert_delegation_total{signal,state}`, `cerbix_service_alert_undeliverable_total{signal}`, `cerbix_service_alert_recipient_missing_total`; scheduler `/readyz` not-ready on the AGE of the last successful pass beyond `lease_multiplier × cadence` (never the API) | domain (silence is only safe while a replacement is ARMED; a close names why it ended) / infra (connection-fenced evaluations, writer/evaluator linearization, DB-owned generations, fenced outbox class) / transport (server-owned fields refused structurally, absent ≠ false) / presentation (the declaration only — no latch, lease or generation is readable) | §16 as frozen, invariants 75–91, D-0168 |
| FR-021 phase 4 — status-page service projection + composite lifecycle (iter-0150, DONE — round 1/2 [314] REJECTED 2 P0+7 P1+2 P2 (contested item disposed [315]/[316]); FINAL round 2/2 [318] REJECTED 2 P0+3 P1+P2, no third reviewer round; both corrected in full and closed at [319] under the delegated implementer authority) | Migration `00081_status_projection.sql` (FOLDED rather than patched by a follow-up: unreleased and untracked, and `cerbix_test` recreated so it applies from ZERO) — components org_id/source_project/source/service_id/revision; status_pages id+org_id UNIQUE, component_generation, shrink-only component_ceiling; total backfill; binding⇔project CHECK keyed on the PRESENCE of a binding (a composite FK is MATCH SIMPLE, so keying on `source` left a dormant foreign binding unconstrained); page-scope guard taking the page row FOR UPDATE (deferral is not serialization) and watching the binding columns too; `monitor_delete_release_components` BEFORE DELETE trigger (the FK's SET NULL alone left `source='monitor'` and broke every monitor delete, and the D-0150 cascade has no application on its path); DB-owned `component_revision_bump` + `component_page_generation_bump`; `status_page_ceiling_shrink_only` admitting at most `max(50,count)` so no write creates headroom; monitors.superseded_by_service_id + retired_at. `internal/domain/statuspage.go` (CompNoData, Measured, Summarize, pending→no_data, `Validate` owning the no_data refusal), `internal/domain/execsemantics.go` (both new fields classified with their reason), `internal/store/statuspageprojection.go` (PAGE-scoped: `(project,service)` pairs, ONE snapshot, constant statements, uptime + withheld reason, UTC-day strips newest-first under the cap, and maintenance read ONCE tagged by project so a project-wide window cannot leak across an org page — [318] P0-1), `internal/store/statuspagecontext.go` (the render's incident/maintenance/timeline/postmortem context batched across all the page's projects — [318] P1-1), `internal/store/monitorpageprojection.go` (three set-wise reads replacing three per component), `internal/store/servicereport.go` (`decideServiceWindow` EXTRACTED as the ONE §11.2/§11.3 owner, called by report and page alike), `internal/store/statusprojection.go` (total public precedence), `internal/store/componentconversion.go` (one shared validator incl. retained-dormant-project validation, CAS on revision + page generation, dormant manual status preserved), `internal/store/monitorretire.go` (composite-only; retire = retired_at + enabled=false + fence + state_sequence + epoch fan-out; annotation permitted under file ownership, retire/reactivate refused), `internal/store/compositeconvert.go` (explicit sli[], partial-loss refusal, exact binary quorum mapping, idempotent on the link, named slug conflict), `internal/store/services.go` (ErrServiceRendered + dormant sweep), `internal/api/statusprojection.go` (ONE resolver for page and preview, batched both halves, public unavailable marker + counter), `internal/api/statuspagecache.go` (5s render cache keyed page+shape+token, with per-key COALESCING so N simultaneous cold misses cost one render, and refusals never stored — [318] P1-2), `internal/api/handlers_statusprojection.go`, `internal/metrics/metrics.go` (`cerbix_status_page_component_unreadable_total`), `openapi.yaml` + regenerated `frontend/src/api/schema.d.ts`, `frontend/src/lib/statuspage.ts` (no_data neutral treatment, withheld-reason sentences), `frontend/src/views/{StatusPagesView,PublicStatusView,MonitorDetailView,ServiceDetailView}.vue`, `internal/web/dist/**` | 25 store + 12 API + 1 domain table + 2 REWRITTEN pinning tests + 15 frontend across 3 spec files. Round-2 additions pin every [314] finding: cross-org dormant insert in three shapes (direct SQL), page-scope race in BOTH commit orders with a blocking witness, rendered-monitor delete (active + dormant holder, counters advanced, nothing deleted), project cascade killing a surviving page's preview, page-first lock order proved by a real block, ceiling 60→59-cannot-regrow + floor + refused direct raise, DB-owned counters via direct SQL, page projection across projects with a crossed pair resolving nothing, MEASURED statement count via pgx QueryTracer (1 service/1 project vs 11/6), page-vs-report agreement on the withheld reason (caught my first version answering `no_sli` where the report said `storage_gap`), explicit-sli refusals + proper-subset conversion + audit carrying both counts, composite-only scope, file-ownership split with NO execution-state movement, ONE call per source for 500 components across 250 projects, 503 over the limit naming both numbers while the authenticated view serves all 501, public `unavailable` distinguished from calm `no_data` with its counter, render cache unreachable without the unlisted token. Round-2 corrections add: cross-project maintenance leak (mutant killed), a REAL deadlock regression that returns `40P01` when the page lock is removed, a statement-count fixture with declared members and a window per project (8 vs 13 on the old loop), one call per context read over 500 components/250 projects/40 incidents, cache expiry on an injected clock + 24-way coalescing + refusals uncached, strictly shrink-only ceiling, page-move bumping BOTH pages, and `TestSchemaMatchesTheFrozenMigration` which fails loudly on a database left at goose 81 from before the fold. Both test databases were RECREATED from the frozen migration before the final runs; `-race` zero FAIL in both storage modes; lint 0; frontend build + tests; e2e on the rebuilt image. the UTC-day fix IS proven: `TestDailyStripDaysAreUTCUnderANonUTCSession` runs the page projection inside a per-run probe database migrated under an `Asia/Baku` session (the iter-0136/0137 harness, dropped with FORCE), seeds a 22:00 UTC bucket that falls on the next Baku day, and the `date_trunc` mutant fails it with `day = 2026-08-16 … want 2026-08-15` | `GET /api/v1/public/status-pages/{slug}` (+`summary_state`, `unmeasured_count`, per-component `unavailable`/`withheld_reason`; 503 `status_page_over_safe_limit`; `X-Cerbix-Cache`), `PATCH /api/v1/components/{id}`, `POST /api/v1/components/{id}/conversion/preview`, `POST /api/v1/components/{id}/conversion` (409 `page_configuration_stale`), `PUT /api/v1/monitors/{id}/successor`, `POST /api/v1/monitors/{id}/{retire,reactivate,convert-to-service}` (the last takes an explicit `sli[]`), `GET .../services/{id}` (+`supersedes`), `cerbix_status_page_component_unreadable_total` | domain (measured vs unmeasured kept apart; no operator-typed unknown; one withholding owner) / infra (one page snapshot, DB-owned counters, headroom-free ceilings, serialized scope guard, rate bound) / transport (previewed CAS conversion, typed refusals, explicit sli consent) / presentation (absence carries its reason; a failed read is not absence) | §15.0/§15.5/§17, invariants 62–74 as amended, D-0167 |
| Honesty fixes found on the running stack (iter-0149, DONE) — the services list claimed phase 2 had not shipped; the committed SPA snapshot that `go install` embeds was three weeks stale with no command to refresh it | `frontend/src/views/ServicesView.vue` (the closing note, with the mock rule cited in a comment), `Makefile` (`spa-snapshot`: build in the pinned node container → replace `internal/web/dist` → restore the `assets/placeholder.txt` FIXTURE), `internal/web/dist/**` (refreshed, 50 files), `CLAUDE.md` (the rule, so the next frontend change runs the target) | `internal/web` 3/3 (`TestServesIndexAtRoot`, `TestSPAFallbackForClientRoute`, `TestServesRealAsset` — the last one is what a naive refresh breaks); frontend production build + 76/76; -race 33 ok; lint 0; diff --check clean | (no metric surface) | docs/process (the durable half is the command + the rule) / application (one line of copy) | — |
| Docs↔code reference integrity (iter-0153) | `scripts/check-docs-references.py` (paths and `Test*` names cited by the LIVING documents must resolve in the tree; brace shorthand expanded; a citation is excused when the sentence around it says the artifact was deleted/renamed), `Makefile` (`docs-check`), `CLAUDE.md` (the command next to `go vet`) | three mutation probes, run and reverted: an invented path and an invented `Test*` name are both NAMED; the same invented path with a removal marker 300 characters away still fails; with the marker adjacent it is excused | n/a (a build-time documentation gate, no runtime surface) | process — the drift it catches was found by hand: `status.md` cited two test names, one of which had never existed in any commit, and five renamed tests were cited as if runnable |
| FR-021 + NFR-016 CLOSURE (iter-0153, D-0169) | `docs/traceability.md` discharge tables (91 invariants + 24 §16.10 scenarios → tests), `scripts/check-docs-references.py` + `make docs-check` as the gate; seven newly written tests for invariants 1, 25, 27, 35, 37, 78 and 86 (`internal/reliability/purity_test.go`, `internal/reliability/provenance_test.go`, `internal/store/decidablefloor_test.go`, `internal/store/serviceingest_internal_test.go`, `internal/store/servicealerteval_internal_test.go`, `internal/outbox/outbox_test.go`) | all 116 numbers discharged and gated; each new test mutation-proven (iter-0153 §2.7); `-race` 33 packages hypertable + store/api/scheduler declarative | n/a (no runtime surface) | process — closing a requirement against a map a reader can check, with §16.9 as a stated NON-GOAL |
| Conditions that had already resolved (iter-0154) | `docs/specs/func-service-reliability.md` (the §12.2 burn-card amendment, the §14 impacts clause and invariant 59's text), `internal/store/servicereport.go` (comment only) | none — nothing executable changed; the production diff is comment lines only, and invariant 59 stays discharged by `TestIncidentImpactsDetailOnlyAndNeverPublic` | n/a | process — "until phase N" outlived phase N three times: once where the phase changed nothing, once where it DECLINED, once pointing a reader at the wrong file |

## FR-021 acceptance discharge — §19 invariants 1–74, §16.8 invariants 75–91, §16.10 matrix

The spec states 91 numbered acceptance invariants and a 24-scenario required test matrix, and until
iter-0153 nothing mapped them: 36 invariant numbers appeared in no document at all, so "FR-021 is
done" could not be checked without re-reading thirty iteration reports. This is that map. It is
enforced rather than reviewed — `make docs-check` fails when a row cites a test the tree does not
have, and when an invariant or scenario number has no row.

Seven rows were GAPS when the map was first built: the properties were specified, believed and
unpinned. Each now has a test written against the code it describes, and each of those tests was
mutation-proven (see [iter-0153](iterations/iter-0153.md) §2.7). Rows marked `INSPECTION:` are
negative or structural claims no scenario test can hold; they carry the mechanical check instead.

### Invariants (§19 for 1–74, §16.8 for 75–91)

| # | What it requires | Discharged by |
| --- | --- | --- |
| 1 | the reducer is a PURE function of its inputs — the package's production source contains no clock read (written in this arc; §2.7) | `TestTheReducerReadsNoClock` |
| 2 | half-open boundary cases; both axes sum exactly to bucket_size on stored rows | `TestHalfOpenEqualityCases`; `TestFactConservationIsEnforcedOnBothAxes` |
| 3 | a stale enabled push member is BAD, a disabled member EXCLUDED, and the §7.1 precedence order holds | `TestPushDeadManExpiresFromArmedAt`; `TestDisabledMemberIsExcluded`; `TestMaintenanceBeatsAStalePushMember` |
| 4 | ignore keeps its own variable and never launders UNKNOWN into EXCLUDED | `TestIgnoreCannotLaunderTheLastUnknownIntoExcluded`; `TestIgnoreDropsAnUnknownMemberWhileOthersDecide` |
| 5 | the clamp bites and is recorded in provenance (Weakened: DeclaredDegradedMin/EffectiveDegraded/ExcludedMaintenance) | `TestQuorumClampsToEligibleAndRecordsIt` |
| 6 | validation reads DECLARED cardinality; a momentary exclusion does not trigger it | `TestQuorumClampsToEligibleAndRecordsIt` |
| 7 | one effective epoch per fact, one effective revision per epoch, no two effective epochs over a bucket | `TestEachBucketResolvesItsOwnEpoch`; `TestOneEffectiveRowPerBoundary` |
| 8 | every revision gets an epoch; execution epochs skipped only on an unchanged hash | `TestExecutionEpochResolvesThePendingDeclarationAtItsOwnBoundary`; `TestEveryRevisionGetsAMatchingEpoch`; `TestRenameCreatesNoEpoch` |
| 9 | the projection is exhaustively classified with no default | `TestEveryEvaluationFieldChangesTheHash`; `TestClassificationHasNoStaleEntries`; `TestSecretMaterialNeverEntersTheProjection` |
| 10 | effective_at ceiling, boundary writes, and retroactive only on first adoption | `TestEffectiveAtIsCeiledToTheNextBoundary`; `TestSameBoundaryWritesLeaveExactlyOneWinner`; `TestOnlyTheFirstRevisionMayBeRetroactive` |
| 11 | one effective row per boundary; the loser is retained, referenced by no fact, its ranges cancelled | `TestOneEffectiveRowPerBoundary`; `TestSupersededRevisionCancelsItsRanges` |
| 12 | the declared reconstruction is labelled in storage, API and UI | `TestReportMarksOnlyTheBackfilledReconstruction`; `TestServiceReportingReturnsTheDomainPayloadVerbatim`; ServiceReliability.spec.ts "withholds every number behind the ∅ banner" |
| 13 | an execution-property change creates epochs and touches no declaration/provider hash/provenance | `TestExecutionChangeLeavesDeclarationsUntouched` |
| 14 | the seal/ingest handshake fires only for an inserted heartbeat | `TestHeartbeatForAnUnclaimedMonitorWritesNothing`; `TestMonitorInNoServiceWritesNothing`; `TestHistoricalHeartbeatUsesMembershipAsOfItsBucket` |
| 15 | late_arrival_grace is its own bound (store const), distinct from result.allowed_skew (config) | `TestHeartbeatIntoASealedBucketBecomesALateArrival`; `TestMaturedBucketIsSealedWithItsGeneration` |
| 16 | sealed facts move only by recompute over validity or an audited admin range | `TestSealedFactIsNotRewrittenByOrdinaryMaterialization`; `TestARetroactiveMutationIsAuditedWithItsActorAndToken` |
| 17 | sealed_through is contiguity-defined and moves back only under an audited retraction (retracted_to) | `TestSealedThroughStopsAtAHole`; `TestConfirmedRetroactiveWindowInvalidatesImmediately` |
| 18 | recompute is bounded by raw availability; older facts keep their epoch and revision | `TestRecomputeOverDestroyedEvidenceFailsClosed`; `TestSealedFactIsNotRewrittenByOrdinaryMaterialization` |
| 19 | durable work is ranges; supersession only by assuming the union; the epoch resolves per bucket | `TestSupersededRevisionCancelsItsRanges`; `TestMaintenanceMutationsCoalesceAndCarryTheirOrigin`; `TestHistoricalHeartbeatUsesMembershipAsOfItsBucket` |
| 20 | incremental over any bucket-aligned partition equals one batch run, both axes, property-tested against an oracle | `TestFactConservationIsEnforcedOnBothAxes`; `TestReducerMatchesOracle` |
| 21 | every fact batch runs on the leader's lock-owning connection; a deposed leader cannot commit | `TestFencedRepairSliceCannotCommitAfterLosingTheLock`; `TestForwardSliceLeavesTheLeaderSessionUnpoisoned` |
| 22 | archive never rewrites sealed past; create/annul over sealed time needs a persisted preview token | `TestConfirmedAnnulActuallyRewritesTheSealedFacts`; `TestAnAnnulTokenIsBoundToTheWindowItNames`; `TestArchiveIsAlwaysPermittedAndLeavesSealedFactsAlone` |
| 23 | one transaction: generation bump, coalesced ranges, repairing reads, watermark retraction; batches commit under the fence | `TestMaintenanceMutationSupersedesARunningBatch`; `TestArchiveFencesAnInFlightRepairBatch`; `TestMaintenanceMutationsCoalesceAndCarryTheirOrigin` |
| 24 | an archived window keeps its effective span, cut at the cancel instant, in every suppression path | `TestArchivedMaintenanceKeepsItsEffectiveSpan`; `TestAnArchivedFutureWindowNeverTakesEffect`; `TestLegacySuppressionStopsAtTheCancelInstant` |
| 25 | §10.10 bounds have defaults and hard maxima; the 0.95 floor is fixed and no config/settings/API path can move it (the floor test written in this arc) | `TestServiceCapsAreRejectedNotReinterpreted`; `TestTheDecidableCoverageFloorIsFixedAndNotAKnob`; `TestServiceGraphConcurrentCycleWriters` |
| 26 | one deadline mechanism per slice, BEGIN through commit, caller-side over the whole slice | `TestConsecutiveStatementsCannotAccumulate`; `TestAClaimCannotOutliveItsBound`; `TestSlicePathsPersistThroughTheEnvelope` |
| 27 | retention prunes RAW heartbeats only — the sealed fact, its provenance and the late-arrival record survive (written in this arc) | `TestHeartbeatRetentionLeavesDerivedFactsAndTheirProvenance` |
| 28 | native RANGE partitions with identical semantics in both storage modes | `TestServiceFactPartitionsAreMaintained`; `TestSealedFactMustCarryItsStamp`; `TestDefaultPartitionAcceptsAnUnplannedBucket` |
| 29 | no generic TSDB primitives | INSPECTION: the migration set introduces no generic time-series primitive — no hypertable, continuous aggregate or extension for service facts (§3 non-goal, a negative claim no test can hold) |
| 30 | the §15.1 matrix in one transaction under the §15.4 lock order, with a system audit entry | `TestDeletingAMonitorDeclaredByAFileOwnedServiceIsRefused`; `TestDeletingAReferencedOwnerClearsTheReferenceAndNotTheTenant`; `TestDeletingAMonitorKeepsItsMaintenanceProvenance` |
| 31 | FOR KEY SHARE on routing rows by id ascending before the service row — no RI lock cycle | `TestServiceOwnerWritesCannotDeadlockWithRoutingDeletes`; `TestServiceOwnerVersusScheduleDeleteCannotDeadlock` |
| 32 | composite tenant-safe FKs, project-scoped background queries, cross-tenant negatives | `TestServiceReportSurfacesAreTenantScoped`; `TestProjectWideMaintenanceDoesNotCrossProjects`; `TestDormantBindingCannotEscapeTenancy` |
| 33 | both result axes as durations, so part-degraded is distinguishable from wholly healthy | `TestFactConservationIsEnforcedOnBothAxes`; `TestGoodThenUnknownInsideOneBucket` |
| 34 | an empty sli[] creates a revision and a matching empty epoch, no facts, no watermark, no SLO | `TestReportWithoutSLIHasNoSLO`; `TestEveryRevisionGetsAMatchingEpoch` |
| 35 | provenance keeps a `partial` window explainable after retention: each cause class is bounded at MaxCauses with the overflow COUNTED (written in this arc) | `TestProvenanceCauseSetsAreBoundedAndCountTheirOverflow` |
| 36 | format-2 slugs resolve identically everywhere; a differing declared slug is rejected by name | `TestDuplicateMonitorSlugIsRejected`; `TestBundleRefusesAnUnknownMonitorSlug`; `TestBackfillPrefersTheProviderSourceUID` |
| 37 | late arrivals aggregate per (service, bucket, monitor), idempotent under redelivery, examples bounded with an overflow count (the bound written in this arc) | `TestHeartbeatIntoASealedBucketBecomesALateArrival`; `TestLateArrivalExamplesAreBoundedAndCountTheOverflow` |
| 38 | the window is [sealed_through − W, sealed_through) and every response carries its stamps | `TestReportWindowEndsAtSealedThroughNotNow`; `TestReportIsAssembledFromOneSnapshot` |
| 39 | continuity and decidable coverage are independent; below the floor everything is partial with its reason | `TestReportChecksContinuityAndCoverageIndependently`; `TestReportNeverManufacturesNumbers` |
| 40 | excluded time enters no denominator; a zero denominator is unavailable, never 100% or 0× | `TestExcludedIsSharedByBothAxes`; `TestReportNeverManufacturesNumbers` |
| 41 | a Service without SLI has no SLO and no budget — never 100% | `TestReportWithoutSLIHasNoSLO`; `TestServiceWithoutSLIProducesNoFacts` |
| 42 | monitors[] does not move the SLO until the monitor joins sli[] | `TestReportDiagnosticMonitorChangesNoNumber`; `TestDiagnosticMonitorChangeCreatesNoEpoch` |
| 43 | a revision-spanning window is segmented with no aggregate across the boundary | `TestReportSegmentsRevisionsAndEpochsDifferently`; `TestReliabilitySeriesNeverMergesAcrossEpochs` |
| 44 | only first-adoption backfill segments are labelled declared reconstructions | `TestReportMarksOnlyTheBackfilledReconstruction` |
| 45 | every budget states the objective that produced it | `TestReportBudgetStatesItsObjective`; `TestNothingSealedReportStillStatesItsObjective` |
| 46 | monitor-level SLO behaviour is unchanged — the pre-existing suite still holds it (store/sla.go was touched by phase 1, so the discharge is the tests, not the file's history) | `TestSLAAndMaintenanceExclusion`; `TestSLATargetUpsert` |
| 47 | SUPERSEDED by §16.4/D-0168; the phase-2 rejection regression was rewritten where it inverted | `TestServiceScopedBurnTargetIsSupported` |
| 48 | edges and impact links are same-project by SCHEMA, composite FKs on both endpoints | `TestServiceGraphTenancyBySchema`; `TestServiceGraphEdgeCarriesOwnership` |
| 49 | an edge-only mutation creates no revision, no epoch, no hash byte and moves no fact | `TestEdgeWriteCreatesNoRevisionNoEpoch`; `TestCanonicalHashMovesOnMeaning` |
| 50 | one validator and one mutator behind graph_generation; a stale token loses cleanly | `TestServiceGraphStaleGenerationLosesCleanly`; `TestServiceDependenciesRoundTrip`; `TestServiceDependenciesWriteErrors` |
| 51 | a desired file edge pins its target; an unresolvable slug is rejected keeping last-known-good | `TestDeleteServicePinnedByFileEdge`; `TestDependencyDAG`; `TestApplyDependencyChangeAudited` |
| 52 | correlation rides its own outbox topic, enqueued in the incident's opening transaction | `TestIncidentEnqueuesOutboxInTx`; `TestCorrelationAndWebhookFailIndependently`; `TestDeliversIncidentCorrelation` |
| 53 | one store transaction: rows locked ascending, open-state rechecked under the lock, links and notes atomic | `TestCorrelateBothInterleavings`; `TestCorrelateNoteFailureRollsBackLinks` |
| 54 | symmetry qualified to the mechanism and the witness bound, both interleavings | `TestCorrelateBothInterleavings`; `TestCorrelateWitnessBoundDeterministic`; `TestCorrelateUnrelatedServiceNeverCountsOverflow` |
| 55 | probable_root/affected mark every service on a path; canonical shortest-then-lexicographic path | `TestCanonicalPathDepthTenChain`; `TestCanonicalPathDiamondAndBounds`; `TestRenderImpactNoteBoundedAndOrdered` |
| 56 | resolved incidents are never annotated; membership resolves via effective monitors[] at attempt time | `TestCorrelateResolveBarrier`; `TestCorrelateSnapshotBarrierMembershipChange` |
| 57 | the 🕸 note is written only for a batch that inserted a link, bounded prose, immutable after deletion | `TestRenderImpactNoteBoundedAndOrdered`; `TestCorrelateNoteFailureRollsBackLinks`; `TestServiceDeletionCascadesTheWholeDomain` |
| 58 | every edge-mutating path serializes on the per-project service_graph lock, ordered after service_membership | `TestServiceGraphConcurrentCycleWriters`; `TestCorrelateSnapshotBarrierGraphReplace`; `TestServiceGraphDepthCapPinned` |
| 59 | impacts are an authenticated-detail enrichment, absent from the model, the list and every projection | `TestIncidentImpactsDetailOnlyAndNeverPublic`; `TestListIncidentImpactsTenantScoped` |
| 60 | read-side work is bounded: no per-row impact query, one batched neighbour-health snapshot | `TestServiceDetailDependenciesBatchedHealth`; `TestNeighbourHealthFixedStatementCount`; `TestNeighbourHealthMatchesSingleServiceOwner` |
| 61 | the fenced class is safe under a mixed fleet BY SCHEMA for the row's whole lifetime | `TestFencedClaimLifecycle`; `TestLegacyTopicLifecycleUnchanged`; `TestMonitorAnchoredOpenEnqueuesFencedCorrelation` |
| 62 | every component row carries its own tenant identity under composite FKs | `TestComponentTenancyBySchema`; `TestSchemaMatchesTheFrozenMigration`; `TestComponentAuthzAndMonitorOrgCheck` |
| 63 | source is an explicit discriminator; the active binding is required by CHECK; inactive stays DORMANT | `TestComponentSourceDiscriminatorAndBackfill`; `TestServiceDeleteClearsDormantBinding`; StatusPagesView.spec.ts "shows the active source and labels the dormant binding as kept for revert" |
| 64 | a monitor-backed component keeps manual_status, a service-backed one refuses it, no_data is refused by CHECK | `TestManualStatusCannotBeNoData`; `TestDeletingARenderedMonitorTurnsItsComponentManual` |
| 65 | the public projection reads the SLI layer alone — no diagnostics, no impact links | `TestIncidentImpactsDetailOnlyAndNeverPublic`; `TestPublicRenderNeverPublishesUnknownAsHealth` |
| 66 | one authoritative batched DTO at one as_of over the existing evaluator — no second semantics owner | `TestServicePageProjectionsStatementCountIsConstant`; `TestServicePageProjectionAgreesWithTheReport` |
| 67 | no_data is outside the severity ladder; the summary is worst-of-MEASURED plus an unmeasured count | `TestSummaryStatusWorstOf`; `TestPublicComponentStatusPrecedence`; `TestPublicRenderNeverPublishesUnknownAsHealth` |
| 68 | three intentional public-output changes, each pinning the OLD behaviour as REMOVED | `TestPublicRenderEmptyPageIsNotOperational`; `TestComponentStatusFromMonitor`; `TestManualStatusCannotBeNoData` |
| 69 | 90-day history from sealed buckets only, at most 90 points, absent days never zeros | `TestDailyStripDaysAreUTCUnderANonUTCSession`; `TestServiceDailyAvailabilityWindowAndCap` |
| 70 | conversion is explicit both ways, previewed, audited in the mutating transaction, CAS-guarded on both tokens | `TestComponentConversionPreviewAndConfirm`; `TestComponentConversionCASOnBothTokens`; `TestPageGenerationBumpsOnAnyComponentMutation` |
| 71 | deletion per case: RESTRICT + 409 for a bound service, SET NULL for a monitor, dormant swept | `TestServiceDeleteRefusedWhileBackingAComponent`; `TestDeletingARenderedMonitorTurnsItsComponentManual`; `TestServiceDeleteClearsDormantBinding` |
| 72 | a converted composite keeps probing and its SLO; the link is one tenant-composite stored fact | `TestSuccessorLinkClearedOnServiceDelete`; `TestConvertCompositeToService` |
| 73 | retire is ONE transaction: retired_at + enabled=false + execution_revision + state_sequence + audit | `TestRetireMonitorEndsLifecycleAndExecution`; `TestCompositeLifecycleEndpoints` |
| 74 | conversion is one serialized transaction; a slug collision is named; quorum translates exactly | `TestConvertCompositeSlugCollision`; `TestConvertCompositeToService`; `TestCompositeQuorumTranslation` |
| 75 | ownership is declared, defaults false, is audited with its actor, and moves no definition axis | `TestPagingWritesAuditTheirActor`; `TestServerOwnedAlertingFieldIsRejected`; `TestAlertingIsOutsideTheCanonicalHash` |
| 76 | suppression is per signal, per polarity, and only while that signal's coverage is ARMED | `TestDelegationSuppressesOnsetButNeverRecovery`; `TestDelegationSuppressesTheEscalationLadder`; `TestBurnDelegationIsIndependentOfLive` |
| 77 | every ambiguity fails open with a reason-labelled counter, and the badge says exactly what delivery decided | `TestDelegationFailsOpen`; `TestAlertingStateAgreesWithWhatDeliveryDecides`; `TestDelegationOutcomesAreCountedPerSignal` |
| 78 | suppression touches DELIVERY only — the queue history, the incident context and correlation are identical armed vs disarmed (written in this arc) | `TestDelegationSuppressesTheEscalationLadder`; `TestDelegationChangesDeliveryAndNothingElse` |
| 79 | suppression is visible: an alert_suppressions row, a counter, and one idempotent `⏸ Suppressed:` note (the marker is asserted through domain.SuppressionMarker) | `TestRecordSuppressionIsIdempotent`; `TestDelegationOutcomesAreCountedPerSignal` |
| 80 | membership is the CURRENT EFFECTIVE revision's members for evaluator and delegation alike | `TestDelegationUsesTheEffectiveRevision`; `TestRevisionChangeDisarmsUntilReevaluated` |
| 81 | the live signal shares the public evaluation path, pages only declared states, only after confirm_evaluations | `TestEvaluatorConfirmsBeforeAnnouncing`; `TestEvaluatorDoesNotPageUnknownByDefault`; `TestEvaluatorBootstrapAnnouncesNothing` |
| 82 | delivery is at-least-once and ORDERED: a superseded event is dropped by sequence; a close still reaches its onset | `TestStaleTransitionSuppressed`; `TestEvaluatorClosesToTheOnsetRecipients`; `TestBurnSequenceIsScopedToItsTarget` |
| 83 | service_alert is a FENCED topic; an old worker neither claims nor dead-letters it | `TestAnOldWorkerNeitherClaimsNorBurnsAServiceAlert` |
| 84 | one burn-math owner over arbitrary windows; both windows must breach; every non-quotable reason HOLDs | `TestBurnEvaluatorFiresOnceWhenBothWindowsBreach`; `TestBurnEvaluatorHoldsWithoutResolvingAFiringRule`; `TestBurnEvaluatorLatchesRulesIndependently` |
| 85 | evaluation is bounded: keyset slices, a hard cap, a per-slice deadline, generation/lease CAS | `TestEvaluatorSliceIsBoundedAndFair`; `TestBurnEvaluatorSliceIsBoundedAndFair`; `TestStandbyEvaluatesNeitherServiceAlertArm` |
| 86 | no service alert opens, resolves or annotates an incident; the only note is the outbox worker's ⏸ on a MONITOR's incident (written in this arc) | `TestAServiceAlertNeverTouchesTheIncidentTables` |
| 87 | one domain validator shared by API and MaC; server-owned fields refused structurally | `TestPagingWritesRejectInvalidInputBeforeWriting`; `TestFileManagedServiceRefusesPagingWrites`; `TestServerOwnedAlertingFieldIsRejected` |
| 88 | an ONSET's recipient snapshot is immutable and every close reaches THAT snapshot | `TestEvaluatorClosesToTheOnsetRecipients`; `TestBurnEvaluatorClosesToTheOnsetRecipients`; `TestServiceAlertThatReachesNobodyIsCounted` |
| 89 | every removal path closes a firing episode in the same transaction, with its named reason | `TestDisowningClosesEachOpenSignalExactlyOnce`; `TestDisablingBurnClosesOnlyThatTargetAndDropsItsLatches`; `TestDeletingAFiringServiceClosesItsAnnouncementOnce` |
| 90 | the burn latch is normalized per (service, project, target, rule_key); the monitor latch is untouched | `TestBurnRuleKeyIsLosslessAndThresholdsAreFinite`; `TestBurnEvaluatorSeparatesTargetsSharingARuleKey`; `TestReplacingARuleInPlaceDisarmsDespiteMatchingCardinality` |
| 91 | freshness is ONE DB-clock predicate; a stalled evaluator marks the SCHEDULER not-ready only | `TestServiceAlertStallMarksSchedulerNotReadyOnly`; `TestAPersistentlyFailingArmMarksTheSchedulerNotReady`; `TestServiceAlertStallGatesReadiness` |

### Required test matrix (§16.10, written before the phase-5 code)

| # | Scenario | Discharged by |
| --- | --- | --- |
| 1 | a rule at CLEAR whose window HOLDs: the member's burn alert is DELIVERED | `TestOneHoldingRuleAmongManyDisarmsBurnCoverage`; `TestBurnDelegationIsIndependentOfLive` |
| 2 | a FIRING rule under every HOLD reason: neither fires again nor resolves | `TestBurnEvaluatorHoldsWithoutResolvingAFiringRule` |
| 3 | a monitor DOWN delivered while dis-armed, then the evaluator arms: the recovery is DELIVERED | `TestDelegationSuppressesOnsetButNeverRecovery` |
| 4 | the burn mirror of that race | `TestBurnDelegationIsIndependentOfLive` |
| 5 | empty schedule with no project fallback, a channel disabled after arming, a routing lookup error: the member PAGES | `TestUnroutableServiceDisarmsBothSignals`; `TestDelegationFailsOpen` |
| 6 | restored routing re-arms | `TestUnroutableServiceDisarmsBothSignals` |
| 7 | a schedule that ROTATES between onset and close: the close reaches the ONSET's recipients | `TestEvaluatorClosesToTheOnsetRecipients`; `TestBurnEvaluatorClosesToTheOnsetRecipients` |
| 8 | every removal path from FIRING produces its named close, and the close delivers after the row is gone | `TestDisowningClosesEachOpenSignalExactlyOnce`; `TestDeletingAFiringServiceClosesItsAnnouncementOnce` |
| 9 | missing / stale / generation-mismatched state and an injected store error: the monitor PAGES | `TestDelegationDisarmsOnStaleGenerationAndError`; `TestDelegationFailsOpen` |
| 10 | burn disabled or its evaluator stale: monitor burn is NOT suppressed | `TestBurnEvaluatorDisarmsWithoutAWatermark`; `TestEnabledTargetWithNoVerdictDisarmsBurnCoverage`; `TestBurnDelegationIsIndependentOfLive` |
| 11 | live policy that pages nothing: transitions are NOT suppressed (reason `policy_pages_nothing`) | `TestAlertingStateAgreesWithWhatDeliveryDecides` |
| 12 | the future-effective membership race | `TestDelegationUsesTheEffectiveRevision` |
| 13 | several owners in every combination of active/stale/error | `TestActiveDelegationArmed`; `TestDelegationOutcomesAreCountedPerSignal` |
| 14 | a real escalation ladder suppressed and its progress preserved | `TestDelegationSuppressesTheEscalationLadder` |
| 15 | out-of-order retried onset after a close | `TestBurnDelegationIsIndependentOfLive`; `TestStaleTransitionSuppressed` |
| 16 | duplicate delivery under at-least-once | `TestRecordSuppressionIsIdempotent` |
| 17 | bootstrap, enable-while-down, disable-while-firing, every config edit, maintenance entry and exit, visibility loss and return | `TestEvaluatorBootstrapAnnouncesNothing`; `TestWideningPageOnClosesNothing`; `TestNarrowingPageOnClosesTheAnnouncementItNoLongerCovers`; `TestConfirmEvaluationsEditClosesNothing` |
| 18 | FIRING burn against every HOLD reason | `TestBurnEvaluatorHoldsWithoutResolvingAFiringRule` |
| 19 | bounded slice statement count with real members, maintenance and several projects | `TestEvaluatorSliceIsBoundedAndFair`; `TestBurnEvaluatorSliceIsBoundedAndFair` |
| 20 | slice cap, deadline, cursor fairness and a deposed evaluator | `TestEvaluatorSliceIsBoundedAndFair`; `TestADeposedLeaderCannotCommitAnEvaluation`; `TestStandbyEvaluatesNeitherServiceAlertArm` |
| 21 | cross-tenant direct SQL and API | `TestForeignProjectCannotEditPagingConfig`; `TestAlertingStateRefusesForeignAndMalformedIdentifiers`; `TestServiceAlertPolicyReadIsCanonicalAndTenantScoped` |
| 22 | old/new worker fence | `TestAnOldWorkerNeitherClaimsNorBurnsAServiceAlert` |
| 23 | zero alerting services | `TestEvaluatorBootstrapAnnouncesNothing` |
| 24 | incident tables untouched except the suppression note (written in this arc) | `TestAServiceAlertNeverTouchesTheIncidentTables` |
