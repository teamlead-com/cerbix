# Local dev (vite dev server runs inside the frontend-dev container).
VITE_PORT=5173
# Where vite proxies /api and /auth. From a container, reach the host backend:
VITE_BACKEND_URL=http://host.docker.internal:8080
